| Date | Change |
|------|--------|
| [2026-07-31](https://github.com/wbniv/llvm-mos-65816/commit/5587462) | fix(jgxcheck/blankscan): require a quiescent baseline — lsystem's apex is not force-blank bleed |

<!--history-meta v1
5587462	author	Will Norris
5587462	added	207
5587462	deleted	0
5587462	files	1
5587462	body	BLANKSCAN flagged lsystem frame 1154 (149 black rows, neighbours 143/142) as a one-frame\nforce-blank bleed. It is a false positive, and the demo is innocent: lsystem.c asserts force\nblank exactly once, at boot (display_add's reserve), and never writes INIDISP inside the frame\nloop, so no mechanism exists for a per-frame blank at 1154.\n\nWhat actually happens is a content sweep. canvas_clear() dirties all 256 canvas tiles while\nbitmap_canvas.h caps the flush at CANVAS_FLUSH_TILES(64)/frame, so the clear reaches VRAM over\n4 frames sweeping top-down (tiles are row-major) while the regrowth restarts from the trunk at\nthe bottom on the next frame. Frame 1154 is where the descending clear front crosses the\nascending regrowth: a local maximum BY CONSTRUCTION. Measured leading-black 47,79,111,143,\n[149],142,135 with total ink collapsing 189 -> 11 -> climbing — a wholesale content change,\nthe opposite of bleed (picture unchanged, top N rows forced black).\n\nThe higher-neighbour comparison was built to exclude the title's monotonic gravity ramp, but an\napex is exactly the shape that survives that exclusion. Locally it is indistinguishable from\nreal bleed (near-equal shoulders, a 6-row excursion), so no threshold on the 3-point comparison\ncan separate them; only a wider window shows the sweep.\n\nFix: flag a spike only when the window +/-JGX_BLANKSCAN_WIN (default 5) around it, excluding the\ncandidate, spans less than JGX_BLANKSCAN_QUIET (default 8) rows. Suppressions print their window\nspread — never silent.\n\nGate (same commit): JGX_BLANKSCAN_SELFTEST=1 build/jgxcheck pins the discrimination on synthetic\nseries with no ROM and no emulation — bleed on a quiescent baseline still FLAGS (a 12-row\nexcursion on flat shoulders), the measured lsystem apex does not, and the title ramp does not.\nThat third case locks in the pre-existing behaviour this change had to preserve.\n\nTrade-off recorded in the plan and in the code: a genuine bleed landing inside a wipe/fade/scene\nchange is now missed. Accepted because bleed is a DMA/v-blank-overrun artifact that shows on\notherwise stable frames, and a standing false FAIL is worse — it trains us to ignore the gate,\nwhich is how a black truncstair shipped for weeks.\n\nverify-web-roms.sh also needed its verdict-line parsing tightened: the new informational\n"BLANKSCAN: frame N spike ... ignored" line precedes the verdict, and the old\n`BLANKSCAN: [A-Z]*` pattern matched it with an empty capture, so head -1 yielded a bare\n"BLANKSCAN: " and failed a demo that had passed both checks. The same hunk guards the greps with\n`|| true` (an unmatched grep under set -e had silently stopped the sweep at demo 33 of 113 and\nlooked like a clean pass).\n\nVerified: selftest 3/3 PASS; shipped ROM 1200-frame gate PASS with frame 1154 attributed and\nskipped, SMOKE still 0x8073; dev/verify-web-roms.sh --only lsystem ALL PASS. The corpus CRC is\nuntouched — this changed only the scan heuristic, not any ROM. The full 114-ROM sweep is\npending, being satisfied by the concurrent 2026-07-31 republish gate run.\n\nAuthored by a prior session 2026-07-30 and left uncommitted; independently root-cause-verified\nand adopted 2026-07-31.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
-->
