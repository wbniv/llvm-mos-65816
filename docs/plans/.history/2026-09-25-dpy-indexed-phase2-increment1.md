| Date | Change |
|------|--------|
| [2026-09-25](https://github.com/wbniv/llvm-mos-65816/commit/dbb84e16) | test(321): farbank gate — [dp],y (b7) across a bank boundary |
| [2026-09-25](https://github.com/wbniv/llvm-mos-65816/commit/2689b74d) | feat(321): select [dp],Y (b7/97) for a small constant far displacement |

<!--history-meta v1
dbb84e16	author	Will Norris
dbb84e16	added	74
dbb84e16	deleted	1
dbb84e16	files	1
dbb84e16	body	The constant-displacement fold landed in the previous commit is only correct\nbecause the 65816 adds Y to the FULL 24-bit pointer and carries INTO the bank\nbyte — unlike `(dp),Y`, which wraps inside the bank. Nothing tested that:\n`dev/run.sh farindex`'s tbl is uint16_t based at $C10000, so an element read is\nalways 2-byte aligned and can never straddle $xxFFFF.\n\nexamples/65816/farbank.c reads that same generated table through a BYTE pointer\nas unaligned 32-bit values at two offsets whose four bytes straddle a bank\nboundary, so Y = 1, 2, 3 (the whole folded window) are exercised in one access\nand Y = 2, 3 are the carry:\n\n  byteoff 65534  -> $C1FFFE $C1FFFF | $C20000 $C20001   ($C1 -> $C2)\n  byteoff 131070 -> $C2FFFE $C2FFFF | $C30000 $C30001   ($C2 -> $C3)\n\nThe read is four bytes wide, not two, ON PURPOSE: under farindex's value\ncontract the $C1 and $C2 bank-base LOW bytes are both 0x00, so a 16-bit\nstraddling read would ALIAS and let a wrapping `[dp],Y` pass. The bank-base\nHIGH bytes differ, so the golden discriminates — 0x00010000 correct,\n0x80000001 if `[dp],Y` wrapped inside the bank.\n\n  PASS: 6 indexed far loads (lda [dp],y, b7) — 2 probes x Y=1,2,3 folded\n  SMOKE: PASS addr=0x7E0208 len=4 got=0x00010000 (ran 120 ticks)         [MAME]\n  SMOKE: PASS off=0x208 len=4 got=0x00010000 (ran 240 frames, bsnes-jg)\n\ndev/run.sh only gains the usage/help entry — its dispatch is already generic\n(`bash /work/dev/${TARGET}.sh`).\n\nPlan: docs/plans/2026-09-25-dpy-indexed-phase2-increment1.md\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_017sAprvTwtKFJHKgQmr1qgr
2689b74d	author	Will Norris
2689b74d	added	322
2689b74d	deleted	0
2689b74d	files	1
2689b74d	body	#321 Phase 2 increment 1 — the "zero-proof" slice of the Phase 1 GO verdict\n(docs/investigations/2026-09-24-dpy-indexed-measurement.md §6 constraint 3).\n\nReaching the adjacent byte of a far (addrspace 2) object used to materialise a\n32-bit pointer add — an inc/bne carry chain plus a whole second Imag32 ZP quad —\nbefore a second plain `lda [dp]`. A compile-time-constant displacement in [1,3]\nnow folds into 65816 indirect-long indexed addressing, `lda [dp],y` (b7) /\n`sta [dp],y` (97), off the SAME quad. `[dp],Y` adds Y to the full 24-bit pointer\nwith carry INTO the bank byte, so it is an exact replacement.\n\nNew: G_LOAD_FAR_INDIR_IDX / G_STORE_FAR_INDIR_IDX (MOSInstrGISel.td),\nLDIndirLongIdx / STIndirLongIdx (MOSInstrLogical.td, mirroring LDIndirIdx on the\nexisting LDA/STA_IndirectIndexedLong MC opcodes — nothing new below the pseudo\nlayer), MOSLegalizerInfo::tryFarIndirectIndexedAddressing, and the two selector\narms. The gate is a pure may-fold: the constant peel STOPS (does not fail) at a\nvariable offset so `tbl[i]`'s high byte still folds, and every other shape falls\nback to the existing, already-correct a7/87 + add path.\n\nMeasured (-c -Os +mos-a16, same C, only the compiler toggled):\n  far uint32 read, 1 access   311 -> 143  (-54%)\n  far uint16 read, 1 access   107 ->  75  (-30%)\n  far uint16 reads, 2         227 -> 204  (-10%)\n  far uint16 reads, 3         397 -> 410  (+13)   <- see below\n  112 examples/65816 fixtures  net 0 (far_arith -8, farindex +8)\n\nThe 3-access regression is a SEPARATE, pre-existing defect the fold exposes: the\npre-RA machine scheduler interleaves the index-scaling shift chain with the\npointer-add chain, leaving two carry values live at once, so one is materialised\ninto a GPR (ldy #1/bcs/ldy #0 ... cpy #1) twelve times. With -enable-misched=false\nthe same shapes are -79 / -86 bytes. Tracked as its own TODO item; no local\ncompile-time predicate correlates with it, so narrowing the gate cannot help.\n\nVerification: farindex PASS (MAME + bsnes-jg, golden 0x0001D8A1, now asserting b7),\ncorpus 80/80, roundtrip 0 divergent in all three modes, lit 164 tests with the\ndocumented 4-failure baseline unchanged and the new test passing.\n\ndev/regen-patch.sh also carries a concurrent worker's insert-rep-sep-cloned-kills\nTESTRELS entry, without which this regeneration of 0002 would have dropped their\ntest file.\n\nPlan: docs/plans/2026-09-25-dpy-indexed-phase2-increment1.md\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_017sAprvTwtKFJHKgQmr1qgr
-->
