| Date | Change |
|------|--------|
| [2026-06-19](https://github.com/wbniv/llvm-mos-65816/commit/95dd127) | #321 corpus-a16: wire into CI (xcheck job) — secret-gated MAME+bsnes a16 differential |

<!--history-meta v1
95dd127	author	Will Norris
95dd127	added	76
95dd127	deleted	0
95dd127	files	1
95dd127	body	corpus-a16 is a third prerequisite combo: from-source patched toolchain (like xcheck;\nstock /opt/llvm-mos has no +mos-a16) + SPC700 IPL secret (like smoke, for MAME) + bsnes-jg\n(like xcheck). It can't run in the smoke job (stock toolchain). So extend the xcheck job,\nwhich already builds the cached toolchain + SDK + bsnes-jg:\n - add "Provide SNES SPC700 IPL (from secret)" (id bios; copied from the smoke job)\n - add a gated "corpus-a16 ..." step (if has_bios; MOS_TOOLCHAIN=/work/build/llvm-mos-install;\n   dev/run.sh corpus-a16), placed after the bsnes cross-check so build/jgxcheck is present.\n\nAdditive: only smoke.yml changed (10 deleted lines are all comments — header/secret notes\nrewritten; no step/run logic removed). Workflow stays workflow_dispatch-only.\n\nVERIFIED locally 2026-06-19: PyYAML parse + structure assertions pass; smoke job + first\nfive xcheck steps unchanged. Green CI dispatch (heavy, needs SNES_SPC700_ROM_B64) is\nuser-triggered — TODO item is [verify], mirroring the xcheck-into-CI item.\n\n(The TODO.md [verify] flip for this item already landed in 9e93c3d — a concurrent agent's\ncommit swept the unstaged hunk; content is correct, so not re-committed here.)\n\n- .github/workflows/smoke.yml                  extend xcheck job (+2 steps) + comments\n- docs/plans/2026-06-19-321-corpus-a16-ci.md   new plan (design + verification)\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
-->
