| Date | Change |
|------|--------|
| [2026-06-15](https://github.com/wbniv/llvm-mos-65816/commit/2a30974) | #321 native s16: 16-bit unsigned-ordering comparisons (rep; lda; cmp; sep; b<cc>) |

<!--history-meta v1
2a30974	author	Will Norris
2a30974	added	86
2a30974	deleted	0
2a30974	files	1
2a30974	body	`if (a16v < b16v)` (and <=, >, >=) compiled to a verbose multi-block 8-bit\ncompare chain (cpy high; bne; cpx low across several basic blocks). Under\n+mos-a16 it now compiles to a single 16-bit compare:\n\n  rep #$20; lda a16v; cmp b16v; sep #$20; bcc ...\n\nlegalizeICmp canonicalizes every predicate to EQ/UGE/SLT; the four unsigned\norderings all reduce to UGE (the C flag), which needs no terminator-fusion (C is\na plain i1 the branch reads via bcc/bcs). So under hasAccum16 the legalizer keeps\ns16 UGE UN-narrowed and emits a width-flexible 16-bit G_SBC (reusing the existing\nC-flag -> branch path verbatim). selectSbc16 lowers a 16-bit G_SBC (compare form:\nresult + N/V/Z dead, carry-in set) to `lda lhs; cmp rhs` producing C; both ops are\nMLow=1 so MOSInsertREPSEP brackets them, and SEP leaves C/Z/N untouched.\n\nNew ops CMPImag16/CMPAbs16/CMPImm16 (MLow=1, isCompare, gated HasAccum16). A\nconstant RHS folds to `cmp #imm16` (CMP_Immediate16 is generated by the CC1_All\nmulticlass, like ADC_Immediate16 — thanks to the catch that it does exist). The\naccumulator is entered only via LDAImag16 — no Ac16<->8-bit COPY (the invariant).\n\nTest a16cmp.c: four orderings + a high-byte-differs case (loval=0x0099 < hival=\n0x0200 — low byte bigger, high byte smaller, so a broken low-byte-only compare\nwould get it wrong). Disasm shows 5 16-bit cmp ops, ZERO 8-bit cpx/cpy chain;\ncorpus_result==0x1103 on MAME and bsnes-jg.\n\nSlice 1 only: equality (== !=, Z flag -> CmpBr fusion), signed (N^V), and\ncompare->select are deferred follow-ups (legalizer EQ/SLT paths untouched).\n\nNon-breaking: corpus 7/7; all 13 a16* tests green; SDK builds; patch 0002\nround-trips (applies on 0001, reproduces vendor MOS dir exactly).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
-->
