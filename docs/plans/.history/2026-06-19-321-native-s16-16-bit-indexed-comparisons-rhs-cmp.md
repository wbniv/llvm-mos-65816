| Date | Change |
|------|--------|
| [2026-06-20](https://github.com/wbniv/llvm-mos-65816/commit/9009260) | #321 native s16: RHS-indexed 16-bit compare fold (cmp (zp), CMPIndir16) + compare-surface audit |

<!--history-meta v1
9009260	author	Will Norris
9009260	added	309
9009260	deleted	0
9009260	files	1
9009260	body	Extend (#2) then harden (#1) the native 16-bit unsigned-ordering compares.\n\nThe gap: `limit < arr[i]` (u16) staged the indexed RHS through an Imag16 temp\n(`lda (zp); sta __rc; lda limit; cmp __rc`) — the store survives because the\nintervening `lda limit` clobbers A16, so threadAccum16 can't remove it.\n\nPhase-0 measurement corrected the scope: a u16 `arr[i]` does NOT become\n`lda abs,x` — the *2 element scaling routes it through a computed pointer to\n`lda (zp)` plain-indirect (G_LOAD16_INDIR). So the fold instruction is\nCMPIndir16 (`cmp (zp)`, $D2), not CMPAbsIdx16 — cleaner: no index register,\nhence no X-flag-lattice coupling and no frame-index concern.\n\nFix (all in 0002-321-accum16.patch):\n- CMPIndir16 MC instr: MOSCMP16 + PseudoInstExpansion<(CMP_Indirect addr8:$addr)>,\n  InOperandList (Ac16:$l, Imag16:$addr).\n- foldableIndirLoad16 helper: single-use + same-BB G_LOAD16_INDIR.\n- selectSbc16 RHS arm: folds the indexed load to `cmp (zp)` instead of CMPImag16.\n\nGated (governing lesson #2): single-use + same-BB makes the fold 1-to-1\nvolatile-safe; multi-use / cross-block / non-indirect RHS falls to today's\nCMPImag16, so a misclassification only ever forgoes a win, never regresses.\nLHS-indexed (`arr[i] < limit`) was already optimal; `lim > arr[i]` swaps the\noperand to the LHS (`lda (zp); cmp`). EQ-indexed + the `cmp abs,x`/`(zp),y`\nindexed forms + xy16 16-bit-index are deferred pending a frequency scan.\n\nPhase 3 (harden): new whole-compare-surface differential-audit harness\nexamples/65816/a16cmpaudit.c + dev/a16cmpaudit.sh — 8 predicates x\n{as-value, as-branch} x RHS shapes {reg, imm, global, global-array[k],\npointer[k], stack-array[k]} + LHS-indexed control, over boundary values; host\noracle 0x5EE0 (reproducible via -DHOST_ORACLE, stable -O0..-Os).\n\nRegression guard + verification (all PASS, 2026-06-20):\n- a16cmpidx 0x1111 (5 cmp (zp) folds, no staged sta, no cpx/cpy) both emulators\n- a16cmpaudit 0x5EE0 host==default==+mos-a16 both emulators, verify-clean\n- a16 compare suite 6/6; fuzz 50 1 = 0 mismatch / 0 crash; torture 60/60\n- -verify-machineinstrs clean; 0002 regen round-trips (only the CMPIndir16 hunk)\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
-->
