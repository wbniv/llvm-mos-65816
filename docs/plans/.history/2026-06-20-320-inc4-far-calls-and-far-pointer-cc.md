| Date | Change |
|------|--------|
| [2026-06-20](https://github.com/wbniv/llvm-mos-65816/commit/1b7f297) | #320 Inc 4 Phase 1: far calls (JSL/RTL) + Phase 0 far-pointer store (sta [dp]) |

<!--history-meta v1
1b7f297	author	Will Norris
1b7f297	added	214
1b7f297	deleted	0
1b7f297	files	1
1b7f297	body	The cross-function half of #320 begins. A direct call to a function the linker\nplaced in another 64 KB bank now emits JSL ($22, pushes a 3-byte PBR:PC return)\nand the callee returns via RTL ($6B, pops 3) — while near calls stay JSR/RTS.\n\nBackend (in 0001, HasW65816-gated, a16-independent):\n- New JSL / RTL pseudos mirror JSR / MOSReturn<RTS> (MOSInstrLogical.td).\n- lowerCall swaps JSR->JSL, lowerReturn swaps RTS->RTL, when the callee/function\n  is in a .far_* section and STI.hasW65816() — default 6502 codegen byte-identical.\n- snes-far/link.ld: .far_text -> rom_1 (bank $01).\n- __far = the section attribute (no clang change); caller (JSL) and callee (RTL)\n  are driven by the same attribute, so they always agree.\n\nMVP scope: the far function is a LEAF. A far fn that JSR'd a near fn would keep\nPBR=$01 and run the wrong bank's bytes; mixed-banking, far function pointers, and\nfar tail calls are follow-ups (tail-call opt auto-skips — it keys on MOS::JSR).\n\nPhase 0 closes the Inc 3 sta [dp] store loose end (the store path existed since\nInc 3 but was ungated): far_store.c + dev/far_store.sh.\n\nGates (examples/65816/, dev/):\n- far_call: jsl at the call + rtl in the far leaf, far_leaf @ 0x18000 (bank $01),\n  value 0xF3 crosses the boundary; MAME + bsnes-jg. a16-independent (8-bit args).\n- far_store: disasm 87 (sta [dp]), store-then-read-back 0xF3; MAME + bsnes-jg.\n\nNo regression: corpus 7/7 (near JSR/RTS intact), a16call/a16ret PASS, Csmith 27/30\n(0 mismatch, 0 crash, 0 error), all 5 prior far ROMs PASS on bsnes-jg.\n\nPatch tooling: new dev/regen-patch-0001.sh regenerates the far patch by isolating\nthe far delta from 0002's interleaved a16 hunks and rebasing onto the 0001-only\ntree (one worktree at a time — three at once overflowed /tmp). Round-trip verified:\n0001 a16-free (accum16=0), 0002/0003 untouched, 0002 still stacks on the new 0001.\n\nGotcha recorded: a .far_text linker change needs dev/run.sh build (SDK rebuild),\nnot just toolchain (compiler) — else far code silently orphans into bank $00.\n\nInc 4 Phase 2 (far-pointer calling convention) is next: build all ABI variants and\nmeasure (no longer upstream-gated).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
-->
