| Date | Change |
|------|--------|
| [2026-09-14](https://github.com/wbniv/llvm-mos-65816/commit/b6ab8b5) | fix(snesgfx): gate the first-frame opt-in behind SNESGFX_FIRST_FRAME_OPTIN |
| [2026-09-14](https://github.com/wbniv/llvm-mos-65816/commit/e2f3cd0) | feat(snesgfx): per-drawable "first frame is complete" opt-in for Display |

<!--history-meta v1
b6ab8b5	author	Will Norris
b6ab8b5	added	218
b6ab8b5	deleted	173
b6ab8b5	files	1
b6ab8b5	body	Follow-up to 59ca122, which implemented the opt-in as a runtime-only gate. That was\nbehaviourally invisible but the wrong shape: measured across the 121 Display demos with\na link map, it cost +4,823 bytes of .text, median +39 per demo, 117 of 121 growing --\npaid by every demo to serve the one scene whose drawables all assert. That is the\nblanket-change trap CLAUDE.md lesson 3 names, and the bytes matter here precisely\nbecause the toolchain multiplies them.\n\nThe feature is now opt-in at compile time as well as per drawable. A demo defines\nSNESGFX_FIRST_FRAME_OPTIN before the snesgfx includes; drawable.h defaults it to 0.\nEvery added line -- the Drawable field and its clearing, Display.ff_all and its init,\nthe AND in display_add, the guard in display_frame, and title_layer.h's backdrop write\n-- is inside #if SNESGFX_FIRST_FRAME_OPTIN, so a non-adopting demo preprocesses to the\ncode it compiled before the feature existed. A reserve() that asserts without the macro\nnow fails to compile, which beats a silently ignored claim.\n\nThe four-token order SNESDQ enforces survives both configurations: the guard is an\n#if-wrapped `if (...)` above a single unconditional scene_emit call, so the tokens\nappear once each and in order in the raw file.\n\nTitleLayer now adopts too, macro-gated. Its reserve() wrote palette 7 only while its\nemit() owns CGRAM[0] -- the backdrop its own blank tilemap shows everywhere -- so two\nlines write CGRAM[0] black (matching the value title_begin starts t->back at; picture\nunchanged, determinism improved). That is what lets 1d-ca and life fire the gate at all,\ntheir scenes being {demo drawable, TitleLayer}, and being macro-gated it leaves the ~120\ndemos that include title_layer.h without opting in byte-for-byte untouched.\n\nAdopters: mandel-oop, 1d-ca, life.\n\nVerified (two hardlink worktrees off the same main tip 22f18cb, before/after):\n  - SNESDQ PASS, 241 sites; the backdrop write registered as exactly one new reviewed\n    direct-PPU site (baseline diffed by finding id, not by line)\n  - ROM byte identity: 118/118 non-adopting demos identical, only the 3 adopters move.\n    .text: mandel-oop -44 (deleted latch outweighs the gate), 1d-ca +71, life +97 --\n    net +124 in three demos, against +4,823 across 117\n  - dev/bootblank.sh --firstframe: ZERO new nondeterministic demos\n  - dev/bootblank.sh: 0 of 117 boot windows moved; life 362 -> 361 at FRAMES=500\n  - dev/m7blank.sh --probe mandel-oop 5 -> 5 WITH the Option J latch deleted (the proof\n    the gate fires); --gate all 12 demos ok\n  - dev/run.sh mandel-oop 0x204F (1 indirect dispatch), life 0xDDF1, 1d-ca 0xAB2C --\n    all host == +mos-a16@bsnes-jg == @MAME\n\nNoted, not claimed: two byte-identical demos (mvscrl, qsortviz) flipped --firstframe\nverdict between runs. Their ROMs provably did not change, so that is the instrument's\ndocumented two-draw stochasticity; mandel-oop's flip in the same run is therefore\nreported as indistinguishable from noise rather than as a fix.\n\nPlan: docs/plans/2026-09-14-display-first-frame-optin.md\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_011AP736JtwzSGYH4bmxDWTa\n(cherry picked from commit 73e7fe2cec5a0e0487ab71bde1ea59e7599fbbc3)
e2f3cd0	author	Will Norris
e2f3cd0	added	370
e2f3cd0	deleted	0
e2f3cd0	files	1
e2f3cd0	body	The safe shape of the blanket first-frame change rejected on 2026-08-05, implemented\nas that plan's own closing recommendation.\n\nDrawable gains first_frame_complete: a correctness claim a reserve() may set to assert\n"everything my first visible frame shows -- tilemap, chr, AND the CGRAM entries those\ntiles index -- I painted myself inside the boot force-blank; nothing it depends on\narrives through the UploadQueue". drawable_reserve() clears it immediately before\ndispatch, so the default is 0 for every construction path (static, automatic,\ndesignated initialiser, memset, ad-hoc demo-local init) and no drawable can opt in by\naccident.\n\nDisplay ANDs it into ff_all in display_add -- once per drawable, not per frame -- and\ndisplay_frame guards the FIRST scene_emit on it, so a scene in which every drawable\nasserts gets a release-only first frame and comes up with reserve()'s content one frame\nearlier (more where the first emit is expensive). One non-asserting drawable disables it\nfor the whole scene, which is the default: 119 of the 122 Display demos deliver their\npalette via upq_push_cgram from emit(), and that is exactly why the blanket version\nregressed five demos.\n\nIt is a GUARD, never a reorder: snes-display-quality.py's emit -> wait-vblank -> flush ->\nmasked-brightness order (the encoded 1dd9317 lesson) is untouched, no INIDISP write is\nadded or removed, and the skipped frame flushes an empty queue.\n\nAdopters: MandelLayer (mandel-oop), CaDisplay (1d-ca), LifeGrid (life). mandel-oop also\nloses its demo-local Option J latch -- with the gate skipping call 1, a latch would have\nswallowed frame 2's compute as well.\n\nVerified (two hardlink worktrees off the same main tip, before/after):\n  - SNESDQ PASS, 240 sites, order invariant intact\n  - dev/bootblank.sh --firstframe, 117 demos: ZERO new nondeterministic first frames\n    (the same 4 flag on the unmodified baseline)\n  - dev/bootblank.sh: 0 of 117 boot force-blank frame counts moved\n  - dev/m7blank.sh --probe mandel-oop 5 -> 5 WITH the latch deleted -- the proof the\n    gate fires; --gate all 12 demos ok\n  - dev/run.sh mandel-oop: corpus_result 0x204F host == +mos-a16@bsnes-jg == @MAME,\n    -verify clean, exactly 1 indirect dispatch\n\nKnown deviation, recorded in the plan: non-adopting ROMs are NOT byte-identical. A\nruntime gate compiles into every Display demo (delta is tens of bytes, both signs);\nbehavioural identity carries the safe-by-default claim instead.\n\nTitleLayer deliberately does not adopt: its reserve() writes palette 7 only, while its\nemit() owns CGRAM[0]. A measured scratch probe (in the plan) shows the two-line fix\nwould be worth -1 frame on life and 0 on 1d-ca -- a follow-up, not a bundled extra.\n\nPlan: docs/plans/2026-09-14-display-first-frame-optin.md\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_011AP736JtwzSGYH4bmxDWTa\n(cherry picked from commit 59ca1226795cdb92f0bfeb888050bb60463ac9e8)
-->
