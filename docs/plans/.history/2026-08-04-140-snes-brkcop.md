| Date | Change |
|------|--------|
| [2026-08-04](https://github.com/wbniv/llvm-mos-65816/commit/304f3c3) | demo(140-brkcop): split the BRK/COP software vectors and gate them |

<!--history-meta v1
304f3c3	author	Will Norris
304f3c3	added	214
304f3c3	deleted	0
304f3c3	files	1
304f3c3	body	Round 7 #140 "Software Vectors" — clean positive, gate hash 0xA34C.\n\nPlatform defect this starts from: every SNES platform wired $FFE4 COP to the\nliteral address $0000 and $FFE6 BRK to the shared `irq` symbol. A native-mode\n`cop` therefore jumped to bank-0 $0000 and executed the imaginary-register page\nas code (silent corruption), and a `brk` was indistinguishable from a hardware\nIRQ. Neither vector had ever fired — `nmi` is the only one this platform has\never exercised.\n\nFix: weak `brk:`/`cop:` rti stubs in the shared crt0 (alongside `irq:`/`nmi:`),\nand dedicated vector slots in all five platform link scripts (native $FFE4/$FFE6,\nplus emulation-mode $FFF4 COP which was also $0000). ABORT stays $0000 — the pin\nis not connected on the SNES; noted in the plan rather than changed silently.\n\nThe demo overrides both stubs with real C __attribute__((interrupt)) handlers and\nfires BRK and COP each from a rep #$30 (M=0/X=0) bracket and from a sep #$30\nbracket, 16 rounds. Native BRK/COP push PC+2, so RTI must resume past the\nsignature byte: a poison-guard immediate load sits exactly there and its value is\nfolded into the gate hash, so a one-byte resume error (landing on $42 = WDM, or\n$5A = PHY) changes the sentinel or desyncs the stream.\n\nGate (dev/run.sh brkcop): host == default == +mos-a16 == +mos-xy16 == 0xA34C on\nbsnes-jg, a16 3x deterministic; non-vacuous -verify-machineinstrs; the #123\ninterrupt envelope asserted in order on BOTH handlers; nmitally-isr-gate.py\n--symbol brk/cop; and a ROM-image vector gate proving no COP/BRK/NMI/IRQ slot\nreads $0000 and that COP/BRK equal the C handler addresses.\n\nAssembler gap found in passing (not a codegen defect, out of scope): the MOS\nassembler has no `cop` mnemonic and no signature operand for `brk`, so both traps\nare emitted as explicit bytes.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
-->
