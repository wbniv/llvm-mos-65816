| Date | Change |
|------|--------|
| [2026-08-04](https://github.com/wbniv/llvm-mos-65816/commit/f8c937db) | fix(dev): make -verify-machineinstrs real in demo gate scripts (-fno-lto + objdump check) |
| [2026-08-04](https://github.com/wbniv/llvm-mos-65816/commit/c9e0f127) | feat(snes): #123 nmitally — record the published demo files + review hardening |
| [2026-08-03](https://github.com/wbniv/llvm-mos-65816/commit/5b80b027) | feat(snes): #123 nmitally — the interrupt CC's first run finds a 65816 width bug |

<!--history-meta v1
f8c937db	author	Will Norris
f8c937db	added	46
f8c937db	deleted	0
f8c937db	files	1
f8c937db	body	Under mos-snes.cfg's default LTO, `mos-clang --config ... -c` (or a full\n`--config ...` link) emits LLVM IR bitcode / never forwards -mllvm to the LTO\nbackend, so -mllvm -verify-machineinstrs on those invocations verified\nNOTHING -- a silent always-PASS gate. Found on wt/321-nmitally and fixed\nthere in dev/nmitally.sh; this sweeps the rest of the battery.\n\n21/23 genuine offenders fixed (dev/_demo5.sh already carried the fix from an\nearlier session under this same TODO item) plus the /snes-demo skill\ntemplate's §6 disasm-probe example. Each fix either adds\n-mllvm -verify-machineinstrs to an existing non-LTO --target=mos -c compile\nalready used for the disasm gate, or (4 scripts with no such compile) adds a\nnew --config ... -fno-lto -mllvm -verify-machineinstrs -c block. Every fix\nis followed by an `llvm-objdump -h` check proving the emitted file is a real\nobject, not bitcode -- proven load-bearing both directions: the old\n--config -c pattern (no -fno-lto) emits bitcode (magic 42 43 c0 de) that\nobjdump -h correctly rejects; the fixed pattern emits a real elf32-mos\nobject that objdump -h accepts.\n\n2 scripts (dev/blossom.sh, dev/mandel-oop.sh) left vacuous and escalated: a\nreal verify leg there trips the already-known a16-rc-undef-ra-pure-virtual\nMachineVerifier false-positive (same signature as the mandel-double/gouraud\nDone entries -- differential-proven correct, not a miscompile). Landing the\nfix needs XFAIL-aware wiring into tools/a16_fuzz.py's KNOWN_ISSUES registry,\nwhich is a design decision outside a bounded per-script edit.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
c9e0f127	author	Will Norris
c9e0f127	added	52
c9e0f127	deleted	0
c9e0f127	files	1
c9e0f127	body	Records on main the published #123 VBlank Interrupt Tally demo (ROM source, shared\nmodel header, host oracle, gate script, plan, and the interrupt-width docs) — the\ncondensed iteration that built the live biohack.net/indri.studio ROM (SHA a2e40ea7…).\n\nReview hardening from today's session:\n- ISR structural gate is now order-aware (asserts the exact rep #$30 save/restore\n  envelope, which the pre-fix ISR's opcode *presence* would have satisfied) and runs\n  on the linked post-LTO nmitally-a16.sfc.elf instead of a separate -fno-lto object.\n- EXPECT extraction anchored on gate_crc; VMA lookup hoisted; neutral FAIL message.\n- Docs reframed: missing a16 interrupt-entry contract (unfinished port work with\n  defect-severity, silent-miscompile stakes), not a latent llvm-mos bug.\n\nPrecedes the wt/321-nmitally merge that preserves the original richer iteration's\nhistory and brings over its branch-only harness files.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
5b80b027	author	Will Norris
5b80b027	added	662
5b80b027	deleted	0
5b80b027	files	1
5b80b027	body	The battery's first __attribute__((interrupt)) handler. An NMI ISR advances\n16/32-bit counters behind an arm/service/clear frame fence while the main loop\nruns +mos-a16 arithmetic; the CRC folds 240 fenced ticks and the host oracle is\nfed the same N.\n\nFOUND A REAL COMPILER DEFECT — the 65816 interrupt prologue/epilogue is not\nwidth-aware. Two obligations, both unmet:\n\n  B1 no entry re-establishment. Native-mode interrupt entry pushes PB/PC/P, sets\n     I, clears D — and leaves M/X as found. The prologue's `pha` and its 1-byte\n     immediates were sized assuming M=1, so preempting a `rep #$20` region\n     desynchronises the instruction stream.\n  B2 A/X/Y saved at the ABI width. An 8-bit `pha` cannot preserve a 16-bit A,\n     and the handler's own `rep #$20; lda` destroys the high byte (B) — silent\n     corruption of the interrupted routine.\n\nMeasured, on the SAME source:\n  default 8-bit   host == 0xBCE6 on bsnes-jg AND MAME       (the control: works)\n  +mos-a16        black screen, corpus_result never seals, 3/3\n  +mos-xy16       likewise; MAME agrees (0x0034 on the a16 build)\n  -verify-machineinstrs clean under both features, on the ISR-bearing TU\n  corpus slice (same arithmetic, no interrupts) 5-way PASS 0xBCE6\n\nLegal MIR, wrong machine code — the Cluster-B class value gates miss. The demo\nis NOT reshaped to make the gate green; it stays red until the backend is fixed,\nso it is deliberately NOT published.\n\nAlso lands:\n- tools/nmitally-isr-gate.py, the mandatory ISR disasm gate (A shape / B1 entry\n  width / B2 save width / C0 soft-stack-pointer balance / C Imag save-restore),\n  derived from the real disassembly.\n- examples/65816/a16isr.c + dev/a16isr.sh — a four-line minimal repro with a\n  disasm-only, emulator-free standing gate for the eventual fix.\n- SECONDARY FINDING: `mos-clang --config … -c` defaults to LTO and emits bitcode,\n  so `-mllvm -verify-machineinstrs` silently verifies NOTHING. dev/_demo5.sh and\n  the /snes-demo template are both affected. dev/nmitally.sh now passes -fno-lto\n  and asserts a real object was emitted so the leg cannot go vacuous again.\n\nFix belongs in MOSFrameLowering::spill/restoreCalleeSavedRegisters (REP #$30 ->\nPHA/PHX/PHY -> SEP #$30 after the CLD, mirrored on the way out) plus\nMOSInsertREPSEP's "entry block is 8-bit by ABI" pin. Not applied here — this\nworktree's toolchain is hardlinked and cannot rebuild.\n\nPlan: docs/plans/2026-08-03-123-snes-nmitally.md\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
-->
