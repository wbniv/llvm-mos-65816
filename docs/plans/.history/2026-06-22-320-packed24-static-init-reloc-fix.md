| Date | Change |
|------|--------|
| [2026-06-22](https://github.com/wbniv/llvm-mos-65816/commit/a76bf18) | #320 packed-24: fix static-init relocation so far-ptr TABLES link (the real use case) |

<!--history-meta v1
a76bf18	author	Will Norris
a76bf18	added	110
a76bf18	deleted	0
a76bf18	files	1
a76bf18	body	packed-24 Increment B (0006) was verified only for the RUNTIME store/load/deref path.\nTask A (measure the win, dev/run.sh measure-packed24) surfaced that the realistic\nshape — a statically-initialized table of packed far pointers (a compact jump/asset\ntable, the whole point of the feature) — did NOT link: each 3-byte AS_FarPacked entry\nemitted a single R_MOS_ADDR8 (the low byte only) and overflowed.\n\nRoot cause: a static (AS_FarPacked*)&far_sym is a ConstantExpr emitted via the generic\nAsmPrinter leaf as emitValue(ME, 3), but there is no 3-byte data fixup\n(MCFixup::getDataKindForSize(3) is unreachable -> degrades to FK_Data_1/R_MOS_ADDR8).\n0006 only handled the runtime GISel path, never the static-data AsmPrinter path.\n\nFix: a generic AsmPrinter::emitNonStandardSizedConstant hook (default no-op) called in\nemitGlobalConstantImpl's leaf, with a MOS override that, gated strictly on\nSize==3 && AS_FarPacked, emits the three address bytes with the ADDR24\nSEGMENT_LO/SEGMENT_HI/BANK relocations (lld writes one byte each) -> the full 24-bit\naddress, bank byte intact. The specifier must ride on the MCSymbolRefExpr itself\n(retagFarByte), mirroring the jump-table low/high-byte emission -- wrapping in MOSMCExpr\nloses it (evaluateAsRelocatableImpl drops the variant on a relocatable result). regen-\npatch-0006.sh's PACKED_FILES extended with the two generic AsmPrinter files (they live\noutside llvm/lib/Target/MOS, so a regen would otherwise silently drop the fix).\n\nVerified (wt/320-packed24-incB, off post-F2 main):\n- dev/run.sh packed24_table (new): static 8x3=24 B table vs 8x4=32 B far (-25%), links,\n  walks 8 bank-$01 entries, sums == 0xA5 on MAME AND bsnes-jg (each static entry's bank\n  byte survives); relocs are the ADDR24 SEGMENT_LO/HI/BANK triple per entry.\n- packed24 runtime e2e still 0xF3; corpus 7/7; csmith fuzz 45/50, 0 mismatch / 0 crash\n  (the gate for the generic AsmPrinter touch -- the hook only fires for AS_FarPacked,\n  which csmith never emits); -verify-machineinstrs clean.\n- 0006 regen round-trips (0001..0006 reproduces every packed-24 file). 0001-0005 SHAs\n  unchanged.\n\nTask A verdict (dev/run.sh measure-packed24): packed-24 wins ~N bytes at every table\nsize, break-even N>=1 -- in an indexed table walk the access code is EQUAL (far loads\nonly 3 of its 4 entry bytes; the x3-vs-x4 stride is a constant), so the feared\nx3-index/byte-2-long cost does NOT apply to indexed table access (only to direct\nsingle-slot access, handoff Task B -- now marginal). The -25% storage win is realizable.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
-->
