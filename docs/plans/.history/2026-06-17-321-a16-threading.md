| Date | Change |
|------|--------|
| [2026-06-17](https://github.com/wbniv/llvm-mos-65816/commit/eaeab18) | #321 docs: record A16-threading in the ROADMAP step-5 narrative; finalize the plan |
| [2026-06-17](https://github.com/wbniv/llvm-mos-65816/commit/c66329b) | #321 A16-threading Phase 1.5: relax the redundant-reload peephole beyond strict adjacency |
| [2026-06-17](https://github.com/wbniv/llvm-mos-65816/commit/fc3dc29) | #321 A16-threading: measure what Phase 1 left -> Phase 2 retired (fold-while-threaded already optimal) |
| [2026-06-17](https://github.com/wbniv/llvm-mos-65816/commit/1a2145e) | #321 A16-threading Phases 0-1: eliminate the redundant Imag16 round-trip (coalescer-free) |
| [2026-06-17](https://github.com/wbniv/llvm-mos-65816/commit/2715579) | #321 plan: A16-threading — reframe the step-5 "biggest win" as redundant-reload elimination |

<!--history-meta v1
eaeab18	author	Will Norris
eaeab18	added	12
eaeab18	deleted	6
eaeab18	files	1
eaeab18	body	ROADMAP.md: add the A16-threading increment to the step-5 chronology — the\n"biggest win" reframed as redundant store/reload elimination and landed\ncoalescer-FREE (Phases 0/1/1.5), Phase 2 retired (fold-while-threaded already\noptimal), the RA-level Ac16-residency form (Tier 2, the actual coalescer-crash\ncore) deferred since the single 65816 accumulator caps its realizable gain.\n\nPlan: coherence pass now that 0/1/1.5 are done — correct the micro-test note (the\nreuse variant exercises the keep-store path, not "Phase 3's domain") and the Risks\ngate (the final guard is modifiesRegister(A16)+home-not-overwritten+call/asm-bail,\nnot strict adjacency; the fuzzer over volatile globals is what validated the\nacross-volatile-store and multi-reload paths).\n\nDocs-only.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
c66329b	author	Will Norris
c66329b	added	43
c66329b	deleted	23
c66329b	files	1
c66329b	body	Generalize threadAccum16 (MOSLateOptimization.cpp) so the STAImag16 -> LDAImag16\npair need not be adjacent. Intervening instructions are allowed if none rewrites\n$a16 (modifiesRegister(MOS::A16) catches 8-bit sub-A writes + xba; calls and\ninline-asm bail) or overwrites the home. A volatile STORE between is fine -- it\nonly reads $a16, so the store stays put and only the non-volatile reload of the\nImag16 temp drops (a volatile LOAD writes $a16 and is caught). Tracks pending\nstores in a small set, notes intervening reads of the home (keeps a still-needed\nstore), handles the multi-reload case (a kept store reloaded more than once keeps\nits pending entry until the store is actually erased), and clears stale $a16 kills\nacross the gap.\n\nMeasurement honesty (measure, don't assume): the opportunity was first mis-sized\nat ~40/300 programs. A mode-aware scan removed the byte-assembly false positives\n(sta lo; stx hi; rep #32; lda assembles a 16-bit value, not a reload); then\ncounting ALL lda (not just lda __rcN) as A16 clobbers removed the indirect/abs-load\nfalse positives (lda (zp)/lda abs write A16, making the next reload genuine). The\nTRUE non-adjacent opportunity was ~14, of which Phase 1.5 captures all but 1/300.\n\nVerified: a16 suite + kernels 47/47, corpus 7/7, fuzz 50/50 (0 mismatch/crash/\nerror), -verify-machineinstrs clean over 47 examples + 150 fuzz programs\n(exercising volatile-store threading), 0002 round-trips (carries threadAccum16 +\nisInlineAsm, zero TXY/TYX -- F4 stays isolated in 0003).\n\nPhase 2 (fold-while-threaded) retired -- already optimal (interior immediates and\nnear-abs globals fold into the threaded chain today). Phase 3 (RA-level Ac16\nresidency) stays deferred (gain capped by the single accumulator + coalescer risk).\n\nBackend source rides patches/llvm-mos/0002 (vendor/ gitignored; dev/regen-patch.sh).\nPlan: docs/plans/2026-06-17-321-a16-threading.md\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
fc3dc29	author	Will Norris
fc3dc29	added	42
fc3dc29	deleted	15
fc3dc29	files	1
fc3dc29	body	Per the "measure, don't assume" lesson, scanned 300 fuzz-generated programs\n(a16_fuzz.py gen, +mos-a16 -Os), mode-aware-classifying each 16-bit `lda __rcN`\nreload, plus targeted disasm of fold shapes:\n\n- Adjacent 16-bit round-trips remaining: 0 (Phase 1 fully captured its target).\n- Fold-while-threaded: ALREADY OPTIMAL. `t=a+b; u=t+0x0345; r=u^0x00FF` ->\n  `lda a; clc; adc b; clc; adc #837; eor #255; sta r`, and `t=a+b; u=t+g; r=u-h`\n  -> `...adc g; sec; sbc h; sta r`. Interior immediates AND near-abs globals fold\n  into the threaded chain today -- the existing selection-time folds compose with\n  the Phase-1 peephole for free. So Phase 2's stated rationale is already done.\n- Non-adjacent removable (relax-adjacency): ~40/300 (~0.13/program). Small. The\n  raw store->reload count is dominated by the byte-assembly non-pattern\n  (sta lo; stx hi; rep #32; lda -- assembling a 16-bit value, NOT a reload); the\n  mode-aware filter removes it, hence ~40 not the ~1180 a naive scan reports.\n- "Genuine reloads" (A16 clobbered): large but mostly unrecoverable -- same\n  byte-assembly contamination, and the residue is capped by the single 65816\n  accumulator (two live 16-bit values must spill to Imag16).\n\nRevised disposition: Phase 1 captured the realizable peephole win. Phase 2\nSUPERSEDED (not pursued). Optional Phase 1.5 (relax adjacency, ~40/300, low-risk).\nPhase 3 (RA-level Ac16 residency) stays deferred -- gain capped by the single\naccumulator + the coalescer-crash risk.\n\nDocs-only (no vendor change). Plan + TODO updated.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
1a2145e	author	Will Norris
1a2145e	added	73
1a2145e	deleted	21
1a2145e	files	1
1a2145e	body	The ROADMAP-step-5 "biggest win" (value stays live in the accumulator across ops),\nde-risked by the Tier-1 corpus. Each native s16 op is self-contained\n(LDAImag16 -> OP -> STAImag16, value home = Imag16 between ops, the 1d-retry\ncoalescer-safe invariant), so a dependent chain stores each intermediate and\nimmediately reloads it (sta __rcN; lda __rcN).\n\nPhase 0 (measure): 20 such round-trips across the test set (12 synthetic, 8 in\nreal kernels) -- dev/measure-a16-threading.sh.\n\nPhase 1 (the fix): new post-RA peephole threadAccum16 in MOSLateOptimization.cpp.\nWhen `$rsN = STAImag16 $a16` is immediately followed by `$a16 = LDAImag16 $rsN`,\nerase the redundant reload ($a16 already holds the value) and DCE the dead store,\nso the value threads through $a16 across the chain (lda;adc;and;sbc;...;sta).\nPost-RA on purpose: RA has already chosen $a16 for both sides, so collapsing the\npair cannot reintroduce the 1d coalescer crash (an 8-bit value coalescing into\nA16's sublo). LDAImag16/STAImag16 model no NZ def -> erasing them is flag-safe;\nstrict adjacency keeps it conservative (a missed non-adjacent case only forgoes a\nwin, never miscompiles).\n\nMeasured (+mos-a16 -Os): all 20 round-trips -> 0; chain3 39->27 B (-31%),\nchain5 55->35 (-36%), k_crc16/k_prng/k_bits/k_isort -4/-8/-8/-10 B.\n\nNew examples/65816/a16thread.c + dev/a16thread.sh (0 round-trips, 1 rep/sep\nbracket, corpus_result 0x2544 host==default==+mos-a16 on MAME + bsnes-jg).\n\nNon-breaking: a16 suite + kernels 47/47, corpus 7/7, fuzz 50/50 (0 mismatch/\ncrash/error), -verify-machineinstrs 47/47 clean (incl. a16localx, the\ncoalescer-crash guard), 0002 round-trips (carries threadAccum16, zero TXY/TYX --\nthe F4 fix stays isolated in 0003). Phases 2 (selection-time fusion) + 3\n(RA-level Ac16 residency) remain.\n\nBackend source rides patches/llvm-mos/0002 (vendor/ gitignored; dev/regen-patch.sh).\nPlan: docs/plans/2026-06-17-321-a16-threading.md\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
2715579	author	Will Norris
2715579	added	147
2715579	deleted	0
2715579	files	1
2715579	body	Plans the deferred ROADMAP-step-5 A16-threading work (keep the running s16 value\nlive in the accumulator across ops), now de-risked by the Tier-1 corpus.\n\nGrounded in a real measurement: today every native s16 op is self-contained\n(LDAImag16 -> OP -> STAImag16, value home = Imag16 between ops, the 1d-retry\ncoalescer-safe invariant), so a dependent chain (t=a+b; u=t&c; r=u-d) stores each\nintermediate and immediately reloads it -- 3 redundant `sta __rc2; lda __rc2`\nround-trips, 0x27 B -> ~0x1B threaded (-30%).\n\nKey reframing from the MIR: each STAImag16 result is single-use, read only by the\nnext LDAImag16, with A16 unchanged between -- so the win is REDUNDANT STORE/RELOAD\nELIMINATION, which touches neither the coalescer nor Ac16-across-ops liveness. The\nfeared RA-level Ac16 residency (the 1d `$a16 = LDImm` coalescer crash) is demoted to\na gated last resort.\n\nFour phases, each gated on measured remaining win: (0) size the prize; (1)\ncoalescer-safe redundant-reload peephole; (2) selection-time threading fusion\n(generalize the chain machinery to heterogeneous dependent trees); (3) RA-level\nresidency via a shouldCoalesce barrier (MOSRegisterInfo.cpp:669 already forbids\ncoalescing for other patterns), only if (1)-(2) leave significant wins.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
-->
