| Date | Change |
|------|--------|
| [2026-06-16](https://github.com/wbniv/llvm-mos-65816/commit/69827b5) | #321 native s16 Tier 1: differential corpus + 2 backend fixes it found |

<!--history-meta v1
69827b5	author	Will Norris
69827b5	added	213
69827b5	deleted	0
69827b5	files	1
69827b5	body	Tier 1 of the #321 native-s16 roadmap — a differential test corpus that\nde-risks the two biggest remaining wins (Tier 2 A16-threading, Tier 3\npeephole unification), both gated behind "a broad corpus." Each program's\nvolatile `corpus_result` must agree across host-computed == default (trusted\n8-bit) == +mos-a16, on BOTH MAME and bsnes-jg, plus -verify-machineinstrs.\n\nDeliverables:\n- tools/a16_fuzz.py + dev/run.sh fuzz [N] [seed]: a seeded generator of\n  random, strictly-UB-free C over mixed 16/8-bit vars and the full +mos-a16\n  operator set (+ - & | ^ << >>(const) == != < <= > >=, if/else, bounded\n  loops, noinline calls, arrays/pointers). Compiles each twice, runs both on\n  both emulators, asserts all agree. Python evaluator validated 500/500 vs\n  host gcc; delta-reduced triage; XFAIL-classified known issues. fuzz 50 1 ->\n  42/50 PASS + 8 xfail, 0 mismatch/crash/error.\n- 6 realistic kernels (k_crc16 0x29B1, k_fxmul, k_prng, k_bits, k_satadd,\n  k_isort) + 2 combinatorial tests (a16mix1/2): committed deterministic\n  regressions holding many 16-bit values live at once.\n\nThe corpus immediately found three real +mos-a16 defects:\n- FIXED (MOSInstrLogical.td): 16-bit asl/lsr clobber the carry but didn't\n  model it, so the scheduler placed ASLAcc16 between a `cmp` and its `bcs`,\n  corrupting the branch carry -> CRC16 miscompiled to 0x036D (vs 0x29B1, both\n  emulators agreeing). `let Defs = [C]` on ASLAcc16/LSRAcc16. Regress k_crc16.\n- FIXED (MOSLegalizerInfo.cpp): a signed `>>` by amount >= 8 hung the\n  compiler (the >=8 byte path built an s16 ICMP_SLT for the sign fill, which\n  under +mos-a16 re-enters the signed-compare-as-value legalization and\n  loops). Compute the sign fill with an 8-bit AShr broadcast instead.\n  Regress a16ashift8.\n- DEFERRED (XFAIL): a 16-bit compare result consumed as a cross-block i1\n  value materializes `SelectImm $a16,...` (GPR where a flag reg is required)\n  -> invalid MIR/segfault. Squarely the tracked compare->stored-bool /\n  select-NZ-lowering follow-up; minimized repro committed under\n  examples/65816/known/, signature XFAIL-classified so the suite stays green.\n\nNon-breaking: full a16 suite + 6 kernels + 2 combinatorial = 40/40 on a quiet\nbox, corpus 7/7, patch 0002 round-trips (20 files). Harness also hardened to\nretry environmental compile timeouts and surface a persistent one as a crash.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
-->
