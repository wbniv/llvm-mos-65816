| Date | Change |
|------|--------|
| [2026-07-26](https://github.com/wbniv/llvm-mos-65816/commit/7fd4a93) | docs: record rebase verification results + two findings from the rebuild |
| [2026-07-26](https://github.com/wbniv/llvm-mos-65816/commit/5ad343f) | patches+dev: rebase fork patch stack onto upstream tip; fix toolchain bootstrap |

<!--history-meta v1
7fd4a93	author	Will Norris
7fd4a93	added	35
7fd4a93	deleted	2
7fd4a93	files	1
7fd4a93	body	Verification (honest status, written into the plan):\n  - toolchain: PASS. Cold `rm -rf vendor/llvm-mos` + `dev/run.sh toolchain`\n    builds clean against upstream tip 8be054612 on the corrected stack.\n  - 4-way differential: NOT RUN. Neither emulator leg exists on this box --\n    bsnes-jg needs a vendored+built vendor/bsnes-jg (no fetch script; manual\n    prereq, absent here) and MAME needs the SPC700 IPL, which is copyrighted\n    Nintendo firmware ("supply out-of-band"). This is a real gap, not a pass;\n    the rebased stack is NOT differentially validated yet.\n  - cpu6502 gate: PARTIAL PASS. Host oracle recomputes 0xAC8A (identical to the\n    2026-07-02 pre-rebase value) and the disasm probe reproduces the recorded\n    shape exactly (jmp_table=4, rep/sep=97), so the 256-entry switch still\n    lowers to a real jump table. But the gate's trailing "host==+mos-a16" is\n    printed unconditionally -- with both emulator legs skipped, no +mos-a16\n    value was observed at runtime. Noted in the plan so it isn't misread later.\n\nTwo findings filed as TODO items:\n  - mandel-double now overflows the 32 KiB bank by 2904 B on the rebased\n    toolchain. It has always been on the edge (#33 records an earlier 2289 B\n    overflow fixed by const-folding); newer upstream LLVM pushes it back over.\n    Impact is wider than one demo: dev/build.sh's ROM loop runs under set -e,\n    so this aborts the remaining 56 examples (mandel-float..wireframe) --\n    worth making that loop continue-on-error regardless.\n  - corpus_result moved WRAM 0x70 -> 0xadd (confirmed in cpu6502.map). Benign\n    layout change, but it is the offset any --selfcheck wiring must use now.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
5ad343f	author	Will Norris
5ad343f	added	166
5ad343f	deleted	0
5ad343f	files	1
5ad343f	body	A from-scratch `dev/run.sh toolchain` (needed to build the #102 cpu6502 demo ROM\non a checkout with no prior build/) failed: the bootstrap's blind\n`for p in patches/llvm-mos/*.patch` apply loop no longer works. Two independent\ncauses, plus one of my own making found while fixing them.\n\n1. Upstream merged two of our contributions, so their patches no longer apply\n   (the fix is already present):\n     - 0003-late-opt-txy-dead-flag  -> PR #562, upstream commit 9142aebae\n     - 0008-mos-dp-arg-cc           -> PR #563, upstream commit 8be054612\n                                       (also auto-closed issue #561)\n   Both patch files retired, per the "drop once merged + the vendor pin bumps"\n   note already carried in upstream-contribution-status.md. Their rows there are\n   flipped to MERGED. NB: discovered by inspecting a fresh upstream clone, not by\n   a live `gh` query (gh is unauthenticated here) -- flagged as such in the doc so\n   someone re-runs dev/upstream-status.sh for a verified snapshot.\n\n2. Pre-existing, independent of drift: 0002 is regenerated by dev/regen-patch.sh\n   as an rsync mirror of the live llvm/lib/Target/MOS dir, so it already carries\n   every MOS-backend-dir hunk that 0004-0015 add. Re-applying those on top\n   double-inserts and fails against ANY base. This went unnoticed because the\n   normal workflow keeps vendor/ warm and snapshots via regen-patch*.sh; the\n   cold-clone path was last exercised when the stack was 0001-0007.\n\n3. CRITICAL, and initially got this wrong: 0002's mirror covers ONLY\n   llvm/lib/Target/MOS/. Hunks outside that dir are NOT in it. 0006 adds the\n   generic AsmPrinter::emitNonStandardSizedConstant virtual hook (AsmPrinter.h +\n   the emitGlobalConstantImpl call site) that 0002's MOSAsmPrinter.cpp overrides,\n   so omitting 0006 breaks the build with "only virtual member functions can be\n   marked 'override'". First diagnosis blamed an upstream API removal; that was\n   wrong -- the hook is ours and had simply never been applied. Re-derived\n   mechanically instead of by symbol-grep: exactly two patches touch anything\n   outside the MOS dir (0001, 0006); 0015's only such file is a lit test.\n\nBuild-time stack is now explicit in dev/toolchain.sh:\n    0001 -> 0002 -> 0006 (non-MOS hunks only, git apply --include=) -> 0016 -> 0017\n0004-0015 stay on disk untouched as standalone, individually reviewable\nupstream-PR artifacts (several are still-unposted PR drafts) but are excluded\nfrom the build loop. The script comments spell out the MOS-dir-only nature of\n0002's mirror so this doesn't silently rot again.\n\n0002's one genuine conflict hand-resolved: its MOSCallingConv.td hunk re-inserted\nthe AS1 direct-page CC rule that PR #563 landed upstream; that block is now\ncontext rather than an addition (functionally identical -- same register pool,\ncomment wording differs only).\n\nVerified: fresh `rm -rf vendor/llvm-mos` + `dev/run.sh toolchain` builds clean\nagainst upstream tip 8be054612. Differential gate (xcheck, cpu6502) still to run.\n\nDeliberately NOT run: dev/regen-patch.sh. It would mirror 0016+0017 (both in\nMOSLegalizerInfo.cpp) into 0002 and make the build list double-apply them.\n\nFollow-up filed in TODO.md: 11 of 12 per-patch regen-patch-000N.sh scripts\nhardcode applying the now-deleted 0003/0008 and will fail until updated.\n\nPlan: docs/plans/2026-07-25-llvm-mos-fork-patch-stack-upstream-rebase.md\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
-->
