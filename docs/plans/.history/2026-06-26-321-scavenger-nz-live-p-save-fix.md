| Date | Change |
|------|--------|
| [2026-06-26](https://github.com/wbniv/llvm-mos-65816/commit/a320cbd) | #321: fix +mos-a16/+mos-xy16 register-scavenger crash ($p is not a GPR) — patches 0011 + 0012 |

<!--history-meta v1
a320cbd	author	Will Norris
a320cbd	added	139
a320cbd	deleted	0
a320cbd	files	1
a320cbd	body	The 8/500-seed +mos-a16 register-scavenger crash (a16scavnz.c; family\n169/173/196/268/271/272/306/420), previously deferred as an upstream\nissue-with-no-fix, is now FIXED — two pristine-upstream fork patches.\n\n0011-mos-scavenger-live-p-save: MOSRegisterInfo::saveScavengerRegister assumed\nN/Z dead at every scavenge point AND that a live $p is only preserved across a\npush/pull-*balanced* range. Both break under 16-bit-accumulator flag live\nranges: a 16-bit compare keeps N (or Z) live across a frame-index\nmaterialization whose carry the scavenger places in $c (a sub-register of $p),\nforcing the whole $p preserved across an *unbalanced* range -> illegal\n`STImag8 $p` ($p is not a GPR) + an undefined-$p `PH $p`. Fix: for the\nunbalanced case, route $p hard-stack-neutrally through a dead 8-bit index\nregister into the reserved RC17 slot (PHP;PL<idx>;ST<idx> RC17 /\nLD<idx> RC17;PH<idx>;PLP) — each half net-0 on the hard stack, so independent of\nthe imbalance; width-safe because MOSInsertREPSEP runs after scavenging and\nforces index ops to XW_X8 even under +mos-xy16. Also flag the no-reaching-def\nPHP $p undef (verifier), drop the stale assertNZDeadAt (its premise is the false\ninvariant — flag preservation is holistic via the scavenger's interleaved\nP-saves), and widen canSaveScavengerRegister(P).\n\n0012-mos-ldcimm-set-lowering: surfaced once the scavenger no longer crashed\n(compilation reached MC lowering). MOSMCInstLower lowered LDCImm only for 0/-1,\nbut a *set* i1 carry can arrive as 1 (e.g. a 16-bit SBC carry-in) ->\nllvm_unreachable("Unexpected LDCImm immediate.") on an asserts build (silent UB\nunder NDEBUG — it happened to emit SEC, so the differential was always green). A\nplain +mos-a16 16-bit subtract reproduces it. Fix: lower any nonzero i1 as SEC.\n\nBoth pristine-upstream (drop from the stack on merge); 0011/0012 round-trip\n(0001..0012 == live tree). DEFAULT 8-bit unaffected; corpus 7/7; builtin\ndifferential fuzz seeds 1-80 = 80/80 PASS (0 mismatch / 0 crash); a16scavnz.c\npromoted to a positive gate (dev/run.sh a16scavnz -> 0x22A6,\nhost==default==+mos-a16==+mos-xy16, MAME + bsnes-jg, asserts-clean);\nKNOWN_ISSUES["scavenger-p-not-gpr"] + its repro row dropped (known-issues guard\nPASS 0/0).\n\nPlan: docs/plans/2026-06-26-321-scavenger-nz-live-p-save-fix.md\nPR bodies: docs/upstream-scavenger-live-p-pr.md, docs/upstream-ldcimm-set-lowering-pr.md\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
-->
