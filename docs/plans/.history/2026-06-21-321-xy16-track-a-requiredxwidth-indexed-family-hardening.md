| Date | Change |
|------|--------|
| [2026-06-21](https://github.com/wbniv/llvm-mos-65816/commit/d551f73) | #321 xy16 Track A: requiredXWidth 8-bit-indexed-family hardening (memory-gated + JMPIdxIndir) |

<!--history-meta v1
d551f73	author	Will Norris
d551f73	added	387
d551f73	deleted	0
d551f73	files	1
d551f73	body	Close the last requiredXWidth index-width omission in MOSInsertREPSEP — the\nthird and broadest member of the family after 55ec505 (value ops: cpx/cpy,\ninc/dec) and 4d8a2bd (transfers/push: ta/tx, ph/pl). The broad 8-bit\nindexed/indirect family — LDAAbsIdx/LDAZpIdx/ST{Abs,Zp}Idx/LDIndirIdx/STIndirIdx,\nthe 18 ALU-indexed {ADC,SBC,AND,ORA,EOR,CMP}{Abs,Zp,Indir}Idx, the 4 RMW\n{ASL,LSR,ROL,ROR}Idx, and the 4 a16-value/8-bit-index *Idx16 forms — fell\nthrough requiredXWidth to XW_None (X-passthrough) instead of XW_X8; a stray\nX=16 ambient at one would dereference a 16-bit index with a garbage high byte\n(the addressing-index sibling of the pr49419 value-compare bug).\n\nFix (HasIndex16-gated, correctness-safe — only ever inserts sep #$10):\n- Memory-gated structural catch-all: (mayLoad()||mayStore()) && readsRegister(\n  X/Y) -> XW_X8, after the XLow check + the value-op switch. The mayLoad/mayStore\n  gate is the refinement over a bare readsRegister: it excludes the M-governed\n  T_A (TXA/TYA), which reads X/Y as a *value* (mayLoad=mayStore=0) — a stray\n  sep #$10 before it would zero a live 16-bit X high byte (a real regression).\n- Index-reading-branch clause: isBranch() && readsRegister(X/Y) -> XW_X8,\n  closing the JMP (abs,X) jump-table dispatch (JMPIdxIndir) — the one family\n  member the memory rule can't reach (a non-mayLoad branch).\n- TRI threaded into requiredXWidth + all 3 call sites.\n\nSafety: verified 32/32 that every 16-bit-index (Xc16/Yc16) op carries XLow=1, so\nit returns XW_X16 *before* the catch-all — the rule can only ever insert a sep,\nnever widen a live 16-bit value.\n\nHardening, not a live bug (the gap is X8-pinned everywhere in real code).\nVerified (clang-23 rebuilt): default+a16 disasm byte-identical 75/75 (gating\nproof) + xy16 byte-identical 75/75 + csmith 247/445 ROM-identical (inertness);\ncorpus 7/7; xy16 suite (basic/ops/indiry/spill/spillr) + k_isort PASS both\nemulators; fuzz csmith 200x2 (seeds 101-500) 0 mismatch/0 crash (one seed-488\nMAME timeout was QUIET-box contention, re-verified PASS in isolation); torture\n60 PASS, 0 FAIL, 0 XFAIL (de-XFAIL'd pr49419/doloop-1/20041011-1/va-arg-22 stay\nXPASS); -verify-machineinstrs clean. 0002 round-trips with 0 foreign content\n(the .td @@-shifts are cosmetic — main's committed 0002 had drifted behind\nfar-cc's 0001 growth; #321 content byte-identical). No standalone RED test\n(X8-pinned + LTO narrows small indices; code-inspection hardening like 55ec505).\n\nPlan: docs/plans/2026-06-21-321-xy16-track-a-requiredxwidth-indexed-family-hardening.md\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
-->
