| Date | Change |
|------|--------|
| [2026-08-01](https://github.com/wbniv/llvm-mos-65816/commit/02c4d20) | docs: post-merge 60 fps re-baseline -- slow ROM now clears 60 fps too |
| [2026-08-01](https://github.com/wbniv/llvm-mos-65816/commit/29c2b90) | codec: staged-keyframe specialization -- Option C measured, 1.12 VBlanks |
| [2026-08-01](https://github.com/wbniv/llvm-mos-65816/commit/7289dcd) | codec: MVN the PackBits keyframe literals; record the 60 fps verdicts |
| [2026-08-01](https://github.com/wbniv/llvm-mos-65816/commit/08d4c06) | bench: functional multi-packet ring refill + keyframe case |

<!--history-meta v1
02c4d20	author	Will Norris
02c4d20	added	46
02c4d20	deleted	0
02c4d20	files	1
02c4d20	body	Re-measured every case on the merged tree (merge 5a25872, measured on\n30e3abb), since main's 21179d7 brought in the delta kernel's in-place\ncopy-span optimization. 600 VBlanks, same harness.\n\n                          pre slow  post slow   pre Fast  post Fast\n  svx-median                   540        530        607        596\n  svx-worst                    581        576        648        643\n  svx-keyframe                 474        474        537        537\n  stream, hardest slice 160    553        674        630        754\n  stream, slice 0              591       1023        665       1159\n\nThree readings, none of them obvious:\n\nThe single-packet delta cases got SLOWER, and that is expected rather than a\nregression to chase. Main's optimization guards each copy span with\nlda __rc2 / cmp __rc4 / beq and skips the MVN when previous and output are the\nsame buffer. The VIDEO_BENCH_PIPELINE cases pass distinct buffers\n(bench_previous in ROM, output in WRAM), so they can never take the fast path\nand pay the test as pure overhead. That configuration is not\nplayer-representative.\n\nThe stream cases, which are player-representative, improved sharply -- hardest\nslice 630 -> 754 FastROM, +20%. The stream decodes in place exactly like\nsnes-video-reel.c, so copy spans collapse to cursor advances.\n\nSlow ROM now clears 60 fps: 674/600 = 67.4 fps on the hardest slice, where it\npreviously fell short at 553. FastROM is a margin lever for this content, not\na prerequisite. Both plans updated; the earlier "slow ROM does not meet 60 fps"\nfinding is marked superseded.\n\nsvx-keyframe is unchanged at exactly 537/474 -- the control confirming the\nstaged keyframe specialization shares no code path with main's delta work.\nKeyframe policy is therefore unaffected: 1.12 VBlanks, still a two-VBlank\nslot, Option A with K=120 (59.50 fps effective) stands.\n\nCorrectness re-proven non-vacuous on the merged tree rather than assumed: the\nunmodified stream ROM passes got=0x00 over 1,200 VBlanks, and flipping one\nbyte at file $01C350 still stops the run with corpus_result=2.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
29c2b90	author	Will Norris
29c2b90	added	102
29c2b90	deleted	4
29c2b90	files	1
29c2b90	body	Settles the unproven lead with numbers. svx_decode_payload_wram_key_asm is a\nseparate entry point beside the bank-agnostic svx_decode_payload_asm, which is\nleft untouched and still serves the non-staged path. Pinning the staged source\nbank to $7F lets it drop the per-token `lda __rc12 / beq` far-check and its\nsep/rep pair at all three read sites, keep X and Y live across tokens instead\nof spilling to __rc0/__rc4, terminate on an output-end compare rather than a\nremaining-byte counter, and fill runs with an overlapping MVN (seed one byte,\nblock-move dest -> dest+1) instead of a 15-cycle store loop. DBR is invariant\nat $00 because every MVN here names $00 as its destination.\n\nCase 7 svx-keyframe: 207 -> 300 -> 537 per 600 VBlanks FastROM, ie 2.90 ->\n2.00 -> 1.12 VBlanks, 2.6x the original. Byte-correct against the host oracle\nvia the case's full 4,480-byte compare. No regression: svx-median and\nsvx-worst stay exactly 607/648, every other case is unchanged, 17/17 host\ncodec tests pass, and the ring-refill stream rises to 630/600 FastROM on the\nhardest slice.\n\nVERDICT: Option C misses the <=1 VBlank threshold, so keyframes keep a\ntwo-VBlank slot and Option A is the policy -- recommended interval K=120,\n2 s seek granularity at 59.50 fps. The kernel is now copy-bound rather than\ndispatch-bound and the residue is irreducible here: both MVN operands are\nWRAM, which runs at 2.68 MHz regardless of FastROM (~46 master cycles/byte),\nso 4,480 bytes plus DMA floor at 0.755 VBlank before any token is dispatched.\nIt still pays: at 2.00 VBlanks the keyframe filled its two-slot budget with\nzero margin, at 1.12 it uses 56%.\n\nAlso records the assembler hazard this hit: immediates are sized by the\nliteral's magnitude and not the M flag, so `and #$00ff` in 16-bit mode\nassembled to the 2-byte form and the CPU ate the following `dec` as its high\noperand byte (corpus_result=0x82, a runaway write). Widened through the file's\nexisting xba/lda #0/xba idiom instead.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
7289dcd	author	Will Norris
7289dcd	added	85
7289dcd	deleted	0
7289dcd	files	1
7289dcd	body	A PackBits literal run is exactly a block copy, but the keyframe path in\nsvx_decode_payload_asm copied it a byte at a time (LDA long / STA abs,Y / INX\n/ INY / DEC dp / BNE, ~24 cycles/byte) while the delta path next to it already\nused MVN at ~7. Give the literal run the same MVN, naming the staged source\nbank explicitly so the high-WRAM crossing stays unambiguous; MVN leaves DBR at\nits destination bank ($00), which is what the following parser accesses expect.\n\nKeyframe case: 207 -> 300 decodes per 600 VBlanks FastROM (2.90 -> 2.00\nVBlanks), byte-correct, with no delta regression -- svx-median and svx-worst\nstay at exactly 607 and 648, and the 17 host codec tests still pass.\n\nIt does not close the gap, and the reason is measured: the frame-180 keyframe\ncarries 290 tokens for 4,480 output bytes (literal mean 21 B, run mean 10 B)\nbecause Floyd-Steinberg dithering fragments PackBits. The kernel is\ntoken-dispatch-bound, not copy-bound, which is why optimizing the copy bought\n45% and no more. The next lever is a staged keyframe specialization with the\nsource bank fixed at $7F -- identified, not implemented.\n\nPlans record the verdicts. Ring refill holds 60 fps on FastROM: 620/600 on the\nhardest 120-frame slice (the obvious slice 0 is unrepresentative -- 1,523 B\nmean packet against 3,290). Keyframes are the residual risk, so a strict\n600/600 gate and periodic keyframes are incompatible with the current kernel.\nCapacity is not the binding constraint: 6 MiB holds ~39 s of true 60 fps\nagainst 2 x 8-10 s of nominated excerpt, and a frame-doubled 30 fps master\ncosts 44 B per duplicate, 1.7% over 30 fps native.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
08d4c06	author	Will Norris
08d4c06	added	145
08d4c06	deleted	0
08d4c06	files	1
08d4c06	body	Turns the SVX2 60 fps bench from a single fixed-address staging proxy into a\nreal stream walk, and measures the keyframe path for the first time.\n\nVIDEO_BENCH_STREAM=1 packs N consecutive corpus frames as a separate HiROM\nbank-$C1 stream and refills them per frame through a 32-bit offset table, the\nA-bus 64 KiB bank split, and a no-split wrapping ring in high WRAM $7F:2000..\n$7F:F000 -- so every frame now pays the costs a player pays. The ring is\nno-split because svx_decode_payload_wram_asm walks its staged cursor with a\n16-bit X and has no wrap handling.\n\nCorrectness is a stop condition: a boot pass decodes the whole loop, checks\nevery frame against a host Fletcher-16, and compares the final frame byte for\nbyte, pinning the complete delta chain to the host oracle. Verified\nnon-vacuous -- one flipped stream byte yields corpus_result=2.\n\nMeasurement is now two-point (iterations at the warmup mark and 600 VBlanks\nlater, differenced), so validation no longer dilutes the throughput number.\n\nFastROM holds 60 fps on the hardest 120-frame slice: 610/600. The obvious\nslice 0 is unrepresentative at 648/600 -- its mean packet is 1,523 B against\n3,290 B for the start=160 window -- so both are reported and the hard slice is\nthe gate. Slow ROM is 544/600.\n\nNew case 7 svx-keyframe shows the remaining cliff: 207/600 FastROM, ie 2.90\nVBlanks per keyframe, because the PackBits literal path copies byte at a time\nwhere the delta path uses MVN.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
-->
