| Date | Change |
|------|--------|
| [2026-07-26](https://github.com/wbniv/llvm-mos-65816/commit/2bfe4f3) | fix(zp-alloc): rename Imag32 far-pointer quads atomically — [dp] read-garbage fixed |

<!--history-meta v1
2bfe4f3	author	Will Norris
2bfe4f3	added	99
2bfe4f3	deleted	0
2bfe4f3	files	1
2bfe4f3	body	Root cause of the far-rodata FONT16 render bug (investigation\n2026-07-26-far-rodata-read-under-pressure-title-upload.md) — and it was NOT\nregister pressure. mos-snes.cfg passes -mlto-zp=224, enabling MOSZeroPageAlloc:\nwith a zp budget it "silently renames" callee-saved imaginary registers to\nzp-static-stack slots (CSRZPOffsets, consumed per operand by MOSMCInstLower).\nThe pass predates the fork's Imag32 quad (#320): it renamed the far pointer's\nfour BYTES (rc20..rc23 -> $c4..$c7) but recorded no entry for the quad\nsuper-register RL5, so LDA_IndirectLong's pointer operand fell through to the\nimag-symbol path and kept reading [$14] — defs renamed, use stale, garbage\nglyphs. Proven from the failing binary (zero writes to $14..$17 before the\nuse) and by a single-function llc replay being correct (no module zp budget ->\npass idle). Every synthetic repro passed because tiny programs keep far\npointers in caller-saved quads the pass never touches — the earlier "pressure"\nhypothesis was the right correlation with the wrong mechanism.\n\nFix in MOSZeroPageAlloc.cpp, mirroring the existing Imag16-pair idiom:\n  - candidate creation: a saved byte whose Imag32 super-register has live uses\n    promotes to ONE size-4 candidate (dedup via Imag32Regs) — atomic selection\n    and four consecutive offsets, which [dp] indirect-long requires;\n  - offset assignment: an Imag32 arm records the quad, both sublo16/subhi16\n    pairs and all four bytes in CSRZPOffsets, so 8/16/32-bit operand forms all\n    rewrite consistently at MC lowering.\nNo MCInstLower change — its map lookup was already correct once complete.\n\nVerified (full battery in the plan):\n  - repro emits lda [$c4] matching its defs; fft + mandel-double titles render\n    real shadowed 16x16 Waldo from bank $01 far rodata (screenshots).\n  - mandel-double gate 0x0EDF, disasm shape unchanged; -verify clean x2.\n  - near code BYTE-IDENTICAL pre/post (fft default-8 and a16-near hashes).\n  - xcheck 16/16; full build 232 programs with exactly one ROM changed:\n    mandel-oop — an a16/far demo evidently also on the rename path (likely\n    second silent victim); its gate now green (0x204F == host).\n\nCarried in 0002 (dev/regen-patch.sh round-trips). The regen also folded 0016 +\n0017 into 0002 — both MOS-dir-only, which IS the documented steady state — so\ndev/toolchain.sh's bootstrap list shrinks to 0001 -> 0002 -> 0006(generic\nhunks); verified to apply cleanly to pristine 8be054612.\n\nUpstream posture: the pass is upstream, Imag32 is fork-only, so the fix rides\nwith the #320 series rather than a standalone PR.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
-->
