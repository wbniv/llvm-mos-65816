| Date | Change |
|------|--------|
| [2026-08-01](https://github.com/wbniv/llvm-mos-65816/commit/47172e2) | feat(snes/seamdemo): P2 Act-2 — the boundary-hostile graph walk |
| [2026-08-01](https://github.com/wbniv/llvm-mos-65816/commit/acd0dc1) | tools: snes-cartmap-viz.py — inline-SVG decode maps for the cartsize pages (model-derived, self-tested) |
| [2026-08-01](https://github.com/wbniv/llvm-mos-65816/commit/dd954dd) | feat(snes/seamdemo): P1 Act-1 ROM — a bytecode VM marching the whole 6 MiB cartridge |
| [2026-08-01](https://github.com/wbniv/llvm-mos-65816/commit/e30673f) | feat(snes/seamdemo): P0 generator + host oracle for the three-act boundary cartridge |
| [2026-08-01](https://github.com/wbniv/llvm-mos-65816/commit/cdb9be8) | docs(plan): seamdemo — ExHiROM three-act boundary synthesis cartridge (supersedes video-reel Phase 2) |

<!--history-meta v1
47172e2	author	Will Norris
47172e2	added	97
47172e2	deleted	17
47172e2	files	1
47172e2	body	Act 2 folds into the same loop skeleton after Act 1's HALT: a covering cycle\nthrough one 16-byte node per DECODE CELL of the cartridge — all 374 of them,\nmirrors included — rendered as a spreading web.\n\nexamples/65816/seamdemo_graph.h holds the walk, on the same one-source-three-\nconsumers discipline Act 1 established: mos-clang compiles it for the 65816, the\nhost cc compiles it inside tools/seamdemo-sim.c, and run_act2() in the Python\noracle is the independent third implementation. All three fold to $36B6, and the\nhost leg caught the encoding before any emulator run — again the cheap catch.\n\nThe traversal is the P0 contract unchanged: all 16 node bytes, then the byte at\npeek0, then at peek1, advance along `next`, and after the last node the node\ncount low byte then high. Addresses are 24-bit CPU addresses straight out of the\npayload with NO file->CPU translation, because a mirror-addressed node is the\ntest. The cycle closing back on its entry is checked separately\n(SEAMGRAPH_ST_CYCLE): a decoder that mis-resolved one mirror would break the\nCHAIN rather than merely perturb the fold, and that deserves its own signal.\n\nThe web IS the address space — a node's canvas position is its own CPU address\n(x = bank >> 1, y = addr >> 9), so a seam-crossing edge is visibly a long jump\nand the $FF->$40 discontinuity reads as one. Seam-crossing edges draw in the hot\naccent. HUD carries the live node address and the EDGE/SEAM/MIR counters; the\nverdict strip shows ACT1 OK + ACT2 live (the full three-act strip is P3).\n\nPacing, measured and tuned on CADENCE — the payload is frozen this phase and the\nCRCs do not move. At one node per frame Act 2 came in at ~700 frames = ~12 s,\nUNDER the 20-30 s window rather than over it: its edges are far cheaper than Act\n1's because they join two points in the address-space picture rather than\nsweeping a turtle across the canvas (~1.9 frames/node). ACT2_FRAME_DIV=4 advances\nthe walk once every 4 frames -> ~1,500 frames = ~25 s, with the divider binding\nrather than the dirty-tile flush. JG_FRAMES raised 2,400 -> 5,400: a full cycle is\n~3,850 frames, so that is real margin rather than the 350 frames P1 shipped with.\n\ncorpus_fold(*crcs) folds a run of act CRCs so a partially-built cartridge reports\nthe same primitive over the acts that exist: P2 ships SEAMDEMO_CORPUS_ACT12\n($0C72), P3 replaces it with the full three-act SEAMDEMO_CORPUS_RESULT ($3277).\n\nContract friction, recorded in the plan's P2 section as P0 feedback:\n  * The host harness needed a MIRROR-aware decode and P0 only emitted the\n    canonical file->CPU table. Re-deriving the mapping in C would have forked the\n    model, so the generator now emits the model's own 380-entry decode-cell table\n    as a separate HOST-ONLY header (emit-decode -> seamdemo-decode.h), kept out\n    of seamdemo-data.h so the ROM never links a table it cannot use.\n  * corpus_result had no defined value for a partially-built cartridge; hence\n    corpus_fold rather than an ad-hoc P2 constant.\n  * The PH $p verifier defect STOPPED reproducing once Act 2 was linked in — the\n    same build is now -verify-machineinstrs clean. That is consistent with the\n    P1 diagnosis (a liveness-modelling gap in a register-pressure-dependent\n    spill-around, not a wrong instruction). The signature-exact tolerance stays,\n    dormant not removed, since seqvm.c still reproduces it.\n\nVerification: dev/run.sh seamdemo -> RESULT: PASS. On console (bsnes-jg,\n+mos-a16): act1_crc $F0E2, act1_status 0, act2_crc $36B6, act2_status 0,\ncorpus_result $0C72; entropy fingerprint still one picture across six boots.\n\nSNESDQ_SKIP=1: the gate scans the WORKING TREE and trips only on\nexamples/snes/video_hud.h:74 (a new REG_MDMAEN write) — another worker's unstaged\nin-flight file, not in this commit's staged set. Verified by running\ndev/snes-display-quality.py directly: that one line is the only finding.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
acd0dc1	author	Will Norris
acd0dc1	added	20
acd0dc1	deleted	1
acd0dc1	files	1
acd0dc1	body	380-cell fold of the 256x2 half-bank space, canonical/mirror per device,\nWRAM banks, the 8 MiB unreachable hole, and a generically-derived seam\ncallout; counts asserted against decode_cells()/holes() two ways\n(selftest: 380/128/252, 380/192/188, 380/254/126+64KiB hole). Deployed to\nbiohack.net as v1.0.345 (cfdb7cb there, CI success, live spot-checked).\n\nSNESDQ_SKIP=1: whole-tree gate trips on another worker's uncommitted\nexamples/snes/video_hud.h, out of scope here.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
dd954dd	author	Will Norris
dd954dd	added	208
dd954dd	deleted	25
dd954dd	files	1
dd954dd	body	Act 1 of the ExHiROM three-act boundary cartridge: a 16-opcode VM whose 24-bit\nprogram counter is a FILE offset, executing one 48-byte chapter per 32 KiB slot,\nJFAR-chained in ascending file order, so the counter marches the entire image.\nThe centrepiece is one VM instruction split across the physical device seam --\nits SYNC opcode is the last byte of ROM 1 (file $3FFFFF = $FF:FFFF) and its\noperand the first byte of ROM 2 (file $400000 = $40:0000). The ROM detects the\ncrossing and holds a freeze-frame beat on it.\n\nThe VM lives in examples/65816/seamdemo_vm.h and is compiled TWICE from one\nsource -- by mos-clang for the 65816 and by the host cc in tools/seamdemo-sim.c.\nThat host leg is the cheap one and it is what catches an ISA transcription slip\nbefore any emulator run; the Python oracle is the independent third\nimplementation. Rejected: writing the VM twice (two places to make one mistake).\n\nThe differential is green:\n  host Python oracle == host C == +mos-a16 @ bsnes-jg, all $F0E2, act1_status 0.\nact1_seam_hits == 1 proves the split instruction actually executed, and\nboot_checks() re-reads file $3FFFFF through the same far cursor the VM uses to\nassert it really is the contract's SYNC opcode -- the P0 contract checked on\nconsole by the thing that depends on it.\n\nDisasm asserts the three codegen shapes the act exists to stress: a7 (lda [dp],\nthe 24-bit cartridge cursor), 7c (jmp (abs,X) -- the dense 16-way switch really\ndoes become a jump table) and __call_indir (the function-pointer ALU table).\n\nPACING: the P0 estimate was wrong by ~70x, so P0 was regenerated against\nmeasurement. Isolating builds (-DSEAMDEMO_NO_DRAW / -DSEAMDEMO_NO_HUD) showed\ndrawing is ~5.6x the VM and the HUD is free, and that drawing does NOT scale\nlinearly with segment count -- the canvas dirty-tile flush is a throughput wall,\nnot a per-line cost, so a model fitted to one payload mispredicts the next.\nMeasured end to end:\n\n  256 B chapters, MOVE 2-in-9 : 65,961 ops / 12,729 seg -> ~245 s\n   48 B chapters, MOVE 1-in-10:  10,224 ops /   689 seg -> ~38 s\n   48 B chapters, MOVE 1-in-23:  10,494 ops /   367 seg -> ~34 s  <- ships\n\nagainst the plan's 20-30 s. Every coverage invariant is untouched: 374/374\navailable decode cells with in-degree >= 1, 1,122 edges, one chapter per slot so\nthe march still spans all 6 MiB, one PC mark per chapter, act 3 sweeping 190/190\npages. New oracle values: act1 $F0E2, act2 $36B6, act3 $6D21 (unchanged -- the\natlas did not move), corpus_result $3277. Nothing was published, so the CRC churn\nwas free.\n\nKNOWN BACKEND DEFECT, filed separately, NOT introduced here: -verify-machineinstrs\nreports "Using an undefined physical register -- instruction: PH $p". php pushes\nthe whole P register but the MIR only defines the individual flag sub-registers,\nso liveness is unmodelled; the emitted php is correct on hardware. It reproduces\nin the already-committed examples/snes/seqvm.c (draw_frame) at -O0/-O1/-O2/-Os\nwith +mos-a16 and is clean at -Oz -- dev/seqvm.sh never ran the verifier, so\nnothing had caught it. dev/seamdemo.sh tolerates EXACTLY that signature (the\nundefined-register message AND "instruction: PH $p" AND exactly one error) and\nfails on any other verifier error.\n\nTwo pipefail traps fixed in the gate along the way: jgxcheck exits non-zero on a\ndeliberate probe mismatch, and WRAM $0000 is imaginary register rc0 (not zero),\nso the entropy-fingerprint probe also exits non-zero. Both needed `|| true`\ninside the command substitution or `set -e` killed the gate mid-run.\n\nAct length measured by bisecting act1_laps: 0 at frame 2,000, 1 at frame 2,100,\nso ~2,050 frames = ~34 s. Drawing does not scale linearly with segment count (the\ncanvas dirty-tile flush is a throughput wall, not a per-line cost), so a model\nfitted to one payload mispredicts the next -- measure the payload you ship.\n\nAlso rebuilt build/jgxcheck from its committed source: the cached binary predated\ndev/jgxcheck.cpp's FRAMESCAN support, so the entropy fingerprint silently had\nnothing to compare. It now reports one picture across all six boots.\n\nVerification: dev/run.sh seamdemo -> RESULT: PASS (recorded in the plan's\nverification section, step 2).\n\nSNESDQ_SKIP=1: the gate scans the WORKING TREE and trips on\nexamples/snes/video_hud.h:74 (a new REG_MDMAEN write) — another worker's\nunstaged in-flight file. It is not in this commit's staged set, and nothing here\ntouches it. Verified by running dev/snes-display-quality.py directly: that one\nline is the only finding.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
e30673f	author	Will Norris
e30673f	added	241
e30673f	deleted	2
e30673f	files	1
e30673f	body	The plan asked for "every decode window gets in-degree > 0". For 6 MiB ExHiROM\n`windows()` reports TWO windows, which is a coverage denominator so coarse the\nassertion would be vacuous -- and it deliberately excludes mirrors, which are\nexactly the decode paths this cartridge exists to exercise. So the model gains\none enumeration, derived entirely from `decode()` rather than written down:\n\n  CartMap.decode_cells()   every 32 KiB CPU view of ROM, mirrors included\n  CartMap.decode_regions() the same, merged into maximal runs, for reports\n\n32 KiB is the right granularity because the board `map` lines only ever split a\nbank at $8000 and `decompose` forces device lengths to a multiple of 32 KiB;\nthe method asserts that instead of assuming it. 6 MiB ExHiROM: 380 cells =\n192 canonical + 188 mirror over 192 file units.\n\nThe generator lays all three acts into ONE slot record per canonical 32 KiB file\nunit, so the acts cannot collide by construction:\n\n  act1  a 16-opcode VM (dense jump-table dispatch, function-pointer ALU table)\n        whose PC is a 24-bit FILE offset. One ~256-byte chapter per slot,\n        JFAR-chained in ascending file order. At the 4 MiB device seam the\n        chapter STRADDLES: a SYNC opcode at file $3FFFFF ($FF:FFFF) with its\n        operand at file $400000 ($40:0000) -- one instruction split across the\n        non-monotonic device boundary. Termination is structural: forward\n        branches only, and LOOP's counter is seeded immediately before a body\n        that provably never writes it.\n  act2  one 16-byte node per decode cell, chained into a seeded permutation\n        CYCLE over every cell -- so in-degree >= 1 is a property of the\n        construction, not of luck -- plus two adversarial read-only peek edges\n        per node, biased to the other physical device and to mirror addresses.\n  act3  one 64x64 data page + 16x16 heights + self-describing metadata per slot,\n        swept boustrophedon. Texels are smooth terrain in the high nibble (so it\n        is a picture) and pattern(file_offset) in the low nibble (so every texel\n        read is a decode probe).\n\nThe oracle is the ONLY host implementation of the three traversals and it reads\nBYTES out of the image through CartMap.decode() -- never the generator's\nconstruction. The generator builds the payload, then asks the oracle what the\nnumbers are, so a byte laid down in the wrong place produces a different oracle\nvalue rather than a matching pair of wrong numbers. `selfcheck` supplies the\nindependent third implementation.\n\n`fold` and `pattern` are imported from snes-cartcanary.py, not re-typed; the\nself-check asserts the function objects are identical so a future copy-paste is\ncaught. Rejected: duplicating the six lines (a second place to relearn why the\nfirst XOR-linear pattern folded to $0000).\n\n6 MiB ExHiROM, seed 0x5EA3DE30: 374 nodes, 1122 edges (755 seam-crossing, 748\nmirror-addressed), 374/374 available cells covered with 6 reserved for linked\ncode + the far arena; act1 $93CF, act2 $B596, act3 $6D21, corpus_result $2B43.\n\nVerification (plan step 1, recorded in the plan):\n  python3 tools/snes-seamdemo-gen.py selfcheck            ALL PASS\n  python3 -m pytest test/snes/cartridge-maps/...          58 passed, 25044 subtests\n\nP0 only; the ROM (P1-P3) is not started.\n\nSNESDQ_SKIP=1: the gate scans the WORKING TREE, and trips on\nexamples/snes/video_hud.h:74 (a new REG_MDMAEN write). That file is another\nworker's unstaged in-flight edit -- it is not in this commit's staged set\n(`git diff --cached --name-only` is the five files above) and nothing here\ntouches examples/snes/ at all.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
cdb9be8	author	Will Norris
cdb9be8	added	110
cdb9be8	deleted	0
cdb9be8	files	1
cdb9be8	body	User-approved shape: all three synthesis concepts in a loop — VM-executes-\nthe-cart (sequential march, seam crossing as the on-screen event), boundary-\nhostile graph walk (scattered access, every decode window covered), Mode 7\nflyover (streaming). Union of access patterns beats any single stream as a\nmapping test; also retires the flat verdict wash. Mockup bundle: four screen\nstates, 1440x900, self-contained. TODO: T4 item filed (blocked on the\nexhirom-canaries merge); ExHiROM item's plan-shape escalation resolved.\n\nSNESDQ_SKIP=1: whole-tree gate, out-of-scope dirty files.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
-->
