| Date | Change |
|------|--------|
| [2026-08-05](https://github.com/wbniv/llvm-mos-65816/commit/ff35036) | fix(321): Mode 7 splash handoff contract — post-title force-blank 720 -> 22 frames |

<!--history-meta v1
ff35036	author	Will Norris
ff35036	added	348
ff35036	deleted	0
ff35036	files	1
ff35036	body	Escalated out of the 121 re-verify: mandel-oop held 11 black frames after the\ntitle and mandel-display 72, from the identical shared handoff.\n\nCorrects the premise. display_init() does not "re-open" the boot force-blank\nwindow (docs/agent-handoff.md's old wording): m7splash_end() returns with\nINIDISP=$80 still asserted and the re-assert costs zero frames. The blackness is\nentirely the WALL-CLOCK of work executed inside the window -- which is why one\nhandoff produced a 1..350 frame spread.\n\nThe contract, now written at the top of snesgfx/m7title.h: compute goes BETWEEN\nm7splash_begin() and m7splash_end(), behind a title the viewer can see; only PPU\nregisters and DMA may sit between end() and the blank release; progressive reveal\ngoes after the release, in v-blank. The bracket already existed (lzss-gallery,\napollo-reel, snes-video-reel use it) -- it was a convention, and the m7splash()\nconvenience wrapper invited the wrong shape.\n\n  mandel-float   350 -> 1   mf_gate_crc() ground 5.8 s of soft-float in the dark,\n                            directly against its own "so the boot isn't a blank\n                            screen ... reads as working, not broken" comment\n  mandel-double  215 -> 1   same defect, same comment (see below)\n  mandel-display  72 -> 1   mandel_fill moved behind the title; the coarse reveal\n                            now lands in v-blank like passes 2 and 3 already did\n  buddha          38 -> 2   grid_clear/build_palette moved behind the title\n  blossom         32 -> 4   likewise\n  julia            1 -> 1   window was already at the floor, but julia_gate_crc()\n                            ran BEFORE the splash = ~70 frames of boot black\n  avalanche        1 -> 1   already at the floor\n  mandel-oop      11 -> 11  see below\n\nThe floor -- _m7t_wipe_vram() plus the Mode 7/CGRAM mode switch, the only part\nthat genuinely cannot happen with the screen on -- measures ONE frame.\n\nNew gate: dev/m7blank.sh --gate. One entropy-pinned JGX_FRAMESCAN run per demo\nreconstructs every frame from the change events and checks the post-title black\nrun against a committed per-demo budget (exit 4 when over). JGX_ENTROPY=0 is\nmandatory or mid-run frames are irreproducible (#121's methodology note).\n\n--probe builds with -DM7BLANK_PROBE so m7_show()/display_frame() paint CGRAM[0]\nwhite at the release. Without it a picture scan cannot separate "still blanked"\nfrom "released onto black art", and buddha/avalanche deliberately bloom onto a\nblack backdrop -- the metric would have credited a fix for repainting a backdrop.\nThe blocks are #ifdef'd out by default: avalanche's ROM is byte-identical before\nand after (dabd87ea...), so a shipped ROM pays nothing. The two probe sites are\nadded to the snes-display-quality baseline by hand, NOT via --update-baseline:\nthat would also have silently dropped two stale nmitally.c entries which are not\nmine to remove (they predate c9e0f12). Semantic diff is +2 findings, -0.\n\nThe gate earned its keep on its first real run. mandel-double opts into the far\nplatform and would not link against mos-snes.cfg, so the baseline never saw it;\nteaching m7blank.sh to pick mos-snes-far.cfg surfaced it at 215 frames OVER\nBUDGET. The demo set is derived from `grep -l m7splash`, not hardcoded -- the\n"seven Mode-7 demos" in agent-handoff.md was already stale at twelve, the same\ndrift class as the 9->11 gallery-badge count.\n\nmandel-oop stays at 11 and is not touched: bisection puts only 4 frames in the\nhandoff (title exit + display_init + _mandel_reserve) and 6 in build_step()'s\nfar-memory work inside snesgfx's FIRST display_frame(), before it writes\nREG_INIDISP. That is the Display first-frame path -- every Display demo pays it --\nand changing it there risks the force-blank-into-active-display flicker class of\n1dd9317. Filed as its own TODO item; budgeted at the measured value so a\nsplash-side regression still trips.\n\nAlso fixes a PRE-EXISTING harness defect, reproduced on pristine sources before\ntouching it: dev/mandel-{float,double}.sh granted MAME -seconds_to_run 18 while\ntheir .lua asserts at SHOT_AT=2200 periodic ticks (~36.7 emulated seconds), so\nMAME exited before the callback fired and the empty SHOT line scored as FAIL.\nRaised to 45 s rather than lowering SHOT_AT -- frame 2200 is the whole-set render\n3d66b05 chose as the published title card.\n\nGates: all eight green, every corpus hash re-derived from its host oracle and\nunchanged (0x204F mandel-display + mandel-oop, 0x4169, 0x0EDF, 0x3490, 0x7C31,\n0x9047, 0x27EA); mandel-oop still exactly 1 indirect dispatch; blossom's pad-log\nreplay differential green. No capture frame needed re-pinning. Nothing published.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
-->
