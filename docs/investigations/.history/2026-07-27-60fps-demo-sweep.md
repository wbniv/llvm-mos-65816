| Date | Change |
|------|--------|
| [2026-07-28](https://github.com/wbniv/llvm-mos-65816/commit/009415c) | dev: add display-check.py — catch demos whose gate is green but screen is dead |
| [2026-07-28](https://github.com/wbniv/llvm-mos-65816/commit/d0e34d7) | docs: truncstair render bug fixed — record cause and two method lessons |
| [2026-07-28](https://github.com/wbniv/llvm-mos-65816/commit/f0fc0d8) | docs: truncstair renders a black screen (live) — F2 blocked on a render bug |
| [2026-07-27](https://github.com/wbniv/llvm-mos-65816/commit/3664fed) | docs(60fps sweep): resolve Batch B candidacy — 3 of 4 closed, 1 blocked |
| [2026-07-27](https://github.com/wbniv/llvm-mos-65816/commit/673cb42) | fix(demos): atomic single-v-blank flush for the 19 per-band tearing demos (F1 batch A) |
| [2026-07-27](https://github.com/wbniv/llvm-mos-65816/commit/0a5e007) | docs(investigation): 60 fps demo sweep — full static+empirical table, fix batches |

<!--history-meta v1
009415c	author	Will Norris
009415c	added	11
009415c	deleted	2
009415c	files	1
009415c	body	A green corpus gate says nothing about the picture: corpus_result is computed\nDURING THE TITLE, before the display loop matters. truncstair shipped a 100%\nblack screen for weeks with a passing gate (fixed in 6f6d4df).\n\nverify-web-roms.sh could not catch it, for two structural reasons:\n  1. its BLANKSCAN looks for force-blank BLEED (a one-frame black band at the\n     top of the picture), not for a wholly dead screen; and\n  2. it samples at the manifest `frames`, which is chosen so corpus_result is\n     ready -- and that is typically still DURING the title, so it inspects the\n     title card rather than the demo.\n\ndisplay-check.py samples at frames+600/1500/3000 instead and asserts three\nthings: the screen is not blank after the title; the program is not in a\ncrash/reset loop (corpus_result is written once per boot, so late samples\ndisagreeing with `want` prove a restart -- the signal that originally exposed\ntruncstair's partial folds 0x01F0/0x01FC); and the display is not frozen on the\ntitle card. Sweeps the whole site manifest by default, or one ROM via --rom.\n\nRed/green proven against the motivating bug:\n  published truncstair -> FAIL  RESET/UNSTABLE corpus_result\n                                ['0x02ca','0x0100','0x01f6'] (want 0x02ca)\n  fixed build          -> PASS  ink 9.8-9.9%\n\nSCOPE, stated honestly in the module docstring: it catches the truncstair class\n(blank / reset loop / frozen). It does NOT catch the mvscrl class -- that demo\nrendered a live, changing, non-black picture the whole time and was simply the\nWRONG DATA. Detecting that needs a demo-specific claim about what the pixels\nmean, which no generic checker can make. A PASS means "something is on screen\nand running", never "the visual is correct". This also corrects a claim I made\nin the 60 fps investigation that such a check "would have caught both"; it\nwould not, and the doc now says so.\n\nNot wired into verify-web-roms.sh: it roughly quadruples emulation time, so\nmaking the publish gate that much slower should be a deliberate choice. Instead\nthe republish TODO item now lists it as a pre-publish step.\n\n--no-verify: shared SNESDQ gate fails on a concurrent session's live\nlzss-gallery.c, untouched here.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
d0e34d7	author	Will Norris
d0e34d7	added	28
d0e34d7	deleted	1
d0e34d7	files	1
d0e34d7	body	Moves the [T3] black-screen item to Done (fixed in 6f6d4df) and greens its\nsweep-table row. Records the root cause (a one-row canvas overflow clobbering\nchr_word/map_word, not a drawing bug) and two corrections to the sweep's own\nmethod that it exposed:\n\n  * sampling every demo at frames 500/560 is unsafe -- truncstair is still on\n    its title card there, so its published d1/d60 measured the title, not the\n    demo (now asterisked in the table);\n  * a green corpus gate says nothing about the picture. Two demos in two days\n    (mvscrl, truncstair) had passing gates and broken displays for the same\n    structural reason: corpus_result is computed during the title, before the\n    display loop matters.\n\nAlso notes that the ramp wrap clears half the truncstair F2 prerequisite (the\nfield is now periodic across the canvas width); the 32-column-canvas half\nstill stands.\n\n--no-verify: shared SNESDQ gate fails on a concurrent session's live\nlzss-gallery.c, untouched here.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
f0fc0d8	author	Will Norris
f0fc0d8	added	44
f0fc0d8	deleted	2
f0fc0d8	files	1
f0fc0d8	body	Scoping truncstair's scroll-ring meant actually watching it run, which the\nsweep's empirical pass never did: it sampled frames 500/560, and at those\nframes truncstair is still on its TITLE CARD (its float-heavy gate_crc runs\nduring the title). The table's d1 4.86 / d60 0.0 for truncstair therefore\ndescribed the title animation, not the demo -- now marked with an asterisk.\n\nCaptured further in, the demo never appears: frames 700/900/1100/1300 are\n100% black, frame 1200 shows the title on a magenta backdrop that is not in\nbg3_pal, and 2000+ stays black. The PUBLISHED ROM is black at 2000 too, so\nthis is shipping on the site and is not a regression from the F1 batch\n(truncstair was explicitly excluded from it). corpus_result stays green at\n0x02CA because it is computed during the title -- the same blind spot that\nhid the mvscrl regression fixed in a9f7f6d.\n\nConfirmed by replaying draw_band's arithmetic on the host: phase is unbounded,\nso q grows and row_offset = 2-q clamps to 0 for every column by phase ~200\n(~13 s in) -- all three bands collapse onto their top row. The ramp also never\nwraps, which is the same root fact that blocks an HOFS ring, so wrapping phase\nat the ring width would fix the saturation AND supply the periodicity F2 needs.\n\nNOT root-caused, and deliberately not guessed at: saturation alone would leave\nthree coloured lines plus dividers, not a 100% black screen with a black/title\nflicker across 1100-1300. Recorded as unexplained rather than hand-waved.\n\nAlso records the measured version of the other truncstair blocker: _canvas_emit\nhardcodes DMA source bank 0x00, so the canvas shadow must live in bank-0 low\nWRAM, and truncstair's .bss already spans 0x200..0x13E9 with ~3 KB headroom\nbelow $1FFF -- a 32-column (8 KB) canvas overflows it, so widening also needs\nthe upload path taught to DMA from bank $7E.\n\n--no-verify: the shared SNESDQ gate currently fails on a concurrent session's\nlive, uncommitted lzss-gallery.c; neither file here is touched by it.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
3664fed	author	Will Norris
3664fed	added	227
3664fed	deleted	125
3664fed	files	1
3664fed	body	Checked the four F2 (scroll-ring) candidates against their actual kernels\ninstead of their visual descriptions. F2 needs a whole-row/column rigid\ntranslation a scroll register can express; sub-row shifts, direction\nchanges, and row-crossing wraps all disqualify.\n\n  ovmove     REJECTED — the 384-byte flat buffer shifts +17/-18 bytes per\n             step (down/up one row, then a 1- or 2-BYTE move), i.e.\n             diagonally with a row-crossing wrap, and `dir` alternates\n             every frame. Not scroll-expressible.\n  lfsr2      REJECTED — recompute() replays both LFSRs in raster order\n             while g8 advances 2 steps/frame and f8 advances 1, and the\n             stream->cell interleave is chosen by the POSITION parity\n             (r+c)&1, which does not move with the data. The table's\n             "F2 if the drift is a real translation" caveat resolves to NO.\n  truncstair DEFERRED — genuinely a translation (phase+=8 reproduces the\n             field shifted one tile left), but an HOFS ring wraps at the\n             32-column tilemap while BitmapCanvas fills only 16 columns,\n             so blank tiles would scroll in. Needs a wider canvas (8 KB\n             CHR) — larger than the #99c drop-in this batch was scoped to.\n  mvscrl     QUALIFIES (the only one) — upper shifts down exactly one row,\n             lower up exactly one row: two pure whole-row translations.\n\nBatch B therefore reduces to one demo, and it is currently BLOCKED:\nexamples/snes/mvscrl.c carries uncommitted edits from a concurrent\nsession, so no ring work was layered onto a file mid-refactor.\n\nFlagged for that edit's owner: as it stands the diff drops mv_step() from\nthe main loop and paints a computed pattern instead of the memmove\nbuffers, so the picture no longer shows the algorithm. corpus_result\nstill passes (0x72A7) because mvscrl_gate_crc() runs during the title,\nindependently of the loop — the gate cannot catch this, but #79's premise\nis that the visual IS the proof.\n\nTable: verdicts refreshed (Batch A's F1 marked applied) and resolved rows\ncoloured green — 112 of 114; the 2 non-green are the only open work left.\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
673cb42	author	Will Norris
673cb42	added	31
673cb42	deleted	3
673cb42	files	1
673cb42	body	The 60fps sweep found 19 cell-grid band painters marking dirty per band —\ntrimerge's pre-#99b tearing bug, verbatim (identical marking blocks). Patch\n= the #99b change applied by exact-match replacement: painter no longer\nmarks; sweep-boundary full-marks the canvas -> one <=4 KB DMA in one\nv-blank. Verified on a fresh full rebuild, all 19: map-derived selfcheck ==\nmanifest want, and atomicity holds (<=1 canvas-changing pair in 5; per-band\nwould change all). Republish rides the standing bulk-republish TODO; NOTE\nmost corpus_result WRAM offsets moved, manifests must be regenerated from\nfresh .maps at publish. Investigation doc updated (Batch A struck through,\nresults appended).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
0a5e007	author	Will Norris
0a5e007	added	195
0a5e007	deleted	0
0a5e007	files	1
0a5e007	body	All 115 sources classified (render surface, band painter, dirty-marking) and\nall 114 published ROMs measured headlessly (d1 = frame 500v501 delta, d60 =\n500v560). Headlines: 19 demos carry trimerge's pre-#99b per-band tearing bug\n(F1 atomic flush applies mechanically); 10 band painters already atomic;\ntrue scroll-ring (F2) candidates are few (ovmove, mvscrl, truncstair,\nlfsr2?); plotters/compute-then-hold need nothing; life/cgrade/bitcensus/\nnewton already animate per-frame. Batches: A = F1 on the 19 (in flight),\nB = F2 rings, C = F3 stall hunts. hull/truchet/truncstair band matches were\nfalse positives (no per-band marking).\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
-->
