| Date | Change |
|------|--------|
| [2026-09-15](https://github.com/wbniv/llvm-mos-65816/commit/ff1ad28) | Cluster G: ship #116 backtrack + #117 csrjmp; #118 finds an xy16 miscompile |

<!--history-meta v1
ff1ad28	author	Will Norris
ff1ad28	added	164
ff1ad28	deleted	0
ff1ad28	files	1
ff1ad28	body	Round 6 Cluster G hardens the 65816-native platforms/snes/setjmp.S fix (#35).\nTwo of the three demos land fully gated; the third is committed un-gated because\nit found a real defect.\n\n#116 backtrack (gate 0x7336) — 8-queens with one setjmp choice point per\nrecursion level; every dead end longjmps straight to the deepest still-viable\nancestor, so a single jump discards a varying number of jsr frames and exercises\nthe page-1 S reconstruct, the soft-SP restore and the __rc18..__rc31 CSR restore\ntogether. Ships its expected.tsv row, dev/backtrack.{sh,lua}, the visual ROM\n(trace replay on an 8x8 board with a title card), and dev/run.sh + Taskfile\nwiring.\n\n#117 csrjmp (gate 0xADD8) — 14 coefficient bytes, the exact width of jmp_buf's\ncsrs[14], held in locals across a setjmp while a noinline worker occupies and\nrewrites every callee-saved slot and then longjmps past the epilogue that would\nrestore them. Codegen puts 12 of the 14 in __rc20..__rc31; all come back intact,\nso the restore offsets are correct. Same full set of artifacts.\n\n#118 retryjmp — STOPPED. Its slice finds an open +mos-xy16 MISCOMPILE: the\nframe-index address materialization for a spill slot takes the live Imag16 pair\nholding the value about to be stored, so a 16-bit indexed store writes the index\nexpression. default and +mos-a16 give the host's 0x3388; xy16 gives 0x82D4.\nsetjmp.S is ruled out (the call site is in 8-bit index mode, and #116/#117 both\npass xy16 with far more longjmp traffic). The logic header, host oracle and\ncorpus slice are committed as the reproduction, deliberately with no expected.tsv\nrow, no driver and no ROM. a16_fuzz.py's KNOWN_ISSUES matches the literal string\n"Using an undefined physical register", so this would be misrecorded as the\nbenign a16-rc-undef-ra-pure-virtual XFAIL — the message text is not a safe\ndiscriminator.\n\nVerification:\n  dev/run.sh backtrack   RESULT: PASS  0x7336, MAME + bsnes-jg (MAME ran)\n  dev/run.sh csrjmp      RESULT: PASS  0xADD8, MAME + bsnes-jg (MAME ran)\n  dev/run.sh corpus      65/65 passed (63 -> 65)\n  dev/run.sh corpus-a16  64/64 passed, 0 xfail (62 -> 64); setjmp_sim 0x2007\n  dev/run.sh build       260 program(s) built / 3 excluded by contract / 0 failed\n  dev/title-charset.sh   PASS, every title character has a glyph\n  -verify-machineinstrs  exit 0 for both new slices in default, +mos-a16, +mos-xy16\n\nCo-Authored-By: Claude Sonnet 5 <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_011AP736JtwzSGYH4bmxDWTa
-->
