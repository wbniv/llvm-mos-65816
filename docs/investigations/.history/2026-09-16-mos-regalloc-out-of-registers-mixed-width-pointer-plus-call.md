| Date | Change |
|------|--------|
| [2026-09-16](https://github.com/wbniv/llvm-mos-65816/commit/d45cf9a) | Round 8 Cluster C: five boundary/width escalations (#151-#155) + a real bug |

<!--history-meta v1
d45cf9a	author	Will Norris
d45cf9a	added	128
d45cf9a	deleted	0
d45cf9a	files	1
d45cf9a	body	Clusters A and B took Round 8's un-entered branches and found no compiler\nbug. Cluster C takes the third group - paths that are covered, but only at\none width or one shape - and asks whether the branch that fired once still\nholds at the edge, at the other width, and when two of them nest. Two of\nthe five sit exactly on a size-gated classifier's boundary constant, which\nis the classic place for an off-by-one in a lowering that has been correct\nfor 141 demos.\n\nAll five ship green on the full bar: host == default == +mos-a16 ==\n+mos-xy16 on MAME AND bsnes-jg, -verify-machineinstrs clean in all three\nmodes, each with a structure gate proving the intended shape reached the\nROM. Unlike A and B, this cluster FOUND A COMPILER DEFECT.\n\nTHE BUG - "ran out of registers during register allocation", found by #154\non its first compile. Twelve lines of C (one uint16_t *, one call, three\nstores of which one is byte-width) hard-ERROR the register allocator at -O1\nand above; clean at -O0. It is NOT a +mos-a16/+mos-xy16/65816 defect and NOT\na fork regression: it reproduces on PRISTINE upstream llc with the PRISTINE\nMOS datalayout at -mcpu=mos6502. Measured one ingredient at a time, the\nnecessary combination is a call plus mixed-width (i8 AND i16) traffic\nthrough the same pointer plus three or more stores; word-only traffic of the\nsame volume is fine, and whether the call is a libcall or an ordinary extern\ndoes not matter. No fix attempted - that needs separate dispatch at a tier\nthat can work in the register allocator. #154 ships GATED: the offending\nlibcall moved out of the 5-byte stage (it is not the corner under test) and\nnothing about the ByVal check, the caller re-read or the -verify requirement\nwas relaxed. Prior art is fork patch 0009, which fixed a DIFFERENT\nout-of-registers deadlock (+mos-a16-gated, A-pinned i8 loop counter) and\ndoes not cover this one.\n\n  #151 vlanest   The DEPTH axis of G_DYN_STACKALLOC: two VLAs in nested\n                 block scopes with independent runtime lengths, the inner\n                 length derived from the OUTER allocation's contents. An\n                 overshooting unwind is silent - no crash, no verifier\n                 complaint, the inner result still right - so the outer\n                 array is re-read after every inner block closes and once\n                 more per row. Measured, and it determined the source shape:\n                 a function-scope VLA plus an inner-block VLA gives 2\n                 G_DYN_STACKALLOC but only ONE save/restore, and so does an\n                 explicit outer block entered once, because the outer restore\n                 coincides with the return and is elided. Only re-entering\n                 both scopes per loop iteration gives 2/2/2.        0x153B\n  #152 jtedge    127, 128 and 129 successors in one ROM, fed the same\n                 in-range opcode stream over three VM copies, so both\n                 legalizeBrJt arms and the exact Table.MBBs.size() <= 128\n                 test are compiled side by side. MEASURED NEGATIVE for the\n                 bug it was built to find: the boundary is exact and\n                 inclusive at 128. Shipped anyway, reframed as Cluster B\n                 reframed #148/#149 - it is the only test in the tree that\n                 pins the constant from both sides, and it adds one new\n                 measured fact: the split arm addresses its high table at a\n                 fixed +256 whatever the entry count, so 129 entries cost a\n                 full 512-byte table.                               0xC199\n  #153 jtsparse  The THIRD switch-lowering strategy, which no demo across\n                 #1-#152 forces deliberately: sparse case values never reach\n                 legalizeBrJt at all and become a binary-search compare tree.\n                 The same sixteen handler bodies run through a dense\n                 dispatcher (jump table) and a sparse one (compare tree) over\n                 independent VM copies, so strategy 3 is differentially\n                 checked against strategy 1 inside one program, both default\n                 arms live.                                         0xA131\n  #154 byvaledge clang's getTypeSize(Ty) > 32 by-value classifier compiled\n                 from both sides over records differing by one byte, every\n                 stage mutating its own parameter and the caller re-reading\n                 its original - the only detector for a missing ByVal=false\n                 call-site copy, which corrupts the CALLER silently.\n                 MEASURED CORRECTION: there is no 33-bit size class on MOS.\n                 getTypeSize is in BITS and a record is always whole bytes,\n                 so a 33-bit-declared bitfield record is sizeof 5 (40 bits)\n                 and goes indirect; the real boundary is sizeof 4 vs 5. The\n                 33-bit record ships anyway - it is what demonstrates the\n                 collapse.                                          0x4FAB\n  #155 ovmatrix  All six overflow opcodes at all three widths in ONE noinline\n                 kernel - 18 cells, every operand from runtime state, every\n                 result live across the others - so the signed and unsigned\n                 forms of each family sit adjacent under real pressure. #44,\n                 #76, #101 and #144 each test one family in isolation.\n                 MEASURED, and it changed the design: a probe with one\n                 CONSTANT operand formed G_UADDO=2 G_SADDO=2 G_UMULO=2\n                 G_SMULO=2 G_USUBO=1 G_SSUBO=0 - folding erases cells and\n                 G_SSUBO, the family #144 exists for, vanished entirely. Both\n                 operands are runtime and the gate asserts every cell fired\n                 BOTH outcomes; with that, all six appear 3x each in all\n                 three modes.                                       0xD4D0\n\nVerified: corpus 80/80, corpus-a16 79/79 (0 xfail), build 289 programs,\ntitle-charset PASS (142 call sites across 155 sources), and each demo's own\ndriver green on both emulators with a structure gate.\n\nPublishing stays out of scope, as for Clusters A and B and Round 6 Cluster G.\n\nCo-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>\nClaude-Session: https://claude.ai/code/session_011AP736JtwzSGYH4bmxDWTa
-->
