# Plan index — #320 / #321 (M0 → M2)

*An at-a-glance index of every plan under [`docs/plans/`](../plans/), one row per plan, **sorted
oldest → newest** by the commit that first introduced it. Each row: the plan (linked), a one-sentence
summary, the commit(s) that touched it (oldest → newest), and a category. This is a reading map over the
M0→M1→M2 arc the [ROADMAP](../ROADMAP.md) and [`TODO.md`](../../TODO.md) track; the plans themselves are
the contract, this table is just the table of contents.*

**Categories:** `M0 bench` · `M1 toolchain` · `#320 far-pointer` · `Emulator / CI` · `a16 codegen`
(native 16-bit-accumulator features) · `Platform` (crt0 / mode-tracking / SNES target) · `xy16` (16-bit
index-register mode) · `ABI / CC` (calling convention + prior art) · `Crash fix` (defects / miscompiles) ·
`Fuzz / corpus` (differential test bench) · `Investigation` (measurement / spike, often WON'T-DO) ·
`DWARF / debug` · `Tooling` (process / repo hygiene).

| Plan | Summary | Commit(s) | Category |
|---|---|---|---|
| [M0 — emulator smoke loop](../plans/2026-06-14-emulator-smoke-loop.md) | Stand up a headless MAME smoke loop so a bank-0 C "hello world" boots and self-asserts pass/fail from `dev/run.sh smoke` and CI — the harness the regression corpus reuses. | [`be5f443`](https://github.com/wbniv/llvm-mos-65816/commit/be5f443), [`982b6f3`](https://github.com/wbniv/llvm-mos-65816/commit/982b6f3), [`025be2f`](https://github.com/wbniv/llvm-mos-65816/commit/025be2f) | M0 bench |
| [M0 — regression corpus](../plans/2026-06-14-m0-regression-corpus-5-self-contained-c-programs.md) | Add ≥5 self-contained C programs with known outputs as a correctness baseline (`dev/run.sh corpus` 7/7) so later codegen rewrites can't silently regress. | [`c50a39c`](https://github.com/wbniv/llvm-mos-65816/commit/c50a39c), [`9caf096`](https://github.com/wbniv/llvm-mos-65816/commit/9caf096) | M0 bench |
| [M1 — from-source toolchain](../plans/2026-06-14-m1-from-source-toolchain.md) | Build llvm-mos from source (lean clang+lld) in the dev env and prove the corpus stays green on the self-built compiler — the prerequisite for all backend edits. | [`dc88c35`](https://github.com/wbniv/llvm-mos-65816/commit/dc88c35), [`93862b3`](https://github.com/wbniv/llvm-mos-65816/commit/93862b3), [`69db84f`](https://github.com/wbniv/llvm-mos-65816/commit/69db84f) | M1 toolchain |
| [#320 — far-pointer codegen](../plans/2026-06-14-320-far-pointer-codegen.md) | First real 65816 codegen: addrspace-2 far pointers lower to absolute-long (`LDA/STA $xxxxxx`) gated on W65816, with 6502 untouched, captured as tracked patch `0001`. | [`7b01c2a`](https://github.com/wbniv/llvm-mos-65816/commit/7b01c2a), [`4057be1`](https://github.com/wbniv/llvm-mos-65816/commit/4057be1), [`3358a1a`](https://github.com/wbniv/llvm-mos-65816/commit/3358a1a) | #320 far-pointer |
| [#320 Increment 2 — far-pointer end-to-end in MAME](../plans/2026-06-14-320-increment-2-far-pointer-emulator-end-to-end-mi.md) | Prove far-pointer codegen *executes* on silicon: a program far-loads a ROM constant and far-stores it, boots in MAME, and reads back `0xF3` — all in plain emulation mode. | [`a8dd8a5`](https://github.com/wbniv/llvm-mos-65816/commit/a8dd8a5) | #320 far-pointer |
| [#320 Increment 2b — multi-bank ROM far-read](../plans/2026-06-14-320-increment-2b-multi-bank-rom-far-read.md) | Far-read a global placed in ROM bank $01 (`lda $018000`) and round-trip it through MAME — the first test that genuinely reaches past the 6502's 64 KiB. | [`ec2aa31`](https://github.com/wbniv/llvm-mos-65816/commit/ec2aa31) | #320 far-pointer |
| [#320 — upstream design note](../plans/2026-06-14-320-upstream-design-note.md) | Write a self-contained design note summarizing the running far-pointer slice to anchor the upstream ABI/address-space discussion (working code earns ABI input, not a spec). | [`3c20dbc`](https://github.com/wbniv/llvm-mos-65816/commit/3c20dbc) | #320 far-pointer |
| [Second-emulator cross-check — bsnes-jg](../plans/2026-06-14-second-emulator-cross-check-bsnes-jg.md) | Add bsnes-jg as an independent second emulator (`dev/run.sh xcheck`) that agrees with MAME on the same WRAM bytes, meeting ROADMAP step 3's "both emulators" gate (Mesen2 abandoned). | [`cbfdefa`](https://github.com/wbniv/llvm-mos-65816/commit/cbfdefa), [`d5f0bef`](https://github.com/wbniv/llvm-mos-65816/commit/d5f0bef) | Emulator / CI |
| [#321 Increment 1 — 16-bit accumulator slice](../plans/2026-06-14-321-increment-1-16bit-accumulator.md) | Smallest real 16-bit-accumulator slice: a 16-bit store-of-zero fuses to `rep #$20; stz; sep #$20` under `+mos-a16`, dual-emulator-verified, proving the REP/SEP-insertion mechanism. | [`f62f8c6`](https://github.com/wbniv/llvm-mos-65816/commit/f62f8c6), [`bf402b9`](https://github.com/wbniv/llvm-mos-65816/commit/bf402b9), [`a7ce88a`](https://github.com/wbniv/llvm-mos-65816/commit/a7ce88a) | a16 codegen |
| [#321 — native-mode crt0](../plans/2026-06-14-321-native-mode-crt0.md) | Make the SNES crt0 enter 65816 native mode during bring-up (M/X default 8-bit) so all programs run native, removing the per-test `clc; xce` hack. | [`5167af8`](https://github.com/wbniv/llvm-mos-65816/commit/5167af8) | Platform |
| [#321 Increment 1b — dual-width accumulator](../plans/2026-06-14-321-increment-1b-dual-width-accumulator.md) | The hard core: real 16-bit add/sub/AND/OR/XOR (+ immediates) flow through the dual-width `A16` accumulator, dual-emulator-verified, with bitwise ops merging into one rep/sep bracket. | [`d34a89d`](https://github.com/wbniv/llvm-mos-65816/commit/d34a89d), [`6736297`](https://github.com/wbniv/llvm-mos-65816/commit/6736297), [`a6123d1`](https://github.com/wbniv/llvm-mos-65816/commit/a6123d1), [`57f55c0`](https://github.com/wbniv/llvm-mos-65816/commit/57f55c0), [`f46103d`](https://github.com/wbniv/llvm-mos-65816/commit/f46103d), [`68e2a23`](https://github.com/wbniv/llvm-mos-65816/commit/68e2a23) | a16 codegen |
| [#321 Increment 1c — chained 16-bit ALU](../plans/2026-06-14-321-increment-1c-chained-16bit-alu.md) | Keep a running sum live in `A16` across a chain (`a+b+c` → one bracket threading both adds) — the first codegen where a 16-bit value survives across operations. | [`6f25977`](https://github.com/wbniv/llvm-mos-65816/commit/6f25977) | a16 codegen |
| [#321 Increment 1d — GISel-native s16 (reverted)](../plans/2026-06-14-321-increment-1d-gisel-native-s16.md) | Prototype a full GISel-native s16 path; it compiled simple cases but crashed the register coalescer on the `A16`↔`A` aliasing, so it was reverted to keep the tree green. | [`0169001`](https://github.com/wbniv/llvm-mos-65816/commit/0169001), [`3e00947`](https://github.com/wbniv/llvm-mos-65816/commit/3e00947), [`2eef05c`](https://github.com/wbniv/llvm-mos-65816/commit/2eef05c), [`95df784`](https://github.com/wbniv/llvm-mos-65816/commit/95df784), [`313f0df`](https://github.com/wbniv/llvm-mos-65816/commit/313f0df) | a16 codegen |
| [#321 Increment 1d-retry — Imag16-resident native s16](../plans/2026-06-14-321-increment-1d-retry-imag16-native-s16.md) | Solve the 1d coalescer crash by making the s16 value reside in an `Imag16` zero-page pair; native add/sub/and/or/xor all ship and the crashing complex shape now compiles clean. | [`88994fb`](https://github.com/wbniv/llvm-mos-65816/commit/88994fb), [`e9b2221`](https://github.com/wbniv/llvm-mos-65816/commit/e9b2221), [`1628484`](https://github.com/wbniv/llvm-mos-65816/commit/1628484) | a16 codegen |
| [#321 native s16 — immediate-operand fold (`adc #imm`)](../plans/2026-06-14-321-native-s16-immediate-operand-optimization-adc.md) | Fold a constant ALU operand to the 16-bit immediate form (`adc #$0345`) instead of materializing it into an `Imag16` pair, saving ~4 instructions per immediate op. | [`1628484`](https://github.com/wbniv/llvm-mos-65816/commit/1628484) | a16 codegen |
| [#321 native s16 — fold global-operand loads](../plans/2026-06-14-321-native-s16-fold-global-operand-loads-into-the.md) | Read near-abs global operands directly via absolute ALU forms (`lda/adc abs`) instead of copying them byte-wise into `Imag16` pairs first. | [`4bfcb1a`](https://github.com/wbniv/llvm-mos-65816/commit/4bfcb1a) | a16 codegen |
| [#321 native s16 — 16-bit comparisons](../plans/2026-06-14-321-native-16bit-compares.md) | Native unsigned-ordering compares: keep s16 `G_SBC` un-narrowed and lower to one `rep; lda; cmp; sep` bracket producing C, replacing the 8-bit cpx/cpy byte chain. | [`2a30974`](https://github.com/wbniv/llvm-mos-65816/commit/2a30974) | a16 codegen |
| [#321 — cross-block REP/SEP mode tracking](../plans/2026-06-15-321-cross-block-repsep-mode-tracking.md) | Propagate the M accumulator-width mode across the CFG and hoist `rep`/`sep` out of loops, so a 16-bit loop body sets 16-bit mode once instead of every iteration. | [`7e05e1c`](https://github.com/wbniv/llvm-mos-65816/commit/7e05e1c) | Platform |
| [#321 native s16 — constant shifts](../plans/2026-06-15-321-native-16bit-constant-shifts.md) | Native 16-bit constant shifts (`<<`, unsigned `>>`) emit `asl`/`lsr` on the accumulator within one rep/sep bracket, with no 8-bit rol/ror and no shift libcall. | [`01bde72`](https://github.com/wbniv/llvm-mos-65816/commit/01bde72) | a16 codegen |
| [#321 native s16 — signed right shift (ASHR)](../plans/2026-06-15-321-native-16bit-signed-shift-ashr.md) | Native arithmetic `>>` via `cmp #$8000; ror` per bit in one rep/sep bracket, replacing the 8-bit lsr chain / shift libcall. | [`915355b`](https://github.com/wbniv/llvm-mos-65816/commit/915355b) | a16 codegen |
| [#321 native s16 — equality compares (`==`/`!=`)](../plans/2026-06-15-321-native-16bit-equality-compares.md) | Native 16-bit equality as a fused compare-branch (`rep; lda; cmp; sep; beq/bne`), replacing the 8-bit cpx/cpy chain. | [`acdba60`](https://github.com/wbniv/llvm-mos-65816/commit/acdba60) | a16 codegen |
| [#321 native s16 — signed ordering compares](../plans/2026-06-15-321-native-16bit-signed-compares.md) | Native signed `< <= > >=` via `eor #$8000` on each operand + a native 16-bit `cmp` + `bcs` under one rep/sep bracket. | [`7748e99`](https://github.com/wbniv/llvm-mos-65816/commit/7748e99) | a16 codegen |
| [Move plan-deferral commit to own branch](../plans/2026-06-15-move-plan-deferral-commit-to-own-branch.md) | Relocate the python-tui-lib plan-deferral auto-capture commit onto its own feature branch, independent of unrelated md-history work (a tooling-hygiene task, not codegen). | [`5d5abae`](https://github.com/wbniv/llvm-mos-65816/commit/5d5abae), [`03cf2f0`](https://github.com/wbniv/llvm-mos-65816/commit/03cf2f0) | Tooling |
| [#321 native s16 — indirect load/store](../plans/2026-06-15-321-native-16bit-indirect-load-store.md) | Native 16-bit `*p`/`a[i]` load and store as a single `lda (zp)` / `sta (zp)` under one rep/sep, dropping the `(zp),y` byte-pair shuffle. | [`05e5271`](https://github.com/wbniv/llvm-mos-65816/commit/05e5271) | a16 codegen |
| [#321 native s16 — absolute load/store](../plans/2026-06-15-321-native-16bit-absolute-load-store.md) | Native 16-bit absolute load/store (`g = gg` → `lda abs`/`sta abs`), with the copy and a following add merging into one rep bracket. | [`b9854af`](https://github.com/wbniv/llvm-mos-65816/commit/b9854af), [`8400d66`](https://github.com/wbniv/llvm-mos-65816/commit/8400d66) | a16 codegen |
| [#321 native s16 — compare abs-operand fold](../plans/2026-06-15-321-native-16bit-compare-abs-operand-fold.md) | Fold a global compare operand directly into the 16-bit compare (`lda abs`/`cmp abs`) instead of materializing both operands into `Imag16` pairs first. | [`ef4671d`](https://github.com/wbniv/llvm-mos-65816/commit/ef4671d) | a16 codegen |
| [#321 native s16 — mixed-operand load-fold](../plans/2026-06-15-321-native-s16-mixed-operand-load-fold.md) | Fold a single near-abs global operand (the other being `Imag16`-resident) directly into the absolute ALU form, mirroring the both-global fold per-operand. | [`75b395e`](https://github.com/wbniv/llvm-mos-65816/commit/75b395e) | a16 codegen |
| [#321 native s16 — single-use-non-store fold](../plans/2026-06-15-321-native-s16-single-use-non-store-fold.md) | Confirm (no code change) that single-use-non-store ALU results already fold their global operands via the mixed-operand fold, since that fold keys on operands not result use. | [`b8585db`](https://github.com/wbniv/llvm-mos-65816/commit/b8585db) | a16 codegen |
| [#321 native s16 — register `inc a`/`dec a`](../plans/2026-06-15-321-native-s16-inc-dec-accumulator.md) | Keep a register/`Imag16` s16 `±1` native as a 1-byte `inc a`/`dec a` instead of dropping to an 8-bit byte `G_INC`/`G_DEC` chain that thrashes M-mode. | [`98c5eac`](https://github.com/wbniv/llvm-mos-65816/commit/98c5eac) | a16 codegen |
| [#321 native s16 — `g ± 1` global RMW](../plans/2026-06-15-321-native-s16-inc-dec-memory-rmw.md) | Compile a global `g = g ± 1` to `lda g; inc/dec a; sta g` (dropping the `clc; adc #imm16`); memory-RMW `inc abs` was prototyped and rejected as DBR-unsafe on this platform. | [`c069393`](https://github.com/wbniv/llvm-mos-65816/commit/c069393) | a16 codegen |
| [#321 native s16 — multi-use add chain](../plans/2026-06-15-321-native-s16-add-chain-multiuse.md) | Thread a multi-use ≥3-term add chain's running sum through `A16` without intermediate `Imag16` round-trips, extending the 1c store-rooted chain to a register result. | [`609c6c4`](https://github.com/wbniv/llvm-mos-65816/commit/609c6c4) | a16 codegen |
| [WDC816CC / ORCA-C ABI prior-art note](../plans/2026-06-15-wdc816cc-orca-c-65816-c-abi-prior-art-note-primary.md) | Document 1990s commercial 65816 C ABI prior art from primary sources only (WDC816CC manual + ORCA/C `Gen.pas`), vendoring the sources, to inform the upstream CC decision. | [`d13970f`](https://github.com/wbniv/llvm-mos-65816/commit/d13970f), [`c8f7d02`](https://github.com/wbniv/llvm-mos-65816/commit/c8f7d02) | ABI / CC |
| [#321 native s16 — immediates in add chains](../plans/2026-06-15-321-native-s16-add-chain-immediate.md) | Allow a constant leaf in a ≥3-term add chain (`a+b+c+K`) so the chain threads through `A16` with the constant folded as `adc #imm` instead of breaking the fusion. | [`9a7a899`](https://github.com/wbniv/llvm-mos-65816/commit/9a7a899) | a16 codegen |
| [Wire bsnes-jg `xcheck` into CI](../plans/2026-06-15-wire-bsnes-jg-xcheck-into-ci.md) | Add the second-emulator bsnes-jg cross-check (proven locally) into the CI smoke workflow, delivering the dual-emulator CI bench. | [`d5f0bef`](https://github.com/wbniv/llvm-mos-65816/commit/d5f0bef), [`e1f155c`](https://github.com/wbniv/llvm-mos-65816/commit/e1f155c), [`b873e6d`](https://github.com/wbniv/llvm-mos-65816/commit/b873e6d) | Emulator / CI |
| [#321 native s16 — bitwise chains](../plans/2026-06-15-321-native-s16-bitwise-chains.md) | Thread homogeneous ≥3-term AND/OR/XOR chains through `A16` (`lda; and; and; sta`, no carry-init), generalizing the add-chain machinery to the carry-free bitwise ops. | [`dda6209`](https://github.com/wbniv/llvm-mos-65816/commit/dda6209) | a16 codegen |
| [#321 native-s16 Tier 1 — broaden corpus](../plans/2026-06-15-321-tier1-broaden-corpus.md) | Build a differential fuzzer + 6 kernels to de-risk A16-threading; found 3 real `+mos-a16` defects (2 fixed with regression tests, 1 XFAIL'd). | [`69827b5`](https://github.com/wbniv/llvm-mos-65816/commit/69827b5), [`56801ba`](https://github.com/wbniv/llvm-mos-65816/commit/56801ba), [`4c603a2`](https://github.com/wbniv/llvm-mos-65816/commit/4c603a2), [`1a39c9b`](https://github.com/wbniv/llvm-mos-65816/commit/1a39c9b) | Fuzz / corpus |
| [#321 — fix compare-as-value SelectImm crash (F3)](../plans/2026-06-16-321-fix-cmp-value-selectimm.md) | Fix the F3 crash (a 16-bit compare result consumed as a cross-block i1) by spilling the `Ac16` accumulator via a 16-bit load/store instead of a COPY through an 8-bit GPR; fuzz 50/50. | [`56801ba`](https://github.com/wbniv/llvm-mos-65816/commit/56801ba), [`4c603a2`](https://github.com/wbniv/llvm-mos-65816/commit/4c603a2), [`1a39c9b`](https://github.com/wbniv/llvm-mos-65816/commit/1a39c9b), [`70d9186`](https://github.com/wbniv/llvm-mos-65816/commit/70d9186) | Crash fix |
| [#321 — s16 load consumed as bytes stays byte-wise](../plans/2026-06-16-321-s16-load-unmerge-bytewise.md) | Remove a `+mos-a16` EQ-as-value regression where s16 loads were emitted 16-bit then immediately unmerged to bytes; keep byte-only-consumed loads byte-wise (parity with default). | [`7c0fe56`](https://github.com/wbniv/llvm-mos-65816/commit/7c0fe56), [`8bce630`](https://github.com/wbniv/llvm-mos-65816/commit/8bce630), [`60d4f32`](https://github.com/wbniv/llvm-mos-65816/commit/60d4f32) | a16 codegen |
| [#321 — soft-stack spill coverage gap](../plans/2026-06-16-321-soft-stack-spill-coverage.md) | Close the gap that the reentrant soft-stack `Ac16` spill was only proven by a hand-written reproducer; extend the fuzzer with recursion, which surfaced upstream bug F4. | [`7c0fe56`](https://github.com/wbniv/llvm-mos-65816/commit/7c0fe56), [`d0f8bd7`](https://github.com/wbniv/llvm-mos-65816/commit/d0f8bd7), [`4f607ed`](https://github.com/wbniv/llvm-mos-65816/commit/4f607ed), [`0b855e0`](https://github.com/wbniv/llvm-mos-65816/commit/0b855e0), [`e0f160a`](https://github.com/wbniv/llvm-mos-65816/commit/e0f160a), [`bf5271f`](https://github.com/wbniv/llvm-mos-65816/commit/bf5271f), [`81263cd`](https://github.com/wbniv/llvm-mos-65816/commit/81263cd) | Fuzz / corpus |
| [#321 — indirect s16 byte-wise load (SKIP)](../plans/2026-06-16-321-indirect-s16-load-bytewise.md) | Re-measurement in realistic 16-bit-ambient code showed the byte-wise indirect-load win is schedule-dependent (+2 B regression when arithmetic intervenes), so it fails the bar and is dropped. | [`d6e07b0`](https://github.com/wbniv/llvm-mos-65816/commit/d6e07b0), [`37674ff`](https://github.com/wbniv/llvm-mos-65816/commit/37674ff) | Investigation |
| [#321 F4 — `mos-late-opt` TYX/TXY dead-flag crash](../plans/2026-06-16-321-f4-late-opt-txy-dead-flag.md) | Fix an upstream llvm-mos `mos-late-opt` crash on a recursive `+mos-a16` function (found by the soft-stack fuzzer); patch `0003` + regression tests, upstream PR ready. | [`0fe82ab`](https://github.com/wbniv/llvm-mos-65816/commit/0fe82ab) | Crash fix |
| [#321 — native s16 EQ-as-value (design/spike)](../plans/2026-06-16-321-native-s16-eq-as-value-cmpsel.md) | Spike the fused compare-select for `b = (a==c)`: blanket form regresses register/global operands, so the recommendation is to gate by operand residency — realized as four gated wins. | [`37674ff`](https://github.com/wbniv/llvm-mos-65816/commit/37674ff), [`af22812`](https://github.com/wbniv/llvm-mos-65816/commit/af22812) | a16 codegen |
| [#321 — gated native s16 EQ-as-value (v1)](../plans/2026-06-16-321-native-s16-eq-gated-impl.md) | Land v1: gate native EQ-as-value to the indirect-load operand shape (`*p==c`, 38→34 B) with no new pseudo and every other shape byte-identical (regression-free). | [`37674ff`](https://github.com/wbniv/llvm-mos-65816/commit/37674ff), [`af22812`](https://github.com/wbniv/llvm-mos-65816/commit/af22812) | a16 codegen |
| [#321 — native s16 EQ-as-value v3 (both-global)](../plans/2026-06-17-321-native-s16-eq-as-value-v3-abs-fold-globals.md) | Fold both-global equality (`g1==g2`) to native `lda abs; cmp abs` (`CmpBrAbsAbs16`), measured −48 B on a chained 3-compare test. | [`efce68f`](https://github.com/wbniv/llvm-mos-65816/commit/efce68f), [`f8a32ae`](https://github.com/wbniv/llvm-mos-65816/commit/f8a32ae) | a16 codegen |
| [#321 — native s16 EQ-vs-immediate (`g==0x1234`)](../plans/2026-06-17-321-native-s16-eq-imm-constant-through-merge.md) | Recover a 16-bit constant byte-split into `G_MERGE_VALUES` so `g==0x1234` folds to `cmp #imm` (`CmpBrAbsImm16`) instead of materializing the constant. | [`f8a32ae`](https://github.com/wbniv/llvm-mos-65816/commit/f8a32ae) | a16 codegen |
| [#321 — native s16 EQ-as-value v2 (computed)](../plans/2026-06-17-321-native-s16-eq-v2-computed-imag16-lhs.md) | Gate-only extension firing native EQ when both operands are `Imag16`-resident/computed (`(a+b)==c`, −3 B), while a register/param operand stays 8-bit byte-identical. | [`fd6b281`](https://github.com/wbniv/llvm-mos-65816/commit/fd6b281) | a16 codegen |
| [#321 — A16-threading](../plans/2026-06-17-321-a16-threading.md) | Realize the step-5 "biggest win" as a coalescer-free redundant-reload peephole keeping the s16 value live in the accumulator across ops; Phase 2 superseded, Phase 3 (RA residency) deferred — then CLOSED 2026-06-26 as measured net-negative (trigger fired; the pre-RA spike gave +24 B / zero peak-ZP relief). | [`2715579`](https://github.com/wbniv/llvm-mos-65816/commit/2715579), [`1a2145e`](https://github.com/wbniv/llvm-mos-65816/commit/1a2145e), [`fc3dc29`](https://github.com/wbniv/llvm-mos-65816/commit/fc3dc29), [`c66329b`](https://github.com/wbniv/llvm-mos-65816/commit/c66329b), [`eaeab18`](https://github.com/wbniv/llvm-mos-65816/commit/eaeab18), [`3eeab5d`](https://github.com/wbniv/llvm-mos-65816/commit/3eeab5d), [`718ccd9`](https://github.com/wbniv/llvm-mos-65816/commit/718ccd9), [`1ccd2f0`](https://github.com/wbniv/llvm-mos-65816/commit/1ccd2f0) | a16 codegen |
| [#321 P2 — hermetic `.ll` soft-stack crash regression](../plans/2026-06-17-p2-hermetic-ll-crash-regression-for-the-soft-stack.md) | Add a frozen LLVM-IR fixture run through `llc` as a hermetic regression guard for the soft-stack `Ac16` spill crash, independent of front-end/optimizer drift. | [`bf5271f`](https://github.com/wbniv/llvm-mos-65816/commit/bf5271f) | Crash fix |
| [#321 Task 7 — EQ residuals, indir-dst, X-flag, var-shift](../plans/2026-06-17-321-task7-eq-residuals-indir-dst-xflag-varshift.md) | Post-A16-threading sweep: implement `computed == global` (`CmpBrImagAbs16`) and disposition the remaining EQ/indir-dst/X-flag/variable-shift residuals with spike measurements. | [`fd304f6`](https://github.com/wbniv/llvm-mos-65816/commit/fd304f6), [`11dc41b`](https://github.com/wbniv/llvm-mos-65816/commit/11dc41b), [`4f4e8ce`](https://github.com/wbniv/llvm-mos-65816/commit/4f4e8ce), [`06b90fe`](https://github.com/wbniv/llvm-mos-65816/commit/06b90fe) | a16 codegen |
| [#321 — lock A(low)/X(high) return convention](../plans/2026-06-17-321-ax-return-convention.md) | Lock the return convention (A low / X high) as a tested ABI invariant — the one free, prior-art-aligned CC piece, already what llvm-mos emits, with no codegen change. | [`7ee904f`](https://github.com/wbniv/llvm-mos-65816/commit/7ee904f), [`514bd4f`](https://github.com/wbniv/llvm-mos-65816/commit/514bd4f), [`a2b9fcf`](https://github.com/wbniv/llvm-mos-65816/commit/a2b9fcf), [`2f9d6d1`](https://github.com/wbniv/llvm-mos-65816/commit/2f9d6d1), [`51a5bae`](https://github.com/wbniv/llvm-mos-65816/commit/51a5bae), [`b2e139c`](https://github.com/wbniv/llvm-mos-65816/commit/b2e139c) | ABI / CC |
| [#321 — retire 1b/1c peephole into native path](../plans/2026-06-17-321-unify-1b-1c-peephole-into-native.md) | Retire the redundant 1b/1c pre-legalizer GISel combiner (~1,400 lines across six files) now that the GISel-native path covers all the all-global ALU shapes. | [`f390a78`](https://github.com/wbniv/llvm-mos-65816/commit/f390a78), [`329fbff`](https://github.com/wbniv/llvm-mos-65816/commit/329fbff), [`a2b9fcf`](https://github.com/wbniv/llvm-mos-65816/commit/a2b9fcf) | a16 codegen |
| [Apply llvm-mos-sdk#415 SNES target + 16-bit variant](../plans/2026-06-15-415-snes-target-apply-and-16bit.md) | Apply and fix the stalled community SNES SDK PR #415 (8-bit emulation-mode target + MMIO map/DMA helpers), then add the 16-bit native variant. | [`c707b8e`](https://github.com/wbniv/llvm-mos-65816/commit/c707b8e) | Platform |
| [#321 — xy16 16-bit index-register mode (plan)](../plans/2026-06-17-321-xy16-index-register-mode.md) | Implementation plan for the X-flag/index dimension (`+mos-xy16`), the second half of ROADMAP step 5, layered 1–5 on the `wt/321-xy16` worktree. | [`749d8e9`](https://github.com/wbniv/llvm-mos-65816/commit/749d8e9), [`8d8515d`](https://github.com/wbniv/llvm-mos-65816/commit/8d8515d), [`c2812d4`](https://github.com/wbniv/llvm-mos-65816/commit/c2812d4), [`c6e6c7c`](https://github.com/wbniv/llvm-mos-65816/commit/c6e6c7c) | xy16 |
| [#321 — fix seed-42 default-build miscompile](../plans/2026-06-18-321-seed42-legalizeicmp-swap-fix.md) | Fix a miscompile where an `0002` `legalizeICmp` EQ-canonicalization swap leaked into the non-a16 path (seed-42 computed wrong value in both builds); gate it on `NativeS16Eq`. | [`b2e139c`](https://github.com/wbniv/llvm-mos-65816/commit/b2e139c) | Crash fix |
| [#321 — indir-dst copy fold (handoff, WON'T-DO)](../plans/2026-06-18-321-indir-dst-copy-fold-handoff.md) | Handoff recording that the indir-dst volatile-pointer copy fold is WON'T-DO: a corpus trigger check found the round-trip pattern in 0/6 programs (0 B), so the selector reorder isn't justified. | [`5f239f3`](https://github.com/wbniv/llvm-mos-65816/commit/5f239f3) | Investigation |
| [#321 — indir-dst copy fold (WON'T-DO)](../plans/2026-06-18-321-indir-dst-copy-fold.md) | Proposed folding `*p = gg` when a volatile dst-pointer load sits between value-load and store; dropped as WON'T-DO since the triggering pattern is absent from the corpus. | [`5f239f3`](https://github.com/wbniv/llvm-mos-65816/commit/5f239f3), [`c638160`](https://github.com/wbniv/llvm-mos-65816/commit/c638160) | Investigation |
| [#321 — xy16 implementation handoff](../plans/2026-06-18-321-xy16-implementation-handoff.md) | Worktree handoff for the xy16 implementation: vendor bootstrap, ccache, commit discipline, and nine audited Layer-3 design corrections (three would have been silent miscompiles). | [`aa53d03`](https://github.com/wbniv/llvm-mos-65816/commit/aa53d03), [`c2812d4`](https://github.com/wbniv/llvm-mos-65816/commit/c2812d4), [`b8a70be`](https://github.com/wbniv/llvm-mos-65816/commit/b8a70be) | xy16 |
| [#321 native s16 — `abs,x` and `(zp),y` indexed load/store](../plans/2026-06-18-321-abs-x-indiry-16bit-indexed-load-store.md) | Add the two deferred indexed 16-bit access forms (`arr[i]` via `abs,x`, `*p` with Y offset via `(zp),y`) as native single rep/sep loads instead of byte-pair sequences. | [`940e26f`](https://github.com/wbniv/llvm-mos-65816/commit/940e26f), [`421c0b9`](https://github.com/wbniv/llvm-mos-65816/commit/421c0b9) | a16 codegen |
| [#321 — native-mode crt0 xy16-era audit](../plans/2026-06-18-321-native-mode-crt0-xy16.md) | Audit the crt0 against the xy16 era: the only real gap is establishing DBR=0 explicitly (load-bearing for the abs path + MMIO); close it with a 2-byte `phk; plb` + a contract gate. | [`05d98c6`](https://github.com/wbniv/llvm-mos-65816/commit/05d98c6) | Platform |
| [#321 — xy16 legalizer integration (`selectXY16`)](../plans/2026-06-18-321-xy16-legalizer-integration.md) | Wire up `selectXY16` (Increment 1e): `LDXImag16`/`STXImag16`/`LDAbsXIdx16`/`G_LOAD_ABS_IDX16` legalizer/selector integration, fixing the seed-56 crash. | [`5d5e1f5`](https://github.com/wbniv/llvm-mos-65816/commit/5d5e1f5), [`f348042`](https://github.com/wbniv/llvm-mos-65816/commit/f348042), [`0b70c2e`](https://github.com/wbniv/llvm-mos-65816/commit/0b70c2e) | xy16 |
| [#321 — 16-bit ALU multi-value register pressure](../plans/2026-06-18-321-16bit-alu-multivalue-register-pressure.md) | Measure the "pressure exceeds one 16-bit slot" premise: there are ~14 `Imag16` pairs, not one slot, so the framing is misleading and the item is mostly already solved; impl gated on a trigger scan. | [`97e58e4`](https://github.com/wbniv/llvm-mos-65816/commit/97e58e4), [`3d5a989`](https://github.com/wbniv/llvm-mos-65816/commit/3d5a989) | Investigation |
| [#321 — full-native EQ materialize (WON'T-IMPLEMENT)](../plans/2026-06-18-321-native-s16-eq-as-value-full-native-materialize.md) | Spike the full one-bracket compare-and-materialize for EQ-as-value: both candidate materializations measured as regressions (+14 / +16–28 B), so the existing select-diamond stands. | [`ef4e6db`](https://github.com/wbniv/llvm-mos-65816/commit/ef4e6db), [`e4858eb`](https://github.com/wbniv/llvm-mos-65816/commit/e4858eb) | Investigation |
| [#321 — prove Option B rol-tail (regression)](../plans/2026-06-18-prove-option-b-rol-tail-materialization-for-native.md) | Prove the Option B branchless `rol`/`adc` tail for EQ-as-value is also a regression (+16–28 B) because it forgoes the `CmpBr` compare-fusion; confirms WON'T-IMPLEMENT. | [`e4858eb`](https://github.com/wbniv/llvm-mos-65816/commit/e4858eb), [`161934f`](https://github.com/wbniv/llvm-mos-65816/commit/161934f) | Investigation |
| [DWARF round-trip — ROADMAP step 6 (drmon)](../plans/2026-06-18-dwarf-round-trip-roadmap-step-6-drmon-tie-in.md) | Front-load the drmon debugger-side DWARF round-trip work (DAP live-MAME, ELF/DWARF loader, read-only `-g` audit), deferring compiler-source changes. | [`fdd1fd4`](https://github.com/wbniv/llvm-mos-65816/commit/fdd1fd4), [`6ff4689`](https://github.com/wbniv/llvm-mos-65816/commit/6ff4689), [`98a4340`](https://github.com/wbniv/llvm-mos-65816/commit/98a4340), [`e609de4`](https://github.com/wbniv/llvm-mos-65816/commit/e609de4), [`cc940f1`](https://github.com/wbniv/llvm-mos-65816/commit/cc940f1), [`fd80f89`](https://github.com/wbniv/llvm-mos-65816/commit/fd80f89), [`911dcf2`](https://github.com/wbniv/llvm-mos-65816/commit/911dcf2) | DWARF / debug |
| [#321 — calling-convention frame decision (phased)](../plans/2026-06-18-321-cc-frame-phased-decision.md) | Record the project-lead steer resolving the CC frame sub-decision: phase it — first-pass ABI keeps the soft static stack, defer the TCD direct-page window behind a ZP-pressure measurement. | [`e30f01c`](https://github.com/wbniv/llvm-mos-65816/commit/e30f01c), [`3d5a989`](https://github.com/wbniv/llvm-mos-65816/commit/3d5a989), [`715668f`](https://github.com/wbniv/llvm-mos-65816/commit/715668f) | ABI / CC |
| [#321 — REPSEP critical-edge X16 liveness (seed-31)](../plans/2026-06-18-321-repsep-critical-edge-x16-liveness.md) | Fix seed-31, where the REPSEP critical-edge bail forced X=8 at terminators and truncated a cross-block-live `$x16`; re-landed after X-governed transfers were annotated (fuzz 492/500, 0 mismatch). | [`8961afb`](https://github.com/wbniv/llvm-mos-65816/commit/8961afb), [`82edb10`](https://github.com/wbniv/llvm-mos-65816/commit/82edb10), [`43697f3`](https://github.com/wbniv/llvm-mos-65816/commit/43697f3) | xy16 |
| [#321 — xy16 hang fix (XHigh annotation)](../plans/2026-06-18-321-xy16-hang-fix-xhigh.md) | Fix 34 xy16 fuzzer hangs by annotating byte-level `ldx/ldy/stx/sty` with `XHigh=1` so the X-width lattice doesn't leave them 16-bit; fuzz 16/50 → 49/50, all hangs cleared. | [`8961afb`](https://github.com/wbniv/llvm-mos-65816/commit/8961afb) | xy16 |
| [#321 — zero-page-pressure measurement](../plans/2026-06-18-321-zp-pressure-measurement.md) | Build `dev/measure-zp-pressure.sh` and capture a baseline (14 usable `Imag16` pairs) as the named revival trigger for the CC-frame TCD window and the multi-value Phase 0 scan; run on a throwaway branch. | [`9fc5cf2`](https://github.com/wbniv/llvm-mos-65816/commit/9fc5cf2), [`3eeab5d`](https://github.com/wbniv/llvm-mos-65816/commit/3eeab5d), [`718ccd9`](https://github.com/wbniv/llvm-mos-65816/commit/718ccd9), [`c998d7f`](https://github.com/wbniv/llvm-mos-65816/commit/c998d7f) | Investigation |
| [#321 — REPSEP X-annotation for X-governed transfers](../plans/2026-06-18-repsep-x-annotation-for-x-governed-transfers-push.md) | Annotate X-governed transfers/push-pull (`TAX`/`TAY`/`PHX`/`PLX` → `XW_X8`) in `requiredXWidth` to fix the latent transfer-width bug that blocked the seed-31 critical-edge fix. | [`4d8a2bd`](https://github.com/wbniv/llvm-mos-65816/commit/4d8a2bd), [`43697f3`](https://github.com/wbniv/llvm-mos-65816/commit/43697f3) | xy16 |
| [#321 — `corpus-a16` differential mode](../plans/2026-06-19-321-corpus-a16-differential-mode.md) | Run the M0/M1 SNES regression corpus under `+mos-a16`/`+mos-xy16` as a standing differential gate (`dev/corpus-a16.sh`, 5 PASS + `globals` XFAIL), closing the gap that hid the `globals.c` RA crash. | [`c998d7f`](https://github.com/wbniv/llvm-mos-65816/commit/c998d7f) | Fuzz / corpus |
| [Wire `corpus-a16` into CI](../plans/2026-06-19-321-corpus-a16-ci.md) | Wire the `corpus-a16` differential gate into the CI smoke workflow, mirroring the bsnes-jg `xcheck` CI pattern. | [`95dd127`](https://github.com/wbniv/llvm-mos-65816/commit/95dd127), [`b873e6d`](https://github.com/wbniv/llvm-mos-65816/commit/b873e6d) | Emulator / CI |
| [#321 — xy16 `(zp),Y16` indirect-indexed gate](../plans/2026-06-19-321-xy16-indiry-gate.md) | Add the missing test for the wired-but-unexercised `(zp),Y16` xy16 sub-path (`G_LOAD_INDIR_IDX16` → `LDYImag16`+`LDIndirYIdx16`); gate passes, path was already correct (no codegen bug). | [`858a53e`](https://github.com/wbniv/llvm-mos-65816/commit/858a53e) | xy16 |
| [Fix stale `// skeleton` comments in `selectXY16`](../plans/2026-06-19-fix-the-stale-skeleton-comments-in-selectxy16-rege.md) | Correct three stale `// skeleton`/`returns false` comments on the fully-implemented `selectXY16` (C1 direct + C2 indexed) that nearly led to re-implementing done work; comment-only `0002` regen. | [`c2882b3`](https://github.com/wbniv/llvm-mos-65816/commit/c2882b3), [`8456462`](https://github.com/wbniv/llvm-mos-65816/commit/8456462) | xy16 |
| [Vendor GCC `c-torture/execute` behind the differential gate](../plans/2026-06-19-321-c-torture-execute-differential-suite.md) | Slot the de-facto-standard execution-correctness suite into the differential engine with the default build as oracle (in-scope iff default passes, then any a16/xy16 disagreement is a real defect); Phases 0+1 + the Phase 2 `-O1` pass done — 1253/1656 in-scope; full `-O1` pass 1098 PASS/136 SKIP/3 known-XFAIL **+ 16 confirmed NEW a16/xy16 runtime miscompiles** (both emulators; `xfails.tsv`). **Full vendoring done 2026-06-26** — filter scans the whole suite (top-level + `ieee/`+`builtins/`): 1288/1779 in-scope; `ieee/` adds 60 fp tests (0 FAIL @ -Os/-O1) + surfaced one new `+mos-xy16` fp compare-as-select miscompile ([full-vendoring plan](../plans/2026-06-26-finish-the-full-vendoring-of-the-gcc-c-torture-exe.md); `xfails`: fp-cmp-8/8l/pr38016). | [`34cd16a`](https://github.com/wbniv/llvm-mos-65816/commit/34cd16a), [`8085d2a`](https://github.com/wbniv/llvm-mos-65816/commit/8085d2a), [`15542ff`](https://github.com/wbniv/llvm-mos-65816/commit/15542ff), [`0d76bfc`](https://github.com/wbniv/llvm-mos-65816/commit/0d76bfc), [`215f74d`](https://github.com/wbniv/llvm-mos-65816/commit/215f74d), [`bae20ba`](https://github.com/wbniv/llvm-mos-65816/commit/bae20ba), [`55ec505`](https://github.com/wbniv/llvm-mos-65816/commit/55ec505), [`e865dff`](https://github.com/wbniv/llvm-mos-65816/commit/e865dff), [`e10d98f`](https://github.com/wbniv/llvm-mos-65816/commit/e10d98f) | Fuzz / corpus |
| [bsnes-jg-only confirmation suite (`JG_ONLY` xcheck)](../plans/2026-06-19-second-emulator-jg-only-confirmation.md) | Add a `JG_ONLY` guard in `dev/_emu.sh` + `dev/xcheck-suite.sh` so the deterministic BIOS-free bsnes-jg leg can run alone across all 45 a16/xy16 value tests, confirming xy16 codegen on the second emulator (45/45 in ~49 s). | [`d26a5e6`](https://github.com/wbniv/llvm-mos-65816/commit/d26a5e6), [`f30c9ea`](https://github.com/wbniv/llvm-mos-65816/commit/f30c9ea), [`373ad80`](https://github.com/wbniv/llvm-mos-65816/commit/373ad80) | Emulator / CI |
| [#321 — Csmith differential fuzzer (default-as-oracle)](../plans/2026-06-19-321-csmith-differential-fuzzer.md) | Replace the hand-rolled fuzzer's generator with Csmith (16-bit-int platform + `safe_math`, default-build-as-oracle), keep the builtin generator selectable via `--gen`, add a sampled `fuzz-csmith` CI job; its first run surfaced the a16-unmerge-s32 legalizer gap. **Closed out + consolidated** into [`docs/investigations/csmith-differential-harness.md`](../investigations/csmith-differential-harness.md). | [`d39d49b`](https://github.com/wbniv/llvm-mos-65816/commit/d39d49b), [`18c2c1f`](https://github.com/wbniv/llvm-mos-65816/commit/18c2c1f), [`1d9fa68`](https://github.com/wbniv/llvm-mos-65816/commit/1d9fa68), [`9bdf5d4`](https://github.com/wbniv/llvm-mos-65816/commit/9bdf5d4), [`e865dff`](https://github.com/wbniv/llvm-mos-65816/commit/e865dff), [`2cfd375`](https://github.com/wbniv/llvm-mos-65816/commit/2cfd375) | Fuzz / corpus |
| [#321 a16 s32 — `G_UNMERGE_VALUES` s32→2×s16 legalizer](../plans/2026-06-19-321-a16-unmerge-s32-legalizer.md) | Add s32 (`long`) support under `+mos-a16` via four `hasAccum16`-gated legalizer rules for `G_ANYEXT`/`G_TRUNC`/`G_MERGE`/`G_UNMERGE` across the s32↔2×s16 boundary, fixing the Csmith seed-11 unable-to-legalize abort. | [`827e88e`](https://github.com/wbniv/llvm-mos-65816/commit/827e88e), [`d093906`](https://github.com/wbniv/llvm-mos-65816/commit/d093906) | a16 codegen |
| [Pre-public polish — LICENSE, NOTICE, README M2, gitignore](../plans/2026-06-14-pre-public-polish-license-readme-m2-gitignore.md) | Add an Apache-2.0 LICENSE and NOTICE, rewrite the README Status section to the accurate mid-M2 snapshot, and gitignore `docs/transcripts/` before the repo goes public. | [`181af86`](https://github.com/wbniv/llvm-mos-65816/commit/181af86), [`322cc1b`](https://github.com/wbniv/llvm-mos-65816/commit/322cc1b) | Tooling |
| [#321 — fix `CmpBrAbsImm16` frame-index scramble](../plans/2026-06-19-321-cmpbrabsimm16-frameindex-elimination-scramble.md) | Fix `eliminateFrameIndex` positionally treating `CmpBrAbsImm16`'s compare immediate as the frame displacement (corrupting stack addresses under a16 LTO) by keying off opcode instead; clears 13 of 18 c-torture xfails. | [`f2d65c2`](https://github.com/wbniv/llvm-mos-65816/commit/f2d65c2) | Crash fix |
| [Honor `dg-require-effective-target` in c-torture filter](../plans/2026-06-19-321-torture-honor-dg-require-effective-target.md) | Teach the c-torture Phase-0 filter to honor gcc `dg-require-effective-target` by denying `int32plus`/`int128` on a 16-bit-int target, reclassifying pr7284-1 as unsupported and shrinking in-scope 1253→1228; harness-only. | [`8622e3f`](https://github.com/wbniv/llvm-mos-65816/commit/8622e3f) | Fuzz / corpus |
| [#321 — root-cause the pr49419 a16/xy16 hang](../plans/2026-06-19-321-c-torture-pr49419-a16-xy16-hang.md) | Root-cause the c-torture `pr49419` infinite-loop hang to the `MOSInsertREPSEP` `requiredXWidth` X-flag gap (a `cpx`/`cpy` index compare ran in ambient X=16); fix in `0002` so pr49419, doloop-1, 20041011-1, va-arg-22, and k_isort all XPASS. | [`185b886`](https://github.com/wbniv/llvm-mos-65816/commit/185b886), [`55ec505`](https://github.com/wbniv/llvm-mos-65816/commit/55ec505) | Crash fix |
| [#321 — xy16 X-flag lattice fix in `MOSInsertREPSEP`](../plans/2026-06-19-321-xy16-xflag-lattice-fix.md) | Add `CMPImm`/`CMPImag8`/`CMPAbs` and `INC`/`DEC` index-value ops to `requiredXWidth` so `cpx`/`cpy`/`inx`/`iny` run at X=8 instead of the ambient X=16, clearing all 5 remaining xy16 defects with one fix. | [`b155b0d`](https://github.com/wbniv/llvm-mos-65816/commit/b155b0d), [`55ec505`](https://github.com/wbniv/llvm-mos-65816/commit/55ec505), [`2c720a0`](https://github.com/wbniv/llvm-mos-65816/commit/2c720a0) | xy16 |
| [#321 native s16 — RHS-indexed 16-bit compare fold](../plans/2026-06-19-321-native-s16-16-bit-indexed-comparisons-rhs-cmp.md) | Add a `hasAccum16`-gated `CMPIndir16` (`cmp (zp)`) fold so `limit < arr[i]` reads the indexed RHS operand in place instead of staging it through an `Imag16` pair (saves the `lda`+`sta` round-trip plus a freed ZP slot). | [`9009260`](https://github.com/wbniv/llvm-mos-65816/commit/9009260) | a16 codegen |
| [#321 — broad c-torture `-Os` differential sweep](../plans/2026-06-20-321-broad-c-torture-sweep.md) | Run the full ~1228-test in-scope gcc c-torture `execute` suite through the 4-way differential gate at `-Os`, surfacing the pr34768-1/-2 cluster (fixed in `86c2602`) and confirming the set green at 1174/0 with full bsnes-jg cross-check. | [`455df83`](https://github.com/wbniv/llvm-mos-65816/commit/455df83), [`364de87`](https://github.com/wbniv/llvm-mos-65816/commit/364de87), [`3e46cf6`](https://github.com/wbniv/llvm-mos-65816/commit/3e46cf6) | Fuzz / corpus |
| [#321 — A16-threading Phase 3 deferral (docs-only)](../plans/2026-06-20-321-a16-threading-phase-3-formalize-the-deferral-r.md) | Formalize the Phase-3 RA-residency deferral with a corrected framing (pre-RA `Ac16` residency is the fix, the `shouldCoalesce` barrier only a safety companion), a concrete re-open trigger, and a gated B0-B1-B2 spike recipe. | [`1ccd2f0`](https://github.com/wbniv/llvm-mos-65816/commit/1ccd2f0) | Investigation |
| [#321 — frame-ABI three-way head-to-head (shelved)](../plans/2026-06-20-321-frame-abi-build-all-three-and-measure.md) | Build and measure all three 65816 stack-frame strategies (soft static stack, TCD direct-page window, stack-relative); the census found 0 of 13 realistic functions can profit since locals are `__rc`-resident, so DP-window and stack-relative are both CONFIRMED-shelved. | [`ad1d6d3`](https://github.com/wbniv/llvm-mos-65816/commit/ad1d6d3), [`5a33180`](https://github.com/wbniv/llvm-mos-65816/commit/5a33180), [`d83dacf`](https://github.com/wbniv/llvm-mos-65816/commit/d83dacf), [`2feb53e`](https://github.com/wbniv/llvm-mos-65816/commit/2feb53e), [`715668f`](https://github.com/wbniv/llvm-mos-65816/commit/715668f), [`8f66273`](https://github.com/wbniv/llvm-mos-65816/commit/8f66273) | ABI / CC |
| [#321 — fix a16 load-fold across clobbering call (pr34768)](../plans/2026-06-20-321-abs-load-fold-across-call-miscompile.md) | Fix the `pr34768` `-Os` miscompile where `foldableAbsLoad16`/`foldableIndirLoad16` fold a 16-bit load across an intervening call that clobbers the memory, by bailing when a `mayStore`/`isCall` sits between load and user; landed `86c2602`. | [`86c2602`](https://github.com/wbniv/llvm-mos-65816/commit/86c2602), [`364de87`](https://github.com/wbniv/llvm-mos-65816/commit/364de87), [`62b77d0`](https://github.com/wbniv/llvm-mos-65816/commit/62b77d0), [`e4772aa`](https://github.com/wbniv/llvm-mos-65816/commit/e4772aa) | Crash fix |
| [#321 — audit a16 load-fold sites for the across-call hazard](../plans/2026-06-20-321-audit-a16-loadfold-call-hazard.md) | Audit every a16 load-fold site for the fold-across-clobber hazard and confirm the two helpers fixed in `86c2602` were the only vulnerable ones; AA-precision (`noClobberBetween`, −26 B) later landed while the volatile-drop was closed as net-negative. | [`e4772aa`](https://github.com/wbniv/llvm-mos-65816/commit/e4772aa), [`6440db0`](https://github.com/wbniv/llvm-mos-65816/commit/6440db0) | Investigation |
| [#321 — fix a16 4×s8→s32 `G_MERGE` gap (seed 113)](../plans/2026-06-20-321-a16-s32-merge-s8x4-legalizer.md) | Fix the Csmith-seed-113 LTO crash where a direct 4×s8→s32 `G_MERGE_VALUES` hits unsupported under a16, by custom-legalizing it into the already-legal two-level merge (two s8→s16 merges then s16→s32), gated on `hasAccum16`. | [`031cc6a`](https://github.com/wbniv/llvm-mos-65816/commit/031cc6a) | Crash fix |
| [#321 — xy16 csmith seed-247/445 miscompile handoff](../plans/2026-06-20-321-xy16-csmith-seed247-mismatch-handoff.md) | Retired handoff for two Csmith-found `+mos-xy16`-only runtime miscompiles (seeds 247/445, one shared root cause) where xy16 zeroes an index high byte, resolved via commit `2d8ab51` and verified four-way on both emulators. | [`031cc6a`](https://github.com/wbniv/llvm-mos-65816/commit/031cc6a), [`70fb20a`](https://github.com/wbniv/llvm-mos-65816/commit/70fb20a), [`f5ec818`](https://github.com/wbniv/llvm-mos-65816/commit/f5ec818) | Crash fix |
| [#321 — unify the a16 load-fold gate](../plans/2026-06-20-321-unify-loadfold-gate-aa-volatile.md) | Measurement-gated attempt to merge a16's two load-fold gates: a landed AA-precise non-aliasing-store fold on the value side (−26 B, 0 regressions) plus a measured-net-negative single-use-volatile relaxation that regressed common shapes and was reverted. | [`8acc183`](https://github.com/wbniv/llvm-mos-65816/commit/8acc183), [`3aa6908`](https://github.com/wbniv/llvm-mos-65816/commit/3aa6908), [`6440db0`](https://github.com/wbniv/llvm-mos-65816/commit/6440db0) | a16 codegen |
| [#321 — fix xy16 Csmith seed-247/445 miscompile](../plans/2026-06-20-321-fix-xy16-csmith-seed247-445-mismatch.md) | Fix the xy16-only Csmith seed-247/445 runtime miscompile where a non-index `Xc16` value loaded into X16 is clobbered by an 8-bit-index `sep`, by emitting `LDXAbs16`/`LDYAbs16` only for genuine index uses; landed `2d8ab51`. | [`62aa64f`](https://github.com/wbniv/llvm-mos-65816/commit/62aa64f), [`f29639c`](https://github.com/wbniv/llvm-mos-65816/commit/f29639c), [`7c773da`](https://github.com/wbniv/llvm-mos-65816/commit/7c773da), [`f410115`](https://github.com/wbniv/llvm-mos-65816/commit/f410115), [`1414510`](https://github.com/wbniv/llvm-mos-65816/commit/1414510), [`f5ec818`](https://github.com/wbniv/llvm-mos-65816/commit/f5ec818), [`d551f73`](https://github.com/wbniv/llvm-mos-65816/commit/d551f73) | Crash fix |
| [#321 — xy16 seed-445 cvise reduction + fix](../plans/2026-06-20-321-xy16-seed445-cvise-reduction.md) | cvise-reduce Csmith seed 445 to an 8-line UB-free repro proving a 16-bit `Xc16` value held live across a `sep #$10` index narrowing zeroes XH/YH, then fix it by gating `selectXY16`'s direct `LDXAbs16`/`LDYAbs16` to genuine index uses (`2d8ab51`). | [`ba93049`](https://github.com/wbniv/llvm-mos-65816/commit/ba93049), [`4a7a46b`](https://github.com/wbniv/llvm-mos-65816/commit/4a7a46b), [`f5ec818`](https://github.com/wbniv/llvm-mos-65816/commit/f5ec818) | Crash fix |
| [#320 Increment 3 — runtime far-pointer codegen](../plans/2026-06-20-320-far-pointer-runtime.md) | Land runtime far-pointer deref (`lda`/`sta [dp]` indirect-long over a new 4-byte `Imag32` ZP class), AS0→AS2 `addrspacecast`, and far-pointer arithmetic (`G_PTR_ADD` with bank carry), all two-emulator verified; far-pointer CC deferred to Inc 4. | [`88b18f0`](https://github.com/wbniv/llvm-mos-65816/commit/88b18f0), [`aa5d054`](https://github.com/wbniv/llvm-mos-65816/commit/aa5d054), [`cbc31da`](https://github.com/wbniv/llvm-mos-65816/commit/cbc31da) | #320 far-pointer |
| [Two-tier memory organization (master vs project repo)](../plans/2026-06-20-memory-organization-two-tier.md) | Split Claude memory into generic user-state in `~/.claude/memory` and project-specific files in the project repo symlinked into the loader, migrate llvm-mos's seven memories, and add an unmirrored-memory housekeeping scan. | [`6779286`](https://github.com/wbniv/llvm-mos-65816/commit/6779286), [`e68ae21`](https://github.com/wbniv/llvm-mos-65816/commit/e68ae21) | Tooling |
| [#320 — Increment 3c far-pointer arithmetic](../plans/2026-06-20-do-3c-finish-320-increment-3-runtime-far-pointer-a.md) | Finish #320 Inc 3c by adding the `hasAccum16`-gated s32→4×s8 `G_UNMERGE_VALUES` legalization mirror so `fp++` on a far pointer lowers to a correct 32-bit add, gated by `far_arith.c` verified on MAME and bsnes-jg. | [`cbc31da`](https://github.com/wbniv/llvm-mos-65816/commit/cbc31da) | #320 far-pointer |
| [#320 Increment 4 — far calls (`JSL`/`RTL`) + CC plan](../plans/2026-06-20-320-inc4-far-calls-and-far-pointer-cc.md) | Land the far-call mechanism (`JSL $22` / `RTL $6B` pseudos with `lowerCall`/`lowerReturn` switching on a `.far_text` section) and the Inc 3 `sta [dp]` far-store loose end, two-emulator verified in `0001`; defer the multi-variant far-pointer CC bake-off to a Phase 2 plan. | [`1b7f297`](https://github.com/wbniv/llvm-mos-65816/commit/1b7f297), [`03a6dfe`](https://github.com/wbniv/llvm-mos-65816/commit/03a6dfe) | #320 far-pointer |
| [#320 Inc 4 Phase 2 — far-pointer CC variant bake-off](../plans/2026-06-20-320-far-pointer-cc-build-all-variants.md) | Build all four far-pointer calling-convention variants (Imag32 quad, Imag16+bank, A:X+Y, hardware stack) behind off-by-default `+mos-farcc-*` features and measure bytes/cycles; variant (a) Imag32 won (70 B, smallest and fastest) and landed as `0004`. | [`03a6dfe`](https://github.com/wbniv/llvm-mos-65816/commit/03a6dfe), [`bd508c3`](https://github.com/wbniv/llvm-mos-65816/commit/bd508c3), [`f1dc0b8`](https://github.com/wbniv/llvm-mos-65816/commit/f1dc0b8) | #320 far-pointer |
| [Far-ptr CC variants (b)/(c)/(d) + measurement](../plans/2026-06-21-320-far-cc-variants-bcd-and-measure.md) | Build the heterogeneous-register-split far-pointer CC variants (b) Imag16+bank, (c) A:X+Y, (d) hw-stack alongside (a) Imag32 via `assignCustomValue`, then measure; RESOLVED with Imag32 winning and landing as `0004`. | [`d18bbca`](https://github.com/wbniv/llvm-mos-65816/commit/d18bbca), [`f1dc0b8`](https://github.com/wbniv/llvm-mos-65816/commit/f1dc0b8) | #320 far-pointer |
| [#321 xy16 Track A — `requiredXWidth` indexed-family hardening](../plans/2026-06-21-321-xy16-track-a-requiredxwidth-indexed-family-hardening.md) | Close the latent `MOSInsertREPSEP` `requiredXWidth` gap where the 8-bit indexed/indirect/ALU/RMW family and the `JMP (abs,X)` dispatch fall through to X-passthrough, via a memory- and branch-gated `sep`-only X8 catch-all; verified byte-identical. | [`d551f73`](https://github.com/wbniv/llvm-mos-65816/commit/d551f73) | xy16 |
| [#321 xy16 — verify leg classifies known issues (symmetric XFAIL)](../plans/2026-06-21-321-xy16-verify-leg-classify-known.md) | Make the xy16 `-verify-machineinstrs` leg in `evaluate()` call `classify_known()` so the `a16regpress`/`a16scavnz` deferred defects report XFAIL under xy16 symmetric with the a16 leg, while a genuinely new xy16 crash still hard-FAILs. | [`bbb44e5`](https://github.com/wbniv/llvm-mos-65816/commit/bbb44e5) | Fuzz / corpus |
| [#321 xy16 — both-legs verify hardening (unmask new crashes)](../plans/2026-06-21-321-xy16-verify-both-legs-hardening.md) | Harden `tools/a16_fuzz.py` `evaluate()` to always run the xy16 verify leg even when the a16 leg is a known XFAIL, so a genuinely new xy16-only crash hard-FAILs instead of being masked, with a NEW-crash-outranks-known-issue priority table. | [`b78a2d5`](https://github.com/wbniv/llvm-mos-65816/commit/b78a2d5) | Fuzz / corpus |
| [#321 known-issues XPASS guard surfaces fixed defects](../plans/2026-06-21-321-known-issues-xpass-guard.md) | Add a `tools/a16_fuzz.py known-issues` subcommand + `smoke.yml` CI step asserting each `KNOWN_ISSUES` repro still crashes `-verify-machineinstrs` under a16 and xy16, failing loudly with a drop-and-promote action the moment one verifies clean. | [`814c4f4`](https://github.com/wbniv/llvm-mos-65816/commit/814c4f4) | Emulator / CI |
| [Enforce worktree-teardown keep-durable policy via hook](../plans/2026-06-21-worktree-teardown-enforcement-hook.md) | Make `dev/worktree-teardown.sh` the only blessed teardown path and add a Claude Code PreToolUse Bash guard that denies raw `git worktree remove` / `git branch -D wt/` so durable scripts and verdicts are never silently dropped; verified 15/15. | [`f2b61a2`](https://github.com/wbniv/llvm-mos-65816/commit/f2b61a2) | Tooling |
| [#321 native s16 — comparison follow-ups close-out](../plans/2026-06-21-321-native-s16-comparison-followups.md) | Audit the native 16-bit compare surface as measured-complete (everything native except optimal byte-wise register-resident equality) and build the ordering-as-value branchless carry-tail, which measures net-negative in 16-bit-ambient context; WON'T-DO, track closed. | [`1142a49`](https://github.com/wbniv/llvm-mos-65816/commit/1142a49), [`e1f77a8`](https://github.com/wbniv/llvm-mos-65816/commit/e1f77a8), [`8dfa73c`](https://github.com/wbniv/llvm-mos-65816/commit/8dfa73c), [`74f04f4`](https://github.com/wbniv/llvm-mos-65816/commit/74f04f4) | a16 codegen |
| [#321 — ordering-as-value branchless tail (16-bit form)](../plans/2026-06-21-321-ordering-value-branchless-banked.md) | Build the 16-bit `ROLAcc16` carry-tail (`G_CARRY_BOOL16` + `selectCarryBool16` + `legalizeZExt` rewrite) for ordering-as-value booleans, which regresses every realistic a16 program (+340 B corpus, zero wins) harder than the 8-bit v1; WON'T-DO, the select-diamond is optimum. | [`ac4a0c6`](https://github.com/wbniv/llvm-mos-65816/commit/ac4a0c6), [`ef21088`](https://github.com/wbniv/llvm-mos-65816/commit/ef21088), [`74f04f4`](https://github.com/wbniv/llvm-mos-65816/commit/74f04f4), [`2bf0a21`](https://github.com/wbniv/llvm-mos-65816/commit/2bf0a21) | a16 codegen |
| [#320 — far-calls follow-ups (far→near, far fn-ptrs)](../plans/2026-06-21-320-far-calls-followups.md) | Combined plan + handoff completing three #320 far-call follow-ups: far→near mixed-banking via a bank-0 thunk, far function pointers via a stash-then-thunk with a clang far attribute, and far tail calls, all verified on both emulators. | [`3aa2944`](https://github.com/wbniv/llvm-mos-65816/commit/3aa2944), [`34e9542`](https://github.com/wbniv/llvm-mos-65816/commit/34e9542), [`b6dde43`](https://github.com/wbniv/llvm-mos-65816/commit/b6dde43), [`827596d`](https://github.com/wbniv/llvm-mos-65816/commit/827596d), [`dd33017`](https://github.com/wbniv/llvm-mos-65816/commit/dd33017), [`5d81d44`](https://github.com/wbniv/llvm-mos-65816/commit/5d81d44), [`5fd0ff5`](https://github.com/wbniv/llvm-mos-65816/commit/5fd0ff5), [`560900c`](https://github.com/wbniv/llvm-mos-65816/commit/560900c), [`93c7336`](https://github.com/wbniv/llvm-mos-65816/commit/93c7336), [`15df5fe`](https://github.com/wbniv/llvm-mos-65816/commit/15df5fe), [`2d2a171`](https://github.com/wbniv/llvm-mos-65816/commit/2d2a171), [`7c0a00f`](https://github.com/wbniv/llvm-mos-65816/commit/7c0a00f), [`5e9cdab`](https://github.com/wbniv/llvm-mos-65816/commit/5e9cdab), [`2cf27a4`](https://github.com/wbniv/llvm-mos-65816/commit/2cf27a4), [`6efabde`](https://github.com/wbniv/llvm-mos-65816/commit/6efabde), [`4adda8b`](https://github.com/wbniv/llvm-mos-65816/commit/4adda8b) | #320 far-pointer |
| [#321 — xy16 cross-call boundary (verify/formalize/measure)](../plans/2026-06-18-321-m2-xy16-calling-convention-verify-formalize-me.md) | Verify the X/Y-8-bit-at-call-boundary xy16 ABI with a new cross-call differential guard (correct by construction via callee-saved ZP pairs, no fix needed); shelve both the i32-return-in-`A16:X16` and `PHX`/`PLX` hardware-stack index-spill levers as measured-null. | [`ebedd1c`](https://github.com/wbniv/llvm-mos-65816/commit/ebedd1c) | ABI / CC |
| [Full five-address-space model (Option A→B)](../plans/2026-06-21-320-five-address-space-model.md) | Grow the shipped 3-space far-pointer slice toward asiekierka's five-address-space #320 proposal, gating new spaces behind a representability experiment + usage census; packed-24 (AS3) landed as `0006`, zero-bank (AS4) measured-null, numbering deferred to upstream. | [`caef3e9`](https://github.com/wbniv/llvm-mos-65816/commit/caef3e9), [`d038c3a`](https://github.com/wbniv/llvm-mos-65816/commit/d038c3a), [`f84efcd`](https://github.com/wbniv/llvm-mos-65816/commit/f84efcd), [`05cda9c`](https://github.com/wbniv/llvm-mos-65816/commit/05cda9c), [`fe55a01`](https://github.com/wbniv/llvm-mos-65816/commit/fe55a01), [`008eab5`](https://github.com/wbniv/llvm-mos-65816/commit/008eab5), [`6e580b5`](https://github.com/wbniv/llvm-mos-65816/commit/6e580b5), [`ebfc95e`](https://github.com/wbniv/llvm-mos-65816/commit/ebfc95e), [`b5b2ddf`](https://github.com/wbniv/llvm-mos-65816/commit/b5b2ddf), [`6591319`](https://github.com/wbniv/llvm-mos-65816/commit/6591319), [`cd99be6`](https://github.com/wbniv/llvm-mos-65816/commit/cd99be6), [`405c147`](https://github.com/wbniv/llvm-mos-65816/commit/405c147), [`fc73c31`](https://github.com/wbniv/llvm-mos-65816/commit/fc73c31) | #320 far-pointer |
| [Typed `far_fn_t` function-pointer variable surface](../plans/2026-06-21-320-far-fnptr-typed-variable.md) | Carry far-ness as a canonical `FunctionType::ExtInfo` bit so a stored `far_fn_t` variable lowers to `ptr addrspace(2)` and indirect calls through it stash the 24-bit target and `JSL __call_indir_far`; DONE end-to-end on both emulators, Layer F `sizeof` deferred. | [`6efabde`](https://github.com/wbniv/llvm-mos-65816/commit/6efabde) | #320 far-pointer |
| [far-pointer `sizeof == 4` (Layer F width fix)](../plans/2026-06-21-320-far-pointer-sizeof.md) | Make `getPointerWidthV(AS2)` return 32 and special-case far function pointers in `getTypeInfoImpl` so `sizeof(far*)`/`sizeof(far_fn_t)` report 4, fixing struct/array layout corruption; also surfaced and fixed a pre-existing `isFarSymbol` over-fire crash. | [`6efabde`](https://github.com/wbniv/llvm-mos-65816/commit/6efabde) | #320 far-pointer |
| [Land `0004` far-cc + fold (a) recipes into `0001`](../plans/2026-06-21-320-far-pointer-integration-land-0004-and-a-recipes.md) | Express the entire #320 far-pointer line living as gitignored `vendor/` recipes as tracked patches via round-trip-verified patch surgery, landing far-cc Imag32 as `0004` and the far-fn-ptr (a) work in `0001`; LANDED, with three measured findings spawning a new `0005`. | [`43f1d84`](https://github.com/wbniv/llvm-mos-65816/commit/43f1d84), [`f1dc0b8`](https://github.com/wbniv/llvm-mos-65816/commit/f1dc0b8) | #320 far-pointer |
| [packed-24 (addrspace 3) Increment B handoff](../plans/2026-06-21-320-packed24-incrementB-handoff.md) | Resume prompt to build packed-24 Increment B codegen (3-byte store/load/deref bridging p3↔3×s8) once the F2 far-value work lands; IMPLEMENTED on `wt/320-packed24-incB` and shipped as `0006` with a 25% storage win verified on both emulators. | [`d26cb18`](https://github.com/wbniv/llvm-mos-65816/commit/d26cb18), [`ebfc95e`](https://github.com/wbniv/llvm-mos-65816/commit/ebfc95e) | #320 far-pointer |
| [#320 packed-24 (AS3) realistic-use validation handoff](../plans/2026-06-21-320-packed24-productionization-handoff.md) | Handoff to measure the packed far-pointer (addrspace 3) storage win in realistic context and fix the byte-2 absolute-long access cost; THREAD CLOSED 2026-06-22 — Task A done, static-init reloc fixed, byte-2 cost shipped as `0007`. | [`b5b2ddf`](https://github.com/wbniv/llvm-mos-65816/commit/b5b2ddf), [`a76bf18`](https://github.com/wbniv/llvm-mos-65816/commit/a76bf18), [`405c147`](https://github.com/wbniv/llvm-mos-65816/commit/405c147) | #320 far-pointer |
| [#320 — packed-24 static-init relocation fix](../plans/2026-06-22-320-packed24-static-init-reloc-fix.md) | Fix statically-initialized packed-far-pointer tables that failed to link (`R_MOS_ADDR8` out of range) by adding a generic AsmPrinter `emitNonStandardSizedConstant` hook emitting the `R_MOS_ADDR24` SEGMENT_LO/HI/BANK triple for 3-byte `AS_FarPacked` constants; landed in `0006`. | [`a76bf18`](https://github.com/wbniv/llvm-mos-65816/commit/a76bf18) | Crash fix |
| [65816 near-global absolute stays 16-bit (no bank-relax)](../plans/2026-06-22-65816-near-abs-bank-relax.md) | Suppress the `MOSAsmBackend` abs→absolute-long bank relaxation for near non-`.far` sections so plain-symbol near-global accesses emit 3-byte `abs` instead of 4-byte long, saving ~1 byte each; landed as patch `0007`, far accesses preserved. | [`ff02726`](https://github.com/wbniv/llvm-mos-65816/commit/ff02726), [`1654e3c`](https://github.com/wbniv/llvm-mos-65816/commit/1654e3c), [`99ca2a7`](https://github.com/wbniv/llvm-mos-65816/commit/99ca2a7) | a16 codegen |
| [#320 — zero-bank (AS4) measure-and-close](../plans/2026-06-22-320-zerobank-as4-measure-and-close.md) | De-lump the previously-asserted close of the fifth address space (`AS_FarZeroBank`, addrspace 4), measuring 0 qualifying bank-0-far sites and zero storage/access advantage over a near pointer + lazy cast — a structural measured-null that completes the five-space model; nothing built. | [`51af94b`](https://github.com/wbniv/llvm-mos-65816/commit/51af94b), [`cd99be6`](https://github.com/wbniv/llvm-mos-65816/commit/cd99be6) | Investigation |
| [SNES near-code budget + near/far code-model framing](../plans/2026-06-22-snes-near-code-budget-and-code-model.md) | Carve the SNES LoROM header and vectors into a separate `romhdr` MEMORY region so an over-budget build fails with a loud byte-quantified region-named link error, and record the near=small / far=medium-large code-model framing (no `-mcmodel` mode warranted); linker-script + docs. | [`5d682b0`](https://github.com/wbniv/llvm-mos-65816/commit/5d682b0), [`c9cfa6a`](https://github.com/wbniv/llvm-mos-65816/commit/c9cfa6a), [`99ca2a7`](https://github.com/wbniv/llvm-mos-65816/commit/99ca2a7) | Platform |
| [#321 scavenger N/Z assert — file upstream issue](../plans/2026-06-22-321-scavenger-crash-upstream-issue.md) | File a high-quality upstream issue for the `saveScavengerRegister` `assertNZDeadAt` assertion (violated when a 16-bit compare/ALU flag is live across a frame-vreg spill), with a time-boxed hunt for a stock-8-bit repro and an analysis-only fallback; posting user-triggered. | [`46a1484`](https://github.com/wbniv/llvm-mos-65816/commit/46a1484) | Crash fix |
| [#321 — native-s16 surface consolidation (measure)](../plans/2026-06-22-321-native-s16-surface-consolidation-and-close.md) | Consolidate the three open native-s16 tracks (compares/branches CLOSED, A16-threading and ALU-chains deferred) into one measured surface map, unifying both deferred tails as the single RA-level 16-bit-residency-under-pressure core behind one trigger; nothing built. | [`95d65df`](https://github.com/wbniv/llvm-mos-65816/commit/95d65df), [`866d530`](https://github.com/wbniv/llvm-mos-65816/commit/866d530), [`a584a78`](https://github.com/wbniv/llvm-mos-65816/commit/a584a78) | Investigation |
| [#320 — packed-24 residuals B and C close-out](../plans/2026-06-22-320-packed24-residuals-close.md) | Close the last two packed-24 residuals: byte-2 absolute-long cost is delivered by the general near-abs bank-relaxation patch `0007` (not packed-specific), and the `__far_packed` ergonomic spelling is dropped since no AS2 spelling exists to mirror; no codegen. | [`405c147`](https://github.com/wbniv/llvm-mos-65816/commit/405c147) | #320 far-pointer |
| [#320 — far-value residuals (DP-arg crash, 8-bit storage)](../plans/2026-06-22-320-far-value-residuals.md) | Close two far-value residuals: draft an upstream issue for the `addrspace(1)` DP-pointer-argument 16-bit-COPY crash (no fork fix), and confirm default-8-bit far-pointer storage is a16-gated-by-design with a clean Legalizer rejection; no `0002` change, 6/6 PASS. | [`a98964f`](https://github.com/wbniv/llvm-mos-65816/commit/a98964f), [`18f9218`](https://github.com/wbniv/llvm-mos-65816/commit/18f9218), [`ca68066`](https://github.com/wbniv/llvm-mos-65816/commit/ca68066) | #320 far-pointer |
| [#321 — native-s16 surface consolidation (TODO bookkeeping)](../plans/2026-06-22-321-native-s16-surface-consolidation-knock-out-the.md) | Finish the measure-and-close native-s16 surface consolidation as `TODO.md`-only bookkeeping, promoting the item to Done and unifying three deferred items (A16-threading Phase 3, ALU >14-live, globals.c `-Os` crash) under one shared register-pressure core and re-open trigger. | [`25ea71c`](https://github.com/wbniv/llvm-mos-65816/commit/25ea71c) | Tooling |
| [#320 — far-to-far tail calls fold to long `jmp`](../plans/2026-06-22-320-far-tail-calls.md) | Add a `TailJML` pseudo expanding to `JMP_AbsoluteLong` (`$5C`) plus a far arm in `MOSLateOptimization::tailJMP` that folds a `JSL far-global; RTL` tail into a direct long jump (−1 B, drops the redundant return push); landed in `0001`, verified on both emulators. | [`3fa16b0`](https://github.com/wbniv/llvm-mos-65816/commit/3fa16b0), [`4adda8b`](https://github.com/wbniv/llvm-mos-65816/commit/4adda8b) | #320 far-pointer |
| [Capture far-ptr CC variant (c) A:X+Y into `0004`](../plans/2026-06-21-320-far-cc-axy-variant-c-hygiene-capture.md) | Re-verify and commit the already-built far-pointer CC variant (c) (16-bit offset in A:X, bank byte in Y via the GISel `assignCustomValue` hook) from gitignored `vendor/` into the tracked `0004` patch as a proper commit, closing the uncommitted half-state. | [`bef8f78`](https://github.com/wbniv/llvm-mos-65816/commit/bef8f78) | #320 far-pointer |
| [#321 — verify 32-bit `long`/`int32_t` support](../plans/2026-06-23-321-32bit-long-verification.md) | Two complementary differential verifications of the existing s32 (`long`) codegen with **no compiler change**: a value-level `a16s32` micro-test (4-way `host==default==+mos-a16==0x50F2B870` on both emulators) and a 32-bit track in the builtin fuzzer so the *deterministic* 4-way oracle also exercises `long` (40/40, 0 mismatch); no-flag byte-identical. | [`c1f44f5`](https://github.com/wbniv/llvm-mos-65816/commit/c1f44f5) | Fuzz / corpus |
| [#321 — scavenger N/Z fix spike (NO-GO)](../plans/2026-06-23-321-scavenger-nz-fix-spike.md) | Spike a narrow scavenger fix; both the conservative `canSaveScavengerRegister` N/Z gate and the `LDCImm`-rematerializable attempt relocate the crash to an unsaveable `Ac`-class `NoRegister`, proving there is no *bounded* patch along those two axes — root cause judged deferred a16 register-pressure exhaustion. **Superseded 2026-06-26:** a third axis works — route the live `$p` through a dead index reg into `RC17` (net-0 on the hard stack) — **FIXED, patch `0011`**. | [`538e7f2`](https://github.com/wbniv/llvm-mos-65816/commit/538e7f2), [`906b7c5`](https://github.com/wbniv/llvm-mos-65816/commit/906b7c5), [`20a282c`](https://github.com/wbniv/llvm-mos-65816/commit/20a282c) | Investigation |
| [#321 — a16 register-pressure crashes fix handoff](../plans/2026-06-23-321-a16-pressure-scavenger-fix-handoff.md) | Handoff to fix the two deferred `+mos-a16` register-pressure crashes (scavenger N/Z and regalloc out-of-registers), originally hypothesized to need the A16-threading Phase-3 `Ac16`-residency rework. **Both fixed by orthogonal targeted patches instead** — regalloc by `0009` (i8 de-pin) and scavenger by `0011` (route `$p` through `RC17`); the residency rework stayed deferred. | [`59e937e`](https://github.com/wbniv/llvm-mos-65816/commit/59e937e) | Crash fix |
| [Reviewer-facing presentation of the patch series](../plans/2026-06-24-reviewer-patch-series-presentation.md) | Build the upstream-submission presentation layer: the per-patch **need/patch/proof** review guide (`docs/65816-patch-series-review-guide.md`) with dependency/sequencing/pass-pipeline/timeline diagrams + Appendices A–D, an LLVM primer for compiler-literate non-LLVM readers, and the Appendix-D upstream-PR accounting; plus two ROM-byte-identical platform refinements (`clang.cfg` default `-mcpu`, crt0 mnemonics). | [`6f94e58`](https://github.com/wbniv/llvm-mos-65816/commit/6f94e58) | Tooling |
| [#321 — beefy SNES Mandelbrot demo](../plans/2026-06-24-snes-mandelbrot-beefy-demo.md) | The first *beefy* `+mos-a16` customer: a fixed-point Mandelbrot taken through the differential gate (host == default == `+mos-a16` on MAME + bsnes-jg), rendered on-console and screenshotted from both emulators, far-stored into high WRAM (#320), then a big 128×128 Mode 7 + DMA image; grew the SNES display HAL (`platforms/snes/snes.h`) + a rendering-library handoff. **Later (2026-06-26) consolidated:** the 128×128 Mode-7 `mandel-mode7` demo was removed and the on-console tester folded into a far/`+mos-a16`-only `mandel-display` ([plan](../plans/2026-06-26-collapse-the-snes-mandelbrot-demos-into-one-far-16.md)). | [`74a9c93`](https://github.com/wbniv/llvm-mos-65816/commit/74a9c93), [`f9161e7`](https://github.com/wbniv/llvm-mos-65816/commit/f9161e7), [`a1e2f6d`](https://github.com/wbniv/llvm-mos-65816/commit/a1e2f6d), [`ce92661`](https://github.com/wbniv/llvm-mos-65816/commit/ce92661), [`46e8b99`](https://github.com/wbniv/llvm-mos-65816/commit/46e8b99) | Platform |
| [Blossom (Hopalong attractor) on SNES — Phase 1](../plans/2026-06-24-blossom-snes.md) | Port the math heart of Blossom 4.0 (Barry Martin's "Hopalong" attractor) to the 65816 the 1989 way — Q8.8 fixed-point + a 512 B sqrt LUT, no FPU — as `examples/65816/k_hopalong.c`; 4-way differential PASS (host==default==`+mos-a16`==`0x1BBC`, both emulators), native 16-bit active, a16 **+14 B** (the expected 8/16-interleave class); on-screen renderer deferred. | [`e554dd6`](https://github.com/wbniv/llvm-mos-65816/commit/e554dd6) | Platform |
| [SNES hardware reference docs + subsystem-split `snes.h`](../plans/2026-06-25-snes-hardware-reference-docs-subsystem-split-snes.md) | Produce a durable SNES reference set **from source**: subsystem-split HAL headers under a thin umbrella `snes.h` (re-exports every prior symbol, compile-verified), a generated full CPU-visible MMIO register/bit map, and a compact 65816 mode-switching reference generated from the in-tree backend TableGen and validated against canonical opcodes. | [`2d23b38`](https://github.com/wbniv/llvm-mos-65816/commit/2d23b38) | Platform |
| [#321 — interactive Mandelbrot (Mode 7 fly-around)](../plans/2026-06-25-321-interactive-mandelbrot-mode7.md) | The interactive follow-up: the 128×128 image baked host-side (tiled into Mode 7 chars) and DMA'd ROM→VRAM for an **instant** boot, then a joypad pan/zoom/rotate fly-around at 60 fps. Builds **default + `+mos-a16`** (image hash host==default==`+mos-a16` + a scripted-input view-math differential — the per-frame Mode 7 matrix multiplies — replayed over the ROM's ground-truth pad log), MAME + bsnes-jg screenshots. Added a joypad HAL + shared `mode7.h`/`view.h`; fixed a stale-header dep (`dev/sync-platform.sh`) and the `dev/run.sh build` far-example break. **Demo removed 2026-06-26** (consolidated into the far/`+mos-a16`-only `mandel-display` tester — [plan](../plans/2026-06-26-collapse-the-snes-mandelbrot-demos-into-one-far-16.md)). | [`f782d3a`](https://github.com/wbniv/llvm-mos-65816/commit/f782d3a) | Platform |
| [#321 — Mandelbrot zoom pyramid (increasing detail)](../plans/2026-06-25-321-mandelbrot-zoom-pyramid.md) | **TRUE increasing detail on zoom-in.** The host bakes a stack of 2×-finer Mandelbrot levels (`double`, any depth); the ROM DMAs the next finer level as Mode-7 zoom crosses each 2× threshold — instant, deep, **zero on-console fractal math**. **Phase 1** = single-bank 64×64 × 6 (32×); **Phase 2** = multi-bank 128×128 × 8 (256×) on a new `snes-zoom` 8-bank/256 KiB platform (one level per bank, swap DMAs from `bank:addr16`). Both build default+`+mos-a16`; per-level image hash + VRAM-readback + scripted-zoom differential green on MAME + bsnes-jg. Found a default-8bit matrix-fold-loop miscompile (independent of #321) **and** the vblank DMA limit (16 KiB swap → force-blank). | [`a54041b`](https://github.com/wbniv/llvm-mos-65816/commit/a54041b), [`6fb3d1b`](https://github.com/wbniv/llvm-mos-65816/commit/6fb3d1b) | Platform |
| [Cross-build the toolchain for Linux arm64 + Windows x86_64 (interim)](../plans/2026-06-25-cross-platform-toolchain-builds.md) | Ship interim `linux-arm64` + `windows-x86_64` toolchain packages cross-built from the Linux Docker (keeping `linux-x86_64` byte-identical) so arm64-Linux / Windows devs can try the fork until #321 lands upstream and upstream CI emits every platform; deliberately the cheapest cross path (no mac/Win runners), each package's README flagging it an "interim preview, prefer upstream once merged". | [`ebac767`](https://github.com/wbniv/llvm-mos-65816/commit/ebac767), [`bff643e`](https://github.com/wbniv/llvm-mos-65816/commit/bff643e), [`2c58288`](https://github.com/wbniv/llvm-mos-65816/commit/2c58288) | Tooling |
| [Clean-room test of the *published* compiler (publish gate)](../plans/2026-06-25-test-published-snes-compiler.md) | Test the published artifact the way a stranger would — take the compiler from the release path, compile a reference Mandelbrot, run it on bsnes-jg (no BIOS / sound) and check against a host oracle for **both** default and `+mos-a16`, in a throwaway Docker — wired as a mandatory `METHOD=local` gate inside `dev/package-release.sh` (every `task package` is clean-room-verified + HTML-reported before `release-upload`). | [`a295256`](https://github.com/wbniv/llvm-mos-65816/commit/a295256), [`a9d3e9e`](https://github.com/wbniv/llvm-mos-65816/commit/a9d3e9e), [`2e580a0`](https://github.com/wbniv/llvm-mos-65816/commit/2e580a0), [`550a855`](https://github.com/wbniv/llvm-mos-65816/commit/550a855), [`0fdc711`](https://github.com/wbniv/llvm-mos-65816/commit/0fdc711), [`a2a8ce4`](https://github.com/wbniv/llvm-mos-65816/commit/a2a8ce4), [`4a8dc85`](https://github.com/wbniv/llvm-mos-65816/commit/4a8dc85) | Tooling |
| [#321 — fix the `+mos-a16` register-pressure regalloc crash (`0009`)](../plans/2026-06-24-321-a16-pressure-fix-implementation.md) | Fix the `+mos-a16 -O1/-Os` "ran out of registers" deadlock on real code (`globals.c` / `a16regpress.c`): the strength-reduced i8 loop counter selected to A-pinned `ADCImm` (class `{A}`) and collided with the 16-bit indexed-load `Ac16` transit; lower a small-constant i8 add/sub (`\|amt\|≤2`) to a relocatable `G_INC`/`G_DEC` chain instead, freeing `A16` — fork patch `0009`, DEFAULT byte-identical, **−123 B** over 122 c-torture (0 worse), `a16regpress` now a positive gate. | [`ad506ed`](https://github.com/wbniv/llvm-mos-65816/commit/ad506ed) | Crash fix |
| [Trig functions as a C-compiler differential test](../plans/2026-06-25-trig-functions-as-a-c-compiler-differential-test-1.md) | Exercise the fork (`+mos-a16` + the s32 libcall paths) with a substantial fixed-point trig workload implemented two ways (16-bit and 32-bit), verified against a host golden reference: Phase 1 is a bit-exact 4-way differential (`corpus_result == 0x068A6933` host==default==`+mos-a16` on both emulators, accuracy within precision, no 64-bit libcall leak); Phases 2–3 (16-bit CORDIC, hyperbolic) deferred. | [`4ae4157`](https://github.com/wbniv/llvm-mos-65816/commit/4ae4157) | Fuzz / corpus |
| [Accurate sin LUT + HiROM big-ROM SNES platform (trig Phase 1c)](../plans/2026-06-25-trig-accurate-sin-lut-hirom.md) | Add a HiROM (big-ROM) SNES platform variant + an accurate sin LUT for the trig test; the blocker was root-caused to clang's `EmitArraySubscriptExpr` promoting **every** GEP index to AS0's 16-bit ptrdiff — fixed to use the base pointer's per-address-space index width so far (AS2) indices ≥ 32768 no longer truncate (landed in `0001`, round-trip verified; `farindex.c` reads all three banks). | [`4ae4157`](https://github.com/wbniv/llvm-mos-65816/commit/4ae4157) | Crash fix |
| [DEFAULT-8bit matrix-fold-loop CRC miscompile — reduce + fix (`0010`)](../plans/2026-06-25-default8-loopfold-miscompile-reduce-and-fix.md) | Reduce, root-cause, and fix a real **default-8bit** (no `+mos-a16`) `mosw65816` CRC-fold miscompile the Mandelbrot-zoom demo caught (`0xE60E` ≠ `0xF56C`): cvise → 43-line repro, then an instruction-level bsnes-core trace **falsified** the wrong-X lead and pinned it to the generic **register coalescer** merging two rotate-referenced values into the A-only `Ac` class (strands a loop-carried CRC byte in `Y`; back-edge `ROL` reads a stale `A`); fixed in `MOSRegisterInfo::shouldCoalesce` as upstream-bound patch `0010` + a `-run-pass=register-coalescer` lit test. | [`ad4d6ae`](https://github.com/wbniv/llvm-mos-65816/commit/ad4d6ae), [`a0498d7`](https://github.com/wbniv/llvm-mos-65816/commit/a0498d7), [`06be924`](https://github.com/wbniv/llvm-mos-65816/commit/06be924), [`25fd083`](https://github.com/wbniv/llvm-mos-65816/commit/25fd083), [`c4f904d`](https://github.com/wbniv/llvm-mos-65816/commit/c4f904d), [`fb48fe7`](https://github.com/wbniv/llvm-mos-65816/commit/fb48fe7), [`554e5ec`](https://github.com/wbniv/llvm-mos-65816/commit/554e5ec), [`cd77eaa`](https://github.com/wbniv/llvm-mos-65816/commit/cd77eaa), [`fb405c2`](https://github.com/wbniv/llvm-mos-65816/commit/fb405c2), [`e737568`](https://github.com/wbniv/llvm-mos-65816/commit/e737568), [`8f09955`](https://github.com/wbniv/llvm-mos-65816/commit/8f09955), [`5237584`](https://github.com/wbniv/llvm-mos-65816/commit/5237584), [`c78db07`](https://github.com/wbniv/llvm-mos-65816/commit/c78db07), [`b75dd46`](https://github.com/wbniv/llvm-mos-65816/commit/b75dd46), [`697f40c`](https://github.com/wbniv/llvm-mos-65816/commit/697f40c) | Crash fix |
| [Shared plan-index tooling — every project gets an auto-checked plan-index.md](../plans/2026-06-26-shared-plan-index-tooling.md) | Lift the plan-index drift check into generic shared infra (python-tui-lib `check-plan-index.sh` + `seed-plan-index.sh` + a generic `post-commit` dispatcher on the global `core.hooksPath`) so every project's commits auto-flag plans missing an index row, with paste-ready stubs (summaries stay hand-authored); retires this repo's superseded llvm-specific copies. | [`63daf1f`](https://github.com/wbniv/llvm-mos-65816/commit/63daf1f) | Tooling |
| [#321 — fix the register-scavenger `$p is not a GPR` crash (live N/Z across a frame-vreg spill)](../plans/2026-06-26-321-scavenger-nz-live-p-save-fix.md) | Fix the 8/500-seed `+mos-a16`/`+mos-xy16` scavenger crash (previously deferred as an upstream issue-with-no-fix): `saveScavengerRegister` must preserve a live `$p` across an *unbalanced* stack range (a 16-bit compare keeps N/Z live across a frame-carry spill, and `$p` has no GPR home) — fixed by routing `$p` hard-stack-neutrally through a dead index reg into `RC17` + dropping the stale `assertNZDeadAt` (patch `0011`); fixing it surfaced a second pristine-upstream bug, `LDCImm 1` → `MCInstLower` unreachable, fixed as `0012`. `a16scavnz.c` now a positive gate (`0x22A6`, both emulators, asserts-clean). | [`a320cbd`](https://github.com/wbniv/llvm-mos-65816/commit/a320cbd) | Crash fix |
| [Formalize "Far data > 2 banks" into a dedicated passing gate](../plans/2026-06-26-formalize-far-data-2-banks-into-a-dedicated-passin.md) | Promote the already-working cross-bank far-load capability (enabled by the `0001` clang far-subscript fix) into a dedicated fast gate `dev/run.sh farindex`: a `const FAR uint16_t tbl[]` spanning banks $C1/$C2/$C3 (`snes-hirom`) read at three runtime indices via `lda [dp]` folds `corpus_result==0x0001D8A1` host == +mos-a16 on both emulators, and flip the stale `implementation-status.md`/ROADMAP "Deferred… >2 banks" line to done (`k_trig32lut` corroborates on the ~200 KiB sin LUT). | [`a9601e5`](https://github.com/wbniv/llvm-mos-65816/commit/a9601e5) | #320 far-pointer |
| [Trig Phase 2 — Q2.14 CORDIC native-s16 / zero-libcall differential](../plans/2026-06-26-trig-phase2-q214-cordic.md) | The complement to Phase 1: a fresh Q2.14 16-bit CORDIC (`sin/cos/atan/atan2`) that is shift-and-add only, so under `+mos-a16` it compiles to native 16-bit code with **zero arithmetic libcalls** — `dev/run.sh k_trig16` asserts the *absence* of every `__*hi3/__*si3/__*di3` mul/div/shift helper, folds `corpus_result==0x9446C734` host==default==`+mos-a16` on both emulators, and a cross-width harness confirms the 16-bit results track Phase 1's Q16.16 libfixmath within libfixmath's own coarse floor (CORDIC `atan` actually beats it). | [`4626ddd`](https://github.com/wbniv/llvm-mos-65816/commit/4626ddd) | Fuzz / corpus |
| [#321 A16-threading Phase 3 — trigger-check pass (re-open or re-affirm the deferral)](../plans/2026-06-26-321-a16-threading-phase-3-trigger-check-pass-re-op.md) | Operationalize the deferred Phase-3 RA-level `Ac16`-residency re-open condition *without building Phase 3* — on a throwaway worktree, re-measure ZP pressure (extend `dev/measure-zp-pressure.sh` with an explicit ~10/14-`Imag16`-pair trigger verdict line + a wider real-code corpus) for trigger **(b)** and run a large-N csmith crash-sweep + corpus/suite under `-verify-machineinstrs` for trigger **(a)** (a *new* realistic `regalloc-out-of-registers`/`a16-zp-pressure-overflow`); neither fires → record dated evidence in Watch and stand pat (expected — last baseline ~5/14 pairs, both prior crashes fixed by `0009`/`0011`+`0012`), either fires → run the already-recorded B0→B1→B2 spike. **Outcome: trigger (b) FIRED** (6 real fns at ~10/14 pairs, `cordic16_atan2` 14/14) → the spike ran and measured residency **net-negative** (+24 B / zero peak-ZP relief), so Phase 3 is **CLOSED**, not merely deferred. | [`e7e2270`](https://github.com/wbniv/llvm-mos-65816/commit/e7e2270) | Investigation |
| [#320 thunk tail calls — far→near + far-indirect both fold](../plans/2026-06-26-320-thunk-tail-calls.md) | Complete the thunk-tail arc. Phase A: fold the **far→near** thunk tail `JSL __call_near_from_far; RTL` → `TailJML` (match the external thunk symbol by exact name; −1 B, stack-safe), flip `dev/far_near_call.sh` to a positive fold gate (`0xE0`), reconcile the self-contradicting far-tail status row. Phase B: land the missing `__call_indir_far` runtime stub (`platforms/snes/call-indir-far.s`) so a far-indirect *call* links/runs (`far_fnptr.c`, `0xFF`), **fix a pre-existing far-indirect-from-far-caller miscompile** (it was mis-routed through `__call_near_from_far` → stack corruption; now JSLs `__call_indir_far` directly), then add the `IndirFarThunk` fold arm (`far_indir_tail.c`, `0xFF`). All three thunk-tail cases fold; both emulators; corpus 7/7; fuzz 0-mismatch. | [`a31ea5b`](https://github.com/wbniv/llvm-mos-65816/commit/a31ea5b) | #320 far-pointer |
| [Trig Phase 3 — derived (tan/asin/acos) + hyperbolic (sinh/cosh/tanh)](../plans/2026-06-26-trig-phase3-derived-hyperbolic.md) | Completes the trig set: Q2.14 derived `tan/asin/acos` (a Q2.14 divide + sqrt, so `__mulsi3`/`__divsi3` fire — the 16-bit analogue of Phase 1's s32-libcall payload) and hyperbolic `sinh/cosh/tanh` via CORDIC *hyperbolic* mode (the repeated i=4,13 schedule). `dev/run.sh k_trig16x` folds `corpus_result==0x759567C4` host==default==`+mos-a16` on both emulators; the cross-width harness derives the 32-bit hyperbolic from the now-compiled `fix16_exp` (vendored-but-uncompiled until Phase 3) and confirms agreement (CORDIC again beats libfixmath on `asin/acos`). | [`6f02ea7`](https://github.com/wbniv/llvm-mos-65816/commit/6f02ea7) | Fuzz / corpus |
| [pr15296 `+mos-a16` link-time ZP overflow — gated narrow-fix spike](../plans/2026-06-26-pr15296-mos-a16-link-time-zp-overflow-gated-narrow.md) | Attempt to clear the lone remaining `+mos-a16` register-pressure XFAIL (`a16-zp-pressure-overflow`/pr15296) with a narrow fix instead of the deferred high-risk RA rework: research refuted the "Imag16-saturation" framing (the allocator hard-caps ZP at `-zp-avail=224`, pristine-upstream `MOSZeroPageAlloc.cpp`), so the 1043-byte `.zp.noinit` overflow is a containable accounting/cap defect — a generic upstream-worthy bug. Throwaway worktree; gating diagnosis of the offset-1043 symbol (F1a/F1b/F2/F3) before any edit; land only if default-8bit byte-identical + regression-clean + pr15296 links/runs correct, else re-affirm the deferral. | [`238de65`](https://github.com/wbniv/llvm-mos-65816/commit/238de65) | Crash fix |
| [Full xy16 backend fix — close the last `+mos-xy16` defect (fp compare-as-select)](../plans/2026-06-26-full-xy16-backend-fix-close-the-last-mos-xy16-defe.md) | Root-cause + fix the single remaining `+mos-xy16` miscompile the `ieee/` full-vendoring sweep found: the fp compare-as-select ("cmove") pattern (`fp-cmp-8.c`/`fp-cmp-8l.c`/`pr38016.c` — one body, one root cause; `+mos-xy16` reads `0xDEAD`, default+a16 PASS). Measurement-led (reproduce → cvise → MIR diff xy16-vs-a16 → fix at origin → 4-way verify); recommended cause is the select-diamond sibling of the seed-445 high-byte clobber (`selectXY16`'s GenuineIndex predicate parks a non-index 16-bit value in X16 across the diamond where a narrowing `sep #$10` zeroes the high byte), fix gated under `HasIndex16` so default/a16 stay byte-identical. | [`3e338fb`](https://github.com/wbniv/llvm-mos-65816/commit/3e338fb) | xy16 |
| [#321 xy16 fp compare-as-select ("cmove") — XFAIL was STALE, resolved to positive gates](../plans/2026-06-26-321-xy16-cmove-stale-xfail-resolution.md) | Resolution of the row above: the cmove defect does NOT reproduce on a clean build of the committed patch stack (`0001..0012`) — all 3 rows XPASS at `-Os` and `-O1` (4-way, MAME + bsnes-jg), the xy16 codegen uses no 16-bit index register at all, and the patch stack is byte-identical at the finding commit and HEAD; the 12:02 sweep measured a stale/dirty shared `build/`, not the tracked patches (same as `pr15296`). De-XFAIL'd the 3 `ieee/` rows to positive gates; no `vendor/`/`0002` change. | [`8535fd1`](https://github.com/wbniv/llvm-mos-65816/commit/8535fd1) | xy16 |
| [Fix the far (addrspace 2) memset/memcpy/memmove silent wrong-bank miscompile](../plans/2026-06-26-fix-the-far-addrspace-2-memset-memcpy-memmove-sile.md) | A far memset/memcpy/memmove the backend can't inline-expand (variable size, or constant > `legalizeMemOp`'s `SizeLimit`) fell through to the generic `createMemLibcall`, which calls the NEAR runtime (`__memset`/`memcpy`, 16-bit `char*`) with the 32-bit far pointer → the **bank byte was silently dropped** (wrong-bank store/load, no diagnostic). Not just the loop-idiom recognizer — clang `EmitAggregateCopy` (any far struct copy), null/const init, `__builtin_mem*`, and MemCpyOpt all converge on the same path, so the fix lives at the `legalizeMemOp` chokepoint: two helpers (`anyFarPointerOperand` + `createFarMemLibcall`, patch `0013`) route far memops to a far-aware runtime (`__memset_far`/`__memcpy_far`/`__memmove_far`, `platforms/snes/mem-far.c`), widening near pointers to far bank `$00` and coercing the length to `size_t`; near path untouched, no generic-LLVM change. Runtime is index-style (a far-pointer loop IV forms an unsupported `G_PHI(p2)`). Gated `dev/run.sh far_memops` (`0x74`, MAME `-Os`/`-O2` + bsnes-jg); no regression (corpus 7/7, corpus-a16 6/6, far suite 14/14, torture 40/40). | [`a81874d`](https://github.com/wbniv/llvm-mos-65816/commit/a81874d) | Crash fix |
| [Fix the far-pointer `G_PHI(p2)` backend gap](../plans/2026-06-26-fix-the-far-pointer-g-phi-p2-backend-gap.md) | A far (addrspace 2) pointer used as a loop induction variable (`for(;n;p++) *p=v;`) forms a `G_PHI` of a far (p2) pointer; the legalizer made `G_PHI` legal only for `{s1,s8,p0,p1}`, not the 32-bit p2, so the backend aborted (`unable to legalize ... G_PHI (p2)`) on valid C — the latent gap noted by the far-memops fix above (why `mem-far.c` is index-style). Fix: `MOSLegalizerInfo::legalizePhi` (`.customFor({PF})` + a `legalizeCustom` arm) custom-legalizes a far-pointer phi to an **s32 phi** — ptrtoint each incoming value in its predecessor, inttoptr the result back to p2 after the phi — the same bridge `legalizePtrAdd`/far load+store use; the s32 phi reuses the standard `narrowScalar`→bytes path. Purely additive (no regression); p3 is memory-only so it can't form a phi. Fork patch `0014` + `dev/regen-patch-0014.sh`. Gated `dev/run.sh far_loop` (far-ptr IV write+read-back of high WRAM `$7E`, `0xC9`, MAME `-Os`/`-O2` + bsnes-jg); the gate uses a runtime (volatile) stride so the far-ptr phi survives indvars at `-Os`/`-O2`, making the compile gate the crash regression-guard. No regression (corpus 7/7, corpus-a16 6/6, far 15 ROMs). | [`d1099db`](https://github.com/wbniv/llvm-mos-65816/commit/d1099db) | Crash fix |
| [Re-imagine SNES rendering as an OOP-in-C library (`snesgfx`)](../plans/2026-06-26-snes-rendering-oop-library.md) | Express the proven SNES rendering mechanics (force-blank/v-blank access window, DMA uploads, VRAM layout, Mode 7) as a **header-only OOP-in-C library** following the [oop-in-c HOWTO](object-oriented-c-and-assembly.md): an `UploadQueue` (the access-window rule in ONE place), a `VramAlloc` bump allocator, a `Drawable` vtable + heterogeneous `Scene` (the *justified* per-object vtable — coarse dispatch, one indirect call per drawable per frame, never per-tile), concrete `Mode7Layer`/`SpriteSet`/`BgLayer`, and a selector-table joypad dispatch (the cheap dynamic form). Verified by a parallel client `examples/snes/mandel-oop.c` reproducing `mandel-display.c`'s CRC `0x204F` (host==a16@MAME==a16@bsnes-jg, `dev/run.sh mandel-oop`, `-verify-machineinstrs` clean) — zero-regression proof — then **measures** the dispatch cost (disasm: N `__call_indir`/frame not per-tile; OOP-vs-procedural size delta; does C reach `JSR (abs,X)`?) and writes the numbers back into oop-in-c.md §4–§5 (measure, don't assume). Header-only because `dev/build.sh` compiles every `examples/snes/**/*.c` as a standalone program. | [`d5c1448`](https://github.com/wbniv/llvm-mos-65816/commit/d5c1448) | Platform |
| [Space Invaders on the `snesgfx` OOP library](../plans/2026-06-26-space-invaders-on-the-snesgfx-oop-library.md) | Build a real, playable, on-screen **Space Invaders** on `snesgfx` — driving out the sprite/OAM front-end (`SpriteSet`, previously "not built"), a `TextLayer`/`ShieldField` HUD+shields (`BgLayer`), and a `Controller`. OOP discipline: objects+methods only, an `App` object, the per-object vtable seam **only** at the 3 render layers (entities are static-dispatch objects, never a per-entity vtable in the hot loop). No far pointers ⇒ builds **default + a16 + xy16** = the full 5-way differential. Verified gold-standard: a portable HAL-free `invaders_logic.h` + host oracle `tools/invaders-sim.c` → a deterministic **attract** state-CRC asserted host==default@MAME==a16@MAME==a16@bsnes-jg (`dev/run.sh invaders`) + a corpus 5-way slice + two-emulator screenshots, `-verify-machineinstrs` clean. Assets via the **existing** `gfx4snes` (foundry `pvsneslib-core`): PNG → committed `.pic`/`.pal` → **objcopied to bank-$00 `.rodata` objects** (`-O elf32-mos`, `--rename-section .data=.rodata`) and linked by `dev/build.sh` — no compiled C arrays. P0 (library core + `SpriteSet`) landed: compiles default/a16/xy16, boots both emulators. | [`1d753fe`](https://github.com/wbniv/llvm-mos-65816/commit/1d753fe) | Platform |
| [#3 — SNES Blossom: on-screen interactive Hopalong-attractor port](../plans/2026-06-24-3-snes-blossom-on-screen-interactive-hopalong-attr.md) | Phase 2/3 of Blossom: render the Q8.8 Hopalong attractor live on the SNES and make it interactive. Reuses the existing Mode 7 demo family (`mode7.h`/`view.h`/`jgxcheck`) — graphics is not greenfield. New `+mos-a16` customer is a far **read-modify-write** plotter accumulating a 64 KB hit grid in bank `$7F`, colorized to a Mode 7 chunky-8bpp framebuffer, amortized per-band DMA + CGRAM palette-cycling, with a `blossom.h` joypad param state machine. Two differential gates: deterministic grid-hash (`host==+mos-a16` both emulators) + host-replayable param machine (`host==default==+mos-a16`). | [`312d20a`](https://github.com/wbniv/llvm-mos-65816/commit/312d20a) | Platform |
| [Blossom: split-screen HUD (Mode 7 plot box + tiled BG3 field bars)](../plans/2026-06-26-blossom-split-screen-hud-mode-7-plot-box-tiled-bg3.md) | Follow-up to the landed Blossom demo, answering "the graphic fills the whole screen / there are no fields & values to adjust": carve the screen with an HDMA mode-switch (`BGMODE`+`TM` streamed per-scanline on channels 1+2) into a **Mode 7 plot box** with **tiled BG3 2bpp text bars** top & bottom — an on-screen value bar (preset name + a/b/c + palette + zoom) and a control legend that update live from the joypad. Text is a tiled BG, not sprites (no per-scanline OBJ limit). First HDMA + BG-text in the repo (reusable `hud.h` + a hermetically-generated `font8.h`); display-only so grid hash `0x9047` holds, and `hud_fmt_q88`/`hud_fmt_zoom` fold into `blossom_crc` so the on-screen number formatting is `host==+mos-a16` verified. | [`2a7901b`](https://github.com/wbniv/llvm-mos-65816/commit/2a7901b) | Platform |
| [#11 — SNES Spirograph (hypotrochoid): (R, r, d) parametric rose curves](../plans/2026-06-27-11-snes-spirograph-hypotrochoid.md) | Demo #11 of the compiler stress-test battery — the sin/cos-LUT + fixed-point-multiply member: four curve families (hypotrochoid/epitrochoid/rose/Lissajous) BLOOM into a **NEAR 2bpp bitmap canvas** (new reusable `snesgfx/bitmap_canvas.h` set-pixel + Bresenham + capped dirty-tile DMA, and `text_layer.h` BG3-cotenant HUD reusing `font8.h`), joypad-interactive with an on-screen `R/W/D/mode/petals` HUD. Shared math `examples/65816/spiro.h` (sin/cos-LUT + two 16×16→32 `__mulsi3` + gear-ratio `__udiv`); **no far pointers ⇒ full 5-way bar.** Verified: `dev/run.sh corpus-a16` 9/9 (`spiro_sim` `0x32D4` curve math + `spiro_ctrl_sim` `0x6A26` controller/HUD-format math, the deterministic-scripted equivalent of a JGX replay — both `host==default==+mos-a16==+mos-xy16` MAME+bsnes), `dev/run.sh spirograph` RESULT PASS (disasm gate `__mulsi3`+`__udiv`+`rep/sep`, bsnes 3× byte-identical, both screenshots == host). Published — live at https://biohack.net/spirograph/ (v1.0.78). | [`8d0fe7d`](https://github.com/wbniv/llvm-mos-65816/commit/8d0fe7d) | Platform |
| [#13 — SNES N-body Orbits: Newtonian gravity · Verlet · 1/r²](../plans/2026-06-27-13-snes-n-body-orbits.md) | Demo #13 of the compiler stress-test battery — the physics/integration member: 3 bodies (Sun + 2 planets) under Newtonian gravity integrated by Symplectic Euler, accumulating orbital trails on a **NEAR 128×128 2bpp canvas** with **CGRAM palette dimming** for the fade effect. Hot path stresses **16×16→32 `__mulsi3`** (r² per body-pair) + **`__udivsi3`** (GRAV_K/r² force) + Verlet multiply-accumulate; **no far pointers ⇒ full 5-way bar.** Publishes to [biohack.net/snes/n-body/](https://biohack.net/snes/n-body/). | `f0eead4` | Platform |
| [#19 — SNES π Spigot + Monte-Carlo](../plans/2026-06-27-19-snes-pi-spigot-montecarlo.md) | Demo #19 of the compiler stress-test battery — Rabinowitz-Wagon π spigot (right-to-left carry sweep across a 676-element uint16 array with uint32 intermediates, `__udivmodsi4` + `__mulsi3` per iteration) displayed in the left panel while the right panel accumulates Monte-Carlo darts (xorshift16 RNG + 16×16→32 r² test). Shared `examples/65816/pi_spigot.h` (host-portable); gate hash 0x771D (`PI_GATE_DIGITS=1`, `PI_GATE_THROWS=256`); **no far pointers ⇒ full 5-way bar.** Verified: `dev/run.sh corpus-a16` 10/10 PASS, `task pi` RESULT PASS (disasm `__udivmodsi4`+`__mulsi3`+`rep/sep`, MAME+bsnes host==+mos-a16). | [`3257fea`](https://github.com/wbniv/llvm-mos-65816/commit/3257fea) | Platform |
| [#16 — SNES Wireframe 3-D Solid](../plans/2026-06-27-16-snes-wireframe-3d-solid.md) | Demo #16 of the compiler stress-test battery — spinning polyhedron (tetrahedron/cube/octahedron/icosahedron): 3×3 Q8.8 rotation matrix, perspective divide (`__divsi3`), and integer Bresenham lines into a 2bpp `BitmapCanvas`; stresses matrix-mul + per-vertex divide together. **No far pointers ⇒ full 5-way bar.** PLANNED. | [`56cf11c`](https://github.com/wbniv/llvm-mos-65816/commit/56cf11c) | Platform |
| [#20 — SNES Bignum Factorial](../plans/2026-06-27-20-snes-bignum-factorial-factorial.md) | Demo #20 of the compiler stress-test battery — 1000! in a base-10000 little-endian `uint16_t[700]` digit array; schoolbook carry-propagation multiply stresses `__mulsi3` (u16×u16→u32) + `__udivmodsi4` (quot+rem in one expression) + `rep`/`sep` under `+mos-a16`; custom 28-row `FactDisplay` drawable with `uint32_t` dirty-row bitmask and capped DMA flush; gate hash `0x772F` (FACT_GATE_N=50, ~32 elements). **No far pointers ⇒ full 5-way bar.** 7/7 verification steps PASS. Published at [biohack.net/snes/factorial/](https://biohack.net/snes/factorial/). | [`7bb4f86`](https://github.com/wbniv/llvm-mos-65816/commit/7bb4f86) | Platform |
| [#17 — SNES Sorting Race: quicksort vs heapsort vs mergesort](../plans/2026-06-28-17-snes-sort-race.md) | Demo #17 of the compiler stress-test battery — the **recursion / soft-stack / frame-ABI** member: recursive `sr_qsort` + `sr_msort` (`noinline`, an array pointer live across the self-`jsr` — the `a16spillr.c` machinery in a real workload) plus iterative `sr_hsort` (the non-recursive contrast) race to sort three 32-bar arrays, animated by **op-log record/replay** (sorts emit `pos:=val` stores; ROM replays 1 op/algo/frame) so genuine recursion stays the stress. Gate `sortrace_gate_crc` `0xB28F` folds 8 rounds, each asserting all three sorts agree on the identity permutation + folding `cmps^moves`; disasm gate recursion(`sr_qsort`/`sr_msort`)+`cmp`+`rep/sep`, **zero 32-bit libcalls**. Custom full-screen `RaceField` drawable (bars + HUD on BG3 2bpp, per-row dirty-bitmask DMA). **No far pointers ⇒ full 5-way bar** (NEAR — host==bsnes-jg `0xB28F` + default/+mos-a16/+mos-xy16 `-verify` clean; on-MAME runtime pending the env-wide SPC700 IPL, non-blocker per the 2026-06-28 policy). DONE on the demo bar; publishes to [biohack.net/snes/sort-race/](https://biohack.net/snes/sort-race/). | [`f8aff1c`](https://github.com/wbniv/llvm-mos-65816/commit/f8aff1c) | Platform |
| [SNES demos — startup garbage fix + title screens](../plans/2026-06-28-snes-demo-startup-garbage-and-title-screens.md) | Fixes Newton's-fractal boot garbage (BG tilemap palette field `root<<12`→`<<10`, which had selected uninitialised CGRAM palettes 4-5) + a factorial HUD-palette bug (`1<<13`→`2<<10`), and adds a title-screen facility — `snesgfx/title_layer.h` (BG2 4bpp overlay) + `display_hold()`/`display_hide_layer()` + `snesgfx/splash.h` (BGMODE_1 BG3 splash for the Mode-7 demos) — wired gate-neutrally into 10 demos. Verified on bsnes-jg (every corpus hash unchanged); re-published to biohack.net v1.0.105. | [`8f87929`](https://github.com/wbniv/llvm-mos-65816/commit/8f87929) | Platform |
| [#2 — SNES Newton's-Method Fractal](../plans/2026-06-27-2-snes-newton-fractal.md) | Demo #2 of the compiler stress-test battery — the complex-division member: Newton's method on z³−1 colours each tile by basin of attraction (root 1/2/3 = red/green/blue, shaded by convergence speed), stressing `__divsi3` (complex division per tile) + `__mulsi3` (z², z³) + `rep`/`sep`; **no far pointers ⇒ full 5-way bar.** 32×28 BG1 4bpp tilemap (16 solid-colour tiles × 4 palettes), fills progressively at 4 tiles/frame. Gate: 8×8 sample grid in [−1.2, 1.2]², 20-iter cap, rotating-XOR CRC; hash `0x4D8B`. XFAIL newton_sim (`a16-newton-step-rc-undef` verifier false-positive; code correct). Published [biohack.net/snes/newton/](https://biohack.net/snes/newton/). | [`b76f8ef`](https://github.com/wbniv/llvm-mos-65816/commit/b76f8ef) | Platform |
| [#8 — SNES Gray-Scott Reaction-Diffusion](../plans/2026-06-27-8-snes-rdiff-gray-scott.md) | Demo #8 of the compiler stress-test battery — heavy fixed-point mul-add PDE: Gray-Scott with 3×`__mulsi3` per cell per step (u×v, uv×v, DU×Laplacian) over a 32×28 toroidal grid; **no far pointers ⇒ full 5-way bar.** Gate: 8×8 grid, 8 steps, CRC `0x8484` (`__attribute__((noinline))` on `gs_step` prevents LTO loop-merge reading nu/nv before fully written); disasm gate mul=3 rep/sep=111. PASS host==default==+mos-a16==+mos-xy16 MAME+bsnes-jg. Published [biohack.net/snes/rdiff/](https://biohack.net/snes/rdiff/). | [`fcaa7b8`](https://github.com/wbniv/llvm-mos-65816/commit/fcaa7b8) | Platform |
| [#14 — SNES Double Pendulum (Chaos)](../../biohack.net/docs/plans/2026-06-27-14-snes-double-pendulum.md) | Demo #14 of the compiler stress-test battery — two double pendulums with a 1-LUT-tick starting-angle difference swing from a shared pivot; mass-2 paths trace diverging white/cyan trails in a `BitmapCanvas` (sensitive dependence on initial conditions). Shared `examples/65816/dpend.h`; corpus gate hash 0xE859 (256 steps, semi-implicit Euler); **no far pointers ⇒ full 5-way bar.** Verified: `dev/run.sh double-pendulum` RESULT PASS (disasm `__mulsi3=6` + `__divsi3=2` + `rep/sep=77`, host==MAME==bsnes-jg `0xE859`). Published at [biohack.net/double-pendulum/](https://biohack.net/double-pendulum/). | PENDING | Platform |
| [Cache-Control headers for biohack.net SNES demos](../plans/2026-06-27-cache-control-headers-for-biohack-net-snes-demos.md) | Add `_headers` to biohack.net so `public/play/**` assets are served with `Cache-Control: immutable` (content-hash busted) and HTML pages with `no-store`, preventing stale ROM/engine assets after redeploy. | [`77d085b`](https://github.com/wbniv/llvm-mos-65816/commit/77d085b) | Platform |
| [#6 — SNES Rule 90/110 1-D Cellular Automaton](../plans/2026-06-27-6-snes-rule90-110-1d-ca.md) | Demo #6 of the compiler stress-test battery — the pure shift+boolean member: 256-cell bitpacked CA (`uint8_t[32]`), sliding-window inner loop (constant-1 shifts only, no variable-amount loops), precomputed 8-entry rule table to avoid `rule>>k` inside the hot path; **no mul/div, no rep/sep in hot path.** Gate: 32 Rule-90 + 32 Rule-110 gens from single-cell seed, CRC-16 of all output rows; hash `0xAB2C`; **no far pointers ⇒ full 5-way bar.** Verified: `dev/run.sh 1d-ca` RESULT PASS (disasm shifts=7 bools=11 bad_mul=0, MAME+bsnes-jg host==+mos-a16 `0xAB2C`); `dev/run.sh corpus-a16` 11/12 PASS. Published at [biohack.net/1d-ca/](https://biohack.net/1d-ca/). | [`dce7db4`](https://github.com/wbniv/llvm-mos-65816/commit/dce7db4) | Platform |
| [Re-verify the "LTO + `+mos-a16` bitmask-loop early-exit" diagnosis (#321 upstream item 11)](../plans/2026-06-28-321-verify-lto-a16-bitmask-early-exit-diagnosis.md) | Controlled rebuild + disassembly proving the drafted LTO+a16 "bitmask-loop early-exit" upstream issue is a **misdiagnosis**: the `cmp #$10` in `_fact_emit` is the `q->n < UPQ_MAX_JOBS` (=16) queue-full exit — confirmed by a `-DUPQ_MAX_JOBS=20` override moving the constant to `cmp #$14` — not a shift-counter row-skip; the real `r<28` bound `cpy #$1c` is intact. Retracts item 11 (do-not-post) and opens a follow-up to root-cause the real factorial stall. | [`c09912a`](https://github.com/wbniv/llvm-mos-65816/commit/c09912a) | Investigation |
| [#12 — SNES CORDIC Rotator](../plans/2026-06-28-12-snes-cordic-clock-rotator.md) | Demo #12 of the compiler stress-test battery — the **multiply-free shift-add** member, and the only one with an **inverted** disasm gate (asserts the *absence* of `__mulsi3`/`__udivsi3`): a CORDIC engine computes sin/cos (rotation mode) and `atan2` (vectoring mode) in Q2.14 by shift-and-add convergence only, building on `examples/65816/cordic16.h` (unrolled, zero arithmetic libcalls) — a different ALU profile from every other demo. A rotating hand sweeps a CORDIC-computed vector field while a live `atan2` reads its own angle back. Gate `cordic_gate_crc` = `0x4D41` (full-circle sweep, GATE_N=96: `cordic16_sincos` + `cordic16_atan2`, rotate-XOR fold); **no far pointers ⇒ full 5-way bar.** Verified: `dev/run.sh cordic` RESULT PASS (inverted disasm `mul=div=0`, `rep/sep`=252, `adc/sbc`=143 — the atan tables constant-fold into immediate `adc/sbc`, no rodata symbol; `-verify-machineinstrs` clean ×3; on-screen `ANGLE 123 / ATAN2 123 OK`; MAME pending the SPC700 IPL — non-blocker). Published [biohack.net/snes/cordic/](https://biohack.net/snes/cordic/). | [`34fd3ad`](https://github.com/wbniv/llvm-mos-65816/commit/34fd3ad) | Platform |
| [#18 — SNES Maze generate + solve](../plans/2026-06-28-18-snes-maze-generate-solve.md) | Demo #18 of the compiler stress-test battery — the **recursion + data-structure** member (multiply/divide-free): recursive-division carve (genuine recursion; centre-biased **~log depth** because the 65816's 256-byte HW stack can't hold a backtracker DFS's O(N) depth — measured ~6 B/level × 199 deep ≫ 256) + **A\* with an indexed binary min-heap** (decrease-key) solve, animated. Gate `maze_gate_crc` = `0x0749`; **no far pointers ⇒ full 5-way bar.** Verified: `dev/run.sh maze` RESULT PASS (disasm recursion(`maze_divide` self-call)=3 + rep/sep=227, zero 32-bit libcalls; bsnes-jg host==default==+mos-a16==+mos-xy16 `0x0749`, `-verify` clean all 3; MAME pending the SPC700 IPL). Surfaced a 65816 GISel "defs don't dominate all uses" mis-schedule (worked around; upstream-worthy). | [`9f3d71e`](https://github.com/wbniv/llvm-mos-65816/commit/9f3d71e) | Platform |
| [#7 — SNES Doom-fire / heat field](../plans/2026-06-28-7-snes-doom-fire.md) | Demo #7 of the compiler stress-test battery — the **array-sweep + PRNG** member (deliberately multiply-/divide-free): a 32×28 heat grid rises + flickers from a constant max-heat source row through a 16-colour black→red→orange→yellow→white CGRAM ramp on BG1 4bpp. The hot loop is a flat-index 8-bit array sweep (`lda/sta (zp),y`, variable write offset) + a 16-bit xorshift16 PRNG per non-zero cell (native `eor`/`asl`/`lsr` under one `rep`/`sep`). Gate `doomfire_gate_crc` = `0x3C59` (16×16 grid, 30 steps, full-grid rotate-XOR CRC); **no far pointers ⇒ full 5-way bar.** Verified: `dev/run.sh doom-fire` RESULT PASS (disasm `eor=6` + `asl/lsr=8` + `rep/sep=21`, **zero** `__mulsi3`/`__udivmodsi4`; bsnes-jg host==+mos-a16 `0x3C59`; MAME pending the SPC700 IPL — non-blocker per the demos-only policy). Published [biohack.net/snes/doom-fire/](https://biohack.net/snes/doom-fire/). | [`32a4114`](https://github.com/wbniv/llvm-mos-65816/commit/32a4114) | Platform |
| [#10 — SNES Fourier Epicycles](../plans/2026-06-28-10-snes-fourier-epicycles.md) | Demo #10 of the compiler stress-test battery — the **many-multiply / sin-cos** member (no divide): a sum of rotating vectors `P(t)=Σ c_k·e^{i2πf_k t}` traces a baked outline; the hot loop is a sin/cos-LUT sweep with **four 16×16→32 multiplies per harmonic** (the complex multiply `__mulsi3`) + 32-bit accumulation. 8 baked DFT coefficients of a 5-pointed star (`tools/gen-epicycles-tables.py`, rotated off-axis so both re/im are non-zero — else the real-part multiplies fold to ×0). Gate `epi_gate_crc` = `0x4F6C`; **no far pointers ⇒ full 5-way bar.** Verified: `dev/run.sh epicycles` RESULT PASS (disasm `__mulsi3=4` + rep/sep=28, divide=0; bsnes-jg host==default==+mos-a16==+mos-xy16 `0x4F6C`, `-verify` clean all 3, UBSan clean; MAME pending the SPC700 IPL). On-screen the star draws itself over its generating circle. | [`070328f`](https://github.com/wbniv/llvm-mos-65816/commit/070328f) | Platform |
| [#5 — SNES Conway's Game of Life](../plans/2026-06-28-5-snes-life.md) | Demo #5 of the compiler stress-test battery — the **2-D bit-manipulation** member (companion to the 1-D #6, deliberately multiply-/divide-free): a 128×112 bit-packed grid (8 cells/byte) evolves by B3/S23, where each byte's eight neighbour bit-vectors are summed **bit-parallel** into a 4-bit-per-cell count via a ripple of SWAR half-adders (`carry=a&b; sum=a^b`) — pure `and`/`eor`/`ora` + constant-1 `asl`/`lsr` for the cross-byte neighbour alignment, plus the two-buffer ping-pong swap. A Gosper glider gun fires gliders into a settling random soup. Gate `life_gate_crc` = `0xDDF1` (Gosper gun on 64×48 grid, 32 gens, CRC-16 of all output rows); **no far pointers ⇒ full 5-way bar.** Verified: `dev/run.sh life` RESULT PASS (disasm `shifts=22` + `bools=51`, **zero** `__mulsi3`/`__udivmodsi4`; bsnes-jg host==+mos-a16 `0xDDF1`; `-verify-machineinstrs` clean on default/+mos-a16/+mos-xy16; MAME pending the SPC700 IPL — non-blocker per the demos-only policy). Published [biohack.net/snes/life/](https://biohack.net/snes/life/). | [`30333e0`](https://github.com/wbniv/llvm-mos-65816/commit/30333e0) | Platform |
| [#9 — SNES Lissajous / Harmonograph](../plans/2026-06-28-9-snes-harmonograph.md) | Demo #9 of the compiler stress-test battery — the **damped sin-LUT** member (the spirograph's damped sibling, multiply-only): four detuned pendulums (two per axis) trace a Lissajous figure that **precesses** and spirals inward as an exponential envelope decays. Per sample the hot loop issues **eight** `__mulsi3` — four amplitude products `sin·env` + four envelope-decay products `env·decay` (the running exponential `env=(env·decay)>>15`, a sustained fixed-point multiply + accumulation) — plus the Q8.8 sine LUT, with **no divide**. Envelope scaled `ENVF=2` so `env·decay` fits int32. Gate `harmo_gate_crc` = `0x0EBB` (256 samples of preset 0, rotate-XOR fold of both coords); **no far pointers ⇒ full 5-way bar.** Verified: `dev/run.sh harmonograph` RESULT PASS (disasm `__mulsi3`=2 + `rep/sep`=36, divide=0; bsnes-jg host==+mos-a16 `0x0EBB`; `-verify-machineinstrs` clean on default/+mos-a16/+mos-xy16; MAME pending the SPC700 IPL — non-blocker per the demos-only policy). Published [biohack.net/snes/harmonograph/](https://biohack.net/snes/harmonograph/). | [`70e1840`](https://github.com/wbniv/llvm-mos-65816/commit/70e1840) | Platform |
| [#15 — SNES Raycaster maze](../plans/2026-06-28-15-snes-raycaster.md) | Demo #15 of the compiler stress-test battery — the **division** member: a Wolfenstein-style grid raycaster auto-walks a 16×16 maze, casting one ray per screen column by integer **DDA** and drawing each wall slice at `screen_h / perpendicular_distance` — **three `__udivsi3` per column** (the two `deltaDist = |1/rayDir|` reciprocals + the `1/dist` projection), the only division-bound demo. Camera-plane basis from a Q8.8 sine LUT → fisheye-free; distance-shaded slices into a `BitmapCanvas`. Gate `rc_gate_crc` = `0xB200` (64-column fan, fixed camera); **no far pointers ⇒ full 5-way bar.** Verified: `dev/run.sh raycaster` RESULT PASS (disasm `__udivsi3`=3 + `rep/sep`=51; bsnes-jg host==+mos-a16 `0xB200`; `-verify` clean ×3; MAME pending the SPC700 IPL). **The differential caught a real bug:** an axis-aligned-ray `deltaDist` sentinel `0x3FFFFFF` made `frac·sentinel` overflow signed `int32` (UB) which host `-O2` and target `-Os` optimised differently (host `0x724B` ≠ target `0xB200`) — shrunk to `0x400000`; also fixed a `128*256` int16 overflow. Published [biohack.net/snes/raycaster/](https://biohack.net/snes/raycaster/). | [`579ad86`](https://github.com/wbniv/llvm-mos-65816/commit/579ad86) | Platform |
| [#1 — SNES Julia Set Explorer](../plans/2026-06-28-1-snes-julia.md) | Demo #1 of the compiler stress-test battery — the **complex multiply + far high-WRAM framebuffer** member (the Mandelbrot demo's morphing cousin): iterates `z²+c` in Q5.10 with **z₀ = the pixel** and `c` a single constant per frame swept along the classic `0.7885·e^{iθ}` orbit, so the set continuously morphs. Three 16×16→32 `__mulsi3` per iteration (`zr²`, `zi²`, `zr·zi`); the escape image is far-stored into a high-WRAM buffer at `$7E2000` (the #320 `sta [dp]` path) and far-loaded back into Mode 7 VRAM. A full 64×56 recompute is ~22 s on the 65816, so the set is re-ground a coarse row per spin frame in the background while the Mode 7 affine matrix spins + zoom-breathes it at 60 fps (the grind is the morph, the hardware is the motion). Gate `julia_gate_crc` = `0x3490` (4 keyframe `c` over a 6×6 grid, maxiter 8 — far-pointer-free, so the **corpus slice is a full 5-way bar**; sized to finish by ~frame 90, inside the 180-frame corpus budget). Verified: `dev/run.sh julia` PASS on the demo bar (disasm `__mulsi3=3` + `rep/sep=46`; bsnes-jg host==+mos-a16 `0x3490`; MAME SKIP — no SPC700 IPL in this env, demos-only non-blocker). Published [biohack.net/snes/julia/](https://biohack.net/snes/julia/). | [`0cabf2d`](https://github.com/wbniv/llvm-mos-65816/commit/0cabf2d) | Platform |
| [#4 — SNES Buddhabrot](../plans/2026-06-28-4-snes-buddhabrot.md) | Demo #4 of the compiler stress-test battery — the **PRNG + far scatter-write** member: random complex `c` (xorshift16) iterated `z²+c`; for the points that **escape**, the orbit is replayed and each visited `(zr,zi)` increments a 128×128 hit-count **far** density grid in high WRAM ($7E2000) — a runtime-indexed far read-modify-write (`lda [dp]`/`inc`/`sta [dp]`), the #320 path. Three Q5.10 `__mulsi3`/iteration ×2 passes (escape test + replay) + a `__umulsi3` per windowed sample. **`+mos-a16`-only (far grid) ⇒ 3-way bar** (host==+mos-a16 on bsnes-jg+MAME), the blossom model. Mode 7 display, ghostly glow palette; gate = far-hash of the grid after a deterministic K_GATE=2000 accumulation == host == `0x7C31`. Verified: `dev/run.sh buddha-grid` + `buddha` RESULT PASS (far RMW `lda/sta [dp]`=2, rep/sep=31/34, `__mulsi3/__umulsi3`=8, `-verify` clean; MAME pending the SPC700 IPL). Published [biohack.net/snes/buddhabrot/](https://biohack.net/snes/buddhabrot/). | [`95ae613`](https://github.com/wbniv/llvm-mos-65816/commit/95ae613) | Platform |
| [#3 — SNES Burning Ship fractal](../plans/2026-06-28-3-snes-burning-ship.md) | Demo #3 of the compiler stress-test battery — the **abs-fold fractal** member: the Mandelbrot's folded cousin `z_{n+1}=(\|Re z\|+i\|Im z\|)²+c`. Per iteration **three Q12 `__mulsi3`** (`zx²`,`zy²`,`\|zx·zy\|`) — same count as Mandelbrot — PLUS the two abs folds that ARE the algorithm; escape-time bands render the black ship silhouette, palette-cycled. Multiply-only (no divide). Gate `bs_gate_crc` = `0x6F2D` (16×16 window over the ship, maxiter 24); **no far pointers ⇒ full 5-way bar.** Verified: `dev/run.sh burning-ship` RESULT PASS (disasm `__mulsi3`=3 + `rep/sep`=26, divide=0; bsnes-jg host==+mos-a16 `0x6F2D`; `-verify` clean ×3; MAME pending the SPC700 IPL). Full-grid escape compute ground out once behind the title (too heavy for a 60 fps reveal) then palette-cycled; gate screenshot at frame 1500. Published [biohack.net/snes/burning-ship/](https://biohack.net/snes/burning-ship/). | [`8e6f647`](https://github.com/wbniv/llvm-mos-65816/commit/8e6f647) | Platform |
| [#21 — SNES Soft-Float Mandelbrot](../plans/2026-06-29-21-snes-mandel-float-softfloat.md) | Demo #21 of the compiler stress-test battery — the **first Round-2 (new-codegen-corner) entry** and the **soft-float** member: the Mandelbrot escape-time `z²+c` done in **IEEE-754 single-precision `float`**, so on the FPU-less 65816 every op is a soft-float libcall (`__mulsf3`/`__addsf3`/`__subsf3`/`__divsf3`/`__gtsf2`/`__floatsisf`) — the library no Round-1 demo touches. **Bit-exact differential**: single precision is fully specified (correctly-rounded `+−×÷`/compare), so host x86 single == target soft-float bit-for-bit; FMA contraction (the one thing that could diverge them) is forbidden BY CONSTRUCTION — every op is its own statement, so no `a*b±c` to fuse (verified: baseline `cc -O2` has no FMA without `-march`; oracle also built `-ffp-contract=off`). 16×14 grid 4×-upscaled into the `$7E2000` far framebuffer, Mode-7 zoom toward the seahorse spiral, progressive boot-paint (each fat pixel literally is hundreds of soft-float instructions). Gate `mf_gate_crc` = `0x4169` (2 zoom windows 6×6 maxiter 12 + a 24-step bit-exact orbit witness; far-pointer-free ⇒ full 5-way bar). Verified: `dev/run.sh mandel-float` bsnes-jg host==+mos-a16 `0x4169` + disasm gate (`__mulsf3=8`, `__add/subsf3=12`, `rep/sep=35`); **5-way confirmed** — default-8bit + +mos-a16 + +mos-xy16 corpus slices all `0x4169` on bsnes-jg (MAME legs pending the SPC700 IPL, demos-only non-blocker). | _uncommitted_ | Platform |

---

## How this index was derived

- **Order** = each plan's *creation* commit (the oldest commit that touched the file), sorted by
  committer date:
  `for f in docs/plans/*.md; do git log --follow --format='%h %cI' -- "$f" | tail -1; done | sort -k2`.
- **Commit(s)** = the full `git log --follow` set for each plan file, oldest → newest — every commit that
  created or revised it (plan authoring, implementation, status updates, deferral triage). Links point at
  `github.com/wbniv/llvm-mos-65816/commit/<sha>` (private repo — clickable for the owner while authenticated).
- **Summaries / categories** are hand-authored from each plan's TL;DR / status header; they are *not*
  auto-generated, so update this table by hand when a plan is added or its disposition changes.
- **Last refreshed** 2026-06-29 — **181 plans**: added the **#12 CORDIC Rotator** row (`34fd3ad`; the multiply-free, inverted-disasm-gate demo — published [biohack.net/snes/cordic/](https://biohack.net/snes/cordic/)), closing a committed-plan drift the pre-commit audit flagged. Then **batch-refreshed the recent demo cluster**: filled the real creation-commit SHA for #18 maze (`9f3d71e`), #7 doom-fire (`32a4114`), #10 fourier (`070328f`), #5 life (`30333e0`), #9 harmonograph (`70e1840`), #15 raycaster (`579ad86`), #1 julia (`0cabf2d`), #4 buddhabrot (`95ae613`), #3 burning-ship (`8e6f647`) — previously all `_uncommitted_` — and re-sorted them into creation-commit order. Only #21 Soft-Float Mandelbrot remains `_uncommitted_` (its plan file is not yet committed).
- **Last refreshed** 2026-06-27 — **162 plans**: added #16 Wireframe 3-D Solid (PLANNED, `56cf11c`).
  Prior **2026-06-27** refresh (161 plans): #20 Bignum Factorial DONE (gate hash `0x772F`, 5-way PASS, published [biohack.net/snes/factorial/](https://biohack.net/snes/factorial/)).
  Prior **2026-06-27** refresh (160 plans): added the Rule 90/110 1-D CA demo plan (`Platform`; PASS, published [biohack.net/1d-ca/](https://biohack.net/1d-ca/)).
  Prior **2026-06-27** refresh (158 plans): added the N-body orbits plan (`Platform`; PLANNED) and the π spigot + Monte-Carlo plan (`Platform`; `3257fea`).
  Prior **2026-06-27** refresh (156 plans): merged `wt/321-blossom` to `main`, adding the 2 Blossom
  plans (`Platform`; the on-screen interactive Hopalong renderer `312d20a` + the split-screen HUD `2a7901b`).
  Prior **154 plans**: added the Space Invaders-on-`snesgfx` plan (`Platform`;
  playable game driving the sprite/OAM front-end, 5-way differential via a HAL-free attract sim, gfx4snes
  assets objcopied into ROM, commit `1d753fe`). Prior **153 plans**: added the `snesgfx` OOP-in-C SNES rendering library plan
  (`Platform`; re-imagine the proven rendering mechanics as header-only objects per the oop-in-c HOWTO —
  UploadQueue/VramAlloc/Drawable+Scene/Mode7Layer/SpriteSet, verified via `mandel-oop.c` CRC `0x204F` then
  measured back into the HOWTO, commit `d5c1448`). Prior **152 plans**: added the far-pointer `G_PHI(p2)` backend-gap fix plan
  (`Crash fix`; custom-legalizes a far-pointer loop-IV phi to an s32 phi via ptrtoint/inttoptr, fork patch
  `0014`, commit `d1099db`) — resolves the latent gap the far-memops plan noted. Prior **151 plans**: added the far addrspace-2 memset/memcpy/memmove wrong-bank
  fix plan (`Crash fix`; routes far memops to a far runtime at the `legalizeMemOp` chokepoint, patch `0013`,
  commit `a81874d`), and the xy16-cmove stale-XFAIL resolution plan (`xy16`;
  the cmove defect doesn't reproduce on the committed stack — de-XFAIL'd to positive gates, commit `8535fd1`),
  the full-xy16-backend-fix plan (`xy16`; close the last
  `+mos-xy16` fp compare-as-select miscompile, commit `3e338fb`), the pr15296 `+mos-a16` link-time ZP-overflow
  gated-narrow-fix-spike plan (`Crash fix`; `MOSZeroPageAlloc` budget defect, commit `238de65`), the trig Phase 3 derived+hyperbolic plan
  (`Fuzz / corpus`; `dev/run.sh k_trig16x`, commit `6f02ea7`) and the #320 thunk-tail-calls plan
  (`#320 far-pointer`; far→near fold, commit `a31ea5b`), the #321 A16-threading Phase-3 trigger-check-pass plan
  (`Investigation`; re-affirm-or-re-open the deferral via `dev/measure-zp-pressure.sh` + a csmith crash-sweep,
  commit `e7e2270`), the trig Phase 2 Q2.14 CORDIC native-s16 /
  zero-libcall differential plan (`Fuzz / corpus`; `dev/run.sh k_trig16`, commit `4626ddd`), the "far data > 2 banks" gate-formalization plan
  (`#320 far-pointer`; `dev/run.sh farindex`, commit `a9601e5`), the #321 register-scavenger live-`$p` fix plan
  (`Crash fix`; patches `0011`/`0012`), the shared plan-index tooling plan (`Tooling`), and —
  same day — slotted the 10 plans added since the 2026-06-25 refresh into
  chronological order — the s32-`long` verification, the reviewer patch-series presentation, Blossom Phase 1,
  the SNES hardware-reference HAL split, the interim cross-platform toolchain builds, the published-compiler
  clean-room test gate, the `0009` a16 register-pressure fix, the trig-as-differential-test + its HiROM/sin-LUT
  follow-up (which fixed a clang far-index miscompile in `0001`), and the `0010` default-8bit
  coalescer-miscompile fix. Prior **2026-06-25** refresh (128 plans): added the SNES Mandelbrot beefy-demo
  row (`Platform`). Prior **2026-06-24** refresh (127 plans): order + every commit column regenerated from
  `git log`, and the 53 plans added since the prior 2026-06-19 refresh summarised and slotted into order.
