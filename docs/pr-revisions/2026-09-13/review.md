# Review of the 2026‑09‑13 PR revisions

Reviewer: Claude (Fable 5.1), 2026‑09‑13. Scope: the six proposed branch updates in this
directory (see [README](README.md)), checked against the upstream review threads they answer
and against the code in the `/tmp/llvm-mos-review*` checkouts.

**Verdict: all six are publishable.** Four are approve-as-is (#578, #588, #589, #590), two carry
small nits worth fixing before the force-push (#584 diff hygiene and test comment, #586 body
should cite the ca65 prior art the reviewer asked about). Nothing found that changes a fix.

## Implemented (same day, at Will's request)

Every recommendation below has been applied to the bundle and, where it touched code, re-validated
(details in [validation.md](validation.md), "Review follow-up"):

- #578: body sentence on the target-only scope; `BRA %bb.3` in the MIR test; the liveness recompute
  collapsed to a fixed-point pass after the erase loop (Will asked for this explicitly). The collapse
  changed only the order of two live-in lists in `copy-opt.mir`; the corpus output is byte-identical.
- #584: stray blank line removed; test header shortened; autogenerated-checks NOTE dropped.
- #586: ca65 prior art added to the body. #589: validation line replaced.
- Bundle: `*-review-changes.patch`, `*-complete.patch`, `*-commits.patch`, `heads.json` and
  `SHA256SUMS` regenerated for #578 and #584.

## What I verified independently

| Check | Result |
| --- | --- |
| #578 `copy-opt-loop.mir` on Codex's built llc (578 head) | both prefixes PASS |
| #578 red check: rebuilt llc with the `MOSCopyOpt.cpp` hunk reverted, reran the test | COPY prefix fails (`bb.3` live-ins lack `$a`); LOWER prefix fails (`dead $a = LDImag8 $rc2` emitted). Exactly the claimed failure. Tree and llc restored afterwards. |
| Post-RA pass order (`llc -debug-pass=Structure`) | `mos-copy-opt → postrapseudos → mos-scavenging → finalize-isel → postrapseudos → mos-scavenging → mos-late-opt → … → block-placement → branch-relaxation` |
| #589 corpus measurement (see below) | 176 of 176 `-O2` asm outputs byte-identical with and without the change; identical verifier/failure set |
| #586 prior art | ca65 documents an optional BRK signature byte: `brk`, `brk $34`, `brk #$34`; bare `brk` emits one byte ([ca65 manual §4](https://cc65.github.io/doc/ca65.html#ss4.1)). |
| Upstream review threads for all six PRs | re-read via `gh`; every inline comment and top-level ask is mapped below |

## #578 — approve

The rediagnosis is right and the evidence chain is complete: `virtregrewriter` emits a correct
`$a = COPY $y` at the loop header; `MOSCopyOpt` forwards it away, making A live around the back
edge; the pass's final loop recomputes each block's live-ins **once, in post-order**, which
visits the latch before the header, so the header's new `$a` live-in never reaches the latch;
`mos-late-opt` then treats A as dead scratch when lowering the `CmpBrZero`. The forwarding loop
itself walks reaching definitions and predecessors directly (`findReachingDefs`, `isClobbered`),
not live-ins, so stale liveness cannot have affected any forwarding decision. Placing
`fullyRecomputeLiveIns` between forwarding and dead-copy cleanup is the minimal correct fix, and
excluding the entry block matches the existing loop's convention.

This answers mysterymath's objection directly: the defect is in a MOS-only post-RA pass, the
coalescer and greedy RA were doing the right thing, and no generic LLVM change is involved.
Say that last part explicitly in the body, since the upstream angle was his stated concern.

Nits, none blocking:

- The pass now recomputes liveness twice (fixed-point pre-pass, then the old single-pass
  per-block `computeAndAddLiveIns`). The second pass is now only a tightening after erasures
  and its single-pass result is merely over-approximate, which is safe. A reviewer may ask to
  collapse it into one `fullyRecomputeLiveIns` after the erase loop; be ready to do that.
- `bb.2` in the MIR test falls through to `bb.3` with no terminator. Legal MIR, but an explicit
  `BRA %bb.3` would spare a reader the question.

Downstream consequences to schedule after publication: fork patch `0010-coalesce-rotate-ac`
ships the now-withdrawn guard and the fork's `MOSCopyOpt` still has the real bug; the drafted RA
companion issue (`docs/upstream-coalesce-rotate-ac-ra-issue.md`, status row 16) is obsolete and
should be retired rather than posted; the body mirror `docs/upstream-coalesce-rotate-ac-pr.md`
and the status rows need re-syncing.

## #584 — approve with nits

Both inline comments are answered precisely: the non-GPR check is folded into the existing
`LDImm` condition and the TA-handler defensive block is gone. The complete diff is four changed
lines of code plus a test.

- Remove the stray blank line the revision leaves after `Register Dst = …`. It is diff noise
  from deleting the block below it.
- The test's 11-line header narrates the old crash ("null-pointer store, i.e. a hard crash").
  That is the style mysterymath rejected on #590 ("Noone cares how code was broken in the
  past"). Two lines suffice: SPC700 widens `LDImm`'s destination to `Anyi8`; `combineLdImm` must
  skip non-GPR destinations without disturbing A/X/Y tracking.
- The `NOTE: Assertions have been autogenerated by utils/update_mir_test_checks.py` line sits
  above hand-shaped CHECK lines. Either regenerate or drop the NOTE.

## #586 — approve; add the prior art

`isAsmParserOnly = 1` on `BRK_Immediate` keeps the decoder tables unchanged, which is exactly
the property johnwbyrd wanted proven, and the new RUN lines prove it three ways: objdump on
MOS6502 and W65816 both decode `00` as a one-byte `brk`, and the round trip uses the NOP opcode
as the signature byte so any two-byte decode would fail.

mysterymath's question about prior art went unanswered in the thread. Add one sentence to the
body: ca65 documents `brk` / `brk $34` / `brk #$34` with the signature byte optional and bare
`brk` emitting one byte ([ca65 manual](https://cc65.github.io/doc/ca65.html#ss4.1)). Note that we
accept only the `#imm` spelling, not ca65's bare-operand form.

## #588 — approve

All three inline comments addressed as asked: the `.td` comment is reduced to the one sentence
the reviewer called useful, the "outside FeatureW65816" framing is replaced by the operand
requirement, and the 65EL02 RUN line and its two checks are gone. The default-CPU rejection in
`asm-errors.s` still pins that COP is feature-gated.

## #589 — approve

Matches the reviewer's direction (`isTerminator`, scan `terminators()`), and the invariant test
uses the verifier itself, which is the right oracle.

Why the terminator flag cannot pessimize codegen: `CmpZero` is created by `expandCmpBr` /
`expandGBR` in the first `postrapseudos` run and every instance is replaced by a real
instruction in `mos-late-opt`. In that window the only passes are `mos-scavenging`,
`finalize-isel` (post-RA custom inserters) and a second `postrapseudos`. `analyzeBranch` does
return "cannot analyze" for a block whose first terminator is a `CmpZero`, but its only
in-window caller is `emitCmpBrZeroMultiByte`, which runs on blocks ending in
`CmpBrZeroMultiByte`, a different conditional branch that never shares a block with a
`CmpZero`. Branch folding runs before the window; block placement, tail duplication and branch
relaxation run after it. So the flag is purely a verifier invariant with no layout effect.

Measurement (not in Codex's validation, added here): I applied `589-complete.patch` on the
578 review tree, rebuilt `llc`, and compiled IR from all 111 `examples/65816/*.c` (two variants
each, with and without the `+mos-a16` feature string; the upstream llc ignores the unknown
feature) at `-O2 -verify-machineinstrs` with and without the change. All 176 outputs that
compile are byte-identical, and the failure set is identical too (46 legalization failures on
fork-only far-pointer shapes plus a pre-existing upstream RA crash on `k_trig16x`, both present
without the change). `llc` rejects `-Os`, so this is `-O2` only. Tree and llc restored
afterwards.

The corrected test expectation is right: the reverse walk meets `CmpZero $a` first, the
intervening `CmpZero $x` clobbers NZ so it cannot fold, and lowering picks the dead X as a
one-byte `TAX` (NZ from A). `CmpZero $x` then folds into `T_A $x`. The final N is A's value,
matching the original last compare.

Body nit: "all 80 enabled MOS CodeGen tests pass across the full run and focused rerun after
correcting one new test expectation" narrates local test-writing history. Replace with
"Validation: MOS CodeGen suite 80 pass, 1 unsupported." Fork patch `0022` carries the original
sticky-`Changed` form and should move to the terminator form when this lands.

## #590 — approve

Exactly the three comment replacements requested, the test comment now says "must be
deterministic" without prescribing an order, and the history paragraph is gone. The test still
pins one concrete winning set, which a FileCheck test cannot avoid. The 20/20 identical-output
evidence and andymccall's independent confirmation are both cited.

## Cross-cutting notes

- Bodies are consistent and in the reviewer's preferred register. Keep the trailing
  "Validation:" lines; they are short.
- The branches were merge-tree-checked against upstream `main` but the merge results were not
  built. Acceptable for a PR-branch force-push; CI builds the merge.
- Publication order: force-push the six branches (recorded heads in `heads.json` must still
  match the remotes at push time), then `gh pr edit` titles and bodies. #578's branch name
  `mos-coalesce-rotate-ac` no longer describes its content; harmless for a PR branch.
- After publication, in the same commit: update `docs/upstream-contribution-status.md` rows 9,
  15, 16, 17, 18, 18a, 18b, the six `docs/upstream-*-pr.md` mirrors, and plan the fork-patch
  follow-ups (`0010` → copy-opt fix, `0022` → terminator form; `0003`/`0021`/`0024` are
  comment-only deltas).
