# Work-session transcript: 0015 revalidation

Visible user and assistant messages; tool payloads and private reasoning are excluded.

## User

implement Coalescing guard (0015): revalidate the diagnosis and recover a stock-upstream reproducer.

## Assistant

I’ll revalidate 0015’s diagnosis and look for a stock-upstream reproducer before changing the guard. I’ll first finish the opcode plan’s tracking updates; the opcode test itself will remain unimplemented as requested.


## Assistant

The existing 0015 MIR test verifies successfully on the preserved unpatched upstream compiler, including after register allocation. Upstream coalesces the value but keeps it in preserved `$rc20` across the call.

That test proves the guard changes coalescing, but it does not reproduce the claimed allocator failure. I’m checking the historical C witness and a build with only this guard disabled to find whether the original failure remains independently reproducible.


## Assistant

The comparison recovered the historical failure: disabling both 0015 and the later undef-lane fix 0028 makes `rcundef.c` fail at `-O1`/`-Os`. Either change prevents it.

This suggests 0015 avoids a trigger of the defect repaired by 0028. I’m tracing the failing MIR to verify that connection and extract a reproducer that runs on stock upstream.


## Assistant

The MIR comparison confirms the connection: 0028 changes only three retained `KILL` pseudos, including the missing definition before the failing `$rc3` read. Register assignments are unchanged.

The existing eight-instruction 0028 model also reproduces on unpatched upstream with plain `mos6502`. I’m recording this as a revalidation of the existing root fix, not a new 0015 fix; stock C reachability remains unproven.


## Assistant

The revalidation is recorded with the failing and passing binaries preserved. I’ve updated the 0015 draft and trackers to point this witness to 0028’s root fix. The opcode plan remains unimplemented.

Reduction also exposed a separate issue: `llvm-reduce -j 2` segfaults even on the small stock MIR model. I’ve captured it as an unfixed tool defect and will leave its repair outside this task.
