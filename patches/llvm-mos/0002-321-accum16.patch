diff --git a/llvm/lib/Target/MOS/CMakeLists.txt b/llvm/lib/Target/MOS/CMakeLists.txt
index 584cc0c4c..51f92653c 100644
--- a/llvm/lib/Target/MOS/CMakeLists.txt
+++ b/llvm/lib/Target/MOS/CMakeLists.txt
@@ -31,6 +31,7 @@ add_llvm_target(MOSCodeGen
   MOSIndexIV.cpp
   MOSInlineAsmLowering.cpp
   MOSInsertCopies.cpp
+  MOSInsertREPSEP.cpp
   MOSInstrCost.cpp
   MOSInstrInfo.cpp
   MOSInstructionSelector.cpp
diff --git a/llvm/lib/Target/MOS/MOS.h b/llvm/lib/Target/MOS/MOS.h
index 66964b1fe..e2cb062d6 100644
--- a/llvm/lib/Target/MOS/MOS.h
+++ b/llvm/lib/Target/MOS/MOS.h
@@ -23,6 +23,7 @@ void initializeMOSCombinerPass(PassRegistry &);
 void initializeMOSCopyOptPass(PassRegistry &);
 void initializeMOSIndexIVPass(PassRegistry &);
 void initializeMOSInsertCopiesPass(PassRegistry &);
+void initializeMOSInsertREPSEPPass(PassRegistry &);
 void initializeMOSInternalizePass(PassRegistry &);
 void initializeMOSLateOptimizationPass(PassRegistry &);
 void initializeMOSLowerSelectPass(PassRegistry &);
diff --git a/llvm/lib/Target/MOS/MOSCombiner.cpp b/llvm/lib/Target/MOS/MOSCombiner.cpp
index bff8495ed..087920a7b 100644
--- a/llvm/lib/Target/MOS/MOSCombiner.cpp
+++ b/llvm/lib/Target/MOS/MOSCombiner.cpp
@@ -443,6 +443,39 @@ void MOSCombinerImpl::applyStoreToMemset(MachineInstr &MI,
   MI.eraseFromParent();
 }
 
+// #321 Increment 1b: returns the G_GLOBAL_VALUE def of `Ptr` iff `Ptr` is a
+// plain near (addrspace 0) global address — the addressing mode the 16-bit
+// LDA/ADC/STA absolute forms can take directly. Null otherwise.
+static MachineInstr *nearAbsGlobalDef(Register Ptr,
+                                      const MachineRegisterInfo &MRI) {
+  using namespace TargetOpcode;
+  if (!Ptr.isVirtual() || MRI.getType(Ptr).getAddressSpace() != 0)
+    return nullptr;
+  MachineInstr *Def = MRI.getVRegDef(Ptr);
+  if (!Def || Def->getOpcode() != G_GLOBAL_VALUE)
+    return nullptr;
+  return Def;
+}
+
+// #321 Increment 1b: a single-use 16-bit plain G_LOAD from a near abs global, or
+// null. (The kind of operand the 16-bit lda/ALU-abs forms can take directly.)
+static GLoad *nearAbsLoad(Register R, const MachineRegisterInfo &MRI) {
+  if (MRI.getType(R) != LLT::scalar(16) || !MRI.hasOneNonDBGUse(R))
+    return nullptr;
+  auto *Ld = dyn_cast_or_null<GLoad>(MRI.getVRegDef(R));
+  if (!Ld || Ld->getOpcode() != TargetOpcode::G_LOAD ||
+      !nearAbsGlobalDef(Ld->getPointerReg(), MRI))
+    return nullptr;
+  return Ld;
+}
+
+// #321 Increment 1b: is this a commutative 16-bit ALU op (operands may be
+// swapped freely)? Subtract is excluded — order-sensitive.
+static bool isCommutativeAlu(unsigned Opc) {
+  using namespace TargetOpcode;
+  return Opc == G_ADD || Opc == G_AND || Opc == G_OR || Opc == G_XOR;
+}
+
 bool MOSCombinerImpl::matchFoldAddE(MachineInstr &MI,
                                     BuildFnTy &MatchInfo) const {
   auto LHS = getIConstantVRegValWithLookThrough(MI.getOperand(2).getReg(), MRI);
diff --git a/llvm/lib/Target/MOS/MOSFeatures.td b/llvm/lib/Target/MOS/MOSFeatures.td
index 76d7409c1..733ef7097 100644
--- a/llvm/lib/Target/MOS/MOSFeatures.td
+++ b/llvm/lib/Target/MOS/MOSFeatures.td
@@ -19,3 +19,19 @@ def FeatureStaticStack :
   SubtargetFeature<
     "static-stack", "StaticStack", "true",
     "Whether to use statically-allocated stack frames if possible.">;
+
+// #321: opt-in 16-bit accumulator mode for the WDC 65816 (REP/SEP-bracketed
+// 16-bit-A codegen). Enable with -mattr=+mos-a16; NOT implied by FamilyW65816,
+// so default mosw65816 (far-pointer) codegen is unaffected.
+def FeatureAccum16 :
+  SubtargetFeature<
+    "mos-a16", "HasAccum16", "true",
+    "16-bit accumulator mode (insert REP/SEP) on the WDC 65816.">;
+
+// #321 xy16: opt-in 16-bit index register mode for the WDC 65816.
+// Implies +mos-a16: soft-stack spill uses TXA→A16→STA (needs M=0 bracket).
+def FeatureIndex16 :
+  SubtargetFeature<
+    "mos-xy16", "HasIndex16", "true",
+    "16-bit index register mode (insert REP/SEP X) on the WDC 65816.",
+    [FeatureAccum16]>;
diff --git a/llvm/lib/Target/MOS/MOSInsertREPSEP.cpp b/llvm/lib/Target/MOS/MOSInsertREPSEP.cpp
new file mode 100644
index 000000000..ffec5eed3
--- /dev/null
+++ b/llvm/lib/Target/MOS/MOSInsertREPSEP.cpp
@@ -0,0 +1,483 @@
+//===-- MOSInsertREPSEP.cpp - Insert 65816 REP/SEP for 16-bit mode --------===//
+//
+// Part of LLVM-MOS, under the Apache License v2.0 with LLVM Exceptions.
+// See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+//
+// #321: a late pass that inserts the 65816's REP/SEP to set the M (accumulator
+// width) and X (index width) status flags where an instruction requires 16-bit
+// mode. Each instruction's required M/X width is carried by the MLow/MHigh/
+// XLow/XHigh TSFlags — the same signals MOSMCELFStreamer reads to emit the
+// $ml/$mh ELF mapping symbols.
+//
+// Increment 2 (cross-block churn minimization): a forward dataflow that
+// propagates the M mode across the CFG and places REP/SEP only at genuine mode
+// *transitions*, hoisting them out of loops. The earlier Increment 1a was
+// per-block and 8-bit-anchored (every block began and ended in 8-bit mode), so a
+// loop with a 16-bit body re-ran `rep … sep` every iteration. Now the mode is
+// held across edges: a 16-bit loop body gets `rep` in the preheader and `sep` in
+// the exit, with no per-iteration toggle.
+//
+// xy16 extension: a parallel X-flag lattice runs alongside the M lattice in the
+// same iteration of the fixpoint. Combined `REP/SEP #$30` is emitted when both
+// M and X switch to the same target mode in one insertSwitch call. The default
+// for most instructions is XW_None (X-agnostic): M16-only ops (LDAbs16,
+// STAbs16, …) do NOT emit surrounding rep/sep #$10 for the X flag.
+//
+// Correctness invariants:
+//   * each instruction runs in the width it requires;
+//   * function entry is 8-bit (ABI), every call/return boundary is 8-bit;
+//   * width-agnostic ops (carry-init, branches) impose no constraint.
+//
+// v1 is conservative: when a mode switch would land on a true critical edge,
+// the whole function falls back to the old per-block 8-bit anchoring.
+//
+// The STZ store-of-zero fusion (Increment 1a) is independent and kept as-is.
+// Gated on FeatureAccum16; a no-op otherwise.
+//
+//===----------------------------------------------------------------------===//
+
+#include "MOSInsertREPSEP.h"
+
+#include "MCTargetDesc/MOSMCTargetDesc.h"
+#include "MOS.h"
+#include "MOSSubtarget.h"
+
+#include "llvm/ADT/DenseMap.h"
+#include "llvm/ADT/SmallPtrSet.h"
+#include "llvm/ADT/SmallVector.h"
+#include "llvm/CodeGen/MachineBasicBlock.h"
+#include "llvm/CodeGen/MachineFunction.h"
+#include "llvm/CodeGen/MachineInstrBuilder.h"
+#include "llvm/CodeGen/TargetInstrInfo.h"
+
+using namespace llvm;
+
+#define DEBUG_TYPE "mos-insert-rep-sep"
+
+// REP/SEP operand bits selecting the M and X status flags.
+static constexpr int64_t MFlagBit = 0x20;
+static constexpr int64_t XFlagBit = 0x10;
+
+// #321 Increment 1b: is this instruction M-width-agnostic — its behavior does not
+// depend on the accumulator (M) width, so it may sit inside a 16-bit run without
+// forcing a SEP back to 8-bit? Conservative: the carry load (LDCImm, i.e. clc/sec)
+// that the ALU selection emits as the carry-in for adc/sbc, plus an LDImm1 that
+// targets only a flag (C/V). Without this, that carry-init landing between `lda`
+// and `adc` splits the REP/SEP bracket. Extend the set as more agnostic ops appear.
+static bool isMWidthAgnostic(const MachineInstr &MI) {
+  switch (MI.getOpcode()) {
+  case MOS::LDCImm: // clc / sec — always defines only the carry flag
+    return true;
+  case MOS::LDImm1:
+    if (MI.getOperand(0).isReg()) {
+      Register D = MI.getOperand(0).getReg();
+      return D == MOS::C || D == MOS::V;
+    }
+    return false;
+  default:
+    return false;
+  }
+}
+
+namespace {
+
+// Accumulator-width lattice. MW_None is the lattice bottom (an unvisited block,
+// or — for a single instruction — "imposes no width requirement"); MW_Conflict is
+// the top (predecessors disagree). The meet of two definite, differing widths is
+// MW_Conflict.
+enum MWidth { MW_None, MW_M8, MW_M16, MW_Conflict };
+
+static MWidth meet(MWidth A, MWidth B) {
+  if (A == MW_None)
+    return B;
+  if (B == MW_None)
+    return A;
+  if (A == B)
+    return A;
+  return MW_Conflict;
+}
+
+// Resolve a lattice value to the concrete width to materialize. M16 stays M16;
+// everything else (None/Conflict/M8) resolves to 8-bit — the always-correct
+// canonical default that matches the ABI entry/boundary state.
+static bool resolveIsM16(MWidth W) { return W == MW_M16; }
+
+// The accumulator width a single instruction requires, or MW_None if it is
+// width-agnostic (carry-init, plain branches/jumps) and so imposes no constraint.
+static MWidth requiredWidth(const MachineInstr &MI) {
+  // ABI: the accumulator is 8-bit across a return and across a call boundary
+  // (the callee assumes 8-bit). Checked before the MLow test — these never carry
+  // MLow, but being explicit documents the invariant.
+  if (MI.isReturn())
+    return MW_M8;
+  if (MI.getDesc().TSFlags & MOS::TSFlagMLow)
+    return MW_M16;
+  if (isMWidthAgnostic(MI))
+    return MW_None;
+  // A plain branch/jump works in either width; the mode it leaves is set by the
+  // CFG dataflow, not by the branch itself. (Returns were handled above.)
+  if (MI.isBranch())
+    return MW_None;
+  if (MI.isCall())
+    return MW_M8;
+  // Every other instruction (ordinary 8-bit ops, our own inserted REP/SEP) runs
+  // 8-bit.
+  return MW_M8;
+}
+
+// ---- X-flag lattice (parallel to M-flag lattice above) ----
+
+// Index-width lattice, parallel to the M lattice.
+enum XWidth { XW_None, XW_X8, XW_X16, XW_Conflict };
+
+static XWidth meet(XWidth A, XWidth B) {
+  if (A == XW_None)
+    return B;
+  if (B == XW_None)
+    return A;
+  if (A == B)
+    return A;
+  return XW_Conflict;
+}
+
+static bool resolveIsX16(XWidth W) { return W == XW_X16; }
+
+// No X-agnostic ops exist yet (no carry-init equivalent for the X flag).
+// Stub retained for symmetry; body returns false so XW_None default covers it.
+static bool isXWidthAgnostic(const MachineInstr &) { return false; }
+
+// The index width a single instruction requires, or XW_None if it is
+// X-width-agnostic (most instructions: M16-only ops, ordinary 8-bit ops, etc.).
+//
+// CRITICAL: most instructions — including LDAbs16, STAbs16, ADCAbs16, … — are
+// completely indifferent to the X flag. The default MUST be XW_None, not XW_X8.
+// Returning XW_X8 by default would force a SEP #$10 after every M16-only op
+// inside an X16 block, defeating 16-bit index mode and generating wrong assembly.
+static XWidth requiredXWidth(const MachineInstr &MI) {
+  // ABI: 8-bit index registers across call/return boundaries.
+  if (MI.isReturn() || MI.isCall())
+    return XW_X8;
+  // XLow=1: our new 16-bit X/Y pseudos (INX16, LDXAbs16, TXA16, TAX16, …).
+  if (MI.getDesc().TSFlags & MOS::TSFlagXLow)
+    return XW_X16;
+  // XHigh=1: CPX_Immediate / CPY_Immediate — "8-bit immediate when X=1".
+  if (MI.getDesc().TSFlags & MOS::TSFlagXHigh)
+    return XW_X8;
+  // Branches are X-agnostic (same treatment as M flag).
+  if (MI.isBranch())
+    return XW_None;
+  (void)isXWidthAgnostic(MI); // stub — always returns false
+  // Everything else — ordinary 8-bit ops, M16-only ops, pseudos — is X-agnostic.
+  // DO NOT return XW_X8 here; that would force X to 8-bit after every LDAbs16.
+  return XW_None;
+}
+
+class MOSInsertREPSEP : public MachineFunctionPass {
+public:
+  static char ID;
+
+  MOSInsertREPSEP() : MachineFunctionPass(ID) {}
+
+  bool runOnMachineFunction(MachineFunction &MF) override;
+
+  StringRef getPassName() const override { return "MOS Insert REP/SEP"; }
+
+private:
+  const TargetInstrInfo *TII = nullptr;
+  bool HasIndex16 = false;
+
+  // Insert REP/SEP instructions at Pos to transition M and/or X to their new
+  // widths. Emits a combined REP/SEP #$30 when both flags switch to the SAME
+  // mode simultaneously; otherwise emits separate instructions per flag.
+  void insertSwitch(MachineBasicBlock &MBB, MachineBasicBlock::iterator Pos,
+                    bool NewM16, bool NewX16, bool MChanged, bool XChanged) {
+    // Combined REP/SEP #$30 when both flags switch to the same mode at once.
+    if (MChanged && XChanged && NewM16 == NewX16) {
+      BuildMI(MBB, Pos, DebugLoc(),
+              TII->get(NewM16 ? MOS::REP_Immediate : MOS::SEP_Immediate))
+          .addImm(MFlagBit | XFlagBit);
+      return;
+    }
+    // Separate instructions for independent or opposite-direction switches.
+    if (MChanged)
+      BuildMI(MBB, Pos, DebugLoc(),
+              TII->get(NewM16 ? MOS::REP_Immediate : MOS::SEP_Immediate))
+          .addImm(MFlagBit);
+    if (XChanged)
+      BuildMI(MBB, Pos, DebugLoc(),
+              TII->get(NewX16 ? MOS::REP_Immediate : MOS::SEP_Immediate))
+          .addImm(XFlagBit);
+  }
+
+  // Seed a block at `EntryIsM16`/`EntryIsX16` and walk it, inserting REP/SEP
+  // only where the next width-requiring instruction disagrees with the running
+  // width. Does NOT force a width at block exit — the running widths flow out.
+  bool placeIntraBlock(MachineBasicBlock &MBB, bool EntryIsM16, bool EntryIsX16);
+
+  // The original Increment-1a behavior: each block begins and ends 8-bit. Always
+  // correct; used as the conservative whole-function fallback.
+  bool placeLegacy(MachineFunction &MF);
+};
+
+} // namespace
+
+bool MOSInsertREPSEP::placeIntraBlock(MachineBasicBlock &MBB,
+                                       bool EntryIsM16, bool EntryIsX16) {
+  bool Changed = false;
+  bool M16 = EntryIsM16, X16 = EntryIsX16;
+  for (MachineInstr &MI : MBB) {
+    MWidth MW = requiredWidth(MI);
+    XWidth XW = HasIndex16 ? requiredXWidth(MI) : XW_None;
+    if (MW == MW_None && XW == XW_None)
+      continue;
+    bool WantM16 = (MW == MW_None) ? M16 : resolveIsM16(MW);
+    bool WantX16 = (XW == XW_None) ? X16 : resolveIsX16(XW);
+    bool MC = (WantM16 != M16), XC = (WantX16 != X16);
+    if (MC || XC) {
+      insertSwitch(MBB, MI, WantM16, WantX16, MC, XC);
+      M16 = WantM16;
+      X16 = WantX16;
+      Changed = true;
+    }
+  }
+  return Changed;
+}
+
+bool MOSInsertREPSEP::placeLegacy(MachineFunction &MF) {
+  bool Changed = false;
+  for (MachineBasicBlock &MBB : MF) {
+    // Determine the final M/X state from the original instructions (before
+    // placeIntraBlock inserts REP/SEP — those would skew a replay).
+    bool EndM16 = false, EndX16 = false;
+    for (MachineInstr &MI : MBB) {
+      MWidth W = requiredWidth(MI);
+      if (W != MW_None)
+        EndM16 = (W == MW_M16);
+      if (HasIndex16) {
+        XWidth XW = requiredXWidth(MI);
+        if (XW != XW_None)
+          EndX16 = (XW == XW_X16);
+      }
+    }
+    // Each block starts 8-bit (both M and X); bracket any 16-bit run, then
+    // restore 8-bit before the terminator so every branch/return runs 8-bit.
+    Changed |= placeIntraBlock(MBB, /*EntryIsM16=*/false, /*EntryIsX16=*/false);
+    if (EndM16 || EndX16) {
+      insertSwitch(MBB, MBB.getFirstTerminator(),
+                   /*NewM16=*/false, /*NewX16=*/false,
+                   /*MChanged=*/EndM16, /*XChanged=*/EndX16);
+      Changed = true;
+    }
+  }
+  return Changed;
+}
+
+bool MOSInsertREPSEP::runOnMachineFunction(MachineFunction &MF) {
+  const MOSSubtarget &STI = MF.getSubtarget<MOSSubtarget>();
+  // Opt-in only: inert unless 16-bit accumulator mode was requested. Default
+  // (6502, plain mosw65816 far-pointer) codegen is untouched.
+  if (!STI.hasAccum16())
+    return false;
+  TII = STI.getInstrInfo();
+  HasIndex16 = STI.hasIndex16();
+
+  bool Changed = false;
+
+  // #321 Increment 1a: fuse a 16-bit store-of-zero. A `uint16_t g = 0` lowers to
+  // two adjacent 8-bit `STZAbs @g` / `STZAbs @g+1`; in 16-bit-A mode a single STZ
+  // writes both bytes, so replace the pair with `REP #$20; STZAbs @g; SEP #$20`
+  // (drop the second). The reusable demonstration of REP/SEP-bracketed 16-bit
+  // codegen — no new instruction form, legalizer, or selector needed.
+  SmallVector<std::pair<MachineInstr *, MachineInstr *>, 4> Fusions;
+  SmallPtrSet<MachineInstr *, 8> Consumed;
+  for (MachineBasicBlock &MBB : MF) {
+    for (MachineInstr &MI : MBB) {
+      if (MI.getOpcode() != MOS::STZAbs || Consumed.count(&MI))
+        continue;
+      MachineInstr *Next = MI.getNextNode();
+      if (!Next || Next->getOpcode() != MOS::STZAbs || Consumed.count(Next))
+        continue;
+      const MachineOperand &Lo = MI.getOperand(0);
+      const MachineOperand &Hi = Next->getOperand(0);
+      if (!Lo.isGlobal() || !Hi.isGlobal() || Lo.getGlobal() != Hi.getGlobal() ||
+          Hi.getOffset() != Lo.getOffset() + 1)
+        continue;
+      Fusions.push_back({&MI, Next});
+      Consumed.insert(&MI);
+      Consumed.insert(Next);
+    }
+  }
+  for (auto &F : Fusions) {
+    MachineInstr *Lo = F.first, *Hi = F.second;
+    MachineBasicBlock &MBB = *Lo->getParent();
+    BuildMI(MBB, *Lo, Lo->getDebugLoc(), TII->get(MOS::REP_Immediate))
+        .addImm(MFlagBit);
+    BuildMI(MBB, *Hi, Lo->getDebugLoc(), TII->get(MOS::SEP_Immediate))
+        .addImm(MFlagBit);
+    Hi->eraseFromParent(); // the kept Lo STZ stores both bytes in 16-bit mode
+    Changed = true;
+  }
+
+  // ---- Cross-block mode propagation (the Increment 2 core) ----
+  //
+  // Step 1 — per-block transfer facts. First[B] / XFirst[B] is the width the
+  // first width-requiring instruction needs (MW_None/XW_None if the block is
+  // "passthrough": it has no width-requiring instruction and merely forwards its
+  // entry width). Last[B] / XLast[B] is the width the last such instruction leaves.
+  DenseMap<MachineBasicBlock *, MWidth> First, Last;
+  DenseMap<MachineBasicBlock *, XWidth> XFirst, XLast;
+  for (MachineBasicBlock &MBB : MF) {
+    MWidth Fst = MW_None, Lst = MW_None;
+    XWidth XFst = XW_None, XLst = XW_None;
+    for (MachineInstr &MI : MBB) {
+      MWidth W = requiredWidth(MI);
+      if (W != MW_None) {
+        if (Fst == MW_None)
+          Fst = W;
+        Lst = W;
+      }
+      if (HasIndex16) {
+        XWidth XW = requiredXWidth(MI);
+        if (XW != XW_None) {
+          if (XFst == XW_None)
+            XFst = XW;
+          XLst = XW;
+        }
+      }
+    }
+    First[&MBB] = Fst;
+    Last[&MBB] = Lst;
+    XFirst[&MBB] = XFst;
+    XLast[&MBB] = XLst;
+  }
+  auto isPassthroughM = [&](MachineBasicBlock *B) { return First[B] == MW_None; };
+  auto isPassthroughX = [&](MachineBasicBlock *B) { return XFirst[B] == XW_None; };
+
+  MachineBasicBlock *Entry = &MF.front();
+
+  // Step 2 — fixpoint for entry modes In[B] / XIn[B]. A non-passthrough block's
+  // entry width is pinned to First[B] / XFirst[B]: the block will start in
+  // exactly the width its first op needs, and entering edges are fixed up to
+  // deliver that width. The entry block is 8-bit by ABI.
+  DenseMap<MachineBasicBlock *, MWidth> In, Out;
+  DenseMap<MachineBasicBlock *, XWidth> XIn, XOut;
+  for (MachineBasicBlock &MBB : MF) {
+    if (&MBB == Entry) {
+      In[&MBB] = MW_M8;
+      XIn[&MBB] = XW_X8; // ABI entry: both flags are 8-bit
+    } else {
+      In[&MBB] = isPassthroughM(&MBB) ? MW_None : First[&MBB];
+      XIn[&MBB] = (HasIndex16 && !isPassthroughX(&MBB)) ? XFirst[&MBB] : XW_None;
+    }
+  }
+  auto outOfM = [&](MachineBasicBlock *B) -> MWidth {
+    return isPassthroughM(B) ? In[B] : Last[B];
+  };
+  auto outOfX = [&](MachineBasicBlock *B) -> XWidth {
+    return isPassthroughX(B) ? XIn[B] : XLast[B];
+  };
+  bool Stable = false;
+  while (!Stable) {
+    Stable = true;
+    for (MachineBasicBlock &MBB : MF) {
+      if (&MBB == Entry)
+        continue;
+      if (isPassthroughM(&MBB)) {
+        MWidth NewIn = MW_None;
+        for (MachineBasicBlock *Pred : MBB.predecessors())
+          NewIn = meet(NewIn, outOfM(Pred));
+        if (NewIn != In[&MBB]) {
+          In[&MBB] = NewIn;
+          Stable = false;
+        }
+      }
+      if (HasIndex16 && isPassthroughX(&MBB)) {
+        XWidth NewXIn = XW_None;
+        for (MachineBasicBlock *Pred : MBB.predecessors())
+          NewXIn = meet(NewXIn, outOfX(Pred));
+        if (NewXIn != XIn[&MBB]) {
+          XIn[&MBB] = NewXIn;
+          Stable = false;
+        }
+      }
+    }
+  }
+  for (MachineBasicBlock &MBB : MF) {
+    Out[&MBB] = outOfM(&MBB);
+    XOut[&MBB] = outOfX(&MBB);
+  }
+
+  // Step 3a — decide where each mode transition lands, and bail the whole
+  // function to the legacy anchoring if any switch would fall on a true critical
+  // edge (source has >1 successor AND target has >1 predecessor). v1 does not
+  // split critical edges.
+  struct Placement {
+    MachineBasicBlock *MBB;
+    bool AtEnd;
+    bool MChanged = false, ToM16 = false;
+    bool XChanged = false, ToX16 = false;
+  };
+  SmallVector<Placement, 8> Placements;
+  bool Bail = false;
+  for (MachineBasicBlock &B : MF) {
+    if (&B == Entry)
+      continue; // entry has no CFG predecessor edges; its In=M8/X8 is the ABI state
+    const bool NeedM16 = resolveIsM16(In[&B]);
+    const bool NeedX16 = HasIndex16 ? resolveIsX16(XIn[&B]) : false;
+    for (MachineBasicBlock *P : B.predecessors()) {
+      bool M_diff = resolveIsM16(Out[P]) != NeedM16;
+      bool X_diff = HasIndex16 && (resolveIsX16(XOut[P]) != NeedX16);
+      if (!M_diff && !X_diff)
+        continue; // edge already delivers the right widths
+      MachineBasicBlock *PlaceMBB;
+      bool AtEnd;
+      if (P->succ_size() == 1) {
+        PlaceMBB = P;
+        AtEnd = true;
+      } else if (B.pred_size() == 1) {
+        PlaceMBB = &B;
+        AtEnd = false;
+      } else {
+        Bail = true; // critical edge — conservative whole-function fallback
+        break;
+      }
+      Placements.push_back({PlaceMBB, AtEnd,
+                            M_diff, NeedM16, X_diff, NeedX16});
+    }
+    if (Bail)
+      break;
+  }
+
+  if (Bail) {
+    Changed |= placeLegacy(MF);
+    return Changed;
+  }
+
+  // Step 3b — intra-block placement, each block seeded with its entry widths.
+  for (MachineBasicBlock &MBB : MF)
+    Changed |= placeIntraBlock(MBB,
+                               resolveIsM16(In[&MBB]),
+                               resolveIsX16(XIn[&MBB]));
+
+  // Step 3c — materialize the edge transitions.
+  for (const Placement &Pl : Placements) {
+    MachineBasicBlock::iterator Pos =
+        Pl.AtEnd ? Pl.MBB->getFirstTerminator() : Pl.MBB->begin();
+    insertSwitch(*Pl.MBB, Pos,
+                 Pl.ToM16, Pl.ToX16, Pl.MChanged, Pl.XChanged);
+    Changed = true;
+  }
+
+  return Changed;
+}
+
+char MOSInsertREPSEP::ID = 0;
+
+INITIALIZE_PASS(MOSInsertREPSEP, DEBUG_TYPE, "MOS Insert REP/SEP", false, false)
+
+MachineFunctionPass *llvm::createMOSInsertREPSEPPass() {
+  return new MOSInsertREPSEP;
+}
diff --git a/llvm/lib/Target/MOS/MOSInsertREPSEP.h b/llvm/lib/Target/MOS/MOSInsertREPSEP.h
new file mode 100644
index 000000000..34f7e16f3
--- /dev/null
+++ b/llvm/lib/Target/MOS/MOSInsertREPSEP.h
@@ -0,0 +1,25 @@
+//===-- MOSInsertREPSEP.h - Insert 65816 REP/SEP for 16-bit mode -*- C++ -*-===//
+//
+// Part of LLVM-MOS, under the Apache License v2.0 with LLVM Exceptions.
+// See https://llvm.org/LICENSE.txt for license information.
+// SPDX-License-Identifier: Apache-2.0 WITH LLVM-exception
+//
+//===----------------------------------------------------------------------===//
+//
+// This file declares the MOS REP/SEP insertion pass (#321 — 16-bit register
+// mode for the WDC 65816).
+//
+//===----------------------------------------------------------------------===//
+
+#ifndef LLVM_LIB_TARGET_MOS_MOSINSERTREPSEP_H
+#define LLVM_LIB_TARGET_MOS_MOSINSERTREPSEP_H
+
+#include "llvm/CodeGen/MachineFunctionPass.h"
+
+namespace llvm {
+
+MachineFunctionPass *createMOSInsertREPSEPPass();
+
+} // namespace llvm
+
+#endif // LLVM_LIB_TARGET_MOS_MOSINSERTREPSEP_H
diff --git a/llvm/lib/Target/MOS/MOSInstrFormats.td b/llvm/lib/Target/MOS/MOSInstrFormats.td
index 12d05bed4..0e37cac4c 100644
--- a/llvm/lib/Target/MOS/MOSInstrFormats.td
+++ b/llvm/lib/Target/MOS/MOSInstrFormats.td
@@ -57,6 +57,21 @@ def HasW65816 : Predicate<"Subtarget->hasW65816()">,
   let PredicateName = "FeatureW65816";
 }
 
+// #321 Increment 1b: opt-in 16-bit-accumulator codegen (FeatureAccum16 / +mos-a16).
+// The SubtargetFeature defines the hasAccum16() accessor; this Predicate exposes it
+// to tablegen `let Predicates = [HasAccum16]` so the 16-bit logical ops / pseudos
+// are only available when the feature is on.
+def HasAccum16 : Predicate<"Subtarget->hasAccum16()">,
+                AssemblerPredicate<(all_of FeatureAccum16), "Accum16"> {
+  let PredicateName = "FeatureAccum16";
+}
+
+// #321 xy16: opt-in 16-bit-index-register codegen (FeatureIndex16 / +mos-xy16).
+def HasIndex16 : Predicate<"Subtarget->hasIndex16()">,
+                AssemblerPredicate<(all_of FeatureIndex16), "mos-xy16"> {
+  let PredicateName = "FeatureIndex16";
+}
+
 def Has65EL02 : Predicate<"Subtarget->has65EL02()">,
                 AssemblerPredicate<(all_of Feature65EL02), "65EL02"> {
   let PredicateName = "Feature65EL02";
diff --git a/llvm/lib/Target/MOS/MOSInstrGISel.td b/llvm/lib/Target/MOS/MOSInstrGISel.td
index e81487e9c..f5527d89a 100644
--- a/llvm/lib/Target/MOS/MOSInstrGISel.td
+++ b/llvm/lib/Target/MOS/MOSInstrGISel.td
@@ -68,6 +68,17 @@ def G_LOAD_ABS : MOSGenericInstruction {
   let mayLoad = true;
 }
 
+// #321 native s16: a 16-bit absolute load `lda abs` in 16-bit-accumulator mode —
+// loads a 16-bit value from a global/static (absolute) address. The legalizer emits
+// this for an s16 G_LOAD whose address is absolute; selectMem16Abs lowers it to
+// LDAbs16 -> STAImag16 (one rep/sep). Mirror of G_LOAD_ABS for 16-bit data.
+def G_LOAD16_ABS : MOSGenericInstruction {
+  let Predicates = [HasAccum16];
+  let OutOperandList = (outs type0:$dst);
+  let InOperandList = (ins unknown:$addr);
+  let mayLoad = true;
+}
+
 // #320: Generalized 8-bit load using 65816 absolute-long (24-bit) addressing,
 // for far (addrspace 2) pointers. The address argument is a global value or a
 // 24-bit immediate. Selects to LDA AbsoluteLong (accumulator only). Additionally
@@ -98,6 +109,17 @@ def G_LOAD_INDIR : MOSGenericInstruction {
   let mayLoad = true;
 }
 
+// #321 native s16: a 16-bit indirect load `lda (zp)` in 16-bit-accumulator mode —
+// loads a 16-bit value through a runtime pointer (the plain-indirect leaf, no
+// index). The legalizer emits this for an s16 G_LOAD whose address is a non-absolute
+// 16-bit pointer; the selector lowers it to LDAIndir16 -> STAImag16 (one rep/sep).
+def G_LOAD16_INDIR : MOSGenericInstruction {
+  let Predicates = [HasAccum16];
+  let OutOperandList = (outs type0:$dst);
+  let InOperandList = (ins ptype1:$base);
+  let mayLoad = true;
+}
+
 // Generalized 8-bit load using the indirect indexed addressing mode. The index
 // argument is an 8-bit scalar. Additionally expects a MachineMemOperand.
 def G_LOAD_INDIR_IDX : MOSGenericInstruction {
@@ -125,6 +147,16 @@ def G_STORE_ABS : MOSGenericInstruction {
   let mayStore = true;
 }
 
+// #321 native s16: a 16-bit absolute store `sta abs` in 16-bit-accumulator mode.
+// Mirror of G_STORE_ABS; selectMem16Abs lowers it to LDAImag16 (val -> A16) ->
+// STAbs16 (one rep/sep).
+def G_STORE16_ABS : MOSGenericInstruction {
+  let Predicates = [HasAccum16];
+  let OutOperandList = (outs);
+  let InOperandList = (ins type0:$src, unknown:$addr);
+  let mayStore = true;
+}
+
 // #320: Generalized 8-bit store using 65816 absolute-long (24-bit) addressing,
 // for far (addrspace 2) pointers. The address argument is a global value or a
 // 24-bit immediate. Selects to STA AbsoluteLong (accumulator only). Additionally
@@ -155,6 +187,50 @@ def G_STORE_INDIR : MOSGenericInstruction {
   let mayStore = true;
 }
 
+// #321 native s16: a 16-bit indirect store `sta (zp)` in 16-bit-accumulator mode —
+// stores a 16-bit value through a runtime pointer. Mirror of G_LOAD16_INDIR; the
+// selector lowers it to LDAImag16 (value -> A16) -> STAIndir16 (one rep/sep).
+def G_STORE16_INDIR : MOSGenericInstruction {
+  let Predicates = [HasAccum16];
+  let OutOperandList = (outs);
+  let InOperandList = (ins type0:$src, ptype1:$base);
+  let mayStore = true;
+}
+
+// #321 native s16: 16-bit abs,x load/store `lda abs,x` / `sta abs,x` in M=0 mode.
+// Mirrors G_LOAD_ABS_IDX / G_STORE_ABS_IDX for 16-bit data; the legalizer emits
+// these when the address is G_PTR_ADD(global/const, 8-bit-index). The selector
+// lowers them to LDAbsIdx16/STAbsIdx16 (one rep/sep bracket via MLow=1).
+def G_LOAD16_ABS_IDX : MOSGenericInstruction {
+  let Predicates = [HasAccum16];
+  let OutOperandList = (outs type0:$dst);
+  let InOperandList = (ins unknown:$base, type1:$index);
+  let mayLoad = true;
+}
+def G_STORE16_ABS_IDX : MOSGenericInstruction {
+  let Predicates = [HasAccum16];
+  let OutOperandList = (outs);
+  let InOperandList = (ins type0:$src, unknown:$base, type1:$index);
+  let mayStore = true;
+}
+
+// #321 native s16: 16-bit (zp),y load/store in M=0 mode. Mirrors G_LOAD_INDIR_IDX /
+// G_STORE_INDIR_IDX for 16-bit data; the legalizer emits these when the address is
+// G_PTR_ADD(runtime_ptr, 8-bit-index). The selector lowers them to LDIndirIdx16 /
+// STIndirIdx16 (one rep/sep bracket via MLow=1).
+def G_LOAD16_INDIR_IDX : MOSGenericInstruction {
+  let Predicates = [HasAccum16];
+  let OutOperandList = (outs type0:$dst);
+  let InOperandList = (ins ptype1:$base, type1:$index);
+  let mayLoad = true;
+}
+def G_STORE16_INDIR_IDX : MOSGenericInstruction {
+  let Predicates = [HasAccum16];
+  let OutOperandList = (outs);
+  let InOperandList = (ins type0:$src, ptype1:$base, type1:$index);
+  let mayStore = true;
+}
+
 // Generalized 8-bit store using the indirect indexed addressing mode. The index
 // argument is an 8-bit scalar. Additionally expects a MachineMemOperand.
 def G_STORE_INDIR_IDX : MOSGenericInstruction {
diff --git a/llvm/lib/Target/MOS/MOSInstrInfo.cpp b/llvm/lib/Target/MOS/MOSInstrInfo.cpp
index 42a94c356..12c9fdea5 100644
--- a/llvm/lib/Target/MOS/MOSInstrInfo.cpp
+++ b/llvm/lib/Target/MOS/MOSInstrInfo.cpp
@@ -352,6 +352,11 @@ MOSInstrInfo::getBranchDestBlock(const MachineInstr &MI) const {
   case MOS::CmpBrAbsIdx:
   case MOS::CmpBrIndir:
   case MOS::CmpBrIndirIdx:
+  case MOS::CmpBrImag16:
+  case MOS::CmpBrImm16:
+  case MOS::CmpBrAbsAbs16:
+  case MOS::CmpBrAbsImm16:
+  case MOS::CmpBrImagAbs16:
     return MI.getOperand(0).getMBB();
   case MOS::JMPIndir:
   case MOS::JMPIdxIndir:
@@ -388,11 +393,13 @@ bool MOSInstrInfo::analyzeBranch(MachineBasicBlock &MBB,
     return true;
   if (FirstBR->isConditionalBranch()) {
     if (!FirstBR->memoperands_empty()) {
-      assert(FirstBR->hasOneMemOperand() &&
-             "CmpBr should have at most one mem operand");
+      // Most CmpBr forms have one mem operand; the #321 v3 16-bit abs-fold
+      // CmpBrAbsAbs16 carries two (the folded `lda abs` + `cmp abs` globals).
       // Don't futz with volatile compare and branches; the compare part has to
       // happen, and we can't lose the MMO that says the compare is volatile.
-      if ((*FirstBR->memoperands_begin())->isVolatile())
+      if (any_of(FirstBR->memoperands(), [](const MachineMemOperand *MMO) {
+            return MMO->isVolatile();
+          }))
         return true;
     }
 
@@ -928,7 +935,24 @@ void MOSInstrInfo::loadStoreRegStackSlot(
       Instr.addDef(Ptr, RegState::EarlyClobber);
     Instr.addFrameIndex(FrameIndex).addImm(0).addMemOperand(MMO);
   } else {
-    if ((Reg.isPhysical() && MOS::Imag16RegClass.contains(Reg)) ||
+    if ((Reg.isPhysical() && MOS::Ac16RegClass.contains(Reg)) ||
+        (Reg.isVirtual() &&
+         MRI.getRegClass(Reg)->hasSuperClassEq(&MOS::Ac16RegClass))) {
+      // #321: spill/reload the 16-bit accumulator A16 with a single 16-bit
+      // load/store (LDAbs16/STAbs16) — NEVER a COPY through an 8-bit GPR. The
+      // generic single-byte fall-through below would emit `GPR = COPY A16`,
+      // which copyPhysRegImpl lowers via its Anyi1 branch to `SelectImm $a16`
+      // ($a16 is not a Flag register) → invalid MIR / link-time segfault (the
+      // F3 fuzzer crash; seeds 1/7/9/11/22/35/41/44). This keeps the native-s16
+      // invariant that A16 is only ever entered/left via a 16-bit load/store
+      // (MOSInstrLogical.td). Ac16 exists only under +mos-a16, so the
+      // HasAccum16-gated LDAbs16/STAbs16 are always legal here; InsertREPSEP
+      // (the final pass) wraps the MLow=1 op in its own rep/sep #$20.
+      Builder.buildInstr(IsLoad ? MOS::LDAbs16 : MOS::STAbs16)
+          .addReg(Reg, getDefRegState(IsLoad) | getKillRegState(IsKill))
+          .addFrameIndex(FrameIndex)
+          .addMemOperand(MMO);
+    } else if ((Reg.isPhysical() && MOS::Imag16RegClass.contains(Reg)) ||
         (Reg.isVirtual() &&
          MRI.getRegClass(Reg)->hasSuperClassEq(&MOS::Imag16RegClass))) {
       MachineOperand Lo = MachineOperand::CreateReg(Reg, IsLoad);
@@ -960,7 +984,30 @@ void MOSInstrInfo::loadStoreRegStackSlot(
                                    MF.getMachineMemOperand(MMO, 1, 1));
       if (IsLoad && Tmp != Reg)
         Builder.buildCopy(Reg, Tmp);
+    } else if ((Reg.isPhysical() && MOS::Xc16RegClass.contains(Reg)) ||
+               (Reg.isVirtual() &&
+                MRI.getRegClass(Reg)->hasSuperClassEq(&MOS::Xc16RegClass))) {
+      // #321 xy16: spill/reload X16 with a single 16-bit abs load/store
+      // (LDXAbs16/STXAbs16), like Ac16 with LDAbs16/STAbs16. InsertREPSEP
+      // wraps the XLow=1 op in its own rep/sep #$10.
+      Builder.buildInstr(IsLoad ? MOS::LDXAbs16 : MOS::STXAbs16)
+          .addReg(Reg, getDefRegState(IsLoad) | getKillRegState(IsKill))
+          .addFrameIndex(FrameIndex)
+          .addMemOperand(MMO);
+    } else if ((Reg.isPhysical() && MOS::Yc16RegClass.contains(Reg)) ||
+               (Reg.isVirtual() &&
+                MRI.getRegClass(Reg)->hasSuperClassEq(&MOS::Yc16RegClass))) {
+      // #321 xy16: spill/reload Y16 with a single 16-bit abs load/store.
+      Builder.buildInstr(IsLoad ? MOS::LDYAbs16 : MOS::STYAbs16)
+          .addReg(Reg, getDefRegState(IsLoad) | getKillRegState(IsKill))
+          .addFrameIndex(FrameIndex)
+          .addMemOperand(MMO);
     } else {
+      // #321 SPILL CONTRACT (static-stack mirror of MOSRegisterInfo::expandLDSTStk):
+      // this single-byte fall-through is correct ONLY for 8-bit classes. Every
+      // spillable >=16-bit register class needs its OWN case above. Handled:
+      //   Ac16  -> LDAbs16/STAbs16; Imag16 -> two byte spills;
+      //   Xc16  -> LDXAbs16/STXAbs16; Yc16 -> LDYAbs16/STYAbs16.
       loadStoreByteStaticStackSlot(
           Builder, MachineOperand::CreateReg(Reg, IsLoad), FrameIndex, 0, MMO);
     }
@@ -1035,6 +1082,13 @@ bool MOSInstrInfo::expandPostRAPseudo(MachineInstr &MI) const {
   case MOS::CmpBrIndirIdx:
     expandCmpBr(Builder);
     break;
+  case MOS::CmpBrImag16:
+  case MOS::CmpBrImm16:
+  case MOS::CmpBrAbsAbs16:
+  case MOS::CmpBrAbsImm16:
+  case MOS::CmpBrImagAbs16:
+    expandCmpBr16(Builder);
+    break;
 
   // Control flow
   case MOS::GBR:
@@ -1355,6 +1409,63 @@ void MOSInstrInfo::expandCmpBr(MachineIRBuilder &Builder) const {
   MI.eraseFromParent();
 }
 
+// #321 native s16 equality: expand a fused 16-bit compare-branch to
+// `LDA{Imag16,bs16} $l; CMP{Imag16,Abs16,Imm16} $r; BR`. The LDA/CMP are MLow=1
+// logical pseudos, so the late REP/SEP pass brackets them with rep/sep; the cmp
+// sets Z (modeled as an implicit def), and the inserted `sep` doesn't touch Z, so
+// the BR reads it correctly. Operands: (0)=tgt (1)=Flag=Z (2)=flag_val (3)=$l
+// (4)=$r. The LHS ($l) is Imag16 (CmpBrImag16/Imm16) or a near-abs GLOBAL read by
+// `lda abs` (CmpBrAbsAbs16, the v3 fold); the RHS ($r) is Imag16, an abs global
+// (`cmp abs`), or a 16-bit immediate. The two folded loads' memrefs ride on the
+// pseudo and are cloned onto the abs lda/cmp.
+void MOSInstrInfo::expandCmpBr16(MachineIRBuilder &Builder) const {
+  MachineInstr &MI = *Builder.getInsertPt();
+  const Register Flag = MI.getOperand(1).getReg();
+  const unsigned Op = MI.getOpcode();
+
+  const bool LhsAbs = Op == MOS::CmpBrAbsAbs16 || Op == MOS::CmpBrAbsImm16;
+  unsigned CmpOpc;
+  switch (Op) {
+  case MOS::CmpBrImm16:
+  case MOS::CmpBrAbsImm16:
+    CmpOpc = MOS::CMPImm16;
+    break;
+  case MOS::CmpBrAbsAbs16:
+    CmpOpc = MOS::CMPAbs16;
+    break;
+  case MOS::CmpBrImagAbs16: // LHS is Imag16 (LhsAbs=false -> LDAImag16); RHS is abs global
+    CmpOpc = MOS::CMPAbs16;
+    break;
+  default: // CmpBrImag16
+    CmpOpc = MOS::CMPImag16;
+    break;
+  }
+
+  // Post-RA: use the physical 16-bit accumulator A16 (the pseudo declares it in
+  // Defs so regalloc kept nothing live across it). lda $l (16-bit) -> A16.
+  auto LDA = Builder.buildInstr(LhsAbs ? MOS::LDAbs16 : MOS::LDAImag16)
+                 .addDef(MOS::A16)
+                 .add(MI.getOperand(3));
+  if (LhsAbs)
+    LDA.cloneMemRefs(MI);
+
+  // cmp $r (16-bit): defs C (dead) and Z (the live flag the branch reads).
+  auto CMP = Builder.buildInstr(CmpOpc)
+                 .addDef(MOS::C, RegState::Dead)
+                 .addUse(MOS::A16)
+                 .add(MI.getOperand(4));
+  if (CmpOpc == MOS::CMPAbs16)
+    CMP.cloneMemRefs(MI);
+  CMP.addDef(Flag, RegState::Implicit);
+
+  Builder.buildInstr(MOS::BR)
+      .add(MI.getOperand(0))
+      .addUse(Flag, RegState::Kill)
+      .add(MI.getOperand(2));
+
+  MI.eraseFromParent();
+}
+
 bool MOSInstrInfo::reverseBranchCondition(
     SmallVectorImpl<MachineOperand> &Cond) const {
   // Condition includes all arguments except the branch target.
diff --git a/llvm/lib/Target/MOS/MOSInstrInfo.h b/llvm/lib/Target/MOS/MOSInstrInfo.h
index be5daff8f..c39c4be70 100644
--- a/llvm/lib/Target/MOS/MOSInstrInfo.h
+++ b/llvm/lib/Target/MOS/MOSInstrInfo.h
@@ -146,6 +146,7 @@ private:
   // Control flow pseudos
   void expandGBR(MachineIRBuilder &Builder) const;
   void expandCmpBr(MachineIRBuilder &Builder) const;
+  void expandCmpBr16(MachineIRBuilder &Builder) const;
 
   bool shouldRematPhysRegCopy() const override { return false; }
 };
diff --git a/llvm/lib/Target/MOS/MOSInstrLogical.td b/llvm/lib/Target/MOS/MOSInstrLogical.td
index f3eb9549f..e78b208b7 100644
--- a/llvm/lib/Target/MOS/MOSInstrLogical.td
+++ b/llvm/lib/Target/MOS/MOSInstrLogical.td
@@ -65,6 +65,23 @@ class MOSLogicalInstr : Instruction {
   let isPseudo = true;
   let isCodeGenOnly = true;
   let isReMaterializable = true;
+
+  // #321 Increment 1b: the M (accumulator width) TSFlag, mirroring the MC `Inst`
+  // base (MOSInstrFormats.td) at the same bit position (TSFlagMLow = bit 0). The
+  // MOSInsertREPSEP pass runs at addPreEmitPass — before MCInst expansion — so it
+  // reads this flag off the *logical* instruction to bracket 16-bit-accumulator
+  // ops with REP/SEP. Defaults to 0 (8-bit); set to 1 on the 16-bit forms.
+  bit MLow = 0;
+  let TSFlags{0} = MLow;
+
+  // #321 xy16: the X (index width) TSFlag, mirroring the MC `Inst` base at the
+  // same bit positions (TSFlagXLow = bit 2, TSFlagXHigh = bit 3). Defaults to 0
+  // (X-width agnostic). XLow=1 means the instruction requires X=16-bit; XHigh=1
+  // means it requires X=8-bit (used for CPX_Immediate / CPY_Immediate forms).
+  bit XLow = 0;
+  bit XHigh = 0;
+  let TSFlags{2} = XLow;
+  let TSFlags{3} = XHigh;
 }
 
 //===---------------------------------------------------------------------===//
@@ -577,6 +594,300 @@ def STAbsLong : MOSStore, PseudoInstExpansion<(STA_AbsoluteLong addr24:$dst)> {
   dag InOperandList = (ins Ac:$src, addr24:$dst);
 }
 
+//===---------------------------------------------------------------------===//
+// #321 Increment 1b: 16-bit-accumulator load/add/store (native M=0 mode)
+//===---------------------------------------------------------------------===//
+//
+// These three logical ops carry a 16-bit value through the A16 accumulator
+// (class Ac16, which holds only A16 = B:A). The MC opcodes are width-agnostic —
+// the same LDA/ADC/STA absolute bytes the M flag governs at runtime — so they
+// PseudoInstExpand to the plain _Absolute forms. MLow=1 tags them so the
+// MOSInsertREPSEP pass wraps the run in one `rep #$20 … sep #$20`. Gated on
+// HasAccum16 (+mos-a16); only ever produced by selectAlu16Native, so the
+// default 8-bit codegen never sees them.
+
+// LDA abs (16-bit) — load a 16-bit absolute global into A16.
+def LDAbs16 : MOSLoad, PseudoInstExpansion<(LDA_Absolute addr16:$src)> {
+  let Predicates = [HasAccum16];
+  let MLow = 1;
+  dag OutOperandList = (outs Ac16:$dst);
+  dag InOperandList = (ins addr16:$src);
+}
+
+// ADC abs (16-bit) — A16 = A16 + [abs] + carry. Mirrors the 8-bit MOSAddSub
+// shape (carry in/out + overflow), but on Ac16 and reading absolute memory.
+def ADCAbs16 : MOSLogicalInstr, PseudoInstExpansion<(ADC_Absolute addr16:$r)> {
+  let Predicates = [HasAccum16];
+  let MLow = 1;
+  let mayLoad = true;
+  let isAdd = true;
+  let Constraints = "$dst = $l";
+  dag OutOperandList = (outs Ac16:$dst, Cc:$carryout, Vc:$vout);
+  dag InOperandList = (ins Ac16:$l, addr16:$r, Cc:$carryin);
+}
+
+// SBC abs (16-bit) — A16 = A16 - [abs] - !carry. Mirrors ADCAbs16; carry-in is
+// SET (sec) for a plain subtract. Non-commutative: $l is the minuend.
+def SBCAbs16 : MOSLogicalInstr, PseudoInstExpansion<(SBC_Absolute addr16:$r)> {
+  let Predicates = [HasAccum16];
+  let MLow = 1;
+  let mayLoad = true;
+  let Constraints = "$dst = $l";
+  dag OutOperandList = (outs Ac16:$dst, Cc:$carryout, Vc:$vout);
+  dag InOperandList = (ins Ac16:$l, addr16:$r, Cc:$carryin);
+}
+
+// Bitwise abs (16-bit): A16 = A16 {& | ^} [abs]. No carry/overflow — simpler than
+// the ADC/SBC shape. MLow=1 so the run is REP/SEP-bracketed.
+class MOSBitAbs16<Instruction real> : MOSLogicalInstr,
+    PseudoInstExpansion<(real addr16:$r)> {
+  let Predicates = [HasAccum16];
+  let MLow = 1;
+  let mayLoad = true;
+  let Constraints = "$dst = $l";
+  dag OutOperandList = (outs Ac16:$dst);
+  dag InOperandList = (ins Ac16:$l, addr16:$r);
+}
+def ANDAbs16 : MOSBitAbs16<AND_Absolute>;
+def ORAAbs16 : MOSBitAbs16<ORA_Absolute>;
+def EORAbs16 : MOSBitAbs16<EOR_Absolute>;
+
+// Immediate-operand 16-bit forms: A16 = A16 OP #imm16. Same accumulator shape as
+// the *Abs16 ops but the second operand is a 16-bit immediate (no memory). Used
+// for `g = a OP constant` (the combiner routes one constant operand here).
+def ADCImm16 : MOSLogicalInstr, PseudoInstExpansion<(ADC_Immediate16 imm16:$r)> {
+  let Predicates = [HasAccum16];
+  let MLow = 1;
+  let isAdd = true;
+  let Constraints = "$dst = $l";
+  dag OutOperandList = (outs Ac16:$dst, Cc:$carryout, Vc:$vout);
+  dag InOperandList = (ins Ac16:$l, imm16:$r, Cc:$carryin);
+}
+class MOSBitImm16<Instruction real> : MOSLogicalInstr,
+    PseudoInstExpansion<(real imm16:$r)> {
+  let Predicates = [HasAccum16];
+  let MLow = 1;
+  let Constraints = "$dst = $l";
+  dag OutOperandList = (outs Ac16:$dst);
+  dag InOperandList = (ins Ac16:$l, imm16:$r);
+}
+def ANDImm16 : MOSBitImm16<AND_Immediate16>;
+def ORAImm16 : MOSBitImm16<ORA_Immediate16>;
+def EORImm16 : MOSBitImm16<EOR_Immediate16>;
+
+// STA abs (16-bit) — store A16 to a 16-bit absolute global.
+def STAbs16 : MOSStore, PseudoInstExpansion<(STA_Absolute addr16:$dst)> {
+  let Predicates = [HasAccum16];
+  let MLow = 1;
+  dag InOperandList = (ins Ac16:$src, addr16:$dst);
+}
+
+// #321 Increment 1e: abs,x and (zp),y indexed 16-bit load/store (M=0).
+// Mirrors the 8-bit LDAAbsIdx/STAbsIdx and LDIndirIdx/STIndirIdx, but on Ac16
+// with MLow=1 so MOSInsertREPSEP wraps the op in one rep #$20 … sep #$20.
+
+// LDA abs,x (16-bit) — load 16-bit from absolute_base + X into A16.
+def LDAbsIdx16 : MOSLoad, PseudoInstExpansion<(LDA_AbsoluteX addr16:$addr)> {
+  let Predicates = [HasAccum16];
+  let MLow = 1;
+  dag OutOperandList = (outs Ac16:$dst);
+  dag InOperandList = (ins addr16:$addr, Xc:$idx);
+}
+
+// STA abs,x (16-bit) — store A16 to absolute_base + X.
+def STAbsIdx16 : MOSStore, PseudoInstExpansion<(STA_AbsoluteX addr16:$addr)> {
+  let Predicates = [HasAccum16];
+  let MLow = 1;
+  dag InOperandList = (ins Ac16:$src, addr16:$addr, Xc:$idx);
+}
+
+// LDA (zp),y (16-bit) — load 16-bit via ZP-resident pointer + Y offset into A16.
+// Follows the same pattern as 8-bit LDIndirIdx: $addr maps to the Imag16 pair;
+// Yc:$offset is an extra constraint operand not referenced in the expansion (LLVM
+// preserves it as an extra use for RA).
+def LDIndirIdx16 : MOSLoad, PseudoInstExpansion<(LDA_IndirectIndexed addr8:$addr)> {
+  let Predicates = [HasAccum16];
+  let MLow = 1;
+  dag OutOperandList = (outs Ac16:$dst);
+  dag InOperandList = (ins Imag16:$addr, Yc:$offset);
+}
+
+// STA (zp),y (16-bit) — store A16 via ZP-resident pointer + Y offset.
+def STIndirIdx16 : MOSStore, PseudoInstExpansion<(STA_IndirectIndexed addr8:$addr)> {
+  let Predicates = [HasAccum16];
+  let MLow = 1;
+  dag InOperandList = (ins Ac16:$src, Imag16:$addr, Yc:$offset);
+}
+
+// #321 Increment 1d-retry: the zero-page (Imag16) analogs of the *Abs16 ops.
+// Where *Abs16 read/write an absolute global, these read/write a 16-bit value
+// resident in an Imag16 zero-page pair — so the GISel pipeline can carry an s16
+// *value* (locals, multi-use intermediates) in Imag16 and the selector emits a
+// self-contained `lda zp; (clc;) adc zp; sta zp` sequence on the transient A16.
+// The Imag16 operand lowers to its LOW-byte __rcN symbol (MOSRegisterInfo.cpp
+// sublo rule); the 16-bit width comes from the surrounding rep #$20 (MLow=1).
+// CRITICAL invariant: the accumulator is entered/left ONLY via these load/store
+// instructions — never a COPY between Ac16 and an 8-bit reg — so the register
+// coalescer can never merge an 8-bit value into A16 (the crash that sank the
+// first 1d prototype). All gated on HasAccum16, inert in default codegen.
+
+// LDA zp (16-bit) — load a 16-bit Imag16 pair into A16.
+def LDAImag16 : MOSLoad, PseudoInstExpansion<(LDA_ZeroPage addr8:$src)> {
+  let Predicates = [HasAccum16];
+  let MLow = 1;
+  dag OutOperandList = (outs Ac16:$dst);
+  dag InOperandList = (ins Imag16:$src);
+}
+
+// ADC zp (16-bit) — A16 = A16 + [Imag16] + carry. Mirrors ADCAbs16.
+def ADCImag16 : MOSLogicalInstr, PseudoInstExpansion<(ADC_ZeroPage addr8:$r)> {
+  let Predicates = [HasAccum16];
+  let MLow = 1;
+  let mayLoad = true;
+  let isAdd = true;
+  let Constraints = "$dst = $l";
+  dag OutOperandList = (outs Ac16:$dst, Cc:$carryout, Vc:$vout);
+  dag InOperandList = (ins Ac16:$l, Imag16:$r, Cc:$carryin);
+}
+
+// SBC zp (16-bit) — A16 = A16 - [Imag16] - !carry. Mirrors SBCAbs16.
+def SBCImag16 : MOSLogicalInstr, PseudoInstExpansion<(SBC_ZeroPage addr8:$r)> {
+  let Predicates = [HasAccum16];
+  let MLow = 1;
+  let mayLoad = true;
+  let Constraints = "$dst = $l";
+  dag OutOperandList = (outs Ac16:$dst, Cc:$carryout, Vc:$vout);
+  dag InOperandList = (ins Ac16:$l, Imag16:$r, Cc:$carryin);
+}
+
+// Bitwise zp (16-bit): A16 = A16 {& | ^} [Imag16]. Mirrors MOSBitAbs16.
+class MOSBitImag16<Instruction real> : MOSLogicalInstr,
+    PseudoInstExpansion<(real addr8:$r)> {
+  let Predicates = [HasAccum16];
+  let MLow = 1;
+  let mayLoad = true;
+  let Constraints = "$dst = $l";
+  dag OutOperandList = (outs Ac16:$dst);
+  dag InOperandList = (ins Ac16:$l, Imag16:$r);
+}
+def ANDImag16 : MOSBitImag16<AND_ZeroPage>;
+def ORAImag16 : MOSBitImag16<ORA_ZeroPage>;
+def EORImag16 : MOSBitImag16<EOR_ZeroPage>;
+
+// STA zp (16-bit) — store A16 into a 16-bit Imag16 pair. Like STImag8, writing a
+// zero-page "register" DEFS it (the Imag16 is an OUT operand), so the s16 add
+// result is a real vreg the G_UNMERGE consumers read. Crucially the value enters
+// the accumulator only via LDAImag16/ADCImag16 and leaves only via this store —
+// never a COPY between Ac16 and an 8-bit reg (the 1d-prototype coalescer crash).
+def STAImag16 : MOSLogicalInstr, PseudoInstExpansion<(STA_ZeroPage addr8:$dst)> {
+  let Predicates = [HasAccum16];
+  let MLow = 1;
+  dag OutOperandList = (outs Imag16:$dst);
+  dag InOperandList = (ins Ac16:$src);
+}
+
+// #321 native s16 indirect memory: LDA (zp) / STA (zp) in 16-bit-accumulator mode —
+// load/store a 16-bit value through a runtime pointer held in a zero-page Imag16
+// pair. Mirror the 8-bit LDIndir/STIndir (LDA_Indirect=$B2 / STA_Indirect=$92,
+// 65C02+; the w65816 has them) with MLow=1 so MOSInsertREPSEP brackets the access.
+// The value enters/leaves A16 only via these / the Imag16 load-store — no Ac16<->
+// 8-bit COPY. Used by selectMem16Indir for *p, a[i], a[i]=v, p[i].
+def LDAIndir16 : MOSLoad, PseudoInstExpansion<(LDA_Indirect addr8:$addr)> {
+  let Predicates = [HasAccum16];
+  let MLow = 1;
+  dag OutOperandList = (outs Ac16:$dst);
+  dag InOperandList = (ins Imag16:$addr);
+}
+def STAIndir16 : MOSStore, PseudoInstExpansion<(STA_Indirect addr8:$addr)> {
+  let Predicates = [HasAccum16];
+  let MLow = 1;
+  dag InOperandList = (ins Ac16:$src, Imag16:$addr);
+}
+
+// #321 native 16-bit compares: A16 - operand, discard the result, keep the flags
+// (Cc = carry; N/Z set implicitly). MLow=1 so MOSInsertREPSEP brackets `lda; cmp`
+// in one rep/sep run; SEP doesn't touch C/Z/N, so a following branch reads them.
+// Used by selectSbc16 for the unsigned-ordering (C-flag) s16 comparisons. There is
+// no CMP_Immediate16 opcode, so a constant RHS is materialized into Imag16 and
+// compared via CMPImag16 (correct, just not folded).
+class MOSCMP16 : MOSCMP {
+  let Predicates = [HasAccum16];
+  let MLow = 1;
+  let mayLoad = true;
+}
+def CMPImag16 : MOSCMP16, PseudoInstExpansion<(CMP_ZeroPage addr8:$r)> {
+  dag InOperandList = (ins Ac16:$l, Imag16:$r);
+}
+def CMPAbs16 : MOSCMP16, PseudoInstExpansion<(CMP_Absolute addr16:$r)> {
+  dag InOperandList = (ins Ac16:$l, addr16:$r);
+}
+// cmp #imm16 — CMP_Immediate16 is generated by the CC1_All multiclass (like
+// ADC_Immediate16), so a constant RHS folds to `cmp #$1234` (common loop bounds)
+// instead of materializing the constant into an Imag16 pair. No mayLoad.
+def CMPImm16 : MOSCMP, PseudoInstExpansion<(CMP_Immediate16 imm16:$r)> {
+  let Predicates = [HasAccum16];
+  let MLow = 1;
+  dag InOperandList = (ins Ac16:$l, imm16:$r);
+}
+
+// #321 native 16-bit constant shifts: in 16-bit-accumulator mode a single `asl a`
+// / `lsr a` shifts all 16 bits at once, so `x << k` / `x >> k` (k a compile-time
+// constant) selects to one `lda; (asl|lsr)*k; sta` run instead of the 8-bit
+// asl/rol byte-pair chain. MLow=1 so MOSInsertREPSEP brackets the run; the value
+// enters A16 only via LDAImag16 and leaves only via STAImag16 — never a COPY
+// between Ac16 and an 8-bit reg (the 1d-prototype coalescer crash). No carry
+// operand in v1: each shift self-fills 0 and consecutive shifts don't thread the
+// carry. Used by selectShift16Native. ASL covers `<<`; LSR covers unsigned `>>`;
+// signed `>>` (ASHR, needs ror+sign), rotates, and the memory forms are follow-ups.
+class MOSShiftAcc16<Instruction real> : MOSLogicalInstr,
+    PseudoInstExpansion<(real)> {
+  let Predicates = [HasAccum16];
+  let MLow = 1;
+  let Constraints = "$dst = $src";
+  dag OutOperandList = (outs Ac16:$dst);
+  dag InOperandList = (ins Ac16:$src);
+}
+// asl a / lsr a CLOBBER the carry (shifted-out bit), even though selectShift16Native
+// leaves the carry-out dead. Model that with Defs=[C] so the scheduler/regalloc never
+// places one inside a live-carry interval — e.g. between a `cmp` and the `bcs` that
+// reads its carry (a CRC `if (crc & 0x8000) ... crc <<= 1` hoists the common shift
+// across the test; without this the asl corrupts the branch carry -> miscompile, found
+// by the Tier-1 differential fuzzer). RORAcc16 models its carry as an operand already;
+// INCAcc16/DECAcc16 correctly do NOT clobber C (inc/dec a set only N/Z), so they stay.
+def ASLAcc16 : MOSShiftAcc16<ASL_Accumulator> { let Defs = [C]; }
+def LSRAcc16 : MOSShiftAcc16<LSR_Accumulator> { let Defs = [C]; }
+
+// #321 native s16 inc/dec: in 16-bit-accumulator mode one `inc a` / `dec a` steps all
+// 16 bits, so a 16-bit `x + 1` / `x - 1` selects to a single accumulator inc/dec
+// (1 byte, no carry-init) instead of `(clc;) adc #$0001` — and far better than the
+// 8-bit byte inc/dec-with-carry chain the default path emits. Same accumulator-RMW
+// shape as ASLAcc16 (MLow=1, $dst=$src, no extra operands); inc a / dec a set N/Z but
+// not C/V, which is correct for a plain ±1 (G_ADD/G_SUB carry out is unused here). The
+// value enters A16 only via LDAImag16/LDAbs16 and leaves only via STAImag16 (no Ac16<->
+// 8-bit COPY). Used by selectAlu16Native (register ±1 and global ±1 via lda/inc-a/sta).
+def INCAcc16 : MOSShiftAcc16<INC_Accumulator>;
+def DECAcc16 : MOSShiftAcc16<DEC_Accumulator>;
+// NB: a memory-RMW `inc abs`/`dec abs` for `g = g ± 1` is deliberately NOT provided.
+// The 65816 has no `inc long`, and `inc abs` is DBR-relative — but this platform
+// addresses all data via 24-bit long (DBR-independent) loads/stores, so a DBR-relative
+// RMW would couple correctness to DBR==0 + bank-0 LoRAM placement (it only happens to
+// work today via the low-8KB WRAM mirror). selectAlu16Native uses `lda long; inc/dec a;
+// sta long` instead — the same long addressing, just dropping the clc + #imm.
+
+// #321 native s16 arithmetic right shift (signed `>>`): the 65816 has no native
+// ASR, but in 16-bit mode `cmp #$8000` sets carry = the sign bit (A >= 0x8000) and
+// `ror a` rotates that carry back into bit 15 while shifting right — so the sign
+// replicates. selectShift16Native emits `cmp #$8000; ror a` per bit. Unlike ASL/LSR
+// this rotate has a real carry-in (the sign from the cmp); its carry-out (old bit 0)
+// is dead (the next bit's cmp recomputes C). MLow=1 so the whole run stays bracketed.
+def RORAcc16 : MOSLogicalInstr, PseudoInstExpansion<(ROR_Accumulator)> {
+  let Predicates = [HasAccum16];
+  let MLow = 1;
+  let Constraints = "$dst = $src";
+  dag OutOperandList = (outs Ac16:$dst, Cc:$carryout);
+  dag InOperandList = (ins Ac16:$src, Cc:$carryin);
+}
+
 let Predicates = [Has65C02] in {
   // STZ abs
   def STZAbs : MOSStore, PseudoInstExpansion<(STZ_ZeroPage addr8:$addr)> {
@@ -639,3 +950,134 @@ def : Pat<(i8 (trunc Imag16:$s)), (EXTRACT_SUBREG Imag16:$s, sublo)>;
 
 def : Pat<(i1 imm:$s), (LDImm1 imm:$s)>;
 def : Pat<(i8 imm:$s), (LDImm imm:$s)>;
+
+//===---------------------------------------------------------------------===//
+// #321 xy16: 16-bit X/Y index-register instructions (+mos-xy16)
+//===---------------------------------------------------------------------===//
+//
+// Each pseudo carries XLow=1 so MOSInsertREPSEP knows X must be 16-bit.
+// TXA16/TAX16/TYA16/TAY16 additionally carry MLow=1 because they run with
+// both M and X in native-16-bit mode (TXA copies X→A, needs A16 not A8).
+// Transfer pseudos are needed for the soft-stack spill path (Xc16 ↔ Ac16).
+
+// ---- X register loads ----
+def LDXAbs16 : MOSLoad, PseudoInstExpansion<(LDX_Absolute addr16:$src)> {
+  let Predicates = [HasIndex16]; let XLow = 1;
+  dag OutOperandList = (outs Xc16:$dst); dag InOperandList = (ins addr16:$src);
+}
+def LDXImag16 : MOSLoad, PseudoInstExpansion<(LDX_ZeroPage addr8:$src)> {
+  let Predicates = [HasIndex16]; let XLow = 1;
+  dag OutOperandList = (outs Xc16:$dst); dag InOperandList = (ins Imag16:$src);
+}
+def LDXImm16 : MOSLoad, PseudoInstExpansion<(LDX_Immediate16 imm16:$src)> {
+  let Predicates = [HasIndex16]; let XLow = 1;
+  dag OutOperandList = (outs Xc16:$dst); dag InOperandList = (ins imm16:$src);
+}
+
+// ---- X register stores ----
+def STXAbs16 : MOSStore, PseudoInstExpansion<(STX_Absolute addr16:$dst)> {
+  let Predicates = [HasIndex16]; let XLow = 1;
+  dag InOperandList = (ins Xc16:$src, addr16:$dst);
+}
+def STXImag16 : MOSLogicalInstr, PseudoInstExpansion<(STX_ZeroPage addr8:$dst)> {
+  let Predicates = [HasIndex16]; let XLow = 1; let mayStore = true;
+  dag OutOperandList = (outs Imag16:$dst); dag InOperandList = (ins Xc16:$src);
+}
+
+// ---- X register compare ----
+def CPXAbs16 : MOSLogicalInstr, PseudoInstExpansion<(CPX_Absolute addr16:$r)> {
+  let Predicates = [HasIndex16]; let XLow = 1;
+  let mayLoad = true; let isCompare = true;
+  dag OutOperandList = (outs Cc:$carry); dag InOperandList = (ins Xc16:$l, addr16:$r);
+}
+def CPXImag16 : MOSLogicalInstr, PseudoInstExpansion<(CPX_ZeroPage addr8:$r)> {
+  let Predicates = [HasIndex16]; let XLow = 1;
+  let mayLoad = true; let isCompare = true;
+  dag OutOperandList = (outs Cc:$carry); dag InOperandList = (ins Xc16:$l, Imag16:$r);
+}
+def CPXImm16 : MOSLogicalInstr, PseudoInstExpansion<(CPX_Immediate16 imm16:$r)> {
+  let Predicates = [HasIndex16]; let XLow = 1;
+  let isCompare = true;
+  dag OutOperandList = (outs Cc:$carry); dag InOperandList = (ins Xc16:$l, imm16:$r);
+}
+
+// ---- X register inc/dec (accumulator RMW, like INCAcc16) ----
+def INX16 : MOSLogicalInstr, PseudoInstExpansion<(INX_Implied)> {
+  let Predicates = [HasIndex16]; let XLow = 1; let Constraints = "$dst = $src";
+  dag OutOperandList = (outs Xc16:$dst); dag InOperandList = (ins Xc16:$src);
+}
+def DEX16 : MOSLogicalInstr, PseudoInstExpansion<(DEX_Implied)> {
+  let Predicates = [HasIndex16]; let XLow = 1; let Constraints = "$dst = $src";
+  dag OutOperandList = (outs Xc16:$dst); dag InOperandList = (ins Xc16:$src);
+}
+
+// ---- Y register loads ----
+def LDYAbs16 : MOSLoad, PseudoInstExpansion<(LDY_Absolute addr16:$src)> {
+  let Predicates = [HasIndex16]; let XLow = 1;
+  dag OutOperandList = (outs Yc16:$dst); dag InOperandList = (ins addr16:$src);
+}
+def LDYImag16 : MOSLoad, PseudoInstExpansion<(LDY_ZeroPage addr8:$src)> {
+  let Predicates = [HasIndex16]; let XLow = 1;
+  dag OutOperandList = (outs Yc16:$dst); dag InOperandList = (ins Imag16:$src);
+}
+def LDYImm16 : MOSLoad, PseudoInstExpansion<(LDY_Immediate16 imm16:$src)> {
+  let Predicates = [HasIndex16]; let XLow = 1;
+  dag OutOperandList = (outs Yc16:$dst); dag InOperandList = (ins imm16:$src);
+}
+
+// ---- Y register stores ----
+def STYAbs16 : MOSStore, PseudoInstExpansion<(STY_Absolute addr16:$dst)> {
+  let Predicates = [HasIndex16]; let XLow = 1;
+  dag InOperandList = (ins Yc16:$src, addr16:$dst);
+}
+def STYImag16 : MOSLogicalInstr, PseudoInstExpansion<(STY_ZeroPage addr8:$dst)> {
+  let Predicates = [HasIndex16]; let XLow = 1; let mayStore = true;
+  dag OutOperandList = (outs Imag16:$dst); dag InOperandList = (ins Yc16:$src);
+}
+
+// ---- Y register compare ----
+def CPYAbs16 : MOSLogicalInstr, PseudoInstExpansion<(CPY_Absolute addr16:$r)> {
+  let Predicates = [HasIndex16]; let XLow = 1;
+  let mayLoad = true; let isCompare = true;
+  dag OutOperandList = (outs Cc:$carry); dag InOperandList = (ins Yc16:$l, addr16:$r);
+}
+def CPYImag16 : MOSLogicalInstr, PseudoInstExpansion<(CPY_ZeroPage addr8:$r)> {
+  let Predicates = [HasIndex16]; let XLow = 1;
+  let mayLoad = true; let isCompare = true;
+  dag OutOperandList = (outs Cc:$carry); dag InOperandList = (ins Yc16:$l, Imag16:$r);
+}
+def CPYImm16 : MOSLogicalInstr, PseudoInstExpansion<(CPY_Immediate16 imm16:$r)> {
+  let Predicates = [HasIndex16]; let XLow = 1;
+  let isCompare = true;
+  dag OutOperandList = (outs Cc:$carry); dag InOperandList = (ins Yc16:$l, imm16:$r);
+}
+
+// ---- Y register inc/dec ----
+def INY16 : MOSLogicalInstr, PseudoInstExpansion<(INY_Implied)> {
+  let Predicates = [HasIndex16]; let XLow = 1; let Constraints = "$dst = $src";
+  dag OutOperandList = (outs Yc16:$dst); dag InOperandList = (ins Yc16:$src);
+}
+def DEY16 : MOSLogicalInstr, PseudoInstExpansion<(DEY_Implied)> {
+  let Predicates = [HasIndex16]; let XLow = 1; let Constraints = "$dst = $src";
+  dag OutOperandList = (outs Yc16:$dst); dag InOperandList = (ins Yc16:$src);
+}
+
+// ---- 16-bit transfer pseudos for soft-stack spill (Xc16 ↔ Ac16 via TXA/TAX) ----
+// Both MLow=1 and XLow=1: these run with M=0 and X=0. The A16 clobber is encoded
+// in the explicit Ac16 operands; no additional implicit annotations needed.
+def TXA16 : MOSLogicalInstr, PseudoInstExpansion<(TXA_Implied)> {
+  let Predicates = [HasIndex16]; let MLow = 1; let XLow = 1;
+  dag OutOperandList = (outs Ac16:$dst); dag InOperandList = (ins Xc16:$src);
+}
+def TAX16 : MOSLogicalInstr, PseudoInstExpansion<(TAX_Implied)> {
+  let Predicates = [HasIndex16]; let MLow = 1; let XLow = 1;
+  dag OutOperandList = (outs Xc16:$dst); dag InOperandList = (ins Ac16:$src);
+}
+def TYA16 : MOSLogicalInstr, PseudoInstExpansion<(TYA_Implied)> {
+  let Predicates = [HasIndex16]; let MLow = 1; let XLow = 1;
+  dag OutOperandList = (outs Ac16:$dst); dag InOperandList = (ins Yc16:$src);
+}
+def TAY16 : MOSLogicalInstr, PseudoInstExpansion<(TAY_Implied)> {
+  let Predicates = [HasIndex16]; let MLow = 1; let XLow = 1;
+  dag OutOperandList = (outs Yc16:$dst); dag InOperandList = (ins Ac16:$src);
+}
diff --git a/llvm/lib/Target/MOS/MOSInstrPseudos.td b/llvm/lib/Target/MOS/MOSInstrPseudos.td
index 21910cfea..e3fece8b0 100644
--- a/llvm/lib/Target/MOS/MOSInstrPseudos.td
+++ b/llvm/lib/Target/MOS/MOSInstrPseudos.td
@@ -359,4 +359,52 @@ let mayLoad = true in {
   def CmpBrIndirIdx : MOSCmpBr {
     dag InOperandList = (ins label:$tgt, Flag:$flag, i1imm:$flag_val, Ac:$l, Imag16:$addr, Yc:$idx);
   }
+  // #321 native s16 equality (== / !=): a fused 16-bit compare-and-branch. The LHS
+  // is an Imag16 pair (loaded into A16 at expansion, unlike the 8-bit forms whose
+  // LHS is already in a GPR); the RHS is an Imag16 pair or a 16-bit immediate.
+  // expandCmpBr lowers these to `LDAImag16; CMPImag16/CMPImm16` (both MLow=1, so the
+  // REP/SEP pass brackets the lda/cmp) + BR reading Z (sep doesn't touch Z).
+  // Defs A16 + NZ: the expansion loads $l into the 16-bit accumulator and the cmp
+  // sets N/Z, so regalloc must treat both as clobbered across the pseudo (C is in
+  // the MOSCmpBr base).
+  def CmpBrImag16 : MOSCmpBr {
+    let Predicates = [HasAccum16];
+    let Defs = [C, A16, NZ];
+    dag InOperandList = (ins label:$tgt, Flag:$flag, i1imm:$flag_val, Imag16:$l, Imag16:$r);
+  }
+  def CmpBrImm16 : MOSCmpBr {
+    let Predicates = [HasAccum16];
+    let Defs = [C, A16, NZ];
+    dag InOperandList = (ins label:$tgt, Flag:$flag, i1imm:$flag_val, Imag16:$l, i16imm:$r);
+  }
+  // #321 v3 — abs-operand fold for the native s16 equality compare-branch (`g1 == g2`):
+  // BOTH operands are near-abs GLOBALS read in place — `lda abs $l` (LDAbs16) + `cmp abs
+  // $r` (CMPAbs16) — instead of round-tripping each global through an Imag16 pair.
+  // expandCmpBr16 lowers this to `LDAbs16 $l; CMPAbs16 $r; BR`; the two folded loads'
+  // memrefs ride on the pseudo (cloned onto the lda/cmp at expansion). Mirrors
+  // selectSbc16's a16abscmp fold on the ordering path. Defs/Predicates as the
+  // Imag16/Imm16 forms above. (`g1 == 0x1234` is a deferred follow-up — see the v3 plan.)
+  def CmpBrAbsAbs16 : MOSCmpBr {
+    let Predicates = [HasAccum16];
+    let Defs = [C, A16, NZ];
+    dag InOperandList = (ins label:$tgt, Flag:$flag, i1imm:$flag_val, addr16:$l, addr16:$r);
+  }
+  // #321 `g == 0x1234`: near-abs GLOBAL LHS read by `lda abs` (LDAbs16), 16-bit
+  // immediate RHS compared by `cmp #imm` (CMPImm16). expandCmpBr16 -> `LDAbs16 $l;
+  // CMPImm16 $r; BR`. Reachable once the EQ matcher recovers a byte-split constant
+  // (getI16Const) — before that the constant materialized and this never fired.
+  def CmpBrAbsImm16 : MOSCmpBr {
+    let Predicates = [HasAccum16];
+    let Defs = [C, A16, NZ];
+    dag InOperandList = (ins label:$tgt, Flag:$flag, i1imm:$flag_val, addr16:$l, i16imm:$r);
+  }
+  // #321 task7 computed-vs-global: Imag16-resident LHS (computed/indirect value in zp) vs near-abs
+  // GLOBAL RHS — `lda zp_l` (LDAImag16, LhsAbs=false) + `cmp abs_r` (CMPAbs16). The global's
+  // memref rides on this pseudo and is cloned to the CMP by expandCmpBr16. Fills the mixed gap
+  // between CmpBrImag16 (both-Imag16) and CmpBrAbsAbs16 (both-global).
+  def CmpBrImagAbs16 : MOSCmpBr {
+    let Predicates = [HasAccum16];
+    let Defs = [C, A16, NZ];
+    dag InOperandList = (ins label:$tgt, Flag:$flag, i1imm:$flag_val, Imag16:$l, addr16:$r);
+  }
 }
diff --git a/llvm/lib/Target/MOS/MOSInstructionSelector.cpp b/llvm/lib/Target/MOS/MOSInstructionSelector.cpp
index cbdd54ab2..18b0a096e 100644
--- a/llvm/lib/Target/MOS/MOSInstructionSelector.cpp
+++ b/llvm/lib/Target/MOS/MOSInstructionSelector.cpp
@@ -83,6 +83,9 @@ private:
   // Post-tablegen selection functions. If these return false, it is an error.
   bool selectBrCondImm(MachineInstr &MI);
   bool selectSbc(MachineInstr &MI);
+  // #321 native 16-bit compares: select an s16 G_SBC (compare form) to one
+  // rep/lda/cmp/sep producing the carry flag, for unsigned-ordering branches.
+  bool selectSbc16(MachineInstr &MI);
   bool selectFrameIndex(MachineInstr &MI);
   std::pair<Register, Register> selectFrameIndexLoHi(MachineInstr &MI);
   bool selectAddr(MachineInstr &MI);
@@ -96,6 +99,36 @@ private:
   bool selectIncDecMB(MachineInstr &MI);
   bool selectUnMergeValues(MachineInstr &MI);
   bool selectBrIndirect(MachineInstr &MI);
+  // #321 Increment 1d-retry: select a native s16 ALU op (G_ADD/G_SUB/G_AND/G_OR/
+  // G_XOR) whose value lives in an Imag16 zero-page pair (locals/multi-use, not
+  // just the all-global peephole).
+  bool selectAlu16Native(MachineInstr &MI);
+  // #321 xy16: G_* ops assigned to Xc16/Yc16; skeleton for M2 legalizer work.
+  bool selectXY16(MachineInstr &MI);
+  // #321 native s16 constant shift (G_SHL / unsigned G_LSHR, amount in [1,7]):
+  // `lda; (asl|lsr)*k; sta` on the Imag16 value, bracketed by one rep/sep.
+  bool selectShift16Native(MachineInstr &MI);
+  // #321 native s16 indirect load/store (G_LOAD16_INDIR / G_STORE16_INDIR):
+  // lda (zp)/sta (zp) in M=0 through a runtime pointer, one rep/sep.
+  bool selectMem16Indir(MachineInstr &MI);
+  // #321 native s16 absolute load/store (G_LOAD16_ABS / G_STORE16_ABS):
+  // lda abs/sta abs in M=0 to a global/static address, one rep/sep.
+  bool selectMem16Abs(MachineInstr &MI);
+  // #321 Increment 1e: 16-bit abs,x indexed load/store (G_LOAD16_ABS_IDX /
+  // G_STORE16_ABS_IDX): lda abs,x / sta abs,x in M=0, one rep/sep.
+  bool selectMem16AbsIdx(MachineInstr &MI);
+  // #321 Increment 1e: 16-bit (zp),y indexed load/store (G_LOAD16_INDIR_IDX /
+  // G_STORE16_INDIR_IDX): lda (zp),y / sta (zp),y in M=0, one rep/sep.
+  bool selectMem16IndirIdx(MachineInstr &MI);
+  // #321 native s16: bring a 16-bit store's value into A16, folding a single-use
+  // G_LOAD16_ABS/INDIR (an indirect/mixed copy) directly into the accumulator load
+  // when safe (shouldFoldMemAccess) — else LDAImag16 from the value's Imag16. The
+  // emitted instr is appended to New; the folded load (to erase) is returned in
+  // FoldedLoad. Used by the store paths of selectMem16Indir/selectMem16Abs.
+  Register loadStoreValueIntoA16(MachineIRBuilder &Builder, MachineInstr &StoreMI,
+                                 Register Value,
+                                 SmallVectorImpl<MachineInstrBuilder> &New,
+                                 MachineInstr *&FoldedLoad);
 
   // Select instructions that correspond 1:1 to a target instruction.
   bool selectGeneric(MachineInstr &MI);
@@ -195,17 +228,54 @@ bool MOSInstructionSelector::select(MachineInstr &MI) {
   switch (MI.getOpcode()) {
   case MOS::G_ADD:
   case MOS::G_SUB:
+    // #321 Increment 1d-retry: a native s16 add/sub (left un-narrowed by the
+    // legalizer under +mos-a16) carries its value in an Imag16 pair.
+    if (STI.hasAccum16() &&
+        MI.getMF()->getRegInfo().getType(MI.getOperand(0).getReg()) ==
+            LLT::scalar(16)) {
+      if (selectAlu16Native(MI))
+        return true;
+      break;
+    }
     if (selectAddSub(MI))
       return true;
     break;
   case MOS::G_AND:
   case MOS::G_OR:
   case MOS::G_XOR:
+    // #321 Increment 1d-retry: native s16 bitwise lives in an Imag16 pair too.
+    if (STI.hasAccum16() &&
+        MI.getMF()->getRegInfo().getType(MI.getOperand(0).getReg()) ==
+            LLT::scalar(16)) {
+      if (selectAlu16Native(MI))
+        return true;
+      break;
+    }
     if (selectLogical(MI))
       return true;
     break;
+  case MOS::G_SHL:
+  case MOS::G_LSHR:
+  case MOS::G_ASHR:
+    // #321 native s16 constant shift: the legalizer leaves a small s16 left /
+    // unsigned-right / signed-right shift un-narrowed under +mos-a16; select it to
+    // one `lda; <shift>*k; sta` run on the Imag16 value (asl / lsr / cmp+ror).
+    if (STI.hasAccum16() &&
+        MI.getMF()->getRegInfo().getType(MI.getOperand(0).getReg()) ==
+            LLT::scalar(16)) {
+      if (selectShift16Native(MI))
+        return true;
+      break;
+    }
+    break;
   }
 
+  // #321 xy16: route G_* ops assigned to Xc16/Yc16 here. Currently a skeleton
+  // that returns false for everything; will be filled in during M2 legalizer
+  // integration (index-register operand widening / load-address forms).
+  if (STI.hasIndex16() && selectXY16(MI))
+    return true;
+
   if (selectImpl(MI, *CoverageInfo))
     return true;
 
@@ -247,6 +317,23 @@ bool MOSInstructionSelector::select(MachineInstr &MI) {
   case MOS::G_BRINDIRECT:
     return selectBrIndirect(MI);
 
+  case MOS::G_LOAD16_INDIR:
+  case MOS::G_STORE16_INDIR:
+    return selectMem16Indir(MI);
+
+  case MOS::G_LOAD16_ABS:
+  case MOS::G_STORE16_ABS:
+    return selectMem16Abs(MI);
+
+  case MOS::G_LOAD16_ABS_IDX:
+  case MOS::G_STORE16_ABS_IDX:
+    return selectMem16AbsIdx(MI);
+
+  case MOS::G_LOAD16_INDIR_IDX:
+  case MOS::G_STORE16_INDIR_IDX:
+    return selectMem16IndirIdx(MI);
+
+
   case MOS::G_IMPLICIT_DEF:
   case MOS::G_LOAD_ZP_IDX:
   case MOS::G_LOAD_ABS:
@@ -762,6 +849,28 @@ static Register getSbcFlagForRegister(const MachineInstr &Sbc, Register Reg) {
   llvm_unreachable("Could not find register in G_SBC outputs.");
 }
 
+// #321 native s16: recover a 16-bit constant whether it survived as a G_CONSTANT or was
+// byte-split into G_MERGE_VALUES(i8 lo, i8 hi) during legalization — how a 16-bit
+// constant operand of a native EQ/ALU op reaches selection under +mos-a16 (a
+// G_CONSTANT i16 is illegal and is narrowed to bytes). Two non-constant byte loads
+// merged together yield nullopt, so a `g == h` (both variables) is not mistaken for an
+// immediate compare. Shared by the EQ matcher (CmpNZImm16_match) and getImm16Operand.
+static std::optional<int64_t> getI16Const(Register R,
+                                          const MachineRegisterInfo &MRI) {
+  if (auto C = getIConstantVRegValWithLookThrough(R, MRI))
+    return C->Value.getZExtValue() & 0xFFFF;
+  MachineInstr *Def = getDefIgnoringCopies(R, MRI);
+  if (Def && Def->getOpcode() == TargetOpcode::G_MERGE_VALUES &&
+      Def->getNumOperands() == 3) {
+    auto Lo = getIConstantVRegValWithLookThrough(Def->getOperand(1).getReg(), MRI);
+    auto Hi = getIConstantVRegValWithLookThrough(Def->getOperand(2).getReg(), MRI);
+    if (Lo && Hi)
+      return ((Hi->Value.getZExtValue() & 0xFF) << 8) |
+             (Lo->Value.getZExtValue() & 0xFF);
+  }
+  return std::nullopt;
+}
+
 // Match criteria common to all Cmp where N or Z are used.
 struct CmpNZ_match {
   Register &LHS;
@@ -777,6 +886,11 @@ struct CmpNZ_match {
     CondMI = DefSrcReg->MI;
     if (CondMI->getOpcode() != MOS::G_SBC)
       return false;
+    // #321 native s16: a 16-bit G_SBC is the native equality form (handled by the
+    // s16 matchers + selectBrCondImm's CmpBrImag16/Imm16). Keep the 8-bit CmpNZ
+    // matchers from ever folding a 16-bit G_SBC into an 8-bit compare-branch.
+    if (MRI.getType(CondMI->getOperand(0).getReg()) != LLT::scalar(8))
+      return false;
 
     auto CInConst =
         getIConstantVRegValWithLookThrough(CondMI->getOperand(7).getReg(), MRI);
@@ -789,6 +903,70 @@ struct CmpNZ_match {
   }
 };
 
+// #321 native s16 equality: match a 16-bit G_SBC whose live flag is Z (the ==/!=
+// form left un-narrowed by legalizeICmp when the result feeds a branch). LHS/RHS are
+// the s16 operands (5/6); selectBrCondImm emits a CmpBrImag16/Imm16 fused branch.
+struct CmpNZ16_match {
+  Register &LHS;
+  Register &Flag;
+  MachineInstr *CondMI = nullptr;
+
+  CmpNZ16_match(Register &LHS, Register &Flag) : LHS(LHS), Flag(Flag) {}
+
+  bool match(const MachineRegisterInfo &MRI, Register CondReg) {
+    auto DefSrcReg = getDefSrcRegIgnoringCopies(CondReg, MRI);
+    CondMI = DefSrcReg->MI;
+    if (CondMI->getOpcode() != MOS::G_SBC)
+      return false;
+    if (MRI.getType(CondMI->getOperand(0).getReg()) != LLT::scalar(16))
+      return false;
+    auto CInConst =
+        getIConstantVRegValWithLookThrough(CondMI->getOperand(7).getReg(), MRI);
+    if (!CInConst || CInConst->Value.isZero())
+      return false;
+    LHS = CondMI->getOperand(5).getReg();
+    Flag = getSbcFlagForRegister(*CondMI, DefSrcReg->Reg);
+    return Flag == MOS::Z; // only equality (Z); native s16 ordering uses C, not NZ
+  }
+};
+
+struct CmpNZImm16_match : public CmpNZ16_match {
+  int64_t &RHS;
+  CmpNZImm16_match(Register &LHS, int64_t &RHS, Register &Flag)
+      : CmpNZ16_match(LHS, Flag), RHS(RHS) {}
+  bool match(const MachineRegisterInfo &MRI, Register CondReg) {
+    if (!CmpNZ16_match::match(MRI, CondReg))
+      return false;
+    // Recover the constant even when legalization byte-split it into G_MERGE_VALUES
+    // (a G_CONSTANT i16 is illegal under +mos-a16) — without this `g == 0x1234` and
+    // `*p == 0x1234` fall to CmpBrImag16 and materialize the constant into Imag16.
+    auto RHSConst = getI16Const(CondMI->getOperand(6).getReg(), MRI);
+    if (!RHSConst)
+      return false;
+    RHS = *RHSConst & 0xFFFF;
+    return true;
+  }
+};
+inline CmpNZImm16_match m_CmpNZImm16(Register &LHS, int64_t &RHS, Register &Flag) {
+  return {LHS, RHS, Flag};
+}
+
+struct CmpNZImag16_match : public CmpNZ16_match {
+  Register &RHS;
+  CmpNZImag16_match(Register &LHS, Register &RHS, Register &Flag)
+      : CmpNZ16_match(LHS, Flag), RHS(RHS) {}
+  bool match(const MachineRegisterInfo &MRI, Register CondReg) {
+    if (!CmpNZ16_match::match(MRI, CondReg))
+      return false;
+    RHS = CondMI->getOperand(6).getReg();
+    return true;
+  }
+};
+inline CmpNZImag16_match m_CmpNZImag16(Register &LHS, Register &RHS,
+                                       Register &Flag) {
+  return {LHS, RHS, Flag};
+}
+
 struct CmpNZZero_match : public CmpNZ_match {
   CmpNZZero_match(Register &LHS, Register &Flag) : CmpNZ_match(LHS, Flag) {}
 
@@ -968,6 +1146,11 @@ inline CmpNZIndirIdx_match m_CmpNZIndirIdx(Register &LHS, Register &Addr,
   return {LHS, Addr, Idx, Flag, Load, AA};
 }
 
+// Defined later (beside selectSbc16): the defining single-use near-abs G_LOAD16_ABS
+// of R in User's block, else null — the #321 v3 EQ abs-fold operand.
+static MachineInstr *foldableAbsLoad16(Register R, const MachineInstr &User,
+                                       const MachineRegisterInfo &MRI);
+
 bool MOSInstructionSelector::selectBrCondImm(MachineInstr &MI) {
   MachineRegisterInfo &MRI = MI.getMF()->getRegInfo();
 
@@ -991,6 +1174,89 @@ bool MOSInstructionSelector::selectBrCondImm(MachineInstr &MI) {
     MI.eraseFromParent();
     return true;
   }
+  // #321 native s16 equality: a 16-bit G_SBC (Z) feeding this branch -> a fused
+  // 16-bit compare-branch. Matched first (type-discriminated) so the 8-bit CmpNZ
+  // cases below never see a 16-bit G_SBC. expandCmpBr lowers these to
+  // LDAImag16; CMPImag16/CMPImm16 (MLow=1 -> rep/sep bracket) + BR reading Z.
+  Register RHS16;
+  int64_t RHS16Const;
+  if (mi_match(CondReg, MRI, m_CmpNZImm16(LHS, RHS16Const, Flag))) {
+    // #321 `g == 0x1234`: fold a single-use near-abs GLOBAL LHS into `lda abs`
+    // (CmpBrAbsImm16) instead of materializing it into an Imag16 pair. Same 1-to-1
+    // volatile-safe fold as CmpBrAbsAbs16; the dead 16-bit G_SBC + COPY are cleaned by
+    // InstructionSelect's isTriviallyDead.
+    if (MachineInstr *LdL = foldableAbsLoad16(LHS, MI, MRI)) {
+      auto Branch = Builder.buildInstr(MOS::CmpBrAbsImm16)
+                        .addMBB(Tgt)
+                        .addUse(Flag, RegState::Undef)
+                        .addImm(FlagVal)
+                        .add(LdL->getOperand(1))
+                        .addImm(RHS16Const)
+                        .cloneMemRefs(*LdL);
+      constrainSelectedInstRegOperands(*Branch, TII, TRI, RBI);
+      LdL->eraseFromParent();
+      MI.eraseFromParent();
+      return true;
+    }
+    auto Branch = Builder.buildInstr(MOS::CmpBrImm16)
+                      .addMBB(Tgt)
+                      .addUse(Flag, RegState::Undef)
+                      .addImm(FlagVal)
+                      .addUse(LHS)
+                      .addImm(RHS16Const);
+    constrainSelectedInstRegOperands(*Branch, TII, TRI, RBI);
+    MI.eraseFromParent();
+    return true;
+  }
+  if (mi_match(CondReg, MRI, m_CmpNZImag16(LHS, RHS16, Flag))) {
+    // #321 v3: fold BOTH single-use near-abs GLOBAL operands into `lda abs; cmp abs`
+    // (CmpBrAbsAbs16) — the `g1 == g2` win. Only when both fold: a lone foldable
+    // operand (mixed global-vs-local/register) stays on the Imag16 path, which is
+    // exactly today's codegen, so no regression. EQ is commutative and the legalizer
+    // already put a lone foldable global on the LHS, so both-foldable means g1 == g2.
+    MachineInstr *LdL = foldableAbsLoad16(LHS, MI, MRI);
+    MachineInstr *LdR = foldableAbsLoad16(RHS16, MI, MRI);
+    if (LdL && LdR) {
+      const MachineInstr *Loads[] = {LdL, LdR};
+      auto Branch = Builder.buildInstr(MOS::CmpBrAbsAbs16)
+                        .addMBB(Tgt)
+                        .addUse(Flag, RegState::Undef)
+                        .addImm(FlagVal)
+                        .add(LdL->getOperand(1))
+                        .add(LdR->getOperand(1))
+                        .cloneMergedMemRefs(Loads);
+      constrainSelectedInstRegOperands(*Branch, TII, TRI, RBI);
+      LdL->eraseFromParent();
+      LdR->eraseFromParent();
+      MI.eraseFromParent();
+      return true;
+    }
+    // #321 task7 computed-vs-global: the legalizer swap ensured the Imag16-resident value is on
+    // LHS and the single-use foldable abs global is on RHS16. Fold the global into `cmp abs`
+    // (CmpBrImagAbs16: LDAImag16 $l + CMPAbs16 $r) instead of materializing it into Imag16.
+    if (LdR) {
+      auto Branch = Builder.buildInstr(MOS::CmpBrImagAbs16)
+                        .addMBB(Tgt)
+                        .addUse(Flag, RegState::Undef)
+                        .addImm(FlagVal)
+                        .addUse(LHS)
+                        .add(LdR->getOperand(1))
+                        .cloneMemRefs(*LdR);
+      constrainSelectedInstRegOperands(*Branch, TII, TRI, RBI);
+      LdR->eraseFromParent();
+      MI.eraseFromParent();
+      return true;
+    }
+    auto Branch = Builder.buildInstr(MOS::CmpBrImag16)
+                      .addMBB(Tgt)
+                      .addUse(Flag, RegState::Undef)
+                      .addImm(FlagVal)
+                      .addUse(LHS)
+                      .addUse(RHS16);
+    constrainSelectedInstRegOperands(*Branch, TII, TRI, RBI);
+    MI.eraseFromParent();
+    return true;
+  }
   if (mi_match(CondReg, MRI, m_CmpNZZero(LHS, Flag))) {
     auto Branch = Builder.buildInstr(MOS::CmpBrZero)
                       .addMBB(Tgt)
@@ -1091,12 +1357,99 @@ bool MOSInstructionSelector::selectBrCondImm(MachineInstr &MI) {
   return true;
 }
 
+// Defined later (beside selectAlu16Native): extract a 16-bit constant operand.
+static std::optional<int64_t> getImm16Operand(Register R,
+                                              MachineRegisterInfo &MRI);
+
+// #321 native s16 compare-operand fold: returns the defining G_LOAD16_ABS iff `R`
+// is a single-use 16-bit near-abs global load in `User`'s block — the operand the
+// absolute compare/load forms (cmp abs / lda abs) can read directly instead of
+// round-tripping the global through an Imag16 pair. Volatile-safe: the load has
+// exactly this one use, so folding preserves the single memory access in program
+// order (single-use check, same as selectAlu16Native's abs operand fold). Same-BB
+// only (mirrors shouldFoldMemAccess's no-cross-block rule).
+static MachineInstr *foldableAbsLoad16(Register R, const MachineInstr &User,
+                                       const MachineRegisterInfo &MRI) {
+  MachineInstr *Def = MRI.getVRegDef(R);
+  if (Def && Def->getOpcode() == MOS::G_LOAD16_ABS &&
+      MRI.hasOneNonDBGUse(R) && Def->getParent() == User.getParent())
+    return Def;
+  return nullptr;
+}
+
+// #321 native 16-bit compares: select an s16 G_SBC that the legalizer left
+// un-narrowed for an unsigned-ordering compare (NativeS16/UGE path). Only the
+// carry-out flag (operand 1) is used; the s16 result and N/V/Z are dead and the
+// carry-in is set. Emits `lda lhs; cmp rhs` (both MLow=1 -> one rep/sep bracket)
+// producing C, which the following branch tests (bcc/bcs). A near-abs global
+// operand is read directly via lda abs / cmp abs (foldableAbsLoad16) instead of
+// being materialized into an Imag16 pair; otherwise the accumulator is
+// entered only via LDAImag16 -> never an Ac16<->8-bit COPY (the native invariant).
+bool MOSInstructionSelector::selectSbc16(MachineInstr &MI) {
+  MachineIRBuilder Builder(MI);
+  MachineRegisterInfo &MRI = *Builder.getMRI();
+
+  Register CarryOut = MI.getOperand(1).getReg(); // C — the only used flag (UGE)
+  Register L = MI.getOperand(5).getReg();        // LHS (s16, in Imag16 or abs load)
+  Register R = MI.getOperand(6).getReg();        // RHS (s16, in Imag16 or abs load)
+
+  SmallVector<MachineInstrBuilder, 2> New;
+  SmallVector<MachineInstr *, 2> Folded; // near-abs loads consumed by the fold
+
+  // lda lhs (16-bit) -> A16. A single-use near-abs global LHS reads via `lda abs`
+  // directly, dropping the `lda abs; sta tmp` round-trip; otherwise from its Imag16.
+  Register Lo = MRI.createVirtualRegister(&MOS::Ac16RegClass);
+  if (MachineInstr *LdL = foldableAbsLoad16(L, MI, MRI)) {
+    New.push_back(Builder.buildInstr(MOS::LDAbs16)
+                      .addDef(Lo)
+                      .add(LdL->getOperand(1))
+                      .cloneMemRefs(*LdL));
+    Folded.push_back(LdL);
+  } else {
+    New.push_back(Builder.buildInstr(MOS::LDAImag16).addDef(Lo).addUse(L));
+  }
+
+  // cmp rhs (16-bit): A16 - rhs, discard result, set C/N/Z. A constant RHS folds to
+  // `cmp #imm16` (common loop bounds); a single-use near-abs global RHS to `cmp abs`
+  // (reading the global directly); otherwise compare the Imag16 operand.
+  if (auto Imm = getImm16Operand(R, MRI)) {
+    New.push_back(
+        Builder.buildInstr(MOS::CMPImm16).addDef(CarryOut).addUse(Lo).addImm(*Imm));
+  } else if (MachineInstr *LdR = foldableAbsLoad16(R, MI, MRI)) {
+    New.push_back(Builder.buildInstr(MOS::CMPAbs16)
+                      .addDef(CarryOut)
+                      .addUse(Lo)
+                      .add(LdR->getOperand(1))
+                      .cloneMemRefs(*LdR));
+    Folded.push_back(LdR);
+  } else {
+    New.push_back(
+        Builder.buildInstr(MOS::CMPImag16).addDef(CarryOut).addUse(Lo).addUse(R));
+  }
+
+  for (MachineInstrBuilder &B : New)
+    constrainSelectedInstRegOperands(*B.getInstr(), TII, TRI, RBI);
+
+  // Erase the folded loads (single-use; this G_SBC was their only consumer, and
+  // selection is bottom-up so they are not yet selected).
+  for (MachineInstr *F : Folded)
+    F->eraseFromParent();
+
+  MI.eraseFromParent();
+  return true;
+}
+
 // Although some G_SBC instructions can be folded in to their (branch) uses,
 // others need to be selected directly.
 bool MOSInstructionSelector::selectSbc(MachineInstr &MI) {
   MachineIRBuilder Builder(MI);
   const auto &MRI = *Builder.getMRI();
 
+  // #321 native 16-bit compare: an s16-result G_SBC is the unsigned-ordering
+  // compare form (selectSbc16). The 8-bit body below assumes 8-bit operands.
+  if (MRI.getType(MI.getOperand(0).getReg()) == LLT::scalar(16))
+    return selectSbc16(MI);
+
   Register A = MI.getOperand(0).getReg();
   Register N = MI.getOperand(2).getReg();
   Register V = MI.getOperand(3).getReg();
@@ -1927,6 +2280,443 @@ bool MOSInstructionSelector::selectBrIndirect(MachineInstr &MI) {
   }
   return selectGeneric(MI);
 }
+// #321 native s16: extract a 16-bit compile-time constant from an operand, if it
+// is one. Handles both forms a 16-bit literal takes by the time it reaches the
+// selector: a direct (look-through) constant, and — the common case — a
+// G_MERGE_VALUES of two byte constants (a 16-bit immediate is split into lo/hi
+// bytes before selection; getIConstantVRegValWithLookThrough does NOT look
+// through the merge, so reconstruct it here). Used to fold `var OP const` into
+// the *Imm16 forms instead of materializing the constant into an Imag16 pair.
+static std::optional<int64_t> getImm16Operand(Register R,
+                                              MachineRegisterInfo &MRI) {
+  // Shared with the EQ matcher (CmpNZImm16_match): direct G_CONSTANT or a byte-split
+  // G_MERGE_VALUES(i8 lo, i8 hi). See getI16Const.
+  return getI16Const(R, MRI);
+}
+
+// #321 Increment 1d-retry: select a native s16 ALU op `%dst = G_OP %a, %b` whose
+// value lives in an Imag16 zero-page pair (locals, multi-use intermediates) —
+// handles all s16 ALU shapes via zero-page operands:
+//
+//   add:        clc ; lda <a:zp> ; adc <b:zp> ; sta <dst:zp>
+//   sub:        sec ; lda <a:zp> ; sbc <b:zp> ; sta <dst:zp>   (a minuend)
+//   and/or/xor:       lda <a:zp> ; and|ora|eor <b:zp> ; sta <dst:zp>
+//
+// all MLow=1 -> one rep/sep bracket; the carry-init (clc/sec) is M-width-agnostic
+// (MOSInsertREPSEP) so it sits outside the 16-bit run. The accumulator A16 (Ac16
+// vregs Lo/Res) is entered only by LDAImag16 and left only by STAImag16 (which
+// DEFS the dst Imag16) — there is NO COPY between Ac16 and an 8-bit reg, so the
+// register coalescer cannot merge an 8-bit value into A16 (the exact crash that
+// sank the first 1d prototype).
+//
+// Immediate fold: if an operand is a compile-time constant, the ALU op uses the
+// *Imm16 form (`adc #$0345`) instead of materializing the constant into an Imag16
+// pair — for the commutative ops (add/and/or/xor) either operand may be the
+// constant (swap so the register is loaded). SUB has no SBCImm16 (and `x - C` is
+// canonicalized to `x + (-C)` upstream), so it never folds an immediate. The now-
+// dead constant def is removed automatically by isTriviallyDead in the generic
+// InstructionSelect pass.
+//
+// Mixed-operand fold: a single near-abs global operand (the other being an Imag16
+// register — a local/multi-use value, the case the both-global combiner peephole
+// can't reach) is read directly instead of being materialized into an Imag16 pair.
+// Two independent fold sites: operand A folds into the LHS `lda abs` (LDAbs16);
+// operand B folds into the absolute ALU form (`adc|sbc|and|ora|eor abs`). SUB stays
+// correct without a swap — the minuend is always A (loaded), the subtrahend B. Same
+// 1-to-1 volatile-safe single-use fold as the compare-operand fold (selectSbc16).
+bool MOSInstructionSelector::selectAlu16Native(MachineInstr &MI) {
+  MachineIRBuilder Builder(MI);
+  MachineRegisterInfo &MRI = *Builder.getMRI();
+
+  Register Dst = MI.getOperand(0).getReg();
+  Register A = MI.getOperand(1).getReg();
+  Register B = MI.getOperand(2).getReg();
+
+  // Map the generic opcode to (zp ALU opcode, abs ALU opcode, imm ALU opcode
+  // [0 = none], carry-init: -1 none / 0 clc / 1 sec, commutative). AbsOpc reads a
+  // near-abs global operand directly (the mixed-operand fold below).
+  unsigned AluOpc, AbsOpc, ImmOpc;
+  int CarryInit;
+  bool Commutative;
+  switch (MI.getOpcode()) {
+  case MOS::G_ADD: AluOpc = MOS::ADCImag16; AbsOpc = MOS::ADCAbs16; ImmOpc = MOS::ADCImm16; CarryInit = 0;  Commutative = true;  break;
+  case MOS::G_SUB: AluOpc = MOS::SBCImag16; AbsOpc = MOS::SBCAbs16; ImmOpc = 0;             CarryInit = 1;  Commutative = false; break;
+  case MOS::G_AND: AluOpc = MOS::ANDImag16; AbsOpc = MOS::ANDAbs16; ImmOpc = MOS::ANDImm16; CarryInit = -1; Commutative = true;  break;
+  case MOS::G_OR:  AluOpc = MOS::ORAImag16; AbsOpc = MOS::ORAAbs16; ImmOpc = MOS::ORAImm16; CarryInit = -1; Commutative = true;  break;
+  default:         AluOpc = MOS::EORImag16; AbsOpc = MOS::EORAbs16; ImmOpc = MOS::EORImm16; CarryInit = -1; Commutative = true;  break; // G_XOR
+  }
+  const bool IsArith = CarryInit >= 0;
+
+  // #321 native 16-bit inc/dec: a 16-bit ±1 is one `inc a`/`dec a` on A16 (1 byte, no
+  // carry-init), not `(clc;) adc #$0001`. Compute the signed step from a constant
+  // operand: ADD by +1 / SUB by -1 -> inc; ADD by -1 (0xFFFF) / SUB by +1 -> dec. For
+  // ADD (commutative) the constant may be either operand; for SUB it must be the
+  // subtrahend B (`1 - x` is not a decrement). The value loads via the LHS abs-fold
+  // (lda abs near-abs global, else lda zp); inc/dec a sets N/Z (carry-out unused). All
+  // MLow=1 -> one rep/sep bracket, value enters/leaves A16 only via the load/store.
+  if (MI.getOpcode() == MOS::G_ADD || MI.getOpcode() == MOS::G_SUB) {
+    auto stepOf = [](int64_t C) -> int { return C == 1 ? 1 : (C == 0xFFFF ? -1 : 0); };
+    int Step = 0;
+    Register Val;
+    if (MI.getOpcode() == MOS::G_ADD) {
+      if (auto C = getImm16Operand(B, MRI)) { Step = stepOf(*C); Val = A; }
+      if (!Step)
+        if (auto C = getImm16Operand(A, MRI)) { Step = stepOf(*C); Val = B; }
+    } else { // G_SUB: only the subtrahend (B) as a constant is an inc/dec
+      if (auto C = getImm16Operand(B, MRI)) { Step = -stepOf(*C); Val = A; }
+    }
+    if (Step) {
+      SmallVector<MachineInstrBuilder, 3> New;
+      MachineInstr *LdV = foldableAbsLoad16(Val, MI, MRI);
+      Register Lo = MRI.createVirtualRegister(&MOS::Ac16RegClass);
+      if (LdV)
+        New.push_back(Builder.buildInstr(MOS::LDAbs16).addDef(Lo)
+                          .add(LdV->getOperand(1)).cloneMemRefs(*LdV));
+      else
+        New.push_back(Builder.buildInstr(MOS::LDAImag16).addDef(Lo).addUse(Val));
+      Register Res = MRI.createVirtualRegister(&MOS::Ac16RegClass);
+      New.push_back(Builder.buildInstr(Step > 0 ? MOS::INCAcc16 : MOS::DECAcc16)
+                        .addDef(Res).addUse(Lo));
+      New.push_back(Builder.buildInstr(MOS::STAImag16).addDef(Dst).addUse(Res));
+      for (MachineInstrBuilder &Bld : New)
+        constrainSelectedInstRegOperands(*Bld.getInstr(), TII, TRI, RBI);
+      if (LdV)
+        LdV->eraseFromParent();
+      MI.eraseFromParent();
+      return true;
+    }
+  }
+
+  // Fold a constant operand into the immediate form (not for SUB: no SBCImm16).
+  // After the (optional) swap, A is the register loaded into A16 and Imm (if set)
+  // is the second operand; otherwise B is the register second operand.
+  std::optional<int64_t> Imm;
+  if (ImmOpc) {
+    if ((Imm = getImm16Operand(B, MRI))) {
+      // B is the immediate; A stays the loaded register.
+    } else if (Commutative && (Imm = getImm16Operand(A, MRI))) {
+      std::swap(A, B); // load the old-B register; the old-A constant is the imm.
+    }
+  }
+
+  // Mixed-operand fold sites: a single-use near-abs global A folds into `lda abs`;
+  // a single-use near-abs global B (no immediate) folds into the `OP abs` form.
+  SmallVector<MachineInstr *, 2> Folded;
+  MachineInstr *LdA = foldableAbsLoad16(A, MI, MRI);
+  MachineInstr *LdB = Imm ? nullptr : foldableAbsLoad16(B, MI, MRI);
+
+  SmallVector<MachineInstrBuilder, 4> New;
+
+  // lda a (16-bit) -> A16. Fold a near-abs global LHS straight into `lda abs`.
+  Register Lo = MRI.createVirtualRegister(&MOS::Ac16RegClass);
+  if (LdA) {
+    New.push_back(Builder.buildInstr(MOS::LDAbs16)
+                      .addDef(Lo)
+                      .add(LdA->getOperand(1))
+                      .cloneMemRefs(*LdA));
+    Folded.push_back(LdA);
+  } else {
+    New.push_back(Builder.buildInstr(MOS::LDAImag16).addDef(Lo).addUse(A));
+  }
+
+  Register Carry;
+  if (IsArith) {
+    // clc (add) / sec (sub) — outside the 16-bit run (carry-init is M-agnostic).
+    Carry = MRI.createVirtualRegister(&MOS::CcRegClass);
+    New.push_back(Builder.buildInstr(MOS::LDImm1).addDef(Carry).addImm(CarryInit));
+  }
+
+  // the ALU op (16-bit): A16 = a OP (b | [b:abs] | #imm) (+carry for add/sub). The
+  // second operand is an immediate, a near-abs global (abs form), or an Imag16 reg.
+  Register Res = MRI.createVirtualRegister(&MOS::Ac16RegClass);
+  unsigned EffAluOpc = Imm ? ImmOpc : (LdB ? AbsOpc : AluOpc);
+  auto Alu = Builder.buildInstr(EffAluOpc).addDef(Res);
+  if (IsArith) {
+    Register CarryOut = MRI.createVirtualRegister(&MOS::CcRegClass);
+    Register VOut = MRI.createVirtualRegister(&MOS::VcRegClass);
+    Alu.addDef(CarryOut).addDef(VOut).addUse(Lo);
+    if (Imm)
+      Alu.addImm(*Imm);
+    else if (LdB)
+      Alu.add(LdB->getOperand(1));
+    else
+      Alu.addUse(B);
+    Alu.addUse(Carry);
+  } else {
+    Alu.addUse(Lo);
+    if (Imm)
+      Alu.addImm(*Imm);
+    else if (LdB)
+      Alu.add(LdB->getOperand(1));
+    else
+      Alu.addUse(B);
+  }
+  if (LdB) {
+    Alu.cloneMemRefs(*LdB);
+    Folded.push_back(LdB);
+  }
+  New.push_back(Alu);
+
+  // sta dst (16-bit): the Imag16 dst is DEF'd by the store (imaginary-reg write).
+  New.push_back(Builder.buildInstr(MOS::STAImag16).addDef(Dst).addUse(Res));
+
+  for (MachineInstrBuilder &Bld : New)
+    constrainSelectedInstRegOperands(*Bld.getInstr(), TII, TRI, RBI);
+
+  // Erase the folded loads (single-use; this op was their only consumer, and
+  // selection is bottom-up so they are not yet selected).
+  for (MachineInstr *F : Folded)
+    F->eraseFromParent();
+
+  MI.eraseFromParent();
+  return true;
+}
+
+// #321 xy16: skeleton selector for G_* ops whose result RA assigned to Xc16/Yc16.
+// Returns false to fall through to selectImpl — actual X/Y-index selection is a
+// follow-on task (M2 legalizer integration: index-operand widening, LEA forms).
+bool MOSInstructionSelector::selectXY16(MachineInstr &MI) {
+  return false;
+}
+
+// #321 native s16 constant shift: `dst = src << k` / `dst = src >> k` (unsigned),
+// k a compile-time constant in [1,7] left un-narrowed by the legalizer under
+// +mos-a16. In 16-bit-accumulator mode one `asl a` / `lsr a` shifts all 16 bits,
+// so emit `lda src; (asl|lsr)*k; sta dst` — all MLow=1, so MOSInsertREPSEP wraps
+// the run in one rep/sep. The value enters A16 only via LDAImag16 and leaves only
+// via STAImag16 (no Ac16<->8-bit COPY). No carry-init: each shift self-fills 0.
+bool MOSInstructionSelector::selectShift16Native(MachineInstr &MI) {
+  MachineIRBuilder Builder(MI);
+  MachineRegisterInfo &MRI = *Builder.getMRI();
+
+  Register Dst = MI.getOperand(0).getReg();
+  Register Src = MI.getOperand(1).getReg();
+  Register AmtReg = MI.getOperand(2).getReg();
+
+  auto AmtVal = getIConstantVRegValWithLookThrough(AmtReg, MRI);
+  if (!AmtVal)
+    return false; // legalizer only leaves constant amounts native; be defensive
+  uint64_t Amt = AmtVal->Value.getZExtValue();
+
+  const unsigned Opc = MI.getOpcode();
+  const bool IsAshr = Opc == MOS::G_ASHR;
+
+  SmallVector<MachineInstrBuilder, 16> New;
+
+  // lda src (16-bit) -> A16
+  Register Cur = MRI.createVirtualRegister(&MOS::Ac16RegClass);
+  New.push_back(Builder.buildInstr(MOS::LDAImag16).addDef(Cur).addUse(Src));
+
+  // Amt iterations on A16, each threading a fresh Ac16 from the previous (the tied
+  // $dst=$src collapses the chain onto A16 in regalloc).
+  //  - G_SHL  -> asl a  (fill 0 at bit 0)
+  //  - G_LSHR -> lsr a  (fill 0 at bit 15)
+  //  - G_ASHR -> cmp #$8000; ror a  (cmp sets C = sign bit; ror rotates it into
+  //              bit 15, replicating the sign; the ror's carry-out is dead).
+  for (uint64_t I = 0; I < Amt; ++I) {
+    Register Next = MRI.createVirtualRegister(&MOS::Ac16RegClass);
+    if (IsAshr) {
+      Register Sign = MRI.createVirtualRegister(&MOS::CcRegClass);
+      New.push_back(Builder.buildInstr(MOS::CMPImm16)
+                        .addDef(Sign)
+                        .addUse(Cur)
+                        .addImm(0x8000));
+      Register CarryOut = MRI.createVirtualRegister(&MOS::CcRegClass);
+      New.push_back(Builder.buildInstr(MOS::RORAcc16)
+                        .addDef(Next)
+                        .addDef(CarryOut)
+                        .addUse(Cur)
+                        .addUse(Sign));
+    } else {
+      const unsigned ShOpc = Opc == MOS::G_SHL ? MOS::ASLAcc16 : MOS::LSRAcc16;
+      New.push_back(Builder.buildInstr(ShOpc).addDef(Next).addUse(Cur));
+    }
+    Cur = Next;
+  }
+
+  // sta dst (16-bit): the Imag16 dst is DEF'd by the store (imaginary-reg write).
+  New.push_back(Builder.buildInstr(MOS::STAImag16).addDef(Dst).addUse(Cur));
+
+  for (MachineInstrBuilder &Bld : New)
+    constrainSelectedInstRegOperands(*Bld.getInstr(), TII, TRI, RBI);
+
+  MI.eraseFromParent();
+  return true;
+}
+
+Register MOSInstructionSelector::loadStoreValueIntoA16(
+    MachineIRBuilder &Builder, MachineInstr &StoreMI, Register Value,
+    SmallVectorImpl<MachineInstrBuilder> &New, MachineInstr *&FoldedLoad) {
+  MachineRegisterInfo &MRI = *Builder.getMRI();
+  FoldedLoad = nullptr;
+  Register A16 = MRI.createVirtualRegister(&MOS::Ac16RegClass);
+  MachineInstr *Def = MRI.getVRegDef(Value);
+  if (Def && MRI.hasOneNonDBGUse(Value) &&
+      (Def->getOpcode() == MOS::G_LOAD16_ABS ||
+       Def->getOpcode() == MOS::G_LOAD16_INDIR) &&
+      shouldFoldMemAccess(StoreMI, *Def, AA)) {
+    // Indirect/mixed copy: fold the load directly into A16 (no Imag16 temp). The
+    // load's operand 1 is its address (abs operand or pointer); operand 0 is the
+    // value vreg this store consumes.
+    const bool LdAbs = Def->getOpcode() == MOS::G_LOAD16_ABS;
+    auto Ld = Builder.buildInstr(LdAbs ? MOS::LDAbs16 : MOS::LDAIndir16)
+                  .addDef(A16)
+                  .add(Def->getOperand(1));
+    Ld.cloneMemRefs(*Def);
+    New.push_back(Ld);
+    FoldedLoad = Def;
+  } else {
+    // Value is already a 16-bit register (Imag16): load it into A16.
+    New.push_back(Builder.buildInstr(MOS::LDAImag16).addDef(A16).addUse(Value));
+  }
+  return A16;
+}
+
+// #321 native s16 indirect load/store: a 16-bit value through a runtime pointer.
+//  G_LOAD16_INDIR  (dst s16, ptr) -> lda (zp)16 -> A16; sta zp16 -> Imag16 dst
+//  G_STORE16_INDIR (val s16, ptr) -> lda zp16 (val -> A16); sta (zp)16
+// The pointer operand is constrained to the Imag16 zero-page pair (same as the
+// 8-bit LDIndir/STIndir); the value enters/leaves A16 only via the Imag16 load/
+// store -> no Ac16<->8-bit COPY. Both ops are MLow=1, so MOSInsertREPSEP brackets
+// the pair with one rep/sep.
+bool MOSInstructionSelector::selectMem16Indir(MachineInstr &MI) {
+  MachineIRBuilder Builder(MI);
+  MachineRegisterInfo &MRI = *Builder.getMRI();
+  const bool IsLoad = MI.getOpcode() == MOS::G_LOAD16_INDIR;
+
+  Register ValOrDst = MI.getOperand(0).getReg();
+  Register Ptr = MI.getOperand(1).getReg();
+
+  // The user's memory access is the indirect op (LDAIndir16 / STAIndir16); carry
+  // the memref there. The Imag16 load/store that moves the value to/from A16 is an
+  // internal zp register transfer with no memref (same pattern as foldableAbsLoad16).
+  SmallVector<MachineInstrBuilder, 2> New;
+  MachineInstr *FoldedLoad = nullptr;
+  if (IsLoad) {
+    Register A16 = MRI.createVirtualRegister(&MOS::Ac16RegClass);
+    auto Ld = Builder.buildInstr(MOS::LDAIndir16).addDef(A16).addUse(Ptr);
+    Ld.cloneMemRefs(MI);
+    New.push_back(Ld);
+    New.push_back(Builder.buildInstr(MOS::STAImag16).addDef(ValOrDst).addUse(A16));
+  } else {
+    Register A16 = loadStoreValueIntoA16(Builder, MI, ValOrDst, New, FoldedLoad);
+    auto St = Builder.buildInstr(MOS::STAIndir16).addUse(A16).addUse(Ptr);
+    St.cloneMemRefs(MI);
+    New.push_back(St);
+  }
+
+  for (MachineInstrBuilder &Bld : New)
+    constrainSelectedInstRegOperands(*Bld.getInstr(), TII, TRI, RBI);
+  if (FoldedLoad)
+    FoldedLoad->eraseFromParent();
+  MI.eraseFromParent();
+  return true;
+}
+
+// #321 native s16 absolute load/store: a 16-bit value at an absolute (global/static)
+// address. Mirror of selectMem16Indir, using LDAbs16/STAbs16 (the abs operand rides
+// the real memory op; the Imag16 transfer carries no memref). One rep/sep each; no
+// Ac16<->8-bit COPY.
+//  G_LOAD16_ABS  (dst s16, absOp) -> lda abs16 -> A16; sta zp16 -> Imag16 dst
+//  G_STORE16_ABS (val s16, absOp) -> lda zp16 (val -> A16); sta abs16
+bool MOSInstructionSelector::selectMem16Abs(MachineInstr &MI) {
+  MachineIRBuilder Builder(MI);
+  MachineRegisterInfo &MRI = *Builder.getMRI();
+  const bool IsLoad = MI.getOpcode() == MOS::G_LOAD16_ABS;
+
+  Register ValOrDst = MI.getOperand(0).getReg();
+  const MachineOperand &AbsOp = MI.getOperand(1);
+
+  SmallVector<MachineInstrBuilder, 2> New;
+  MachineInstr *FoldedLoad = nullptr;
+  if (IsLoad) {
+    Register A16 = MRI.createVirtualRegister(&MOS::Ac16RegClass);
+    auto Ld = Builder.buildInstr(MOS::LDAbs16).addDef(A16).add(AbsOp);
+    Ld.cloneMemRefs(MI);
+    New.push_back(Ld);
+    New.push_back(Builder.buildInstr(MOS::STAImag16).addDef(ValOrDst).addUse(A16));
+  } else {
+    Register A16 = loadStoreValueIntoA16(Builder, MI, ValOrDst, New, FoldedLoad);
+    auto St = Builder.buildInstr(MOS::STAbs16).addUse(A16).add(AbsOp);
+    St.cloneMemRefs(MI);
+    New.push_back(St);
+  }
+
+  for (MachineInstrBuilder &Bld : New)
+    constrainSelectedInstRegOperands(*Bld.getInstr(), TII, TRI, RBI);
+  if (FoldedLoad)
+    FoldedLoad->eraseFromParent();
+  MI.eraseFromParent();
+  return true;
+}
+
+bool MOSInstructionSelector::selectMem16AbsIdx(MachineInstr &MI) {
+  MachineIRBuilder Builder(MI);
+  MachineRegisterInfo &MRI = *Builder.getMRI();
+  const bool IsLoad = MI.getOpcode() == MOS::G_LOAD16_ABS_IDX;
+
+  Register ValOrDst = MI.getOperand(0).getReg();
+  const MachineOperand &BaseOp = MI.getOperand(1);
+  Register IdxReg = MI.getOperand(2).getReg();
+
+  SmallVector<MachineInstrBuilder, 2> New;
+  MachineInstr *FoldedLoad = nullptr;
+  if (IsLoad) {
+    Register A16 = MRI.createVirtualRegister(&MOS::Ac16RegClass);
+    auto Ld = Builder.buildInstr(MOS::LDAbsIdx16).addDef(A16).add(BaseOp).addUse(IdxReg);
+    Ld.cloneMemRefs(MI);
+    New.push_back(Ld);
+    New.push_back(Builder.buildInstr(MOS::STAImag16).addDef(ValOrDst).addUse(A16));
+  } else {
+    Register A16 = loadStoreValueIntoA16(Builder, MI, ValOrDst, New, FoldedLoad);
+    auto St = Builder.buildInstr(MOS::STAbsIdx16).addUse(A16).add(BaseOp).addUse(IdxReg);
+    St.cloneMemRefs(MI);
+    New.push_back(St);
+  }
+
+  for (MachineInstrBuilder &Bld : New)
+    constrainSelectedInstRegOperands(*Bld.getInstr(), TII, TRI, RBI);
+  if (FoldedLoad)
+    FoldedLoad->eraseFromParent();
+  MI.eraseFromParent();
+  return true;
+}
+
+// #321 Increment 1e: 16-bit (zp),y indexed load/store.
+//  G_LOAD16_INDIR_IDX  (dst s16, base_ptr, idx_reg) → LDIndirIdx16(A16, base, idx) + STAImag16(dst, A16)
+//  G_STORE16_INDIR_IDX (val s16, base_ptr, idx_reg) → LDAImag16(A16, val) + STIndirIdx16(A16, base, idx)
+// One rep/sep via MLow=1 on LDIndirIdx16/STIndirIdx16; no Ac16<->8-bit COPY.
+bool MOSInstructionSelector::selectMem16IndirIdx(MachineInstr &MI) {
+  MachineIRBuilder Builder(MI);
+  MachineRegisterInfo &MRI = *Builder.getMRI();
+  const bool IsLoad = MI.getOpcode() == MOS::G_LOAD16_INDIR_IDX;
+
+  Register ValOrDst = MI.getOperand(0).getReg();
+  Register BasePtr = MI.getOperand(1).getReg();
+  Register IdxReg = MI.getOperand(2).getReg();
+
+  SmallVector<MachineInstrBuilder, 2> New;
+  MachineInstr *FoldedLoad = nullptr;
+  if (IsLoad) {
+    Register A16 = MRI.createVirtualRegister(&MOS::Ac16RegClass);
+    auto Ld = Builder.buildInstr(MOS::LDIndirIdx16).addDef(A16).addUse(BasePtr).addUse(IdxReg);
+    Ld.cloneMemRefs(MI);
+    New.push_back(Ld);
+    New.push_back(Builder.buildInstr(MOS::STAImag16).addDef(ValOrDst).addUse(A16));
+  } else {
+    Register A16 = loadStoreValueIntoA16(Builder, MI, ValOrDst, New, FoldedLoad);
+    auto St = Builder.buildInstr(MOS::STIndirIdx16).addUse(A16).addUse(BasePtr).addUse(IdxReg);
+    St.cloneMemRefs(MI);
+    New.push_back(St);
+  }
+
+  for (MachineInstrBuilder &Bld : New)
+    constrainSelectedInstRegOperands(*Bld.getInstr(), TII, TRI, RBI);
+  if (FoldedLoad)
+    FoldedLoad->eraseFromParent();
+  MI.eraseFromParent();
+  return true;
+}
 
 bool MOSInstructionSelector::selectGeneric(MachineInstr &MI) {
   unsigned Opcode;
diff --git a/llvm/lib/Target/MOS/MOSLateOptimization.cpp b/llvm/lib/Target/MOS/MOSLateOptimization.cpp
index fbedd8d46..d329144e2 100644
--- a/llvm/lib/Target/MOS/MOSLateOptimization.cpp
+++ b/llvm/lib/Target/MOS/MOSLateOptimization.cpp
@@ -48,6 +48,7 @@ public:
   bool lowerCmpZeros(MachineBasicBlock &MBB) const;
   void lowerCmpZero(MachineInstr &MI) const;
   bool combineLdImm(MachineBasicBlock &MBB) const;
+  bool threadAccum16(MachineBasicBlock &MBB) const;
   bool tailJMP(MachineBasicBlock &MBB) const;
 };
 
@@ -56,6 +57,7 @@ bool MOSLateOptimization::runOnMachineFunction(MachineFunction &MF) {
   for (MachineBasicBlock &MBB : MF) {
     Changed |= lowerCmpZeros(MBB);
     Changed |= combineLdImm(MBB);
+    Changed |= threadAccum16(MBB);
     Changed |= tailJMP(MBB);
   }
   return Changed;
@@ -398,6 +400,111 @@ bool MOSLateOptimization::combineLdImm(MachineBasicBlock &MBB) const {
   return Changed;
 }
 
+// #321 A16-threading: drop the redundant Imag16 round-trip between dependent
+// native-s16 ops. Each native s16 op is self-contained (LDAImag16 -> OP ->
+// STAImag16, value home = Imag16 between ops — the 1d-retry coalescer-safe
+// invariant), so a dependent chain stores each intermediate and reloads it:
+//     $rsN = STAImag16 $a16        ; store the value into its Imag16 home
+//     $a16 = LDAImag16 $rsN        ; reload it — but $a16 still holds the value!
+// The reload is redundant; erase it, and if the home is dead afterward (only the
+// reload read it, with no reader in between) erase the store too — the value then
+// threads through $a16 across the ops (e.g. lda;adc;and;sbc;sta).
+//
+// POST-RA on purpose: RA has already chosen $a16 for both sides, so collapsing the
+// pair cannot reintroduce the 1d coalescer crash (an 8-bit value coalescing into
+// A16). LDAImag16/STAImag16 model no NZ/flag def, so erasing them is flag-safe.
+//
+// Phase 1.5: the store and reload need NOT be adjacent — intervening instructions
+// are allowed as long as none rewrites $a16 (modifiesRegister(A16) also catches
+// 8-bit sub-A writes and xba; calls/side-effect ops bail explicitly) or overwrites
+// the home $rsN. A *read* of $a16 between (e.g. a second `sta gg`) is fine; we just
+// clear stale $a16 kills across the gap so it stays live to the next op. A missed
+// case only forgoes a win, never miscompiles.
+bool MOSLateOptimization::threadAccum16(MachineBasicBlock &MBB) const {
+  const auto *TRI = MBB.getParent()->getSubtarget().getRegisterInfo();
+  bool Changed = false;
+
+  // Stores whose value still provably resides in $a16, awaiting a redundant reload.
+  struct Pend { Register Home; MachineInstr *Sta; bool HomeRead; };
+  SmallVector<Pend, 4> Pending;
+  auto indexOf = [&](Register H) -> int {
+    for (unsigned I = 0, E = Pending.size(); I != E; ++I)
+      if (Pending[I].Home == H)
+        return (int)I;
+    return -1;
+  };
+
+  for (MachineInstr &MI : make_early_inc_range(MBB)) {
+    if (MI.isDebugInstr())
+      continue;
+
+    if (MI.getOpcode() == MOS::LDAImag16) {
+      Register Home = MI.getOperand(1).getReg();           // Imag16 use ($rsN)
+      int Idx = indexOf(Home);
+      if (Idx >= 0 &&
+          MI.getOperand(0).getReg() == Pending[Idx].Sta->getOperand(1).getReg()) {
+        // Redundant reload: $a16 already holds Home's value.
+        MachineInstr *Sta = Pending[Idx].Sta;
+        Register Acc = MI.getOperand(0).getReg();          // $a16
+        bool EraseStore = MI.getOperand(1).isKill() && !Pending[Idx].HomeRead;
+        // $a16 now stays live from the store across the gap to the next op — clear
+        // any stale kills on it so the verifier/later passes see it live throughout.
+        for (auto J = MachineBasicBlock::iterator(Sta),
+                  E = MachineBasicBlock::iterator(MI);
+             J != E; ++J)
+          J->clearRegisterKills(Acc, TRI);
+        MI.eraseFromParent();
+        Changed = true;
+        if (EraseStore) {
+          Sta->eraseFromParent();
+          Pending.erase(Pending.begin() + Idx); // store gone -> stop tracking
+        }
+        // Else keep BOTH the store and the pending entry: the value still resides
+        // in $a16 *and* the home, so a later reload of the same home is also
+        // redundant (this is the multi-reload case, e.g. `sta h; <store h>; lda h;
+        // … ; lda h`). $a16 is unchanged, so other pendings stay valid too.
+        continue;
+      }
+      // Genuine reload: it rewrites $a16, so no resident value survives.
+      Pending.clear();
+      continue;
+    }
+
+    if (MI.getOpcode() == MOS::STAImag16) {
+      // A store of the current $a16 value. STA reads $a16 (doesn't write it), so
+      // prior pendings keep holding the same resident value.
+      Register Home = MI.getOperand(0).getReg();           // Imag16 def ($rsN)
+      if (int Old = indexOf(Home); Old >= 0)
+        Pending.erase(Pending.begin() + Old);
+      Pending.push_back({Home, &MI, /*HomeRead=*/false});
+      continue;
+    }
+
+    // Any other instruction. Bail the whole resident set if $a16 is rewritten
+    // (incl. 8-bit sub-A / xba via aliasing -> modifiesRegister), clobbered by a
+    // call (regmask), or touched opaquely by inline asm. A volatile *store* only
+    // READS $a16, so it's fine to thread across (the store stays put; only the
+    // non-volatile reload of the Imag16 temp is dropped) — a volatile *load* would
+    // write $a16 and is caught by modifiesRegister above.
+    if (MI.modifiesRegister(MOS::A16, TRI) || MI.isCall() || MI.isInlineAsm()) {
+      Pending.clear();
+      continue;
+    }
+    // $a16 untouched: drop any home this instruction overwrites; note reads (a read
+    // means the store is still needed and may not be erased).
+    for (unsigned I = 0; I != Pending.size();) {
+      if (MI.modifiesRegister(Pending[I].Home, TRI)) {
+        Pending.erase(Pending.begin() + I);
+      } else {
+        if (MI.readsRegister(Pending[I].Home, TRI))
+          Pending[I].HomeRead = true;
+        ++I;
+      }
+    }
+  }
+  return Changed;
+}
+
 bool MOSLateOptimization::tailJMP(MachineBasicBlock &MBB) const {
   if (MBB.size() < 2)
     return false;
diff --git a/llvm/lib/Target/MOS/MOSLegalizerInfo.cpp b/llvm/lib/Target/MOS/MOSLegalizerInfo.cpp
index 212ede1a0..879eaca65 100644
--- a/llvm/lib/Target/MOS/MOSLegalizerInfo.cpp
+++ b/llvm/lib/Target/MOS/MOSLegalizerInfo.cpp
@@ -150,18 +150,24 @@ MOSLegalizerInfo::MOSLegalizerInfo(const MOSSubtarget &STI) {
       .widenScalarToNextMultipleOf(0, 8)
       .custom();
 
-  getActionDefinitionsBuilder({G_AND, G_OR})
-      .legalFor({S8})
-      .widenScalarToNextMultipleOf(0, 8)
-      .maxScalar(0, S8)
-      .unsupported();
+  // #321 Increment 1d-retry: under +mos-a16, s16 bitwise stays native (legal,
+  // selected by selectAlu16Native into an Imag16 pair) instead of narrowing to
+  // S8; larger scalars clamp to S16. Gated on hasAccum16 so default codegen is
+  // untouched. The 1b combiner still fast-paths all-global `g = a & b` shapes.
+  const LLT MaxBitwise = STI.hasAccum16() ? S16 : S8;
+  {
+    auto &B = getActionDefinitionsBuilder({G_AND, G_OR}).legalFor({S8});
+    if (STI.hasAccum16())
+      B.legalFor({S16});
+    B.widenScalarToNextMultipleOf(0, 8).maxScalar(0, MaxBitwise).unsupported();
+  }
 
-  getActionDefinitionsBuilder(G_XOR)
-      .legalFor({S8})
-      .customFor({S1})
-      .widenScalarToNextMultipleOf(0, 8)
-      .maxScalar(0, S8)
-      .unsupported();
+  {
+    auto &B = getActionDefinitionsBuilder(G_XOR).legalFor({S8}).customFor({S1});
+    if (STI.hasAccum16())
+      B.legalFor({S16});
+    B.widenScalarToNextMultipleOf(0, 8).maxScalar(0, MaxBitwise).unsupported();
+  }
 
   getActionDefinitionsBuilder(G_MUL)
       .libcallFor({S8, S16, S32, S64})
@@ -323,14 +329,20 @@ MOSLegalizerInfo::MOSLegalizerInfo(const MOSSubtarget &STI) {
 
   // Memory Operations
 
-  getActionDefinitionsBuilder({G_LOAD, G_STORE})
-      // Convert to int to load/store; that way the operation can be narrowed to
-      // 8 bits. Once 8-bit, select an addressing mode to replace the generic
-      // G_LOAD and G_STORE. PF (far) pointers select to absolute-long. (#320)
-      .customForCartesianProduct({S8, PZ, P}, {PZ, P, PF})
-      .widenScalarToNextMultipleOf(0, 8)
-      .maxScalar(0, S8)
-      .unsupported();
+  {
+    // Convert to int to load/store; that way the operation can be narrowed to
+    // 8 bits. Once 8-bit, select an addressing mode to replace the generic
+    // G_LOAD and G_STORE. PF (far) pointers select to absolute-long. (#320)
+    // #321 native s16: under +mos-a16 an s16 data load/store is ALSO custom, so it
+    // reaches legalizeLoad/legalizeStore, which routes a non-absolute 16-bit-pointer
+    // access to a native 16-bit indirect op (else narrows to two 8-bit ops as before).
+    auto &B = getActionDefinitionsBuilder({G_LOAD, G_STORE});
+    if (STI.hasAccum16())
+      B.customForCartesianProduct({S8, S16, PZ, P}, {PZ, P, PF});
+    else
+      B.customForCartesianProduct({S8, PZ, P}, {PZ, P, PF});
+    B.widenScalarToNextMultipleOf(0, 8).maxScalar(0, S8).unsupported();
+  }
 
   getActionDefinitionsBuilder({G_SEXTLOAD, G_ZEXTLOAD}).custom();
 
@@ -670,11 +682,23 @@ bool MOSLegalizerInfo::legalizeAddSub(LegalizerHelper &Helper,
   auto [Dst, Src] = MI.getFirst2Regs();
   assert(MRI.getType(Dst).isByteSized());
 
+  // #321 Increment 1d-retry: under +mos-a16, leave an s16 add/sub UN-narrowed for the
+  // selector (selectAlu16Native), which carries the value in an Imag16 zero-page pair
+  // and emits a single 16-bit lda/adc/sta on the transient A16. This now includes ±1:
+  // selectAlu16Native turns a 16-bit ±1 into one `inc a`/`dec a` (the 8-bit byte
+  // inc/dec-with-carry chain below thrashes M-mode inside a 16-bit region). Gated on
+  // hasAccum16 so default 8-bit codegen keeps narrowing (incl. the byte inc/dec path).
+  const MOSSubtarget &STI = Builder.getMF().getSubtarget<MOSSubtarget>();
+  const LLT Ty = MRI.getType(Dst);
+  if (STI.hasAccum16() && Ty == LLT::scalar(16))
+    return true; // legal as-is; selected natively (selectAlu16Native)
+
   auto RHSConst =
       getIConstantVRegValWithLookThrough(MI.getOperand(2).getReg(), MRI);
-  if (!RHSConst || std::abs(RHSConst->Value.getSExtValue()) != 1)
+  if (!RHSConst || std::abs(RHSConst->Value.getSExtValue()) != 1) {
     return Helper.narrowScalarAddSub(MI, 0, S8) !=
            LegalizerHelper::UnableToLegalize;
+  }
 
   // Handle multi-byte increments and decrements.
 
@@ -862,6 +886,24 @@ bool MOSLegalizerInfo::legalizeShiftRotate(
     return true;
   }
 
+  // #321 native 16-bit constant shifts: under +mos-a16 a small s16 shift is one
+  // `lda; <shift>*Amt; sta` run in 16-bit-A mode, far cheaper than the 8-bit
+  // byte-pair decomposition below. Leave the s16 G_SHL/G_LSHR/G_ASHR un-narrowed
+  // (mirrors legalizeAddSub's native passthrough) for selectShift16Native: left and
+  // unsigned-right shift to one `asl`/`lsr` per bit; signed-right (ASHR) to
+  // `cmp #$8000; ror` per bit (the cmp sets carry from the sign, ror replicates it).
+  // Gated to Amt in [1,7]: Amt>=8 keeps the byte path (it relabels bytes with a
+  // sign/zero fill — free for byte-aligned shifts). After the Amt==0 copy, before
+  // the byte/over-correct paths.
+  {
+    const MOSSubtarget &STI = Builder.getMF().getSubtarget<MOSSubtarget>();
+    if (STI.hasAccum16() && Ty == LLT::scalar(16) &&
+        (MI.getOpcode() == MOS::G_SHL || MI.getOpcode() == MOS::G_LSHR ||
+         MI.getOpcode() == MOS::G_ASHR) &&
+        Amt >= 1 && Amt <= 7)
+      return true; // native; selected by selectShift16Native
+  }
+
   Register Partial;
   Register NewAmt;
   // Shift by one multiples of one byte.
@@ -878,11 +920,16 @@ bool MOSLegalizerInfo::legalizeShiftRotate(
     case MOS::G_ROTR:
       break;
     case MOS::G_ASHR: {
-      Register Sign = Builder
-                          .buildICmp(ICmpInst::ICMP_SLT, S1, Src,
-                                     Builder.buildConstant(Ty, 0))
-                          .getReg(0);
-      Fill = Builder.buildSExt(S8, Sign).getReg(0);
+      // Sign-fill byte = the high byte's sign bit broadcast across all 8 bits
+      // (arithmetic-shift-right by 7). The previous formulation built an s16
+      // ICMP_SLT(Src, 0); under +mos-a16 that re-enters the native signed-compare
+      // legalization (the SLT -> ULT sign-flip rewrite) as a compare-result-AS-VALUE
+      // and the legalizer loops — a signed (arithmetic) right shift by >= 8 hung the
+      // compiler. The pure 8-bit sign broadcast is equivalent and never builds an s16
+      // compare. (Found + minimized by the Tier-1 differential fuzzer; see
+      // docs/plans/2026-06-15-321-tier1-broaden-corpus.md and examples/65816/known.)
+      Fill = Builder.buildAShr(S8, DstBytes.back(), Builder.buildConstant(S8, 7))
+                 .getReg(0);
       break;
     }
     case MOS::G_LSHR:
@@ -1252,6 +1299,28 @@ bool MOSLegalizerInfo::legalizeICmp(LegalizerHelper &Helper,
 
   LLT Type = MRI.getType(LHS);
 
+  // #321 native s16 signed ordering: signed order equals unsigned order after
+  // flipping the sign bit, a <ₛ b ⟺ (a ^ 0x8000) <ᵤ (b ^ 0x8000). Under +mos-a16
+  // rewrite the canonical signed primitive (SLT) to an unsigned ULT on the
+  // sign-flipped operands, reusing the already-native 16-bit EOR (the XORs) and the
+  // native unsigned UGE carry path (ULT re-legalizes to !UGE). No new flag handling.
+  // A constant RHS stays constant (b ^ 0x8000) and folds into cmp #imm.
+  {
+    const MOSSubtarget &STI = Builder.getMF().getSubtarget<MOSSubtarget>();
+    if (STI.hasAccum16() && Type == LLT::scalar(16) &&
+        Pred == CmpInst::ICMP_SLT) {
+      auto Mask = Builder.buildConstant(Type, 0x8000);
+      Register LHSf = Builder.buildXor(Type, LHS, Mask).getReg(0);
+      Register RHSf = Builder.buildXor(Type, RHS, Mask).getReg(0);
+      Helper.Observer.changingInstr(MI);
+      MI.getOperand(1).setPredicate(CmpInst::ICMP_ULT);
+      MI.getOperand(2).setReg(LHSf);
+      MI.getOperand(3).setReg(RHSf);
+      Helper.Observer.changedInstr(MI);
+      return true;
+    }
+  }
+
   // Compare pointers by first converting to integer. This allows the
   // comparison to be reduced to 8-bit comparisons.
   if (Type.isPointer()) {
@@ -1270,7 +1339,142 @@ bool MOSLegalizerInfo::legalizeICmp(LegalizerHelper &Helper,
   bool RHSIsZero = mi_match(RHS, MRI, m_SpecificICst(0));
   Register CIn;
 
-  if (Type != S8) {
+  // #321 native s16: under +mos-a16, keep an s16 UGE comparison UN-narrowed and
+  // lower it via a 16-bit G_SBC (the C-flag path) — selectSbc16 emits one
+  // rep/lda/cmp/sep. The other canonical predicates (EQ via Z, SLT via N^V) still
+  // narrow to the 8-bit byte chain (deferred follow-ups). UGE is the slice that
+  // needs no terminator-fusion (C is a plain i1), so the branch reads it directly.
+  const MOSSubtarget &STI = Builder.getMF().getSubtarget<MOSSubtarget>();
+  // #321 native s16 equality: take the native EQ path (one rep/lda/cmp/sep producing Z,
+  // selected via CmpBrImag16) when either (a) the result feeds a branch (every use is
+  // G_BRCOND_IMM — Z fuses into the terminator directly), or (b) an operand is a
+  // non-absolute (indirect) s16 load. Case (b) is the EQ-as-VALUE win: the loaded value
+  // already lands in Imag16, so the native 16-bit compare reads it directly instead of
+  // unmerging it back into bytes for an 8-bit chain (measured -4 B on `*p == c`); the
+  // value materializes via buildNZSelect -> MOSLowerSelect -> G_BRCOND_IMM -> CmpBrImag16.
+  // Register/global/computed operands stay 8-bit: there the native form would force an
+  // Imag16 spill the tight 8-bit `cpx;cmp` avoids (measured regressions) — see
+  // docs/plans/2026-06-16-321-native-s16-eq-gated-impl.md (v2/v3 cover those).
+  auto isIndirectS16Load = [&](Register Reg) -> bool {
+    MachineInstr *Def = getDefIgnoringCopies(Reg, MRI);
+    if (!Def || (Def->getOpcode() != TargetOpcode::G_LOAD &&
+                 Def->getOpcode() != MOS::G_LOAD16_INDIR))
+      return false;
+    if (MRI.getType(Def->getOperand(0).getReg()) != LLT::scalar(16))
+      return false;
+    // Non-absolute pointer => indirect load => value is in Imag16 (native wins).
+    return !matchAbsoluteAddressing(MRI, Def->getOperand(1).getReg()).has_value();
+  };
+  // #321 v3: a SINGLE-USE s16 load of an ABSOLUTE (global) address — the operand
+  // selectBrCondImm's abs-fold reads in place via `lda abs` / `cmp abs`
+  // (foldableAbsLoad16) instead of materializing the global into an Imag16 pair.
+  // Mirror of isIndirectS16Load with the abs test inverted + single-use (the fold is
+  // 1-to-1 / volatile-safe only for one consumer); the single-use check keeps this
+  // gate in step with the selector's fold (so firing native never ships the round-
+  // trip the spike measured as a regression). Checks the generic G_LOAD (before
+  // legalizeLoadStore16) and the lowered G_LOAD16_ABS.
+  auto isFoldableAbsS16Load = [&](Register Reg) -> bool {
+    MachineInstr *Def = getDefIgnoringCopies(Reg, MRI);
+    if (!Def || (Def->getOpcode() != TargetOpcode::G_LOAD &&
+                 Def->getOpcode() != MOS::G_LOAD16_ABS))
+      return false;
+    if (MRI.getType(Def->getOperand(0).getReg()) != LLT::scalar(16))
+      return false;
+    if (!MRI.hasOneNonDBGUse(Def->getOperand(0).getReg()))
+      return false;
+    // G_LOAD16_ABS is already a confirmed absolute load; a generic G_LOAD is
+    // absolute iff its pointer matches the absolute-addressing pattern.
+    if (Def->getOpcode() == MOS::G_LOAD16_ABS)
+      return true;
+    return matchAbsoluteAddressing(MRI, Def->getOperand(1).getReg()).has_value();
+  };
+  // v3: a both-global s16 equality-as-value (`g1 == g2`) goes native and folds each
+  // operand in place via `lda abs; cmp abs` (selectBrCondImm -> CmpBrAbsAbs16), and a
+  // global-vs-nonzero-immediate (`g == 0x1234`) folds the global LHS via `lda abs` and
+  // the constant via `cmp #imm` (-> CmpBrAbsImm16) — turning the blanket-native Imag16
+  // round-trip the spike measured into a win. Mixed global-vs-register/local stays
+  // 8-bit (only one operand folds — the other would force a spill); `g == 0` stays on
+  // the G_CMPZ path (!RHSIsZero / !LHSIsZero). The constant is still a plain G_CONSTANT
+  // here (it is byte-split only later), so m_ICst sees it; the selector recovers the
+  // byte-split form via getI16Const. Single-use => 1-to-1 volatile-safe fold.
+  int64_t CstTmp;
+  const bool RHSIsCst = mi_match(RHS, MRI, m_ICst(CstTmp));
+  const bool LHSIsCst = mi_match(LHS, MRI, m_ICst(CstTmp));
+  const bool LHSIsZero = mi_match(LHS, MRI, m_SpecificICst(0));
+  const bool BothGlobal =
+      isFoldableAbsS16Load(LHS) && isFoldableAbsS16Load(RHS);
+  const bool GlobalVsImm =
+      (isFoldableAbsS16Load(LHS) && RHSIsCst && !RHSIsZero) ||
+      (isFoldableAbsS16Load(RHS) && LHSIsCst && !LHSIsZero);
+  // #321 v2: an s16 value already resident in Imag16 — a computed native-s16 ALU/shift
+  // result (selectAlu16Native writes Imag16) or an indirect load. NOT a register-arg
+  // (G_MERGE_VALUES of COPY $physreg, which would force a spill for CMPImag16), a global
+  // (v3's abs-fold), or a constant (handled separately). getDefIgnoringCopies looks
+  // through COPYs. Fire native EQ only when both operands are Imag16-resident (or one is
+  // a constant) — the -3 B win the spike measured for `(a+b) == c`, with the +8 B
+  // register-operand spill avoided by construction.
+  auto isComputedS16 = [&](Register Reg) -> bool {
+    MachineInstr *Def = getDefIgnoringCopies(Reg, MRI);
+    if (!Def || MRI.getType(Def->getOperand(0).getReg()) != LLT::scalar(16))
+      return false;
+    switch (Def->getOpcode()) {
+    // Generic native-s16 ALU/shift (a single-use computed value -> Imag16).
+    case TargetOpcode::G_ADD:
+    case TargetOpcode::G_SUB:
+    case TargetOpcode::G_AND:
+    case TargetOpcode::G_OR:
+    case TargetOpcode::G_XOR:
+    case TargetOpcode::G_SHL:
+    case TargetOpcode::G_LSHR:
+    case TargetOpcode::G_ASHR:
+      return true;
+    default:
+      return false;
+    }
+  };
+  auto isImag16Resident = [&](Register Reg) -> bool {
+    return isComputedS16(Reg) || isIndirectS16Load(Reg);
+  };
+  const bool ComputedEq =
+      (isImag16Resident(LHS) && (isImag16Resident(RHS) || RHSIsCst)) ||
+      (isImag16Resident(RHS) && (isImag16Resident(LHS) || LHSIsCst));
+  // #321 task7: one operand Imag16-resident (computed/indirect), the other a foldable near-abs
+  // global. Fills the gap between ComputedEq (both-Imag16) and BothGlobal (both-abs): selects
+  // `lda zp_computed; cmp abs_global` via CmpBrImagAbs16. The canonicalization swaps below keep
+  // the Imag16-resident value on LHS and the global on RHS so selectBrCondImm finds them.
+  const bool ComputedVsGlobal =
+      (isImag16Resident(LHS) && isFoldableAbsS16Load(RHS)) ||
+      (isImag16Resident(RHS) && isFoldableAbsS16Load(LHS));
+  const bool NativeS16Eq =
+      STI.hasAccum16() && Type == LLT::scalar(16) && Pred == CmpInst::ICMP_EQ &&
+      !RHSIsZero &&
+      (isIndirectS16Load(LHS) || isIndirectS16Load(RHS) || // v1: indirect-load operand
+       BothGlobal || GlobalVsImm ||                        // v3: abs-fold globals
+       ComputedEq || ComputedVsGlobal ||                   // v2: Imag16-resident; task7: mixed
+       all_of(MRI.use_instructions(Dst), [](const MachineInstr &U) {
+         return U.getOpcode() == MOS::G_BRCOND_IMM;
+       }));
+  const bool NativeS16 =
+      STI.hasAccum16() && Type == LLT::scalar(16) &&
+      (Pred == CmpInst::ICMP_UGE || NativeS16Eq);
+
+  // #321 canonicalization: EQ's Z is symmetric (a-b==0 iff b-a==0); swaps below are safe.
+  // task7: for ComputedVsGlobal, put the Imag16-resident value on LHS (for `lda zp` via
+  // LDAImag16) and the global on RHS (for `cmp abs` via CMPAbs16), so foldableAbsLoad16(RHS16)
+  // fires in selectBrCondImm. Covers both `(a+b)==g` (LHS is already computed — swap is no-op
+  // since isFoldableAbsS16Load(LHS) is false) and `g==(a+b)` (global on LHS — swaps them).
+  if (NativeS16Eq && ComputedVsGlobal && isFoldableAbsS16Load(LHS) &&
+      isImag16Resident(RHS))
+    std::swap(LHS, RHS);
+  // GlobalVsImm: put a lone foldable-abs global on LHS (read by `lda abs`) and any constant on
+  // RHS (`cmp #imm`), so CmpBrAbsImm16 fires for `0x1234 == g` too. Guard !isImag16Resident(LHS)
+  // so ComputedVsGlobal cases (computed LHS + global RHS, after the swap above) are not
+  // accidentally re-swapped back.
+  if (NativeS16Eq && isFoldableAbsS16Load(RHS) && !isFoldableAbsS16Load(LHS) &&
+      !isImag16Resident(LHS))
+    std::swap(LHS, RHS);
+
+  if (Type != S8 && !NativeS16) {
     if (RHSIsZero && Pred == CmpInst::ICMP_EQ &&
         all_of(MRI.use_instructions(Dst), [](const MachineInstr &MI) {
           return MI.getOpcode() == MOS::G_BRCOND_IMM;
@@ -1341,16 +1545,22 @@ bool MOSLegalizerInfo::legalizeICmp(LegalizerHelper &Helper,
     CIn = Builder.buildConstant(S1, 1).getReg(0);
   }
 
-  assert(Type == S8);
+  assert(Type == S8 || NativeS16);
 
   // Lower 8-bit comparisons to a generic G_SBC instruction with similar
   // capabilities to the 6502's SBC and CMP instructions.  See
   // www.6502.org/tutorials/compare_beyond.html.
   switch (Pred) {
   case CmpInst::ICMP_EQ: {
+    // #321 native s16 equality (branch-fused): one 16-bit G_SBC -> selectBrCondImm
+    // emits rep/lda/cmp/sep/beq. Z (getReg(4)) is the live flag; the branch reads it.
+    LLT CmpTy = NativeS16 ? LLT::scalar(16) : S8;
     auto Sbc =
-        Builder.buildInstr(MOS::G_SBC, {S8, S1, S1, S1, S1}, {LHS, RHS, CIn});
+        Builder.buildInstr(MOS::G_SBC, {CmpTy, S1, S1, S1, S1}, {LHS, RHS, CIn});
     Register Z = Sbc.getReg(4);
+    // Value use of a native 16-bit Z: wrap it in a select so MOSLowerSelect turns it into
+    // a G_BRCOND_IMM that selectBrCondImm fuses into CmpBrImag16 (the materialization
+    // diamond loads 0/1). For a branch use isNZUseLegal is true, so this is skipped.
     if (!isNZUseLegal(Dst, MRI))
       Z = buildNZSelect(Z, Builder);
     Builder.buildCopy(Dst, Z);
@@ -1358,8 +1568,11 @@ bool MOSLegalizerInfo::legalizeICmp(LegalizerHelper &Helper,
     break;
   }
   case CmpInst::ICMP_UGE: {
+    // Native 16-bit under +mos-a16 (NativeS16), else 8-bit. The 16-bit G_SBC is
+    // selected to one rep/lda/cmp/sep producing C; its result + N/V/Z are unused.
+    LLT CmpTy = NativeS16 ? LLT::scalar(16) : S8;
     auto Sbc =
-        Builder.buildInstr(MOS::G_SBC, {S8, S1, S1, S1, S1}, {LHS, RHS, CIn});
+        Builder.buildInstr(MOS::G_SBC, {CmpTy, S1, S1, S1, S1}, {LHS, RHS, CIn});
     Builder.buildCopy(Dst, Sbc.getReg(1) /*=C*/);
     MI.eraseFromParent();
     break;
@@ -1601,14 +1814,89 @@ bool MOSLegalizerInfo::legalizeSubE(LegalizerHelper &Helper,
 // Memory Operations
 //===----------------------------------------------------------------------===//
 
+// #321 native s16 indirect memory: route an s16 G_LOAD/G_STORE through a
+// non-absolute 16-bit pointer to a single native 16-bit indirect op
+// (G_LOAD16_INDIR/G_STORE16_INDIR -> lda (zp)/sta (zp) in M=0). The whole pointer
+// (incl. any G_PTR_ADD for a[i]) is materialized into the zero-page Imag16 pair, so
+// the plain-indirect form is always correct — just occasionally less optimal than
+// (zp),y, which (with absolute/abs-indexed s16 access) stays on the byte-pair path
+// as a follow-up. Absolute addresses (globals) narrow here so they keep their
+// byte-wise lowering for now.
+bool MOSLegalizerInfo::legalizeLoadStore16(LegalizerHelper &Helper,
+                                           MachineRegisterInfo &MRI,
+                                           GLoadStore &MI) const {
+  MachineIRBuilder &Builder = Helper.MIRBuilder;
+  const MOSSubtarget &STI = Builder.getMF().getSubtarget<MOSSubtarget>();
+  Register Ptr = MI.getPointerReg();
+  const bool IsLoad = isa<GLoad>(MI);
+
+  // Don't take a constant-valued store native: materializing the constant into an
+  // Imag16 pair just to `lda zp; sta` is worse than the byte path — a store of zero
+  // wants the STZ-fusion peephole (`rep; stz; sep`), and a small immediate stays a
+  // cheap pair of 8-bit immediate stores. Only register-valued stores win here.
+  if (!IsLoad &&
+      getIConstantVRegValWithLookThrough(cast<GStore>(MI).getValueReg(), MRI))
+    return Helper.narrowScalar(MI, 0, LLT::scalar(8)) ==
+           LegalizerHelper::Legalized;
+
+  // #321: an s16 LOAD whose result is only ever split into bytes (every use is a
+  // G_UNMERGE — e.g. an s16 global feeding an 8-bit-narrowed compare like
+  // `b = (a == c)`) should keep its byte-wise lowering. Forming the native 16-bit
+  // `lda abs -> A16; sta imag16` here only for the consumer to read the bytes straight
+  // back is a wasteful round-trip (worse than the default byte loads). Narrow it
+  // instead. (Stores always take the native form; the indirect-load-then-unmerge case
+  // is a separate follow-up — see TODO.)
+  const bool AllUsesUnmerge =
+      IsLoad && !MRI.use_nodbg_empty(MI.getOperand(0).getReg()) &&
+      all_of(MRI.use_nodbg_instructions(MI.getOperand(0).getReg()),
+             [](const MachineInstr &U) {
+               return U.getOpcode() == TargetOpcode::G_UNMERGE_VALUES;
+             });
+
+  if (STI.hasAccum16() && MRI.getType(Ptr).getScalarSizeInBits() == 16) {
+    // Absolute address (global/static/const) -> native 16-bit `lda abs`/`sta abs`.
+    if (auto AbsOp = matchAbsoluteAddressing(MRI, Ptr)) {
+      if (!AllUsesUnmerge) {
+        Builder
+            .buildInstr(IsLoad ? MOS::G_LOAD16_ABS : MOS::G_STORE16_ABS)
+            .add(MI.getOperand(0)) // dst (load) / value (store)
+            .add(*AbsOp)
+            .addMemOperand(*MI.memoperands_begin());
+        MI.eraseFromParent();
+        return true;
+      }
+      // else: all-uses-unmerge load -> fall through to the byte-wise narrowing below.
+    } else if (tryIndexedAddressing16(Helper, MRI, MI)) {
+      // abs,x or (zp),y indexed 16-bit load/store (#321 Increment 1e).
+      return true;
+    } else if (STI.has65C02()) {
+      // Runtime 16-bit pointer -> native 16-bit `lda (zp)`/`sta (zp)`.
+      Builder.buildInstr(IsLoad ? MOS::G_LOAD16_INDIR : MOS::G_STORE16_INDIR)
+          .add(MI.getOperand(0))
+          .addUse(Ptr)
+          .addMemOperand(*MI.memoperands_begin());
+      MI.eraseFromParent();
+      return true;
+    }
+  }
+  // zp-pointer / far / non-a16: fall back to the 8-bit byte-pair lowering.
+  return Helper.narrowScalar(MI, 0, LLT::scalar(8)) ==
+         LegalizerHelper::Legalized;
+}
+
 // Load pointers by loading a 16-bit integer, then converting to pointer. This
 // allows the 16-bit loads to be reduced to a pair of 8-bit loads.
 bool MOSLegalizerInfo::legalizeLoad(LegalizerHelper &Helper,
                                     MachineRegisterInfo &MRI,
                                     GAnyLoad &MI) const {
-  if (auto *Load = dyn_cast<GLoad>(&MI))
+  if (auto *Load = dyn_cast<GLoad>(&MI)) {
     if (MRI.getType(Load->getDstReg()) == LLT::scalar(8))
       return selectAddressingMode(Helper, MRI, MI);
+    // #321 native s16: a 16-bit value through a non-absolute 16-bit pointer ->
+    // one native indirect load (else narrow to two 8-bit, as before).
+    if (MRI.getType(Load->getDstReg()) == LLT::scalar(16))
+      return legalizeLoadStore16(Helper, MRI, MI);
+  }
 
   MachineIRBuilder &Builder = Helper.MIRBuilder;
   Builder.setInsertPt(Builder.getMBB(), std::next(Builder.getInsertPt()));
@@ -1645,6 +1933,10 @@ bool MOSLegalizerInfo::legalizeStore(LegalizerHelper &Helper,
                                      GStore &MI) const {
   if (MRI.getType(MI.getValueReg()) == LLT::scalar(8))
     return selectAddressingMode(Helper, MRI, MI);
+  // #321 native s16: a 16-bit value through a non-absolute 16-bit pointer -> one
+  // native indirect store (else narrow to two 8-bit, as before).
+  if (MRI.getType(MI.getValueReg()) == LLT::scalar(16))
+    return legalizeLoadStore16(Helper, MRI, MI);
 
   MachineIRBuilder &Builder = Helper.MIRBuilder;
   auto ValueType = MRI.getType(MI.getValueReg());
@@ -1671,6 +1963,125 @@ static bool willBeStaticallyAllocated(const MachineOperand &MO) {
   return !MF.getFrameInfo().isFixedObjectIndex(MO.getIndex());
 }
 
+bool MOSLegalizerInfo::tryIndexedAddressing16(LegalizerHelper &Helper,
+                                              MachineRegisterInfo &MRI,
+                                              GLoadStore &MI) const {
+  LLT S8 = LLT::scalar(8);
+  MachineIRBuilder &Builder = Helper.MIRBuilder;
+  const MOSSubtarget &STI = Builder.getMF().getSubtarget<MOSSubtarget>();
+
+  if (!STI.hasAccum16() || !STI.has65C02())
+    return false;
+
+  const bool IsLoad = isa<GLoad>(MI);
+  Register Addr = MI.getPointerReg();
+  int64_t ConstOffset = 0;
+  Register VarIndex = 0;
+
+  while (true) {
+    // Absolute base cases — require a VarIndex already found.
+    if (auto ConstAddr = getIConstantVRegValWithLookThrough(Addr, MRI)) {
+      if (!VarIndex)
+        return false;
+      int64_t Base = ConstAddr->Value.getSExtValue() + ConstOffset;
+      unsigned Opc = IsLoad ? MOS::G_LOAD16_ABS_IDX : MOS::G_STORE16_ABS_IDX;
+      Builder.buildInstr(Opc)
+          .add(MI.getOperand(0))
+          .addImm(Base)
+          .addUse(VarIndex)
+          .addMemOperand(*MI.memoperands_begin());
+      MI.eraseFromParent();
+      return true;
+    }
+    if (const MachineInstr *GVAddr = getOpcodeDef(G_GLOBAL_VALUE, Addr, MRI)) {
+      if (!VarIndex)
+        return false;
+      const MachineOperand &GV = GVAddr->getOperand(1);
+      unsigned Opc = IsLoad ? MOS::G_LOAD16_ABS_IDX : MOS::G_STORE16_ABS_IDX;
+      Builder.buildInstr(Opc)
+          .add(MI.getOperand(0))
+          .addGlobalAddress(GV.getGlobal(), GV.getOffset() + ConstOffset)
+          .addUse(VarIndex)
+          .addMemOperand(*MI.memoperands_begin());
+      MI.eraseFromParent();
+      return true;
+    }
+    if (const MachineInstr *FIAddr = getOpcodeDef(G_FRAME_INDEX, Addr, MRI)) {
+      const MachineOperand &FI = FIAddr->getOperand(1);
+      if (!VarIndex || !willBeStaticallyAllocated(FI))
+        return false;
+      unsigned Opc = IsLoad ? MOS::G_LOAD16_ABS_IDX : MOS::G_STORE16_ABS_IDX;
+      Builder.buildInstr(Opc)
+          .add(MI.getOperand(0))
+          .addFrameIndex(FI.getIndex(), FI.getOffset() + ConstOffset)
+          .addUse(VarIndex)
+          .addMemOperand(*MI.memoperands_begin());
+      MI.eraseFromParent();
+      return true;
+    }
+    // Peel a G_PTR_ADD: constant offsets accumulate; a single 8-bit variable
+    // becomes VarIndex (first one wins; a second means we can't fold both).
+    if (const auto *PtrAdd =
+            cast_if_present<GPtrAdd>(getOpcodeDef(G_PTR_ADD, Addr, MRI))) {
+      Addr = PtrAdd->getBaseReg();
+      Register Off = PtrAdd->getOffsetReg();
+      if (auto C = getIConstantVRegValWithLookThrough(Off, MRI)) {
+        ConstOffset += C->Value.getSExtValue();
+        continue;
+      }
+      if (MachineInstr *ZExt = getOpcodeDef(G_ZEXT, Off, MRI)) {
+        if (VarIndex)
+          return false;
+        Register Src = ZExt->getOperand(1).getReg();
+        LLT SrcTy = MRI.getType(Src);
+        if (SrcTy.getSizeInBits() > 8)
+          return false;
+        if (SrcTy.getSizeInBits() < 8)
+          Src = Builder.buildZExt(S8, Src).getReg(0);
+        VarIndex = Src;
+        continue;
+      }
+      if (Helper.getValueTracking()->getKnownBits(Off).countMaxActiveBits() <=
+          8) {
+        if (VarIndex)
+          return false;
+        VarIndex = Builder.buildZExtOrTrunc(S8, Off).getReg(0);
+        continue;
+      }
+      return false;
+    }
+    // Non-absolute base (runtime ZP pointer).
+    // Case 1: variable index, zero accumulated const → `lda (zp),y` with VarIndex.
+    // Case 2: no variable index, const offset in [1,255] → materialise as Y.
+    // Mixed (VarIndex + ConstOffset != 0) can't be expressed in one indexed op.
+    if (VarIndex && ConstOffset == 0) {
+      unsigned Opc =
+          IsLoad ? MOS::G_LOAD16_INDIR_IDX : MOS::G_STORE16_INDIR_IDX;
+      Builder.buildInstr(Opc)
+          .add(MI.getOperand(0))
+          .addUse(Addr)
+          .addUse(VarIndex)
+          .addMemOperand(*MI.memoperands_begin());
+      MI.eraseFromParent();
+      return true;
+    }
+    if (!VarIndex && ConstOffset >= 1 && ConstOffset <= 255) {
+      Register Idx = Builder.buildConstant(S8, ConstOffset).getReg(0);
+      unsigned Opc =
+          IsLoad ? MOS::G_LOAD16_INDIR_IDX : MOS::G_STORE16_INDIR_IDX;
+      Builder.buildInstr(Opc)
+          .add(MI.getOperand(0))
+          .addUse(Addr)
+          .addUse(Idx)
+          .addMemOperand(*MI.memoperands_begin());
+      MI.eraseFromParent();
+      return true;
+    }
+    return false;
+  }
+  return false;
+}
+
 bool MOSLegalizerInfo::selectAddressingMode(LegalizerHelper &Helper,
                                             MachineRegisterInfo &MRI,
                                             GLoadStore &MI) const {
diff --git a/llvm/lib/Target/MOS/MOSLegalizerInfo.h b/llvm/lib/Target/MOS/MOSLegalizerInfo.h
index 48cb1f356..583ca910d 100644
--- a/llvm/lib/Target/MOS/MOSLegalizerInfo.h
+++ b/llvm/lib/Target/MOS/MOSLegalizerInfo.h
@@ -79,6 +79,11 @@ private:
                     GAnyLoad &MI) const;
   bool legalizeStore(LegalizerHelper &Helper, MachineRegisterInfo &MRI,
                      GStore &MI) const;
+  // #321 native s16 indirect load/store (one 16-bit op vs two 8-bit).
+  bool legalizeLoadStore16(LegalizerHelper &Helper, MachineRegisterInfo &MRI,
+                           GLoadStore &MI) const;
+  bool tryIndexedAddressing16(LegalizerHelper &Helper, MachineRegisterInfo &MRI,
+                              GLoadStore &MI) const;
   bool selectAddressingMode(LegalizerHelper &Helper, MachineRegisterInfo &MRI,
                             GLoadStore &MI) const;
   std::optional<MachineOperand>
diff --git a/llvm/lib/Target/MOS/MOSRegisterBanks.td b/llvm/lib/Target/MOS/MOSRegisterBanks.td
index 868f6c712..7b5100bfe 100644
--- a/llvm/lib/Target/MOS/MOSRegisterBanks.td
+++ b/llvm/lib/Target/MOS/MOSRegisterBanks.td
@@ -11,4 +11,7 @@
 //===----------------------------------------------------------------------===//
 
 // Register banks are unused, so place everything in one big bank.
-def AnyRegBank : RegisterBank<"Any", [Anyi1, Anyi8, Imag16, Fakei32, Fakei64]>;
+// #321 Increment 1b: Ac16 (the 16-bit accumulator A16) joins the bank so 16-bit
+// accumulator ops can be allocated. Allocation to A16 is forced by the Ac16
+// register class on the selected ops, not by the bank (banks are unused here).
+def AnyRegBank : RegisterBank<"Any", [Anyi1, Anyi8, Imag16, Ac16, Fakei32, Fakei64]>;
diff --git a/llvm/lib/Target/MOS/MOSRegisterInfo.cpp b/llvm/lib/Target/MOS/MOSRegisterInfo.cpp
index baf6de604..2194945af 100644
--- a/llvm/lib/Target/MOS/MOSRegisterInfo.cpp
+++ b/llvm/lib/Target/MOS/MOSRegisterInfo.cpp
@@ -427,10 +427,25 @@ void MOSRegisterInfo::expandLDSTStk(MachineBasicBlock::iterator MI) const {
       IsLoad ? MI->getOperand(0).getReg() : MI->getOperand(1).getReg();
   int64_t Offset = MI->getOperand(3).getImm();
 
-  if (Offset >= 256) {
+  // #321: the 16-bit accumulator (Ac16 = A16) is spilled with a single 16-bit
+  // indirect load/store (see below) — never the Imag16 byte-pair split (A16's high
+  // byte B is not byte-addressable without XBA) nor the 8-bit fall-through (which
+  // would COPY A16 to a GPR -> the SelectImm $a16 / register-scavenger crash).
+  const bool IsAc16 = MOS::Ac16RegClass.contains(Loc);
+  // #321 xy16: X16/Y16 spilled via TXA16/STAIndir16 or LDAIndir16/TAX16 (same
+  // indirect-only constraint as Ac16 — the (zp)-indirect ops have no index).
+  const bool IsXc16 = MOS::Xc16RegClass.contains(Loc);
+  const bool IsYc16 = MOS::Yc16RegClass.contains(Loc);
+
+  // Form an exact slot pointer in the reserved scratch when the access can't use a
+  // base+index addressing mode: a far offset (>= 256), OR a 16-bit spill at a nonzero
+  // offset (STAIndir16/LDAIndir16 and their X/Y variants are (zp)-indirect with no
+  // index). After this the pointer is in operand 2 and the offset is 0, so re-entry
+  // lands in the offset-0 paths below.
+  if (Offset >= 256 || ((IsAc16 || IsXc16 || IsYc16) && Offset != 0)) {
     Register P = MRI.createVirtualRegister(&MOS::PcRegClass);
-    // Far stack accesses need a virtual base register, so materialize one
-    // here using the pointer provided.
+    // These accesses need a concrete base pointer, so materialize one here using
+    // the pointer provided (the earlyclobber scratch operand).
     Register NewBase =
         IsLoad ? MI->getOperand(1).getReg() : MI->getOperand(0).getReg();
     // We can't scavenge a 16-bit register, so this can't be virtual here
@@ -461,6 +476,69 @@ void MOSRegisterInfo::expandLDSTStk(MachineBasicBlock::iterator MI) const {
     return;
   }
 
+  // #321: Ac16 spill/reload at offset 0 — operand 2 is now the exact slot pointer
+  // (the frame register for a near offset-0 access, or the scratch pointer the block
+  // above formed). One 16-bit indirect store/load of A16 through it; the MLow=1 op is
+  // rep/sep-bracketed by the later MOSInsertREPSEP pass.
+  if (IsAc16) {
+    assert(Offset == 0 && "Ac16 nonzero offset should have formed a pointer above");
+    Builder.buildInstr(IsLoad ? MOS::LDAIndir16 : MOS::STAIndir16)
+        .addReg(Loc, getDefRegState(IsLoad))
+        .add(MI->getOperand(2))
+        .addMemOperand(*MI->memoperands_begin());
+    MI->eraseFromParent();
+    return;
+  }
+
+  // #321 xy16: X16/Y16 spill/reload at offset 0, via A16 as a staging register.
+  // Store: TXA16 (X16→A16) → STAIndir16 [ptr].
+  // Load:  LDAIndir16 [ptr] → TAX16 (A16→X16).
+  // Both the XLow=1 and MLow=1 brackets are inserted by MOSInsertREPSEP.
+  if (IsXc16) {
+    assert(Offset == 0 && "Xc16 nonzero offset should have formed a pointer above");
+    if (!IsLoad) {
+      Builder.buildInstr(MOS::TXA16)
+          .addReg(MOS::A16, RegState::Define)
+          .addReg(MOS::X16);
+      Builder.buildInstr(MOS::STAIndir16)
+          .addReg(MOS::A16, RegState::Kill)
+          .add(MI->getOperand(2))
+          .addMemOperand(*MI->memoperands_begin());
+    } else {
+      Builder.buildInstr(MOS::LDAIndir16)
+          .addReg(MOS::A16, RegState::Define)
+          .add(MI->getOperand(2))
+          .addMemOperand(*MI->memoperands_begin());
+      Builder.buildInstr(MOS::TAX16)
+          .addReg(MOS::X16, RegState::Define)
+          .addReg(MOS::A16, RegState::Kill);
+    }
+    MI->eraseFromParent();
+    return;
+  }
+  if (IsYc16) {
+    assert(Offset == 0 && "Yc16 nonzero offset should have formed a pointer above");
+    if (!IsLoad) {
+      Builder.buildInstr(MOS::TYA16)
+          .addReg(MOS::A16, RegState::Define)
+          .addReg(MOS::Y16);
+      Builder.buildInstr(MOS::STAIndir16)
+          .addReg(MOS::A16, RegState::Kill)
+          .add(MI->getOperand(2))
+          .addMemOperand(*MI->memoperands_begin());
+    } else {
+      Builder.buildInstr(MOS::LDAIndir16)
+          .addReg(MOS::A16, RegState::Define)
+          .add(MI->getOperand(2))
+          .addMemOperand(*MI->memoperands_begin());
+      Builder.buildInstr(MOS::TAY16)
+          .addReg(MOS::Y16, RegState::Define)
+          .addReg(MOS::A16, RegState::Kill);
+    }
+    MI->eraseFromParent();
+    return;
+  }
+
   if (MOS::Imag16RegClass.contains(Loc)) {
     if (!IsLoad) {
       // Loc may not be fully alive at this point, which would create uses of
@@ -500,6 +578,15 @@ void MOSRegisterInfo::expandLDSTStk(MachineBasicBlock::iterator MI) const {
   if (Loc8)
     Loc = Loc8;
 
+  // #321 SPILL CONTRACT: everything reaching the 8-bit tail below must be C, V,
+  // or an 8-bit (Anyi8) class. Every spillable >=16-bit register class needs its
+  // OWN explicit expansion case ABOVE this point — it cannot fall through to the
+  // single-byte path, which would COPY the wide value through an 8-bit GPR and
+  // emit invalid MIR (the F3 `SelectImm $a16` crash class). Handled:
+  //   - Ac16   (16-bit accumulator): one 16-bit indirect STAIndir16/LDAIndir16.
+  //   - Xc16   (16-bit X index): TXA16 → STAIndir16 / LDAIndir16 → TAX16.
+  //   - Yc16   (16-bit Y index): TYA16 → STAIndir16 / LDAIndir16 → TAY16.
+  //   - Imag16 (zp pair): split into two byte spills, each re-expanded.
   assert(Loc == MOS::C || Loc == MOS::V || MOS::Anyi8RegClass.contains(Loc));
 
   Register A = Loc;
diff --git a/llvm/lib/Target/MOS/MOSRegisterInfo.td b/llvm/lib/Target/MOS/MOSRegisterInfo.td
index 94e229944..2df125248 100644
--- a/llvm/lib/Target/MOS/MOSRegisterInfo.td
+++ b/llvm/lib/Target/MOS/MOSRegisterInfo.td
@@ -90,6 +90,38 @@ def P : MOSReg<12, "P"> {
   let SubRegIndices = [subcarry, subnz, subv];
 }
 
+// #321 Increment 1b: the 65816's 16-bit accumulator, modeled as A16 = B:A. WDC
+// calls this combined register "C", but we deliberately do NOT use that name —
+// `def C` (register 7, a subreg of P) is the *carry flag*, and conflating the
+// 16-bit accumulator with carry is a classic 65816 footgun. So the 16-bit
+// accumulator is `A16` (low byte A, high byte B), unrelated to carry.
+//
+// `B` is the accumulator's hidden high byte (swapped with A via XBA), only live
+// as the high half of A16 in native 16-bit-accumulator mode (M=0); 8-bit ops
+// never name it. A16 aliases A as its low byte so the register allocator knows a
+// 16-bit accumulator op clobbers A. Numbers 14/15 are free (reals end at
+// Fake=13, imaginaries start at 0x10). Used only under +mos-a16; inert otherwise.
+def B : MOSReg<14, "b">;
+def A16 : MOSReg<15, "a16"> {
+  let SubRegs = [A, B];
+  let SubRegIndices = [sublo, subhi];
+  let CoveredBySubRegs = 1;
+}
+
+// #321 xy16: high bytes of X and Y in 16-bit index mode (M=0, X=0). Like B for
+// A16, XH/YH are not directly addressable — only live as the high half of X16/Y16.
+// Numbers above both imaginary ranges (Imag8 max 0x20F, Imag16 max 0x28F).
+def XH : MOSReg<0x500, "xh">;
+def YH : MOSReg<0x501, "yh">;
+def X16 : MOSReg<0x502, "x16"> {
+  let SubRegs = [X, XH]; let SubRegIndices = [sublo, subhi];
+  let CoveredBySubRegs = 1;
+}
+def Y16 : MOSReg<0x503, "y16"> {
+  let SubRegs = [Y, YH]; let SubRegIndices = [sublo, subhi];
+  let CoveredBySubRegs = 1;
+}
+
 //===----------------------------------------------------------------------===//
 //  Imaginary registers
 //===----------------------------------------------------------------------===//
@@ -180,6 +212,16 @@ let isPressureFineGrained = true in {
 def Imag8 : MOSReg8Class<(sequence "RC%u", 0, !add(MaxImag8Regs, -1))>;
 def Imag16 : MOSReg16Class<(sequence "RS%u", 0, !add(MaxImag16Regs, -1))>;
 
+// #321 Increment 1b: the 16-bit accumulator as a register class (single member,
+// A16 = B:A — NOT the carry flag C). Target of 16-bit-accumulator load/add/store
+// under +mos-a16. Named Ac16 to parallel the 8-bit accumulator class `Ac`.
+def Ac16 : MOSReg16Class<(add A16)>;
+
+// #321 xy16: 16-bit index register classes, one member each. Used only under
+// +mos-xy16; inert otherwise. Mirror Ac16.
+def Xc16 : MOSReg16Class<(add X16)>;
+def Yc16 : MOSReg16Class<(add Y16)>;
+
 // Instruction-specific register classes.
 def AImag8 : MOSReg8Class<(add Imag8, A)>;
 let isPressureFineGrained = true in {
diff --git a/llvm/lib/Target/MOS/MOSSubtarget.h b/llvm/lib/Target/MOS/MOSSubtarget.h
index 6d303255c..bc43100cd 100644
--- a/llvm/lib/Target/MOS/MOSSubtarget.h
+++ b/llvm/lib/Target/MOS/MOSSubtarget.h
@@ -115,6 +115,12 @@ public:
   bool hasJMPIdxIndir() const { return has65C02() || hasSPC700(); }
   bool staticStack() const { return StaticStack; }
 
+  // #321: opt-in 16-bit accumulator mode (REP/SEP-bracketed 16-bit-A codegen).
+  bool hasAccum16() const { return HasAccum16; }
+
+  // #321 xy16: opt-in 16-bit index register mode (REP/SEP-bracketed 16-bit-X/Y).
+  bool hasIndex16() const { return HasIndex16; }
+
   bool hasW65816Or65EL02() const { return HasW65816Insns || Has65EL02Insns; }
 
 private:
@@ -140,6 +146,8 @@ private:
 
   bool LongRegisterNames = false;
   bool StaticStack = false;
+  bool HasAccum16 = false;
+  bool HasIndex16 = false;
 
   // Dummy member, used by FeatureSet's. We cannot have a SubtargetFeature with
   // no variable, so we instead bind pseudo features to this variable.
diff --git a/llvm/lib/Target/MOS/MOSTargetMachine.cpp b/llvm/lib/Target/MOS/MOSTargetMachine.cpp
index ceb0afe8a..e12e57622 100644
--- a/llvm/lib/Target/MOS/MOSTargetMachine.cpp
+++ b/llvm/lib/Target/MOS/MOSTargetMachine.cpp
@@ -39,6 +39,7 @@
 #include "MOSIndexIV.h"
 #include "MOSInsertCopies.h"
 #include "MOSInternalize.h"
+#include "MOSInsertREPSEP.h"
 #include "MOSLateOptimization.h"
 #include "MOSLowerSelect.h"
 #include "MOSMachineFunctionInfo.h"
@@ -62,6 +63,7 @@ extern "C" LLVM_EXTERNAL_VISIBILITY void LLVMInitializeMOSTarget() {
   initializeMOSCombinerPass(PR);
   initializeMOSCopyOptPass(PR);
   initializeMOSInsertCopiesPass(PR);
+  initializeMOSInsertREPSEPPass(PR);
   initializeMOSInternalizePass(PR);
   initializeMOSLateOptimizationPass(PR);
   initializeMOSLowerSelectPass(PR);
@@ -325,7 +327,13 @@ void MOSPassConfig::addPreSched2() {
     addPass(createMOSStaticStackAllocPass());
 }
 
-void MOSPassConfig::addPreEmitPass() { addPass(&BranchRelaxationPassID); }
+void MOSPassConfig::addPreEmitPass() {
+  // #321: insert REP/SEP for 16-bit-accumulator instructions (opt-in, +mos-a16;
+  // a no-op otherwise). Runs before branch relaxation so the added REP/SEP are
+  // accounted for in branch distances.
+  addPass(createMOSInsertREPSEPPass());
+  addPass(&BranchRelaxationPassID);
+}
 
 namespace {
 
